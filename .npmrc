# Project registry routing — scope -> GitHub Packages. Auth is runtime-only
# (local ~/.npmrc for the manual publish; actions/setup-node in CI). No token here.
@willramdev:registry=https://npm.pkg.github.com
