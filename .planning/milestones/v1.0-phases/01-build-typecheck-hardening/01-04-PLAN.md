---
phase: 01-build-typecheck-hardening
plan: 04
type: execute
wave: 4
gap_closure: true
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - packages/forms/vite.config.ts
  - packages/router/src/define.ts
  - packages/router/src/router-lit/router-outlet.ts
  - packages/router/src/router-lit/router-link.ts
  - packages/router/src/router-lit/router-provider.ts
  - packages/router/src/test/no-double-register.test.ts
autonomous: true
requirements: [BUILD-02, BUILD-03]
closes_gaps:
  - "CR-01: @willram/forms fails to externalize lit/decorators.js (VERIFICATION truth #6)"
  - "CR-02: router double-registers custom elements across dist/router.js + dist/router-lit.js (VERIFICATION truth #7)"
estimate:
  tokens: 45000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "@willram/forms externalizes every lit/* subpath — dist/forms.js imports lit/decorators.js as an external specifier and inlines no lit/* module body (closes CR-01, BUILD-02/BUILD-03). Verify: grep -q 'lit/decorators.js' packages/forms/dist/forms.js && ! grep -q 'node_modules/lit/' packages/forms/dist/forms.js"
    - "@willram/router registers each custom element exactly once across shipped entries — importing both @willram/router and @willram/router/lit does not throw a duplicate-definition DOMException (closes CR-02, BUILD-03). Verify: npm run build -w @willram/router && npm run test -w @willram/router (the no-double-register smoke test runs green)"
    - "Registration still survives tree-shaking — both dist/router.js and dist/router-lit.js still register router-outlet/router-link/router-provider (BUILD-03 unregressed). Verify: grep -q router-outlet packages/router/dist/router.js && grep -q router-outlet packages/router/dist/router-lit.js"
  artifacts:
    - "packages/forms/vite.config.ts — external array uses a /^lit\\// catch-all (mirrors kit/store), no enumerated lit subpaths"
    - "packages/router/src/define.ts — local idempotent define(tag, ctor, options?) guard (no @willram/kit dependency)"
    - "packages/router/src/router-lit/router-outlet.ts, router-link.ts, router-provider.ts — register via define(...) instead of bare @customElement(...)"
    - "packages/router/src/test/no-double-register.test.ts — smoke test importing both built dist entries"
    - "packages/forms/dist/forms.js, packages/router/dist/router.js, packages/router/dist/router-lit.js — rebuilt artifacts reflecting the fixes"
  key_links:
    - "forms vite.config /^lit\\// external → Rollup keeps every lit/* import (incl. lit/decorators.js) external in dist/forms.js"
    - "router element modules define() idempotent guard → the second customElements.define (from the other shipped entry) is a no-op instead of a throw"
---

<objective>
Close the two BLOCKER verification gaps from 01-VERIFICATION.md without replanning the phase. Plans 01-01/01-02/01-03 are done; this plan makes exactly two isolated defects pass:

- **CR-01 (GAP 1):** `@willram/forms` bundles `lit/decorators.js` into `dist/forms.js` instead of externalizing it — the exact bundle-duplication the phase exists to prevent (BUILD-02/BUILD-03, CLAUDE.md externalization constraint).
- **CR-02 (GAP 2):** `@willram/router` registers `router-outlet`/`router-link`/`router-provider` in BOTH `dist/router.js` and `dist/router-lit.js`; a consumer importing both entries throws `DOMException: 'router-outlet' has already been defined` (BUILD-03, phase goal "correctly configured").

Purpose: restore the phase goal's "green, correctly configured" clause so nothing downstream (Phase 2 CI, Phase 5 consumer verification) trusts a broken baseline.
Output: fixed `forms/vite.config.ts`, a local idempotent registration guard + three converted element modules in router, one smoke test, and rebuilt dist artifacts whose grep/runtime checks flip to passing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-typecheck-hardening/01-VERIFICATION.md
@.planning/phases/01-build-typecheck-hardening/01-REVIEW.md
@.planning/phases/01-build-typecheck-hardening/01-CONTEXT.md
@.claude/CLAUDE.md

# Correct-reference analogs (already externalize lit/* correctly)
@packages/query/vite.config.ts
@packages/kit/vite.config.ts
@packages/store/vite.config.ts

# Idempotent-define analog (kit's guard — router MUST NOT import kit; replicate locally)
@packages/kit/src/define.ts
</context>

<constraints>
- ESM-only; `erasableSyntaxOnly: true` (no constructor parameter properties, explicit class fields).
- All relative imports include the `.ts` extension (repo convention).
- Every Vite build must externalize `lit`, `lit/*`, and `@tanstack/*` (CLAUDE.md).
- **Unidirectional deps:** router does NOT declare `@willram/kit` (verified: `packages/router/package.json` devDependencies = jsdom, lit, typescript, vite, vitest). Do NOT add a cross-package dep — replicate the tiny guard in `packages/router/src/define.ts`.
- No `build.js` changes: the per-entry self-contained bundling is intentional (D-03/BUILD-03 keeps registrations inside the sideEffects-allowlisted entries). The fix must make double registration *safe*, not merge the entries.
- Do not touch `packages/router/vite.config.ts` (WR-03 dead-config warning) or `engine.ts` read-accessor bugs (WR-01) — both are out of scope for these two gaps (deferred to Phase 2).
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Externalize all lit/* in @willram/forms (closes CR-01 / GAP 1)</name>
  <files>packages/forms/vite.config.ts</files>
  <read_first>
    - packages/forms/vite.config.ts — the broken external array (lines 14–23): lists `lit`, `lit/directive.js`, `lit/async-directive.js`, `/^@lit/`, `/^@tanstack/`, `/^zod/`. It omits `lit/decorators.js` and has no `/^lit\//` catch-all; `/^@lit/` matches the `@lit/*` scope only, NOT bare `lit/*`.
    - packages/query/vite.config.ts — CORRECT reference: externalizes `lit/decorators.js` explicitly.
    - packages/kit/vite.config.ts and packages/store/vite.config.ts — CORRECT reference: use `['lit', /^lit\//]`, the catch-all shape to mirror.
    - packages/forms/src/lit-form.ts (line 2) — imports `customElement` from `lit/decorators.js`; this is the un-externalized import Rollup currently inlines.
  </read_first>
  <action>
    In `packages/forms/vite.config.ts`, replace the enumerated `lit` subpaths in `build.rollupOptions.external` with a `/^lit\//` catch-all so no `lit/*` import can ever be inlined. Set the external array, in order, to: the bare specifier `lit`, then the regex `/^lit\//`, then `/^@lit/`, then `/^@tanstack/`, then `/^zod/`. Remove the two now-redundant string entries `lit/directive.js` and `lit/async-directive.js` (subsumed by `/^lit\//`). Keep `lit` bare (bare-specifier import) and keep `/^@lit/` (the separate `@lit/*` scope). Leave the `lib.entry` (forms + zod), `formats: ['es']`, and `test` blocks untouched. Then rebuild forms so the corrected externalization is reflected in `dist/forms.js`; the `customElement` import from `lit-form.ts` must survive as an external `import ... from "lit/decorators.js"` with no inlined shim body remaining.
  </action>
  <verify>
    <automated>npm run build -w @willram/forms && grep -q 'lit/decorators.js' packages/forms/dist/forms.js && ! grep -q 'node_modules/lit/' packages/forms/dist/forms.js</automated>
  </verify>
  <acceptance_criteria>
    - `packages/forms/vite.config.ts` `external` array equals `['lit', /^lit\//, /^@lit/, /^@tanstack/, /^zod/]` (order not significant; no enumerated `lit/*` string entries remain).
    - After `npm run build -w @willram/forms`: `grep -q 'lit/decorators.js' packages/forms/dist/forms.js` succeeds (external import present).
    - `! grep -q 'node_modules/lit/' packages/forms/dist/forms.js` succeeds — no inlined `lit/*` module region remains (the empirically-confirmed inlined decorators shim from VERIFICATION is gone). This negative check discriminates the external import (`lit/decorators.js`) from the old inlined region (which referenced a `node_modules/...` path).
  </acceptance_criteria>
  <reversibility rating="reversible">One-line externals-config change; revert restores the prior array.</reversibility>
  <done>`dist/forms.js` externalizes `lit/decorators.js` (and any other `lit/*` subpath) with no inlined lit module body; forms build stays exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Idempotent router element registration via local define() guard (closes CR-02 / GAP 2 — the fix)</name>
  <files>packages/router/src/define.ts, packages/router/src/router-lit/router-outlet.ts, packages/router/src/router-lit/router-link.ts, packages/router/src/router-lit/router-provider.ts</files>
  <read_first>
    - packages/kit/src/define.ts — the idempotent guard to replicate: `if (!customElements.get(tag)) customElements.define(tag, ctor, options)`. Router MUST NOT import this from kit (no `@willram/kit` dep); copy the tiny function into router.
    - packages/router/src/router-lit/router-outlet.ts (line 2, line 8) — imports `{ customElement, property, state }` from `lit/decorators.js`; registers via bare `@customElement("router-outlet")`.
    - packages/router/src/router-lit/router-link.ts (line 2, line 24) — imports `{ customElement, property }`; `@customElement("router-link")`.
    - packages/router/src/router-lit/router-provider.ts (line 2, line 20) — imports `{ customElement, property }`; `@customElement("router-provider")`.
    - packages/router/src/index.ts (lines 53–68) — aggregate entry re-exports RouterOutlet/RouterProvider/RouterLink; this re-export is intentional (public API) and STAYS. The fix keeps the classes exported and makes their registration idempotent instead.
    - packages/router/scripts/build.js — emits self-contained dist/router.js + dist/router-lit.js (both in sideEffects allowlist); unchanged by this task.
    - packages/router/tsconfig.build.json — includes `src/index.ts`, `src/router-core`, `src/router-lit`; `src/define.ts` is picked up transitively via the element imports and gets a `.d.ts` emitted. No tsconfig change needed.
  </read_first>
  <action>
    Chosen approach (justification): Option A — route registration through an idempotent guard — over Option B (stop re-exporting element classes from `src/index.ts`). Rationale: the review names guarding "the more robust option"; it makes double-import safe regardless of the per-entry bundling strategy, preserves the public `RouterOutlet`/`RouterProvider`/`RouterLink` exports from the aggregate entry (Option B would be a breaking API removal), and mirrors the established `packages/kit/src/define.ts` pattern already used in router tests. Because router must not depend on `@willram/kit`, replicate the guard locally.

    Step 1 — Create `packages/router/src/define.ts`: export a function `define(tag: string, ctor: CustomElementConstructor, options?: ElementDefinitionOptions): void` that calls `customElements.define(tag, ctor, options)` only when `customElements.get(tag)` is falsy. Copy the shape from `packages/kit/src/define.ts` verbatim (JSDoc + guard); explicit params, no constructor parameter properties.

    Step 2 — In each of `router-outlet.ts`, `router-link.ts`, `router-provider.ts`: remove `customElement` from the `lit/decorators.js` import destructure (keep `property`, and `state` where present); add `import { define } from "../define.ts";`; delete the `@customElement("router-...")` decorator line above the class; and after the class declaration body (before the type-only `declare global` block) add a module-scope call registering the class through the guard — `define("router-outlet", RouterOutlet)`, `define("router-link", RouterLink)`, `define("router-provider", RouterProvider)` respectively. Keep all `@property`/`@state` decorators unchanged. Do not alter class bodies, exports, or the `declare global` HTMLElementTagNameMap augmentations.

    Registration remains an import-time module side effect (retained because dist entries are sideEffects-allowlisted), so BUILD-03 tree-shaking survival is preserved; only the second define across shipped entries becomes a no-op.
  </action>
  <verify>
    <automated>npm run typecheck -w @willram/router && npm run build -w @willram/router && ! grep -q '@customElement' packages/router/src/router-lit/router-outlet.ts && ! grep -q '@customElement' packages/router/src/router-lit/router-link.ts && ! grep -q '@customElement' packages/router/src/router-lit/router-provider.ts && grep -q 'define("router-outlet"' packages/router/src/router-lit/router-outlet.ts && grep -q 'router-outlet' packages/router/dist/router.js && grep -q 'router-outlet' packages/router/dist/router-lit.js</automated>
  </verify>
  <acceptance_criteria>
    - `packages/router/src/define.ts` exists and exports `define(tag, ctor, options?)` guarding on `customElements.get(tag)`; router still declares no `@willram/kit` dependency.
    - None of the three element files contains `@customElement` any longer: `! grep -q '@customElement' packages/router/src/router-lit/router-outlet.ts` (and `router-link.ts`, `router-provider.ts`) all succeed.
    - Each element file registers via the guard: `grep -q 'define("router-outlet"' packages/router/src/router-lit/router-outlet.ts` (and the analogous `router-link`/`router-provider` calls) succeed.
    - `npm run typecheck -w @willram/router` exits 0 (no unused-import / decorator errors) and `npm run build -w @willram/router` exits 0.
    - Registration survives the build (BUILD-03 unregressed): `grep -q 'router-outlet' packages/router/dist/router.js` and `grep -q 'router-outlet' packages/router/dist/router-lit.js` both succeed.
  </acceptance_criteria>
  <reversibility rating="reversible">Config/code-only change; reverting restores the `@customElement` decorators and deletes `define.ts`.</reversibility>
  <done>All three router elements register through the idempotent guard; both shipped dist entries still register each tag, and typecheck/build stay exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke test — importing both router entries does not double-register (closes CR-02 / GAP 2 — the check)</name>
  <files>packages/router/src/test/no-double-register.test.ts</files>
  <read_first>
    - packages/router/src/test/router-outlet.test.ts (lines 1–19) — existing router test conventions: `import { describe, it, expect } from 'vitest'`, jsdom env, and the already-used idempotent `if (!customElements.get(tag)) customElements.define(...)` pattern.
    - packages/router/scripts/build.js — confirms dist emits `dist/router.js` (from `src/index.ts`) and `dist/router-lit.js` (from `src/router-lit/index.ts`) as SEPARATE self-contained bundles. The double-registration only reproduces across these two built bundles (ESM dedupe hides it at source level), so the test MUST import the built dist entries, not source.
    - packages/router/package.json (line 48: `"test": "vitest run"`) — the default suite runs this file; jsdom supplies the DOM globals Lit needs.
  </read_first>
  <action>
    Create `packages/router/src/test/no-double-register.test.ts` proving that importing both shipped router entries is safe after the Task 2 guard.

    Import `describe, it, expect` from `vitest`, `existsSync` from `node:fs`, and `fileURLToPath` from `node:url` (with `new URL(..., import.meta.url)`) to compute absolute paths to the two built entries relative to this file: `../../dist/router.js` and `../../dist/router-lit.js`. Guard the whole suite with `describe.skipIf(!(existsSync(routerJs) && existsSync(routerLitJs)))(...)` so a bare `npm test` without a prior build skips cleanly rather than false-failing (the plan verification builds first, so the test actually runs and asserts).

    Inside the suite, one async test: dynamically `await import('../../dist/router.js')` then `await import('../../dist/router-lit.js')` in sequence within the same jsdom registry. Reaching the assertions without a thrown `DOMException` is the pass condition (a double `customElements.define` on the same tag would reject the second import and fail the test). Then assert `customElements.get('router-outlet')`, `customElements.get('router-link')`, and `customElements.get('router-provider')` are each truthy (registration present and idempotent). Use static string specifiers in the dynamic imports so Vite can resolve them.
  </action>
  <verify>
    <automated>npm run build -w @willram/router && npm run test -w @willram/router</automated>
  </verify>
  <acceptance_criteria>
    - `packages/router/src/test/no-double-register.test.ts` exists and, after `npm run build -w @willram/router`, runs (not skipped) and passes under `npm run test -w @willram/router` (whole router suite green).
    - The test imports the two BUILT dist entries (`../../dist/router.js` and `../../dist/router-lit.js`), not source modules.
    - The test asserts `customElements.get('router-outlet' | 'router-link' | 'router-provider')` are all truthy and that neither import threw a duplicate-definition `DOMException`.
    - Reverting Task 2 (restoring bare `@customElement`) makes this test throw on the second import — i.e. the test genuinely discriminates the fix.
  </acceptance_criteria>
  <reversibility rating="reversible">Test-only addition; removable with no runtime impact.</reversibility>
  <done>A green smoke test proves importing both `@willram/router` and `@willram/router/lit` registers each element exactly once with no duplicate-definition throw.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New / changed symbols and files introduced by this gap-closure plan:

**New files**
- `packages/router/src/define.ts` — exports `define(tag: string, ctor: CustomElementConstructor, options?: ElementDefinitionOptions): void`, a local idempotent registration guard (mirrors `packages/kit/src/define.ts`; deliberately NOT imported from kit to respect the unidirectional dep constraint).
- `packages/router/src/test/no-double-register.test.ts` — vitest+jsdom smoke test importing the two built dist entries and asserting single, throw-free registration.

**Changed files**
- `packages/forms/vite.config.ts` — `build.rollupOptions.external` becomes `['lit', /^lit\//, /^@lit/, /^@tanstack/, /^zod/]` (enumerated `lit/*` string entries removed in favor of the `/^lit\//` catch-all).
- `packages/router/src/router-lit/router-outlet.ts` — drops `customElement` from the `lit/decorators.js` import and the `@customElement("router-outlet")` decorator; imports `define` from `../define.ts` and calls `define("router-outlet", RouterOutlet)` at module scope.
- `packages/router/src/router-lit/router-link.ts` — same conversion for `router-link` / `RouterLink`.
- `packages/router/src/router-lit/router-provider.ts` — same conversion for `router-provider` / `RouterProvider`.

**Rebuilt artifacts (not committed source, but the observable outputs the gaps assert on)**
- `packages/forms/dist/forms.js` — `lit/decorators.js` external, no inlined `lit/*` region.
- `packages/router/dist/router.js`, `packages/router/dist/router-lit.js` — each still registers all three tags, now through the idempotent guard.

No public API is removed: `RouterOutlet` / `RouterProvider` / `RouterLink` remain exported from `packages/router/src/index.ts` and `./router-lit`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published bundle → consumer app | The only boundary in scope: Rollup-emitted `dist/*.js` is imported and its module-scope `customElements.define` side effects execute in the consumer's browser/jsdom. |

Both changes are build-config and custom-element-registration hardening. They introduce **no new external runtime inputs, no new network/IO surface, no new dependencies** (the router guard is a local file; forms only edits an externals list). Attack surface is unchanged from the already-verified baseline.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-D | Denial of Service | router element registration | low | mitigate | This plan *removes* a real availability defect: the duplicate-definition `DOMException` that crashes a consumer importing both router entries. The idempotent guard makes the second define a no-op (fixed, not introduced). |
| T-01-T | Tampering | forms bundle externalization | low | accept | Externalizing `lit/*` reduces (not adds) duplicated code in the consumer bundle; no new tamperable input. No new supply-chain risk — **no package installs in this plan** (local file + config edit only), so the package-legitimacy gate does not apply. |

No fabricated threats: this plan adds no parsing, deserialization, auth, secrets, or network paths.
</threat_model>

<verification>
Run from the repo root after all three tasks, in this order — all must be exit 0 and the two dist-level checks must be passing:

1. `npm run typecheck` — exit 0 across all five packages (unregressed).
2. `npm run build -w @willram/forms` then `npm run build -w @willram/router` — targeted rebuilds, exit 0.
3. `npm run build` — full workspace build, exit 0, all dist emitted, no `.cjs`.
4. `npm run typecheck:smoke` — exit 0 (both node16 + bundler; 8 subpaths still resolve).
5. `npm run test -w @willram/router` — exit 0, including the new `no-double-register` smoke test (runs, not skipped).
6. Forms dist assertion (CR-01 flips to passing): `grep -q 'lit/decorators.js' packages/forms/dist/forms.js && ! grep -q 'node_modules/lit/' packages/forms/dist/forms.js`.
7. Router dist assertion (CR-02 registration survives): `grep -q 'router-outlet' packages/router/dist/router.js && grep -q 'router-outlet' packages/router/dist/router-lit.js`.
</verification>

<success_criteria>
- CR-01 closed: `@willram/forms` externalizes `lit/decorators.js` (and all `lit/*`); `dist/forms.js` inlines no lit module body.
- CR-02 closed: router registers each element exactly once across shipped entries; importing both `@willram/router` and `@willram/router/lit` does not throw; a smoke test proves it.
- No regression: `npm run typecheck`, `npm run build`, and `npm run typecheck:smoke` all stay exit 0; the five original ROADMAP success criteria remain satisfied.
- Public API unchanged; no new dependency added to router.
</success_criteria>

<output>
Create `.planning/phases/01-build-typecheck-hardening/01-04-SUMMARY.md` when done.
</output>