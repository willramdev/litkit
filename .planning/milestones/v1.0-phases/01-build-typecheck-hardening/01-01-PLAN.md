---
phase: 01-build-typecheck-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/router/package.json
  - packages/router/vite.config.ts
  - packages/router/scripts/build.js
  - tools/typecheck-smoke/consumer-router.ts
  - tools/typecheck-smoke/tsconfig.node16.json
  - tools/typecheck-smoke/tsconfig.bundler.json
  - package.json
autonomous: true
requirements: [BUILD-05, BUILD-03, BUILD-02, BUILD-06, BUILD-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "`npm run build -w @willram/router` exits 0 and emits dist/router.js, dist/router-core.js, dist/router-lit.js with matching .d.ts"
    - "router dist contains no .cjs file (ESM-only policy applied, D-01/BUILD-05)"
    - "router package.json exports has no `require` condition on any subpath"
    - "router `sideEffects` names the built element entries dist/router.js and dist/router-lit.js and NOT dist/router-core.js"
    - "a tsc smoke consumer resolves router `.`, `./core`, `./lit` .d.ts under both node16 and bundler resolution (both tsc runs exit 0)"
  artifacts:
    - packages/router/package.json
    - packages/router/vite.config.ts
    - tools/typecheck-smoke/consumer-router.ts
    - tools/typecheck-smoke/tsconfig.node16.json
    - tools/typecheck-smoke/tsconfig.bundler.json
  key_links:
    - "router exports `types` condition path → the .d.ts tsc actually emits (dist/index.d.ts, dist/router-core/index.d.ts, dist/router-lit/index.d.ts)"
    - "sideEffects allowlist path strings → the actual Vite-emitted entry filenames (dist/router.js, dist/router-lit.js)"
---

<objective>
Prove the whole Phase-1 hardening pipeline end-to-end on the hardest package (router) before expanding to the other four. Router is the only package whose module-format decision (D-01: ESM-only) changes its published `exports` shape, and it is the only package with subpaths (`./core`, `./lit`), so it exercises every layer this phase touches: package.json config, Vite build, tsc `.d.ts` emit, and dual-resolution (`node16` + `bundler`) `.d.ts` verification.

Purpose: De-risk the BUILD-06 smoke-consumer architecture and the ESM-only `exports` reshape on one package, so Plans 02-03 expand a proven pattern instead of discovering a dead-end after five packages are already changed.
Output: Router built ESM-only with a `sideEffects` allowlist naming its real element entries; a reusable `tools/typecheck-smoke/` harness that resolves router's three subpaths under both `node16` and `bundler`; a root `typecheck:smoke` script.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-typecheck-hardening/01-CONTEXT.md
@.claude/CLAUDE.md
@packages/router/package.json
@packages/router/vite.config.ts
@packages/router/scripts/build.js
@packages/router/tsconfig.build.json
</context>

## Artifacts this phase produces (this plan)

- `packages/router/package.json` — `main` → `./dist/router.js`; `exports` `.`/`./core`/`./lit` with `require` conditions removed (ESM-only, D-01/BUILD-05); `sideEffects` changed from `false` to `["dist/router.js", "dist/router-lit.js"]` (D-03/BUILD-03); `build` script changed to `vite build && tsc -p tsconfig.build.json`.
- `packages/router/vite.config.ts` — `build.lib.formats` changed from `["es", "cjs"]` to `["es"]`.
- `packages/router/scripts/build.js` — deleted (dual-build machinery removed, D-01).
- `tools/typecheck-smoke/consumer-router.ts` — new BUILD-06 smoke consumer covering router `.`/`./core`/`./lit`.
- `tools/typecheck-smoke/tsconfig.node16.json`, `tools/typecheck-smoke/tsconfig.bundler.json` — new dual-resolution tsconfigs (`moduleResolution: node16` and `bundler`, `noEmit`).
- root `package.json` — new `typecheck:smoke` script.

<tasks>

<task type="tracer">
  <name>Task 1: Router ESM-only — config + build (tracer, one package through every layer)</name>
  <reversibility rating="costly">D-01 (locked in CONTEXT): re-adding CJS later means restoring the dual-build machinery and cutting a fresh router release once router-core is published — a coordinated change across build config and a published contract, but no data migration. Decision already made by the developer; do not re-litigate.</reversibility>
  <files>packages/router/package.json, packages/router/vite.config.ts, packages/router/scripts/build.js</files>
  <read_first>
    - packages/router/package.json (current dual exports map with `require` conditions, `main` → `./dist/router.cjs`, `sideEffects: false`, `build` script that runs `node scripts/build.js`)
    - packages/router/vite.config.ts (multi-entry lib: `router`, `router-core`, `router-lit`; `formats: ["es", "cjs"]`)
    - packages/router/scripts/build.js (the dual es+cjs build loop to delete)
    - packages/router/tsconfig.build.json (emits dist/index.d.ts + dist/router-core/index.d.ts + dist/router-lit/index.d.ts — the `types` targets)
    - .claude/CLAUDE.md (constraint: every Vite build must externalize `lit`, `lit/*`, `@tanstack/*`; ESM-only decision is repo policy)
  </read_first>
  <action>
    Apply the ESM-only module-format policy (D-01/BUILD-05) to router and only router. In packages/router/vite.config.ts change `build.lib.formats` from `["es", "cjs"]` to `["es"]`, leaving the three entries (`router`, `router-core`, `router-lit`) and the `lit`/`lit/*` externals unchanged. Delete packages/router/scripts/build.js entirely. In packages/router/package.json: change the `build` script to `vite build && tsc -p tsconfig.build.json` (matching the other four packages, dropping the `node scripts/build.js` step and the redundant leading `tsc --noEmit`); set `main` to `./dist/router.js`; in the `exports` map remove the `require` condition from all three entries (`.`, `./core`, `./lit`) so each entry has only `types` (first) then `import`; keep the `types` paths exactly as-is (`./dist/index.d.ts`, `./dist/router-core/index.d.ts`, `./dist/router-lit/index.d.ts`). Also in packages/router/package.json set `sideEffects` from `false` to the array `["dist/router.js", "dist/router-lit.js"]` — these are the two built entries that carry the `@customElement` registrations for `router-link`/`router-outlet`/`router-provider` (router-lit modules, re-exported through the main entry), per D-03; do NOT list `dist/router-core.js` because router-core is framework-neutral and must stay tree-shakeable. Then run the router build and confirm the emitted dist matches the new policy.
  </action>
  <verify>
    <automated>npm run build -w @willram/router && node -e "const fs=require('fs');const d='packages/router/dist';const j=fs.readdirSync(d);if(j.some(f=>f.endsWith('.cjs')))throw new Error('cjs emitted');for(const f of ['router.js','router-core.js','router-lit.js','index.d.ts'])if(!j.includes(f))throw new Error('missing '+f);const p=require('./packages/router/package.json');const s=JSON.stringify(p.exports);if(s.includes('require'))throw new Error('require condition still present');if(JSON.stringify(p.sideEffects)!=='[\"dist/router.js\",\"dist/router-lit.js\"]')throw new Error('sideEffects wrong');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build -w @willram/router` exits 0.
    - `packages/router/dist` contains `router.js`, `router-core.js`, `router-lit.js`, and `index.d.ts`, and contains NO `*.cjs` file.
    - `packages/router/scripts/build.js` no longer exists.
    - `packages/router/package.json` `exports` (serialized) contains no `require` key; `main` equals `./dist/router.js`.
    - `packages/router/package.json` `sideEffects` equals `["dist/router.js", "dist/router-lit.js"]`.
    - `grep -q 'router-outlet' packages/router/dist/router-lit.js` succeeds (the element registration lives in the allowlisted entry).
  </acceptance_criteria>
  <done>Router builds ESM-only (no `.cjs`), its `exports` has no `require` conditions, and its `sideEffects` allowlist names the two element-carrying built entries while leaving router-core tree-shakeable.</done>
</task>

<task type="auto">
  <name>Task 2: BUILD-06 smoke-consumer harness — router subpaths under node16 + bundler</name>
  <files>tools/typecheck-smoke/consumer-router.ts, tools/typecheck-smoke/tsconfig.node16.json, tools/typecheck-smoke/tsconfig.bundler.json, package.json</files>
  <read_first>
    - packages/router/package.json (the just-reshaped `exports` map — the `types` condition targets the smoke consumer must resolve)
    - packages/router/src/index.ts (real exported symbols to import by name: e.g. `createRouter`, `RouterOutlet`, and a core-only symbol such as `CompiledPathMatcher`)
    - tsconfig.base.json (repo compiler baseline; the smoke tsconfigs must NOT set `allowImportingTsExtensions` and must resolve via package `exports`, not workspace `src`)
    - .planning/research/PITFALLS.md section "Types resolution: `types` basename ≠ JS basename, and subpath d.ts" (why node16 follows `exports` conditions strictly)
  </read_first>
  <action>
    Create the reusable BUILD-06 smoke consumer under a committed (non-`dist`, non-workspace) directory `tools/typecheck-smoke/`. Write consumer-router.ts that imports representative real symbols from all three router subpaths so an unresolved subpath becomes a hard TS2307 error rather than a silent `any`: a value + type from `@willram/router` (e.g. `createRouter` and type `Router`), a Lit-only symbol from `@willram/router/lit` (e.g. `RouterOutlet`), and a core-only symbol from `@willram/router/core` (e.g. `CompiledPathMatcher`); reference each imported binding (e.g. in a `void` expression or a typed local) so `noUnusedLocals` does not strip the import. Write two tsconfigs that both set `noEmit: true`, `strict: true`, `skipLibCheck: true`, `types: []`, `module: nodenext` for node16 / `esnext` for bundler, and `include: ["consumer-router.ts"]`: tsconfig.node16.json sets `moduleResolution: "node16"`; tsconfig.bundler.json sets `moduleResolution: "bundler"`. Neither tsconfig may set `allowImportingTsExtensions` (that would let it resolve workspace `src` and defeat the exports-map check). Add a root package.json script `typecheck:smoke` that runs `tsc -p tools/typecheck-smoke/tsconfig.node16.json && tsc -p tools/typecheck-smoke/tsconfig.bundler.json`. Router must already be built (Task 1) so `node_modules/@willram/router` resolves to `dist` via its `exports` map.
  </action>
  <verify>
    <automated>npm run typecheck:smoke</automated>
  </verify>
  <acceptance_criteria>
    - `tsc -p tools/typecheck-smoke/tsconfig.node16.json` exits 0.
    - `tsc -p tools/typecheck-smoke/tsconfig.bundler.json` exits 0.
    - `npm run typecheck:smoke` (both runs chained) exits 0.
    - Sanity: temporarily importing from a non-existent subpath `@willram/router/nope` makes both tsc runs fail with TS2307 (confirming the harness detects unresolved subpaths, then revert the probe).
    - `tools/typecheck-smoke/tsconfig.node16.json` contains `"moduleResolution": "node16"`; `tools/typecheck-smoke/tsconfig.bundler.json` contains `"moduleResolution": "bundler"`.
  </acceptance_criteria>
  <done>A committed smoke harness resolves router `.`, `./core`, and `./lit` `.d.ts` under both `node16` and `bundler`, runnable via `npm run typecheck:smoke`, and provably fails on an unresolved subpath.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| litkit published package → consumer bundler | Consumer's Rollup/Vite reads router's `sideEffects` + `exports` to decide tree-shaking and module resolution |
| litkit build config → published `dist` contract | Vite `formats` + tsc `.d.ts` emit define the surface consumers resolve against |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | router `sideEffects` field | high | mitigate | Allowlist `dist/router.js` + `dist/router-lit.js` (the element-registering entries) instead of `false`, so a consumer prod build cannot tree-shake `customElements.define`; verified by grep of the built entry (Phase-1 bar; runtime bar is VER-02) |
| T-01-02 | Tampering | router `exports` map (ESM-only reshape) | medium | mitigate | Keep `types` as first condition on every subpath; smoke consumer proves `node16`+`bundler` resolution so removing `require` cannot silently break a subpath's `.d.ts` |
| T-01-SC | Tampering | package installs | low | accept | No new package-manager installs in this plan — router change is config-only; `@tanstack/*` untouched here. No package-legitimacy gate required. |
</threat_model>

<verification>
- `npm run build -w @willram/router` exits 0; dist is ESM-only (no `.cjs`) with the three `.js` entries and their `.d.ts`.
- `npm run typecheck:smoke` exits 0 (router `.`/`./core`/`./lit` resolve under node16 and bundler).
- `npm run typecheck -w @willram/router` exits 0 (BUILD-01 confirmed green for router).
</verification>

<success_criteria>
- Router applies the ESM-only policy (D-01/BUILD-05): no `.cjs`, no `require` conditions.
- Router `sideEffects` allowlists its two element-carrying built entries (D-03/BUILD-03) and leaves router-core tree-shakeable.
- The BUILD-06 smoke harness exists, is wired to `npm run typecheck:smoke`, and resolves all three router subpaths under both resolution modes.
</success_criteria>

<output>
Create `.planning/phases/01-build-typecheck-hardening/01-01-SUMMARY.md` when done.
</output>
