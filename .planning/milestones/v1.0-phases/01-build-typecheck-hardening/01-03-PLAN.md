---
phase: 01-build-typecheck-hardening
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - packages/kit/package.json
  - packages/store/package.json
  - tools/typecheck-smoke/consumer-rest.ts
  - tools/typecheck-smoke/tsconfig.node16.json
  - tools/typecheck-smoke/tsconfig.bundler.json
autonomous: true
requirements: [BUILD-02, BUILD-06, BUILD-01, BUILD-03]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "kit and store register no production custom elements and correctly stay `sideEffects: false`"
    - "`npm run build` exits 0 and every package emits a dist/*.js entry"
    - "`npm run typecheck` exits 0 across all five packages"
    - "packages/query/src/query-controller.ts contains no `any` (D-04 query portion confirmed complete)"
    - "the smoke consumer resolves a .d.ts for all eight exports subpaths (kit ., store ., query ., forms ., forms ./zod, router ., ./core, ./lit) under both node16 and bundler"
  artifacts:
    - tools/typecheck-smoke/consumer-rest.ts
    - tools/typecheck-smoke/tsconfig.node16.json
    - tools/typecheck-smoke/tsconfig.bundler.json
  key_links:
    - "smoke consumer resolution ↔ every package being built first (BUILD-06 depends on BUILD-02 dist present)"
    - "forms `./zod` .d.ts ↔ zod resolvable from the hoisted workspace devDependency"
---

<objective>
Close the phase with the goal-backward verification sweep: confirm the two element-free packages (kit, store) correctly stay `sideEffects: false`, prove the entire workspace is green (`npm run typecheck` and `npm run build` across all five), and extend the tracer's smoke harness so it resolves a `.d.ts` for every one of the eight `exports` subpaths under both `node16` and `bundler` resolution.

Purpose: This is the "nothing downstream can trust without it" gate — the single place that proves all five success criteria hold together, not package-by-package. It is the Phase-1 analogue of the Phase-5 tarball verification, done here against the built workspace.
Output: kit/store confirmed, a full green build+typecheck, and a smoke consumer covering all eight subpaths in both resolution modes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-typecheck-hardening/01-CONTEXT.md
@.claude/CLAUDE.md
@packages/kit/package.json
@packages/store/package.json
@tools/typecheck-smoke/tsconfig.node16.json
</context>

## Artifacts this phase produces (this plan)

- `packages/kit/package.json` / `packages/store/package.json` — `sideEffects: false` confirmed (or an allowlist added only if a production `@customElement` is found; none expected).
- `tools/typecheck-smoke/consumer-rest.ts` — new smoke consumer covering kit `.`, store `.`, query `.`, forms `.`, and forms `./zod`.
- `tools/typecheck-smoke/tsconfig.node16.json` / `tsconfig.bundler.json` — `include` broadened to `["*.ts"]` so both consumer files (router from Plan 01 + rest) are checked.

<tasks>

<task type="auto">
  <name>Task 1: Confirm kit + store correctly stay sideEffects:false</name>
  <files>packages/kit/package.json, packages/store/package.json</files>
  <read_first>
    - packages/kit/src/index.ts and packages/kit/src/define.ts (define.ts is an idempotent `customElements.define` HELPER function, not a top-level registration — importing it registers nothing)
    - packages/store/src/index.ts (store, store-slice, derived — no elements)
    - .planning/phases/01-build-typecheck-hardening/01-CONTEXT.md decision D-03 ("kit and store register no production elements — they stay `sideEffects: false`")
  </read_first>
  <action>
    Confirm the D-03 carve-out for the two element-free packages. Grep both kit and store `src` trees (excluding `*.test.ts`) for any top-level `@customElement(` decorator (the import-time registration pattern) — there must be none, because kit's `define` is a helper that only registers when a consumer calls it, so it is not a package-level side effect. Leave `packages/kit/package.json` and `packages/store/package.json` at `"sideEffects": false`. Only if a genuine top-level `@customElement` is discovered in either src tree, add a precise allowlist for that built entry and note it in the SUMMARY; otherwise make no edit and record the confirmation in the SUMMARY.
  </action>
  <verify>
    <automated>node -e "const k=require('./packages/kit/package.json');const s=require('./packages/store/package.json');if(k.sideEffects!==false)throw new Error('kit sideEffects changed unexpectedly');if(s.sideEffects!==false)throw new Error('store sideEffects changed unexpectedly');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/kit/package.json` and `packages/store/package.json` both keep `"sideEffects": false`.
    - `grep -rn "@customElement(" packages/kit/src packages/store/src --include="*.ts" | grep -v "\.test\.ts"` returns no matches (no production element registration in either package).
  </acceptance_criteria>
  <done>kit and store are confirmed element-free and correctly retain `sideEffects: false` per D-03.</done>
</task>

<task type="auto">
  <name>Task 2: Full-workspace green build + typecheck (BUILD-01, BUILD-02)</name>
  <files>packages/query/src/query-controller.ts</files>
  <read_first>
    - package.json (root — `build` = `npm run build --workspaces`, `typecheck` = `npm run typecheck --workspaces --if-present`)
    - packages/query/src/query-controller.ts (already `any`-free after the fix/typecheck-query-derived work — this task confirms the D-04 query portion needs no further change)
    - .planning/ROADMAP.md Phase 1 success criteria 1 and 2 (typecheck + build green across all five)
  </read_first>
  <action>
    Prove the whole workspace is green after the config changes from Plans 01-02. Run `npm run build` at the repo root and confirm every package emits its `dist/*.js` entry (kit.js, store.js, query.js, forms.js + zod.js, router.js + router-core.js + router-lit.js) with no `.cjs` anywhere (ESM-only policy). Run `npm run typecheck` at the repo root and confirm zero errors across all five packages. Confirm the D-04 query portion is already complete: `packages/query/src/query-controller.ts` carries no `any` type annotation or `as any` cast (the fix/typecheck-query-derived work landed this) — make no change to that file unless the grep finds a real `any`, in which case reduce it within the D-04 bounded scope (no public-type change). This task changes no config; it is the phase's build/typecheck gate.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build && node -e "const fs=require('fs');const map={kit:['kit.js'],store:['store.js'],query:['query.js'],forms:['forms.js','zod.js'],router:['router.js','router-core.js','router-lit.js']};for(const[p,files]of Object.entries(map)){const d='packages/'+p+'/dist';const j=fs.readdirSync(d);for(const f of files)if(!j.includes(f))throw new Error(p+' missing '+f);if(j.some(f=>f.endsWith('.cjs')))throw new Error(p+' emitted cjs');}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 (all five packages).
    - `npm run build` exits 0; every package's `dist` contains its expected `*.js` entries and no package emits a `*.cjs` file.
    - `grep -nE ':\s*any\b|as any|<any>|\bany\[\]' packages/query/src/query-controller.ts` returns no matches (D-04 query portion confirmed complete).
  </acceptance_criteria>
  <done>The full workspace typechecks and builds green with an ESM-only dist surface, and query-controller.ts is confirmed `any`-free.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the BUILD-06 smoke consumer to all eight subpaths (node16 + bundler)</name>
  <files>tools/typecheck-smoke/consumer-rest.ts, tools/typecheck-smoke/tsconfig.node16.json, tools/typecheck-smoke/tsconfig.bundler.json</files>
  <read_first>
    - tools/typecheck-smoke/consumer-router.ts and tools/typecheck-smoke/tsconfig.node16.json (the tracer harness from Plan 01 to extend — same options, broadened include)
    - packages/kit/src/index.ts, packages/store/src/index.ts, packages/query/src/index.ts, packages/forms/src/index.ts, packages/forms/src/zod.ts (to pick a real exported value + type from each entry so an unresolved subpath is a hard TS2307, not silent `any`)
    - .planning/REQUIREMENTS.md BUILD-06 (every subpath must resolve a `.d.ts` under node16 AND bundler, verified by tsc resolution not file-presence)
  </read_first>
  <action>
    Extend the tracer smoke harness to cover the remaining five subpaths. Create tools/typecheck-smoke/consumer-rest.ts importing a real exported value + a type from each of: `@willram/kit`, `@willram/store` (e.g. `createStore` + type `Store`), `@willram/query` (e.g. `createQueryClient` + type `QueryControllerConfig`), `@willram/forms` (e.g. `createForm`/`form` + a form type), and `@willram/forms/zod` (the exported zod adapter — read `packages/forms/src/zod.ts` for the real symbol name); reference each imported binding so `noUnusedLocals` does not strip it. Broaden the `include` array in both tools/typecheck-smoke/tsconfig.node16.json and tsconfig.bundler.json from the router-only entry to `["*.ts"]` so both `consumer-router.ts` and `consumer-rest.ts` are checked. Do not otherwise change the tsconfig options (node16 / bundler `moduleResolution`, `noEmit`, no `allowImportingTsExtensions`). All five packages must already be built (Task 2) so each `@willram/*` resolves to `dist` via its `exports` map; forms `./zod` resolves zod types from the hoisted workspace devDependency.
  </action>
  <verify>
    <automated>npm run typecheck:smoke</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck:smoke` exits 0 (both node16 and bundler runs, covering both consumer files → all eight subpaths).
    - `tools/typecheck-smoke/consumer-rest.ts` imports from `@willram/kit`, `@willram/store`, `@willram/query`, `@willram/forms`, and `@willram/forms/zod`.
    - Both tsconfigs' `include` is `["*.ts"]`.
    - Sanity: temporarily changing one import in consumer-rest.ts to a non-existent subpath makes both tsc runs fail with TS2307, then revert.
  </acceptance_criteria>
  <done>The smoke consumer resolves a `.d.ts` for all eight `exports` subpaths under both `node16` and `bundler`, satisfying BUILD-06 for the entire workspace.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| litkit `exports`/`types` maps → consumer TypeScript resolver | A consumer on `node16`/`nodenext` follows `exports` conditions strictly; a subpath whose `.d.ts` does not resolve degrades to `any` or a build error |
| built `dist` → published contract | The green build here is the artifact Phase 4 publishes; a stale/partial dist would ship broken |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Spoofing | subpath `types` resolution (all eight subpaths) | high | mitigate | tsc smoke consumer resolves every subpath under both node16 and bundler, so a missing/misordered `types` condition fails CI-visibly instead of silently degrading a consumer to `any` |
| T-01-06 | Tampering | kit/store `sideEffects` | low | accept | No element registration exists in either package; staying `false` is correct and grep-verified — no consumer tree-shaking risk to mitigate |
| T-01-SC | Tampering | package installs | low | accept | No package installs in this plan — verification-only against the already-built workspace. No package-legitimacy gate required. |
</threat_model>

<verification>
- `npm run typecheck` exits 0 (all five packages).
- `npm run build` exits 0 with an ESM-only dist for every package.
- `npm run typecheck:smoke` exits 0 (all eight subpaths resolve under node16 and bundler).
- kit/store confirmed element-free and `sideEffects: false`.
</verification>

<success_criteria>
- All five success criteria of the phase hold together: typecheck green (1), build green with dist (2), sideEffects allowlist + TanStack peers in place across Plans 01-03 (3), ESM-only module policy applied (4), and every subpath's `.d.ts` resolves under both resolution modes (5).
</success_criteria>

<output>
Create `.planning/phases/01-build-typecheck-hardening/01-03-SUMMARY.md` when done.
</output>
