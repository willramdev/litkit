---
phase: 05-consumer-install-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-consumer.mjs
  - tools/verify-consumer/package.json.tmpl
  - tools/verify-consumer/consumer-router.ts
  - tools/verify-consumer/consumer-rest.ts
  - tools/verify-consumer/tsconfig.node16.json
  - tools/verify-consumer/tsconfig.bundler.json
  - tools/verify-consumer/src/subpath-smoke.mjs
  - package.json
autonomous: false
requirements: [VER-01, VER-04]
user_setup:
  - service: github-packages
    why: "GitHub Packages requires an access token even to READ/install; there is no anonymous install. VER-01 mandates a classic read:packages PAT for the real-registry run."
    env_vars:
      - name: GITHUB_TOKEN
        source: "GitHub -> Settings -> Developer settings -> Personal access tokens -> Tokens (classic) -> scope read:packages ONLY (fine-grained PATs are NOT supported for the npm registry)"

estimate:
  tokens: 55000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "VER-01: npm install of all five @willramdev/*@1.0.0 from npm.pkg.github.com into an out-of-tree os.tmpdir() consumer succeeds with a classic read:packages PAT, and the resolved @willramdev/kit path is under the temp consumer's node_modules (NOT the workspace)."
    - "VER-04: all eight published entries/subpaths (@willramdev/kit `.`, /store `.`, /query `.`, /forms `.`, /forms/zod, /router `.`, /router/core, /router/lit) resolve to a real .d.ts under `tsc --noEmit` in BOTH node16 and bundler resolution from the installed node_modules, and every one is importable at runtime via `import()`."
  artifacts:
    - scripts/verify-consumer.mjs
    - tools/verify-consumer/package.json.tmpl
    - tools/verify-consumer/consumer-router.ts
    - tools/verify-consumer/consumer-rest.ts
    - tools/verify-consumer/tsconfig.node16.json
    - tools/verify-consumer/tsconfig.bundler.json
    - tools/verify-consumer/src/subpath-smoke.mjs
  key_links:
    - "Temp consumer `.npmrc` scope->registry map + env-expanded `${GITHUB_TOKEN}` — the seam that routes ONLY @willramdev to GitHub Packages (never a global registry=, never public npm) so the real published tarball is fetched."
    - "Resolved-path-under-tmpdir guard — the seam that prevents a false positive from npm workspace shadowing resolving @willramdev/* to local packages/* source."
    - "The eight-target exports/.d.ts map — the seam VER-04 proves survived publish, re-run against the installed tarball instead of workspace symlinks."
---

<objective>
Stand up the committed consumer-install verification harness and prove the two resolution-layer requirements against the REAL published tarballs: VER-01 (clean-machine install from GitHub Packages with a read:packages PAT) and VER-04 (all eight entries/subpaths resolve for tsc under node16+bundler and at runtime).

This plan LEADS with a production-quality end-to-end tracer — `scripts/verify-consumer.mjs` scaffolds a throwaway consumer in `os.tmpdir()` (outside the monorepo so workspace resolution cannot shadow the registry), installs the five packages from `npm.pkg.github.com`, and proves one smoke import — before the VER-04 assertion layer expands out from that proven slice.

Purpose: The installed-from-registry consumer is the ONLY real proof that the Phase 1 correctness fixes survived publish. This harness is committed as reproducible proof.
Output: `scripts/verify-consumer.mjs` orchestrator + `tools/verify-consumer/` fixtures + a `verify:consumer` npm script; VER-01 and VER-04 green against the live registry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-consumer-install-verification/05-RESEARCH.md
@.planning/phases/05-consumer-install-verification/05-VALIDATION.md

# Existing analog harness this plan mirrors against the INSTALLED tarball (not workspace symlinks):
@tools/typecheck-smoke/consumer-router.ts
@tools/typecheck-smoke/consumer-rest.ts
@tools/typecheck-smoke/tsconfig.node16.json
@tools/typecheck-smoke/tsconfig.bundler.json
# Consumer auth/scope shape to copy verbatim (env-expanded token, scope-only mapping):
@.npmrc.example
</context>

<artifacts_produced>
## Artifacts this phase produces (full phase inventory — this plan owns the ones marked [P1])

Paths:
- `scripts/verify-consumer.mjs` — [P1] Node ESM orchestrator: scaffold temp consumer, install, dispatch `--check`, exit-code semantics
- `tools/verify-consumer/package.json.tmpl` — [P1] deps template written into the temp consumer
- `tools/verify-consumer/consumer-router.ts` — [P1] VER-04 type layer, copy of `tools/typecheck-smoke/consumer-router.ts`
- `tools/verify-consumer/consumer-rest.ts` — [P1] VER-04 type layer, copy of `tools/typecheck-smoke/consumer-rest.ts`
- `tools/verify-consumer/tsconfig.node16.json` — [P1] copy of the node16 smoke tsconfig
- `tools/verify-consumer/tsconfig.bundler.json` — [P1] copy of the bundler smoke tsconfig
- `tools/verify-consumer/src/subpath-smoke.mjs` — [P1] VER-04 runtime `import()` of all 8 targets
- `tools/verify-consumer/src/tree-shake-entry.ts` — [P2] VER-02 bare side-effect entry (plan 05-02)
- `tools/verify-consumer/src/single-instance.mjs` — [P2] VER-03 class-identity + shared-cache assert (plan 05-02)
- `tools/verify-consumer/vite.config.ts` — [P2] production consumer build for VER-02 (plan 05-02)
- `.github/workflows/verify-consumer.yml` — [P2] OPTIONAL `workflow_dispatch` CI job using `GITHUB_TOKEN` (plan 05-02)

New npm scripts (root `package.json`):
- `verify:consumer` — [P1] `node scripts/verify-consumer.mjs` (runs all available checks)

New CLI surface on `scripts/verify-consumer.mjs`:
- `--check install` (VER-01), `--check resolve` (VER-04) — [P1]
- `--check treeshake` (VER-02), `--check single-instance` (VER-03) — [P2]
- `--dry-run` — [P1] offline scaffold + token-safety validation, no network
</artifacts_produced>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — scaffold out-of-tree consumer, install five packages from the real registry, prove one smoke import (VER-01 slice)</name>
  <files>scripts/verify-consumer.mjs, tools/verify-consumer/package.json.tmpl, package.json</files>
  <read_first>
    - scripts/verify-consumer.mjs (does not exist yet — this task creates it)
    - .npmrc.example (copy the two lines verbatim: `@willramdev:registry=https://npm.pkg.github.com` and the env-expanded `_authToken` line)
    - package.json (root — add the `verify:consumer` script; confirm `workspaces: ["packages/*"]` is why the consumer must live OUTSIDE the repo)
    - .planning/phases/05-consumer-install-verification/05-RESEARCH.md (Pattern 1, Pitfall 1, Pitfall 3, Pitfall 5)
  </read_first>
  <action>
    Create `scripts/verify-consumer.mjs` as a cross-platform Node ESM orchestrator (use `node:fs`, `node:os`, `node:path`, `node:child_process`, `node:url`; NO bash, NO POSIX-only paths, NO symlinks — the dev box is win32). Implement the thinnest end-to-end slice ONLY in this task: scaffold, install, one smoke import, exit code. Later tasks/plans expand the `--check` dispatch.

    Resolve `repoRoot` from the script location. Compute `consumerDir = path.join(os.tmpdir(), 'litkit-verify-consumer')`. HARD GUARD: assert `path.resolve(consumerDir)` does NOT start with `path.resolve(repoRoot)` and has no `node_modules` ancestor — throw and exit non-zero if it does (this is the anti-workspace-shadowing guarantee; see Pitfall 1). Recreate `consumerDir` fresh (remove if present, mkdir).

    Write `tools/verify-consumer/package.json.tmpl` declaring, as dependencies, the five packages pinned to the published version — `@willramdev/kit` 1.0.0, `@willramdev/router` 1.0.0, `@willramdev/query` 1.0.0, `@willramdev/forms` 1.0.0, `@willramdev/store` 1.0.0 — plus the peers the consumer must supply itself: `lit` ^3.3.2, `@tanstack/query-core` ^5.91.0, `@tanstack/form-core` ^1.28.5, `zod` ^4.3.6; and devDependencies `vite` ^8.0.1, `typescript` ^6.0.3, `jsdom` ^29.0.1. Mark it `"type": "module"` and `"private": true`. The script copies this template into `consumerDir/package.json`.

    Write `consumerDir/.npmrc` with EXACTLY the two lines from `.npmrc.example`: the scope->registry map and the env-expanded auth line. Never write a literal token — the auth line uses `${GITHUB_TOKEN}` expansion which npm resolves from the environment at install time on Windows and POSIX alike. Do NOT emit a global `registry=` line (that would route lit/tanstack/vite/typescript to GitHub Packages and 404 them — Pitfall 3).

    Add a `--dry-run` flag: perform the scaffold (temp dir, template copy, `.npmrc` write, the tmpdir guard) but SKIP the network `npm install`; then assert the generated `.npmrc` contains the `${GITHUB_TOKEN}` token and no literal secret, and print the resolved `consumerDir`. `--dry-run` must exit 0 with NO network access and NO token required.

    Add `--check install` (VER-01): run `npm install` via `child_process` with `cwd = consumerDir`, inheriting `process.env` (so `GITHUB_TOKEN` reaches npm); fail on non-zero exit. Then, using `import.meta.resolve` (or `createRequire(consumerDir).resolve`), assert the resolved path of `@willramdev/kit` is under `consumerDir/node_modules` (Spoofing guard — Pitfall 1). Then dynamically `import()` `@willramdev/kit` from the consumer and touch one real export (`KitElement`) to prove one package loads. Print `VER-01 PASS`/`VER-01 FAIL` and exit accordingly.

    Add the root `package.json` script `verify:consumer` = `node scripts/verify-consumer.mjs` (the no-flag full runner; wire it now even though later checks land in plan 05-02).

    Print a PASS/FAIL line per check and exit non-zero on the first failure. Never `echo`/log the token value anywhere.
  </action>
  <verify>
    <automated>node --check scripts/verify-consumer.mjs && node scripts/verify-consumer.mjs --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/verify-consumer.mjs` exits 0 (valid ESM syntax).
    - `node scripts/verify-consumer.mjs --dry-run` exits 0 with NO network and NO `GITHUB_TOKEN` set, printing a temp `consumerDir` path that is under the OS temp dir and NOT under `C:\repos\litkit`.
    - The generated temp `.npmrc` contains `@willramdev:registry=https://npm.pkg.github.com` and an auth line using `${GITHUB_TOKEN}`; `git grep -nE 'ghp_[A-Za-z0-9]{20,}' -- . ':!*.lock'` over the repo returns no matches (exit 1 / empty).
    - `tools/verify-consumer/package.json.tmpl` pins all five `@willramdev/*` to `1.0.0` and includes `lit`, `@tanstack/query-core`, `@tanstack/form-core`, `zod`, `vite`, `typescript`, `jsdom`.
    - Root `package.json` has a `verify:consumer` script.
    - The real `--check install` green run is confirmed in Task 2 (requires the maintainer PAT).
  </acceptance_criteria>
  <done>The harness scaffolds a temp-dir consumer offline, is token-safe (no literal secret, scope-only routing), guards against workspace shadowing, and the install slice is coded end-to-end; the networked green is confirmed at the Task 2 checkpoint.</done>
  <reversibility rating="reversible">Committed test tooling in a new `tools/verify-consumer/` tree; no library source or registry state changes.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING CHECKPOINT — maintainer supplies the read:packages PAT and confirms the real-registry install (VER-01 green)</name>
  <read_first>
    - .planning/phases/05-consumer-install-verification/05-RESEARCH.md (Environment Availability; Pitfall 2 — 401/403 causes)
    - scripts/verify-consumer.mjs (the Task 1 harness being run)
  </read_first>
  <precondition>A classic GitHub PAT scoped to `read:packages` ONLY is available and the maintainer can export it as `GITHUB_TOKEN`. This is external human setup that plan ordering cannot guarantee.</precondition>
  <what-built>Task 1 built `scripts/verify-consumer.mjs` with `--check install`: it scaffolds a throwaway consumer in `os.tmpdir()`, writes a scope-only `.npmrc` with `${GITHUB_TOKEN}` expansion, installs the five `@willramdev/*@1.0.0` packages plus their peers from `npm.pkg.github.com`, verifies the resolved package path is under the temp dir (not the workspace), and smoke-imports `@willramdev/kit`. GitHub Packages has NO anonymous read, so this run cannot pass without a maintainer-supplied token.</what-built>
  <how-to-verify>
    1. Create a classic PAT (NOT fine-grained — fine-grained PATs do not work for the npm registry) scoped to `read:packages` ONLY, at GitHub -> Settings -> Developer settings -> Tokens (classic).
    2. In the shell, export it: PowerShell `$env:GITHUB_TOKEN = "<paste PAT>"` (or bash `export GITHUB_TOKEN=<paste PAT>`). Do NOT paste it into any file.
    3. Confirm auth reaches the registry: `npm whoami --registry=https://npm.pkg.github.com` returns your username (not E401).
    4. Run `node scripts/verify-consumer.mjs --check install`.
    5. Confirm the output prints `VER-01 PASS`, that all five `@willramdev/*` were fetched from the network (not resolved locally), and that the printed resolved `@willramdev/kit` path is under the OS temp dir.
    6. Keep `GITHUB_TOKEN` exported in this shell session so the Task 3 and plan 05-02 network checks can run.
    If you hit E401/E403: verify the token is classic + has `read:packages`, both `.npmrc` lines are present, and `GITHUB_TOKEN` is actually set in the current shell (Pitfall 2).
  </how-to-verify>
  <acceptance_criteria>
    - `npm whoami --registry=https://npm.pkg.github.com` succeeds (auth confirmed).
    - `node scripts/verify-consumer.mjs --check install` exits 0 and prints `VER-01 PASS`.
    - The resolved `@willramdev/kit` path shown is under the OS temp dir, proving the tarball (not workspace source) was installed.
  </acceptance_criteria>
  <resume-signal>Type "approved" once VER-01 PASS is confirmed with the real registry, or describe the install error. This checkpoint is NOT auto-approvable — do not honor workflow.auto_advance; a human must confirm the token-bearing install.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: VER-04 — resolve all eight entries/subpaths from the installed tarball (tsc node16+bundler + runtime import)</name>
  <files>tools/verify-consumer/consumer-router.ts, tools/verify-consumer/consumer-rest.ts, tools/verify-consumer/tsconfig.node16.json, tools/verify-consumer/tsconfig.bundler.json, tools/verify-consumer/src/subpath-smoke.mjs, scripts/verify-consumer.mjs</files>
  <read_first>
    - tools/typecheck-smoke/consumer-router.ts (verbatim analog to copy — covers router `.`/`./core`/`./lit`)
    - tools/typecheck-smoke/consumer-rest.ts (verbatim analog to copy — covers kit/store/query/forms `.` + forms/zod)
    - tools/typecheck-smoke/tsconfig.node16.json (copy — moduleResolution node16)
    - tools/typecheck-smoke/tsconfig.bundler.json (copy — moduleResolution bundler)
    - scripts/verify-consumer.mjs (Task 1 harness — add the `--check resolve` dispatch)
    - .planning/phases/05-consumer-install-verification/05-RESEARCH.md (Pattern 4; eight-target list; the allowImportingTsExtensions warning)
  </read_first>
  <precondition>`GITHUB_TOKEN` (classic read:packages PAT) is present in the shell and the Task 2 checkpoint established a warm installed consumer — the `--check resolve` run reads from the installed `node_modules`.</precondition>
  <action>
    Copy `tools/typecheck-smoke/consumer-router.ts` and `consumer-rest.ts` to `tools/verify-consumer/` unchanged (they already import all eight targets and `void`-reference each binding so an unresolved subpath is a hard TS2307, not a silent `any`). Copy `tsconfig.node16.json` and `tsconfig.bundler.json` to `tools/verify-consumer/` unchanged. CRITICAL: do NOT add `allowImportingTsExtensions` to either tsconfig — that lets tsc fall back to workspace `src/*.ts` and defeats the exports-map resolution this check exists to prove (the copied header comment already warns of this).

    Create `tools/verify-consumer/src/subpath-smoke.mjs` (plain runtime ESM, NO type syntax — decoupled from any type-stripping flag) that awaits a dynamic `import()` of each of the eight published targets and touches one real export from each so the ESM loader actually resolves the `exports` conditions: `@willramdev/kit` (KitElement), `@willramdev/store` (createStore), `@willramdev/query` (createQueryClient), `@willramdev/forms` (form), `@willramdev/forms/zod` (zodValidator), `@willramdev/router` (createRouter), `@willramdev/router/core` (CompiledPathMatcher), `@willramdev/router/lit` (RouterOutlet). Throw with a clear per-target message on any failed import so a missing subpath is a hard failure.

    Extend `scripts/verify-consumer.mjs` with `--check resolve` (VER-04): the harness copies the four fixture files and `src/subpath-smoke.mjs` into `consumerDir`, then (a) runs the consumer's LOCAL typescript — `consumerDir/node_modules/.bin/tsc --noEmit -p tsconfig.node16.json` and again `-p tsconfig.bundler.json` — so `@willramdev/*` resolve from the installed tarball, and (b) runs `node src/subpath-smoke.mjs` from `consumerDir`. Any non-zero exit fails the check. Print `VER-04 PASS`/`VER-04 FAIL`. Ensure the no-flag full runner includes `resolve` after `install`.
  </action>
  <verify>
    <automated>node scripts/verify-consumer.mjs --check resolve</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-consumer.mjs --check resolve` exits 0 and prints `VER-04 PASS` (requires `GITHUB_TOKEN` in env + warm installed consumer).
    - Both `tsc -p tsconfig.node16.json` and `-p tsconfig.bundler.json` over the two copied consumer `.ts` files complete with zero errors, resolving all eight targets from `consumerDir/node_modules/@willramdev/*` (not the workspace).
    - `node src/subpath-smoke.mjs` imports and touches an export from all eight targets including `@willramdev/router/core`, `@willramdev/router/lit`, and `@willramdev/forms/zod` without throwing.
    - Neither copied tsconfig contains `allowImportingTsExtensions`.
  </acceptance_criteria>
  <done>All eight published entries/subpaths resolve to a real `.d.ts` under node16+bundler AND import at runtime, from the installed tarball — VER-04 proven post-publish.</done>
  <reversibility rating="reversible">Copied fixtures + additive `--check` branch; no source or registry changes.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer shell -> npm -> GitHub Packages | The `read:packages` PAT (`GITHUB_TOKEN`) crosses here as install auth; it must never be persisted to a committed file or logs. |
| registry -> temp consumer `node_modules` | Installed tarballs are the artifacts under test; the harness must prove they resolve from the temp dir, not the workspace. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-5-01 | Information Disclosure | scripts/verify-consumer.mjs + generated temp `.npmrc` | high | mitigate | PAT read ONLY from `GITHUB_TOKEN` env; generated temp `.npmrc` uses `${GITHUB_TOKEN}` expansion (never a literal); `.npmrc` written under `os.tmpdir()`, never under the repo; token never echoed/logged; `git grep` for token-shaped literals returns none. |
| T-5-02 | Spoofing (false proof) | temp consumer module resolution | high | mitigate | Scaffold under `os.tmpdir()` outside the `workspaces` glob with no `node_modules` ancestor; assert resolved `@willramdev/kit` path is under `consumerDir/node_modules` before trusting any check. |
| T-5-03 | Elevation of Privilege | PAT scope | medium | mitigate | Checkpoint instructs a classic PAT scoped to `read:packages` ONLY (least privilege), matching the shipped `.npmrc.example` guidance. |
| T-5-SC | Tampering (supply chain) | npm install of five `@willramdev/*` + pinned peers | medium | accept | Per RESEARCH `## Package Legitimacy Audit`: no new/untrusted package is introduced — every install is either an artifact under test or a pre-existing version-pinned repo dep; none `[SUS]`/`[SLOP]`. Versions are pinned in `package.json.tmpl`. |

All high-severity threats are mitigated; no unmitigated `high` remains → plan is not blocked.
</threat_model>

<verification>
- `node --check scripts/verify-consumer.mjs` exits 0.
- `node scripts/verify-consumer.mjs --dry-run` exits 0 offline with no token.
- With `GITHUB_TOKEN` (classic read:packages PAT) exported: `node scripts/verify-consumer.mjs --check install` prints `VER-01 PASS`; `--check resolve` prints `VER-04 PASS`.
- `git grep -nE 'ghp_[A-Za-z0-9]{20,}'` over the repo returns no matches.
</verification>

<success_criteria>
- VER-01: clean-machine install of all five packages from GitHub Packages with a read:packages PAT succeeds; resolved paths are under the temp consumer, not the workspace.
- VER-04: all eight entries/subpaths resolve for tsc (node16+bundler) and at runtime from the installed tarball.
- Harness is committed, token-safe, and re-runnable via `npm run verify:consumer`.
</success_criteria>

<output>
Create `.planning/phases/05-consumer-install-verification/05-01-SUMMARY.md` when done.
</output>
