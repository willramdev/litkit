---
phase: 05-consumer-install-verification
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - tools/verify-consumer/src/tree-shake-entry.ts
  - tools/verify-consumer/vite.config.ts
  - tools/verify-consumer/src/single-instance.mjs
  - scripts/verify-consumer.mjs
  - .github/workflows/verify-consumer.yml
autonomous: true
requirements: [VER-02, VER-03]

estimate:
  tokens: 46000
  raw_tokens: 23000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "VER-02: a production `vite build` of a consumer entry that does bare side-effect imports of @willramdev/forms, /query, and /router, loaded under jsdom, has `customElements.get(tag)` defined for all five registered tags — lit-form, lit-query-client-provider, router-outlet, router-provider, router-link — proving the BUILD-03 sideEffects allowlist survived tree-shaking."
    - "VER-03: `QueryClient` imported from `@tanstack/query-core` is reference-equal (`===`) to `QueryClient` re-exported by `@willramdev/query`, and a QueryClient-seeded cache is read back through litkit's QueryObserver — proving `@tanstack/query-core` is a single deduped peer instance (BUILD-04), not bundled."
    - "The full `node scripts/verify-consumer.mjs` runs all four VER checks and exits 0 against the live registry."
  artifacts:
    - tools/verify-consumer/src/tree-shake-entry.ts
    - tools/verify-consumer/vite.config.ts
    - tools/verify-consumer/src/single-instance.mjs
    - .github/workflows/verify-consumer.yml
  key_links:
    - "sideEffects allowlist survival through a PRODUCTION (minified) bundle — the seam VER-02 relies on; a dev build tree-shakes nothing and would pass vacuously."
    - "`export * from '@tanstack/query-core'` at packages/query/src/index.ts:11 — the re-export that makes VER-03's `Direct === ViaKit` a valid single-instance proof."
    - "workflow_dispatch-only CI job using the built-in GITHUB_TOKEN — kept OUT of the required ci.yml push gate to preserve Phase 4's read-only/publish-token split."
---

<objective>
Expand the verification harness from the proven install+resolve slice (plan 05-01) to the two runtime-behavior requirements: VER-02 (tree-shaking survival of custom-element registration through a production consumer build) and VER-03 (TanStack single-instance / peer-dependency dedupe). Then commit an OPTIONAL, manually-triggerable CI job so the proof is reproducible without storing a PAT.

Purpose: VER-02 and VER-03 are the behavioral proofs that the BUILD-03 (sideEffects) and BUILD-04 (peerDependencies) fixes actually survived publish — not just that the packages resolve.
Output: `src/tree-shake-entry.ts` + `vite.config.ts` + `src/single-instance.mjs` fixtures, two new `--check` branches, and a `workflow_dispatch` CI job; all four VER checks green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-consumer-install-verification/05-RESEARCH.md
@.planning/phases/05-consumer-install-verification/05-01-SUMMARY.md

# The orchestrator this plan extends (created in 05-01):
@scripts/verify-consumer.mjs
# VER-03 mechanism source of truth — the re-export the identity assert relies on:
@packages/query/src/index.ts
# Existing read-only push gate to leave UNTOUCHED (the optional job is a separate file):
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: VER-02 — production vite build + jsdom assert that custom-element registration survives tree-shaking</name>
  <files>tools/verify-consumer/src/tree-shake-entry.ts, tools/verify-consumer/vite.config.ts, scripts/verify-consumer.mjs</files>
  <read_first>
    - scripts/verify-consumer.mjs (05-01 orchestrator — add the `--check treeshake` dispatch)
    - .planning/phases/05-consumer-install-verification/05-RESEARCH.md (Pattern 2 — target packages + tags; Pitfall 4 — vacuous dev-build trap)
    - packages/forms/src/lit-form.ts (registers `lit-form`), packages/query/src/query-client-provider.ts (registers `lit-query-client-provider`), packages/router/src/router-lit/router-outlet.ts + router-provider.ts + router-link.ts (register `router-outlet`/`router-provider`/`router-link`)
  </read_first>
  <precondition>`GITHUB_TOKEN` (classic read:packages PAT) is present in the shell and the 05-01 checkpoint established a warm installed consumer with the three element-registering packages present in `node_modules`.</precondition>
  <action>
    Create `tools/verify-consumer/src/tree-shake-entry.ts` that does BARE side-effect imports of exactly the three element-registering main entries — `@willramdev/forms`, `@willramdev/query`, `@willramdev/router` — and references NOTHING from them. Because nothing is referenced, a production bundler is free to drop these modules and their module-scope `customElements.define(...)` calls UNLESS the packages' `sideEffects` allowlist preserves them; that survival is exactly what VER-02 proves. Do NOT import `@willramdev/kit` or `@willramdev/store` here — they are `sideEffects:false` and register nothing.

    Create `tools/verify-consumer/vite.config.ts` as a minimal library/app build: entry `src/tree-shake-entry.ts`, `mode: 'production'` with minification ON, output a single self-contained ESM bundle into the consumer's `dist/`. Externalize NOTHING (bundle lit + the litkit packages in) so the emitted bundle loads standalone under jsdom. This is a PRODUCTION build on purpose — a dev/un-minified build tree-shakes nothing and would pass vacuously (Pitfall 4).

    Extend `scripts/verify-consumer.mjs` with `--check treeshake` (VER-02): copy `src/tree-shake-entry.ts` and `vite.config.ts` into `consumerDir`, run the consumer's local vite (`consumerDir/node_modules/.bin/vite build`) in production mode, then load the emitted bundle under jsdom in Node — construct a `JSDOM` instance, expose its `window.customElements`/`window.HTMLElement` as globals, import the built bundle, and assert `customElements.get(tag)` is defined for every tag in [`lit-form`, `lit-query-client-provider`, `router-outlet`, `router-provider`, `router-link`]. Throw a per-tag error naming any tag that was tree-shaken away. Print `VER-02 PASS`/`VER-02 FAIL`. Add `treeshake` to the no-flag full runner sequence.

    Document the cheaper static fallback in a comment in the harness (count `customElements.define(` occurrences in the emitted bundle, expect at least five) and note the negative control: temporarily flipping any target package's `sideEffects` to false must make this check FAIL — if it still passes, the test is not exercising tree-shaking.
  </action>
  <verify>
    <automated>node scripts/verify-consumer.mjs --check treeshake</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-consumer.mjs --check treeshake` exits 0 and prints `VER-02 PASS` (requires `GITHUB_TOKEN` + warm consumer).
    - The consumer `vite build` runs in production mode with minification and emits a bundle to the consumer's `dist/`.
    - Under jsdom, `customElements.get(tag)` is truthy for all five tags: `lit-form`, `lit-query-client-provider`, `router-outlet`, `router-provider`, `router-link`.
    - As a static cross-check, the emitted bundle contains at least five `customElements.define(` occurrences.
  </acceptance_criteria>
  <done>A production consumer bundle preserves every custom-element registration and all five tags register at runtime under jsdom — BUILD-03 sideEffects survival proven post-publish.</done>
  <reversibility rating="reversible">Additive consumer fixtures + `--check` branch; no library source or registry changes.</reversibility>
</task>

<task type="auto">
  <name>Task 2: VER-03 — single-instance assert (class identity + shared cache) proving @tanstack/query-core is a deduped peer</name>
  <files>tools/verify-consumer/src/single-instance.mjs, scripts/verify-consumer.mjs</files>
  <read_first>
    - packages/query/src/index.ts (line 11 `export * from '@tanstack/query-core'` — the re-export the identity assert depends on)
    - packages/query/package.json (confirm `@tanstack/query-core` is under peerDependencies and ABSENT from dependencies)
    - scripts/verify-consumer.mjs (05-01 orchestrator — add the `--check single-instance` dispatch)
    - .planning/phases/05-consumer-install-verification/05-RESEARCH.md (Pattern 3 — class-identity + shared-cache proofs)
  </read_first>
  <precondition>`GITHUB_TOKEN` (classic read:packages PAT) is present in the shell and the 05-01 checkpoint established a warm installed consumer where the consumer's own `@tanstack/query-core` and `@willramdev/query` are both installed.</precondition>
  <action>
    Create `tools/verify-consumer/src/single-instance.mjs` (plain runtime ESM). Proof 1 (class identity, the gate): import `QueryClient` from `@tanstack/query-core` as one binding and `QueryClient` re-exported by `@willramdev/query` as another, and assert they are reference-equal (`===`); throw a `VER-03 FAIL` error if not — inequality means `@tanstack/query-core` was duplicated rather than deduped to a single peer copy. Proof 2 (shared cache, behavioral): construct a `QueryClient` from the consumer's `@tanstack/query-core`, `setQueryData(['ping'], 'pong')` on it, then construct a litkit `QueryObserver` (from `@willramdev/query`'s re-exported surface) bound to that same client and assert the observer reads back `'pong'` — if query-core were duplicated, litkit's observer would not recognize the consumer's cache.

    Extend `scripts/verify-consumer.mjs` with `--check single-instance` (VER-03): copy `src/single-instance.mjs` into `consumerDir`, run `node src/single-instance.mjs` from `consumerDir`, and additionally run `npm ls @tanstack/query-core` in the consumer as supporting evidence that a SINGLE resolved version exists (print it; treat a duplicate-version tree as a warning surfaced in the output). Print `VER-03 PASS`/`VER-03 FAIL`. Add `single-instance` to the no-flag full runner so `node scripts/verify-consumer.mjs` now runs install -> resolve -> treeshake -> single-instance and exits non-zero on any failure.
  </action>
  <verify>
    <automated>node scripts/verify-consumer.mjs --check single-instance</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-consumer.mjs --check single-instance` exits 0 and prints `VER-03 PASS` (requires `GITHUB_TOKEN` + warm consumer).
    - The class-identity assert (`Direct === ViaKit`) holds.
    - The shared-cache proof reads back the consumer-seeded value through litkit's QueryObserver.
    - `npm ls @tanstack/query-core` in the consumer shows a single resolved version.
  </acceptance_criteria>
  <done>The consumer's `@tanstack/query-core` is the exact single instance litkit uses (class identity + shared cache) — BUILD-04 peerDependency dedupe proven post-publish.</done>
  <reversibility rating="reversible">Additive fixture + `--check` branch; no source or registry changes.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Optional workflow_dispatch CI job — reproducible proof via built-in GITHUB_TOKEN, OUT of the required ci.yml gate</name>
  <files>.github/workflows/verify-consumer.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the read-only push gate — leave it UNTOUCHED; this job is a separate file)
    - .github/workflows/release.yml (Phase 4 auth-bearing sibling — mirror its least-privilege permissions style)
    - .planning/phases/05-consumer-install-verification/05-RESEARCH.md (Open Question 1 — committed script + optional workflow_dispatch job using GITHUB_TOKEN; State of the Art — in CI prefer the built-in GITHUB_TOKEN over a stored PAT)
  </read_first>
  <action>
    Create `.github/workflows/verify-consumer.yml` as a `workflow_dispatch`-ONLY job (manual trigger; NO `push`/`pull_request` triggers) — this deliberately keeps it OUT of the always-on `ci.yml` push gate so Phase 4's read-only/publish-token split stays intact. Do NOT modify `ci.yml`.

    The job: least-privilege `permissions: { contents: read, packages: read }`; checkout; setup-node with a supported Node (22 or 24, matching the ci.yml matrix baseline); `npm ci` at the repo root; then run `node scripts/verify-consumer.mjs` with `env: GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}` — the repo's own Actions token can read the same repo's packages, so no PAT is stored. Do not `echo` the token; rely on Actions secret masking. Add a top-of-file comment stating this is an opt-in reproducible-proof job, that promoting it to an always-on `ci.yml` gate is a deferred team decision, and that a local run instead uses a maintainer classic `read:packages` PAT exported as `GITHUB_TOKEN`.
  </action>
  <precondition>none</precondition>
  <verify>
    <automated>grep -q 'workflow_dispatch' .github/workflows/verify-consumer.yml && grep -q 'secrets.GITHUB_TOKEN' .github/workflows/verify-consumer.yml && echo workflow-ok</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/verify-consumer.yml` exists and its `on:` block declares ONLY a manual trigger (`workflow_dispatch`) — no automatic on-commit or on-PR trigger.
    - The job runs `node scripts/verify-consumer.mjs` with `env.GITHUB_TOKEN` set from `secrets.GITHUB_TOKEN` (the built-in Actions token) — no stored PAT.
    - `permissions:` grants at most `contents: read` and `packages: read`.
    - `.github/workflows/ci.yml` is unchanged (`git diff --stat .github/workflows/ci.yml` is empty).
  </acceptance_criteria>
  <done>An opt-in, manually-triggerable CI job reproduces the four-check verification using the repo's built-in token; the required `ci.yml` push gate is untouched.</done>
  <reversibility rating="reversible">A new, manual-only workflow file; deleting it fully reverts. It does not gate any push/PR.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Actions runner -> npm -> GitHub Packages | In CI the built-in `GITHUB_TOKEN` crosses here as package-read auth; it must never be echoed into logs. |
| consumer bundle -> jsdom runtime | Built output is loaded and executed under jsdom to assert element registration; input is the harness's own fixtures only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-5-01 | Information Disclosure | .github/workflows/verify-consumer.yml | high | mitigate | CI job uses the built-in `secrets.GITHUB_TOKEN` via the runner's env (never echoed); `workflow_dispatch`-only; not wired into `ci.yml`; rely on Actions secret masking; no PAT stored. |
| T-5-04 | Tampering (vacuous / false-pass test) | VER-02 consumer vite build | medium | mitigate | Assert on a PRODUCTION (minified) build with bare side-effect imports so the `sideEffects` allowlist is the only thing preserving `define()`; documented negative control (flip a target's `sideEffects` to false → check must fail). |
| T-5-02 | Spoofing (false proof) | reused temp consumer from 05-01 | medium | mitigate | The 05-01 tmpdir + resolved-path guard already ensures the reused consumer resolves `@willramdev/*` from the installed tarball, not the workspace. |

All high-severity threats are mitigated; no unmitigated `high` remains → plan is not blocked.
</threat_model>

<verification>
- With `GITHUB_TOKEN` (classic read:packages PAT) exported and a warm consumer from plan 05-01:
  - `node scripts/verify-consumer.mjs --check treeshake` prints `VER-02 PASS`.
  - `node scripts/verify-consumer.mjs --check single-instance` prints `VER-03 PASS`.
  - `node scripts/verify-consumer.mjs` (no flag) runs install -> resolve -> treeshake -> single-instance and exits 0.
- `.github/workflows/verify-consumer.yml` is `workflow_dispatch`-only and `.github/workflows/ci.yml` is unchanged.
</verification>

<success_criteria>
- VER-02: custom-element registration survives a production consumer bundle; all five tags register under jsdom.
- VER-03: the consumer's `@tanstack/query-core` is the single deduped instance litkit uses (class identity + shared cache).
- The full `node scripts/verify-consumer.mjs` proves all four VER requirements green; an optional manual CI job reproduces it without a stored PAT.
</success_criteria>

<output>
Create `.planning/phases/05-consumer-install-verification/05-02-SUMMARY.md` when done.
</output>