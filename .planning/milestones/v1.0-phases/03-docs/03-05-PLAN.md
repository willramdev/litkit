---
phase: 03-docs
plan: 05
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - LICENSE
  - packages/kit/LICENSE
  - packages/router/LICENSE
  - packages/query/LICENSE
  - packages/forms/LICENSE
  - packages/store/LICENSE
  - package.json
  - .changeset/docs-phase-3.md
autonomous: true
requirements: [DOCS-04]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A non-empty LICENSE file exists at the repo root AND in every packages/* dir (6 total), each containing 'MIT License' and 'Copyright (c) 2026 Will Ramanand' (DOCS-04)"
    - "Root LICENSE and each packages/*/LICENSE are 6 distinct identical-text copies, never a single merged/symlinked file (DOCS-04 adjacency)"
    - "Root package.json carries \"license\": \"MIT\" (the five package.json already have it and are left untouched — verify-then-fill) (DOCS-04)"
    - "A single changeset covers all five @willram/* packages so `changeset status --since origin/main` passes for the phase's package (README + LICENSE) changes"
  artifacts:
    - "LICENSE (root) + packages/{kit,router,query,forms,store}/LICENSE — 6 identical MIT files"
    - "root package.json \"license\": \"MIT\" field"
    - ".changeset/docs-phase-3.md covering the five @willram/* packages"
  key_links:
    - "LICENSE copyright line reads exactly 'Copyright (c) 2026 Will Ramanand' (D-05), NOT the package.json author 'William Ramanand' — leave author fields untouched"
    - "tarball inclusion via the files allowlist is Phase 4 RLS-02, NOT this phase — this phase only creates the files"
    - "Phase 4 seam: this patch changeset is consumed by Phase 4's changesets version step (first post-1.0.0 bump); RLS-07 publishes 1.0.0 before adopting changesets versioning"
---

<objective>
Create the licensing artifacts for DOCS-04: an MIT LICENSE file at the repo root and one in each of the five packages (6 identical files, copyright "Will Ramanand", year 2026), fill the missing root package.json license field (verify-then-fill — the five packages already declare it), and add one covering changeset so the Phase 2 changeset-status CI gate stays green given this phase's package (README + LICENSE) changes.

Purpose: every package carries the license text it must ship, and the phase's package changes are changeset-covered so the PR can merge.
Output: 6 LICENSE files, the root license field, and one changeset.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-docs/03-CONTEXT.md
@.planning/phases/03-docs/03-RESEARCH.md
@.planning/phases/03-docs/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the root and per-package MIT LICENSE files</name>
  <files>LICENSE, packages/kit/LICENSE, packages/router/LICENSE, packages/query/LICENSE, packages/forms/LICENSE, packages/store/LICENSE</files>
  <read_first>
    - 03-RESEARCH.md Code Example 4 (canonical OSI MIT license text to use verbatim)
    - 03-CONTEXT.md D-05 (MIT, copyright "Will Ramanand", year 2026; reversibility costly)
    - packages/kit/package.json:11-12 (confirms license MIT already set and author is "William Ramanand" — the LICENSE copyright deliberately uses "Will Ramanand", author fields stay untouched)
  </read_first>
  <reversibility rating="costly">D-05 costly seam: once v1.0 tarballs ship with a bundled LICENSE, changing the license retroactively is a published-contract change to internal consumers. Lock the MIT choice and the "Copyright (c) 2026 Will Ramanand" line before the Phase 4 publish. Not a one-way door at authoring time (files are freely editable now), so recorded, not gated.</reversibility>
  <action>
    Create six LICENSE files with identical content — one at the repo root and one in each of packages/kit, packages/router, packages/query, packages/forms, packages/store. Use the canonical OSI MIT License text from RESEARCH Code Example 4 verbatim, with the copyright line reading exactly "Copyright (c) 2026 Will Ramanand". These are six independent copies (not a symlink or a single shared file). Do NOT edit any package.json author field (they read "William Ramanand" and are out of scope). Do NOT add a files allowlist entry — tarball inclusion is Phase 4 RLS-02.
  </action>
  <verify>
    <automated>for f in LICENSE packages/kit/LICENSE packages/router/LICENSE packages/query/LICENSE packages/forms/LICENSE packages/store/LICENSE; do grep -q "MIT License" "$f" && grep -q "Copyright (c) 2026 Will Ramanand" "$f" || { echo "MISSING-OR-WRONG $f"; exit 1; }; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All six files exist: LICENSE, packages/kit/LICENSE, packages/router/LICENSE, packages/query/LICENSE, packages/forms/LICENSE, packages/store/LICENSE.
    - Each file contains the literal `MIT License` and the literal `Copyright (c) 2026 Will Ramanand`.
    - Each file is non-empty and independently present (not a symlink): `test -f <path> -a ! -L <path>` for each.
    - No package.json author field is modified (`git diff --name-only` shows no packages/*/package.json among changes for this task).
  </acceptance_criteria>
  <done>Six identical MIT LICENSE files exist (root + five packages), each with the exact copyright line, and no author field was touched.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the root package.json license field and add the covering changeset</name>
  <files>package.json, .changeset/docs-phase-3.md</files>
  <read_first>
    - package.json (root — confirms there is NO license field today; add "license": "MIT")
    - packages/kit/package.json:11 (the "license": "MIT" line shape to mirror; the five packages already have it — do NOT edit them)
    - .changeset/config.json (baseBranch main, access restricted — the config the changeset is written against)
    - .github/workflows/ci.yml:62-64 (the `changeset status --since origin/main` gate this changeset must satisfy)
    - 03-RESEARCH.md Pitfall 5 (verify-then-fill for the license field; changeset gate)
  </read_first>
  <reversibility rating="reversible">A metadata field and a changeset file; both freely editable. The Phase-4 version-bump consequence is a documented seam, not a lock.</reversibility>
  <action>
    Add a "license": "MIT" field to the root package.json (it currently has none). Do NOT touch the five packages/*/package.json — they already declare it (verify-then-fill: root only). Do NOT touch the author field.

    Create one changeset file at .changeset/docs-phase-3.md whose frontmatter lists all five @willram/* packages — @willram/kit, @willram/router, @willram/query, @willram/forms, @willram/store — each at a patch bump, followed by a one-line summary describing the Phase 3 docs work (normalized READMEs with verified quickstarts + LICENSE files). This single changeset covers every package touched across the phase (READMEs in 03-01/02/03, LICENSE files in this plan), so the CI `changeset status --since origin/main` gate passes. Note in the summary or a comment that this changeset is consumed by Phase 4's changesets versioning (the first post-1.0.0 bump); RLS-07 publishes 1.0.0 before adopting the version bump, so a pending patch changeset here is expected.
  </action>
  <verify>
    <automated>grep -q '"license": "MIT"' package.json && for p in kit router query forms store; do grep -q "@willram/$p" .changeset/docs-phase-3.md || { echo "MISSING $p in changeset"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"license": "MIT"' package.json` returns 1 (root license field present).
    - `git diff --name-only -- packages/*/package.json` is empty (the five package.json are untouched).
    - `.changeset/docs-phase-3.md` exists and its frontmatter names all five @willram/* packages at patch.
    - `npx changeset status --since origin/main` exits 0 (no changed package lacks a covering changeset).
  </acceptance_criteria>
  <done>Root package.json declares "license": "MIT", the five package.json are untouched, and one changeset covers all five packages so the changeset-status gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Static legal files + a metadata field + a changeset; no runtime code path, external input, or secret handled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05a | Repudiation | wrong copyright holder / license mismatch | low | mitigate | Copyright line pinned to "Copyright (c) 2026 Will Ramanand" (D-05); verify greps the exact literal; author fields deliberately left as-is. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs this plan. |
</threat_model>

<verification>
- Six MIT LICENSE files present with the exact copyright line.
- Root package.json has "license": "MIT"; the five package.json are unchanged.
- One changeset covers all five packages; `changeset status --since origin/main` exits 0.
</verification>

<success_criteria>
Every package (root + five) has an MIT LICENSE file it can ship, the root package.json license field is filled, and the phase's package changes are changeset-covered — satisfying DOCS-04 and keeping the Phase 2 changeset gate green.
</success_criteria>

<output>
Create `.planning/phases/03-docs/03-05-SUMMARY.md` when done.
</output>
