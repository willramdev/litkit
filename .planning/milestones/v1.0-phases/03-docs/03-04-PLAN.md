---
phase: 03-docs
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - README.md
  - .npmrc.example
autonomous: true
requirements: [DOCS-02, DOCS-03]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Root README.md exists and contains a 5-row monorepo map table (one row per @willram package with purpose + install line) (DOCS-02)"
    - "Root README.md contains one marked, self-contained cross-package integration block wiring router + query + forms + store into a single KitElement that compiles under BOTH node16 and bundler via `npm run doc-check` — a single compilable snippet, not a prose walkthrough or example app (DOCS-02, D-02)"
    - "Root README.md contains a 'Consuming from GitHub Packages' section covering the consumer read:packages PAT (DOCS-03)"
    - ".npmrc.example exists at repo root, contains @willram:registry=https://npm.pkg.github.com AND an _authToken line using environment-variable expansion, and carries no real token value (DOCS-03)"
    - ".npmrc.example is a committed CONSUMER template distinct from Phase 4's project .npmrc (RLS-03); it carries BOTH the scope map AND the auth line (D-07)"
  artifacts:
    - "README.md (root, net-new): monorepo map + cross-package integration snippet + Consuming from GitHub Packages section"
    - ".npmrc.example (root, net-new): scope->registry map + env-expanded auth placeholder"
  key_links:
    - "the integration block's exact option shapes (query(options,{client}), form(config)) MUST be confirmed by running the doc-check before locking (RESEARCH A3)"
    - "html imported from lit, KitElement/define from @willram/kit; every other symbol a verified sibling export (RESEARCH Code Example 3)"
    - "D-07 costly seam: do NOT merge/overwrite this consumer .npmrc.example with the Phase 4 project .npmrc"
---

<objective>
Author the net-new root README (monorepo map + one compilable cross-package integration snippet + a Consuming from GitHub Packages section) and the net-new consumer .npmrc.example. The integration snippet is a marked doc-check block, so DOCS-02's "working cross-package example" is verified by compilation, not eyeball. The .npmrc.example is the consumer-side template for installing @willram/* from GitHub Packages (DOCS-03) and is deliberately kept DISTINCT from Phase 4's project .npmrc (D-07, costly seam).

Purpose: give a consumer a single discoverable entry point (root README) that maps the five packages, shows them composing, and explains authenticated install.
Output: root README.md and root .npmrc.example.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-docs/03-CONTEXT.md
@.planning/phases/03-docs/03-RESEARCH.md
@.planning/phases/03-docs/03-PATTERNS.md
@.planning/codebase/STRUCTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the root README — monorepo map, marked cross-package integration snippet, and Consuming from GitHub Packages section</name>
  <files>README.md</files>
  <read_first>
    - packages/kit/README.md (structural template for headings)
    - 03-RESEARCH.md Pattern 3 (monorepo map table shape) and Code Example 3 (the verified cross-package integration snippet seed)
    - tools/typecheck-smoke/consumer-rest.ts and consumer-router.ts (verified exports the integration block may use: KitElement/define from kit, createRouter/defineRoutes from router, createQueryClient/query from query, form from forms, createStore/storeSlice from store)
    - packages/{kit,router,query,forms,store}/package.json (exports maps + peer sets, for the per-package install lines in the map table)
    - 03-PATTERNS.md "README (root, CREATE)" section
  </read_first>
  <reversibility rating="reversible">Net-new documentation; revertible.</reversibility>
  <action>
    Create the root README.md with: a project title + one-line description of the five-package litkit set; a monorepo map as a 5-row table (columns Package / Purpose / Install), one row per @willram package, each install line listing the real peer set (query needs @tanstack/query-core, forms needs @tanstack/form-core, all need lit) and linking to the package dir; a Cross-package example section holding ONE block marked with the doc-check HTML-comment marker that wires router + query + forms + store into a single KitElement/app shell as a single compilable snippet (D-02 — not a prose staged walkthrough and not a separate example app, which is deferred DX-03), seeded from RESEARCH Code Example 3, importing html from the lit package and KitElement plus define from @willram/kit, with every other symbol a verified sibling export; and a Consuming from GitHub Packages section (DOCS-03) that explains the scope->registry mapping, points to the .npmrc.example template from Task 2, and documents that the consumer needs a GitHub PAT scoped to read:packages only (least privilege).

    The integration block MUST be self-contained and MUST compile: confirm the exact option shapes against the emitted dist/*.d.ts by running the doc-check (RESEARCH A3 flags query(options,{client}) and form(config) as needing confirmation) and adjust the snippet until it is green — do NOT lock an unverified snippet. No shields/badges and no docs-site links (both Out of Scope). ESM import syntax only; no CommonJS calls.

    Run the harness after authoring to confirm the marked integration block compiles under both resolutions.
  </action>
  <verify>
    <automated>npm run doc-check:snippets</automated>
  </verify>
  <acceptance_criteria>
    - `npm run doc-check:snippets` exits 0 (the marked cross-package block compiles under both resolutions).
    - `test -f README.md` succeeds (root README exists).
    - `grep -c 'doc-check' README.md` returns >=1 (marked integration block present).
    - `grep -c '@willram/kit' README.md`, `@willram/router`, `@willram/query`, `@willram/forms`, `@willram/store` each return >=1 (all five mapped).
    - `grep -c 'Consuming from GitHub Packages' README.md` returns >=1.
    - `grep -c 'read:packages' README.md` returns >=1.
    - `grep -c 'require(' README.md` returns 0.
  </acceptance_criteria>
  <done>Root README.md exists with a 5-row monorepo map, a compiling marked cross-package integration snippet, and a Consuming from GitHub Packages section documenting the read:packages PAT.</done>
</task>

<task type="auto">
  <name>Task 2: Create the consumer .npmrc.example template</name>
  <files>.npmrc.example</files>
  <read_first>
    - 03-RESEARCH.md Code Example 1 (.npmrc.example contents) and Security Domain (secret hygiene)
    - 03-CONTEXT.md D-06 and D-07 (consumer template vs Phase 4 project .npmrc — keep DISTINCT)
    - 03-PATTERNS.md ".npmrc.example (root, CREATE)" section
  </read_first>
  <reversibility rating="costly">D-07 seam: this consumer template must NOT be merged with or overwritten by the Phase 4 project .npmrc (RLS-03). A conflation would break either consumer-auth docs or the project's publish resolution. Keep as a separate file named .npmrc.example; lock the naming before Phase 4.</reversibility>
  <action>
    Create .npmrc.example at the repo root as the consumer-side install template. It contains, at minimum, the scope-to-registry mapping line @willram:registry=https://npm.pkg.github.com and an auth line for the GitHub Packages host using an _authToken value supplied via environment-variable expansion (so the committed example never embeds a real personal access token). Add header comments instructing the consumer to copy it to their own project's .npmrc, to provide a GitHub PAT scoped to read:packages only, and to keep any real token out of source control (export it as an environment variable instead). Do NOT write a real token value into the file.

    Note in a comment that the scope->registry line is the part shared with the project's own .npmrc, while the auth line is what makes THIS file the consumer template — flag it as distinct from Phase 4's project .npmrc (D-07).
  </action>
  <verify>
    <automated>grep -q '@willram:registry=https://npm.pkg.github.com' .npmrc.example && grep -q '_authToken' .npmrc.example && grep -q 'read:packages' .npmrc.example && ! grep -Eq 'ghp_[A-Za-z0-9]' .npmrc.example</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .npmrc.example` succeeds.
    - `grep -c '@willram:registry=https://npm.pkg.github.com' .npmrc.example` returns 1.
    - `grep -c '_authToken' .npmrc.example` returns >=1.
    - `grep -c 'read:packages' .npmrc.example` returns >=1.
    - The file contains no real token value (an eight+ alphanumeric token-prefix pattern is absent; env-expansion placeholder only).
  </acceptance_criteria>
  <done>.npmrc.example exists as a committed consumer template with the scope->registry map and an env-expanded auth placeholder, no real token, kept distinct from the Phase 4 project .npmrc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed config -> source control | .npmrc.example is committed; a real credential here would be disclosed to everyone with repo access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04a | Information Disclosure | .npmrc.example committed with a real PAT | high | mitigate | Placeholder/env-expansion only (_authToken via ${GITHUB_TOKEN}); verify step negative-greps for a token-prefix pattern; real .npmrc stays out of source control (documented). |
| T-03-04b | Elevation of Privilege | over-scoped PAT documented | medium | mitigate | Docs and template specify read:packages scope only (least privilege). |
| T-03-04c | Tampering | cross-package snippet drifting from shipped API | low | mitigate | Integration block is a marked doc-check block; compiled against dist/*.d.ts under both resolutions before locking. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs this plan. |
</threat_model>

<verification>
- `npm run doc-check` (full) exits 0 with the root integration block compiling.
- Root README maps all five packages, has the integration snippet, and the Consuming from GitHub Packages section.
- .npmrc.example present, correct scope+auth lines, no real token, distinct from the Phase 4 project .npmrc.
</verification>

<success_criteria>
The root README maps the monorepo and shows a verified cross-package integration example (DOCS-02), and a Consuming from GitHub Packages doc plus an .npmrc.example template cover the consumer read:packages PAT (DOCS-03).
</success_criteria>

<output>
Create `.planning/phases/03-docs/03-04-SUMMARY.md` when done.
</output>
