---
phase: 03-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/doc-check/extract-snippets.mjs
  - tools/doc-check/tsconfig.node16.json
  - tools/doc-check/tsconfig.bundler.json
  - .gitignore
  - package.json
  - packages/kit/README.md
autonomous: true
requirements: [DOCS-01]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "`npm run doc-check` exits 0: the @willram/kit README Quickstart compiles against kit's shipped dist/*.d.ts under BOTH node16 and bundler resolution (DOCS-01)"
    - "The extractor harvests only ```ts fences immediately preceded by a doc-check HTML-comment marker; unmarked illustrative fragments are never extracted or compiled (DOCS-01 adjacency)"
    - "Re-running `npm run doc-check` on unchanged docs is deterministic and exits 0 — the extractor rmSync-clears and regenerates tools/doc-check/.snippets/ each run (DOCS-02 idempotency seam, proven by the harness here)"
    - "The extractor is existsSync-guarded and skips doc files that do not yet exist, so partial phase states (root README not yet created) do not break the run (DOCS-01 empty/missing input)"
    - "One deterministic snippet .ts is written per marked block, named by slugged source path plus document-order index; order is stable across runs (DOCS-01 ordering)"
  artifacts:
    - tools/doc-check/extract-snippets.mjs
    - tools/doc-check/tsconfig.node16.json
    - tools/doc-check/tsconfig.bundler.json
    - "root package.json scripts.doc-check and scripts.doc-check:snippets"
    - ".gitignore entry tools/doc-check/.snippets/"
    - "packages/kit/README.md normalized to the shared section template with one marked, self-contained Quickstart"
  key_links:
    - "doc-check tsconfigs set experimentalDecorators:true + useDefineForClassFields:false so kit @bind/@watch snippets compile (Pitfall 3, tsconfig.base.json:4-5)"
    - "doc-check runs `npm run build` first so @willram/* imports resolve exports-map -> dist/*.d.ts (Pitfall 4)"
    - "extractor writes to gitignored tools/doc-check/.snippets/; both tsconfigs include [.snippets/*.ts] and must NOT set allowImportingTsExtensions or include workspace src/*.ts"
---

<objective>
TRACER SLICE — stand up the standalone authoring-time doc-check harness and prove ONE package's README quickstart compiles end-to-end against the shipped types under both module resolutions. This is the thinnest vertical path that touches every layer this phase relies on: fenced-block extraction -> generated scratch .ts -> tsc under node16 AND bundler -> exports-map resolution to dist/*.d.ts -> exit 0. Once green, Wave 2 expands the same guarantee to the remaining 4 package READMEs, the root README integration snippet, LICENSE files, and .npmrc.example.

The harness is a direct extension of the Phase 1 BUILD-06 smoke harness at tools/typecheck-smoke/ (D-03). It stays a standalone script wired only into root package.json — it MUST NOT touch Phase 2's .github/workflows/ci.yml (D-04).

Purpose: make the "examples actually run" guarantee (DOCS-01) real from task one rather than bolted on last.
Output: tools/doc-check/ (extractor + two tsconfigs), the `doc-check` npm scripts, the .gitignore entry, and a normalized @willram/kit README whose Quickstart is the first marked-and-compiled block.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-docs/03-CONTEXT.md
@.planning/phases/03-docs/03-RESEARCH.md
@.planning/phases/03-docs/03-PATTERNS.md
</context>

## Artifacts This Phase Produces (whole-phase ledger)

Net-new files/symbols created across all Phase 3 plans (this plan owns the harness + kit README; the rest are owned by the plans noted):

| Artifact | Owning plan | Kind |
|----------|-------------|------|
| `tools/doc-check/extract-snippets.mjs` | 03-01 | zero-dep Node/ESM extractor |
| `tools/doc-check/tsconfig.node16.json` | 03-01 | tsc config (nodenext/node16 + decorator opts) |
| `tools/doc-check/tsconfig.bundler.json` | 03-01 | tsc config (esnext/bundler + decorator opts) |
| root `package.json` `scripts.doc-check` + `scripts.doc-check:snippets` | 03-01 | npm scripts |
| `.gitignore` entry `tools/doc-check/.snippets/` | 03-01 | ignore rule |
| the `<!-- doc-check -->` fenced-block opt-in marker convention | 03-01 | authoring convention |
| `packages/kit/README.md` marked Quickstart + template normalization | 03-01 | doc |
| `packages/query/README.md`, `packages/forms/README.md` normalized + marked | 03-02 | doc |
| `packages/router/README.md`, `packages/store/README.md` normalized + marked | 03-03 | doc |
| root `README.md` (monorepo map + cross-package snippet + Consuming section) | 03-04 | doc (net-new) |
| root `.npmrc.example` | 03-04 | config (net-new) |
| root `LICENSE` + `packages/{kit,router,query,forms,store}/LICENSE` (6 files) | 03-05 | legal (net-new) |
| root `package.json` `"license": "MIT"` field | 03-05 | config |
| one changeset covering the five `@willram/*` packages | 03-05 | changeset |

## Spec-less Edge-Probe Dispositions (authoritative, 11/11 — no silent drops)

Every predicate from the phase edge-probe is either authored as a resolved truth (in the owning plan's must_haves) or flagged N/A. Count equals the 11 applicable items.

| # | Requirement | Category | Status | Disposition |
|---|-------------|----------|--------|-------------|
| 1 | DOCS-01 | adjacency | resolved | Extractor matches only ```ts fences immediately preceded by the doc-check marker; adjacent marked/unmarked blocks extract as separate .ts files, never merged. (03-01) |
| 2 | DOCS-01 | empty | resolved | Zero marked blocks in a file -> zero snippets (extractor skips); but every packages/*/README.md is REQUIRED to carry >=1 marked Quickstart, presence-grep asserted. (03-01/02/03) |
| 3 | DOCS-01 | ordering | resolved | One deterministic .ts per marked block, named slugged-path plus document-order index; stable across runs. (03-01) |
| 4 | DOCS-01 | concurrency | N/A (flagged) | Single-process authoring-time script over static files; no concurrent/parallel runtime semantics. |
| 5 | DOCS-02 | idempotency | resolved | Re-running `npm run doc-check` on unchanged docs is deterministic and exits 0 (.snippets/ rmSync-regenerated each run). (03-01/04) |
| 6 | DOCS-02 | concurrency | N/A (flagged) | Static docs, single-process check; no concurrent runtime behavior. |
| 7 | DOCS-03 | unclassified | resolved | `.npmrc.example` is a committed consumer template distinct from Phase 4's project `.npmrc` (D-07); carries BOTH scope->registry map AND an env-expanded auth line placeholder, never a real token. (03-04) |
| 8 | DOCS-04 | adjacency | resolved | Root LICENSE and each packages/*/LICENSE are 6 distinct identical-text copies, never merged/symlinked. (03-05) |
| 9 | DOCS-04 | empty | resolved | A non-empty LICENSE exists at repo root AND every packages/* dir, each containing `MIT License` + `Copyright (c) 2026 Will Ramanand`. (03-05) |
| 10 | DOCS-04 | ordering | N/A (flagged) | LICENSE files are independent static artifacts; no ordering/stability semantics apply. |
| 11 | DOCS-04 | concurrency | N/A (flagged) | Static license files; no concurrent/interrupted runtime behavior. |

<tasks>

<task type="tracer">
  <name>Task 1: Stand up the doc-check harness and prove kit's Quickstart compiles end-to-end</name>
  <files>tools/doc-check/extract-snippets.mjs, tools/doc-check/tsconfig.node16.json, tools/doc-check/tsconfig.bundler.json, .gitignore, package.json, packages/kit/README.md</files>
  <read_first>
    - tools/typecheck-smoke/tsconfig.node16.json and tsconfig.bundler.json (the BUILD-06 base to copy verbatim, then add two options)
    - tools/typecheck-smoke/consumer-router.ts:9-11 (the "do NOT add allowImportingTsExtensions / do NOT include workspace src" convention to carry over)
    - packages/kit/README.md:13-46 (the existing Quickstart block that becomes the marked one) and packages/kit/README.md:14-15 (correct html-from-lit import pattern)
    - packages/kit/src/index.ts (kit's real export surface — confirms no html export; the Quickstart may only import symbols listed here)
    - tsconfig.base.json:4-5 (experimentalDecorators + useDefineForClassFields settings the doc-check tsconfigs must mirror for @bind/@watch)
    - package.json:12 (root scripts.typecheck:smoke — the sibling script pattern to mirror for doc-check)
    - 03-RESEARCH.md Pattern 1 (extractor shape) and Code Example 2 (tsconfig)
  </read_first>
  <reversibility rating="reversible">Authoring-time tooling and one README marker; fully revertible, no published contract.</reversibility>
  <action>
    Create tools/doc-check/extract-snippets.mjs as a zero-dependency Node/ESM script using only node:fs. It iterates a fixed list of six doc paths — README.md (root), packages/kit/README.md, packages/router/README.md, packages/query/README.md, packages/forms/README.md, packages/store/README.md — existsSync-guarding each so a not-yet-created file (root README during this tracer) is skipped rather than throwing. For each existing file it selects only fenced ts blocks that are immediately preceded by the HTML-comment marker doc-check (regex over the file text, matching the marker then a ts-tagged fence and capturing the block body). Before extraction it rmSync-clears (recursive, force) then mkdirSync (recursive) the scratch dir tools/doc-check/.snippets/, then writes one .ts file per marked block named as the slugged source path plus a document-order index. The scratch dir is gitignored, so nothing generated is committed. Do NOT add a markdown-parser dependency — the marker regex over readFileSync is the whole extractor (research Standard Stack: zero new deps, no legitimacy audit).

    Create tools/doc-check/tsconfig.node16.json by copying tools/typecheck-smoke/tsconfig.node16.json verbatim, then making exactly three changes: set experimentalDecorators to true, set useDefineForClassFields to false (both required because kit quickstarts use @bind/@watch — Pitfall 3), and change include to [".snippets/*.ts"]. Create tools/doc-check/tsconfig.bundler.json the same way from the smoke bundler config (it differs only by module esnext / moduleResolution bundler). Do NOT set allowImportingTsExtensions and do NOT include workspace src/*.ts on either config — that would resolve internal source instead of the exports-map dist/*.d.ts and defeat the check (same warning the BUILD-06 consumers carry).

    Add tools/doc-check/.snippets/ to .gitignore (alongside the existing dist/coverage entries).

    Add two scripts to the root package.json: doc-check runs npm run build, then node tools/doc-check/extract-snippets.mjs, then tsc -p tools/doc-check/tsconfig.node16.json, then tsc -p tools/doc-check/tsconfig.bundler.json (chained so a failure at any step fails the run); and doc-check:snippets as a fast authoring path that runs the same chain WITHOUT the leading npm run build (Pitfall 4 / research Open Question 1 — build-first for correctness, skip-build for iteration against an existing dist/). Editing root package.json does NOT trip the changeset gate (root is private, not a published workspace package) — the covering changeset for the five packages is handled in 03-05.

    In packages/kit/README.md, place the doc-check HTML-comment marker on the line immediately before the existing Quickstart ts fence (kit README:13-46) so the harness picks it up. Ensure that block is fully self-contained: every imported symbol must be a real @willram/kit export (per kit/src/index.ts) and html must come from the lit package, not kit. Trim any imported-but-unused symbols so the block reads as copy-pasteable. Leave the rest of the README untouched in this task (full template normalization is Task 2). Confirm the block type-checks under both resolutions by running the harness.
  </action>
  <verify>
    <automated>npm run build && node tools/doc-check/extract-snippets.mjs && tsc -p tools/doc-check/tsconfig.node16.json && tsc -p tools/doc-check/tsconfig.bundler.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm run doc-check` exits 0.
    - `git status --porcelain tools/doc-check/.snippets` is empty (scratch dir is gitignored, nothing staged).
    - After running the extractor, `ls tools/doc-check/.snippets` lists at least one generated .ts file whose source slug is packages-kit-README.
    - `grep -c experimentalDecorators tools/doc-check/tsconfig.node16.json` and `...tsconfig.bundler.json` each return 1; the same for useDefineForClassFields.
    - `grep -c allowImportingTsExtensions tools/doc-check/tsconfig.node16.json` returns 0.
    - `grep -c '"doc-check"' package.json` returns 1 and `grep -c '"doc-check:snippets"' package.json` returns 1.
    - `grep -c 'tools/doc-check/.snippets' .gitignore` returns 1.
    - Re-running the extractor twice in a row leaves an identical `.snippets/` file set (idempotent).
  </acceptance_criteria>
  <done>The doc-check harness exists, is wired into root package.json, ignores its scratch output, and `npm run doc-check` proves the @willram/kit Quickstart compiles against dist/*.d.ts under both node16 and bundler resolution. ci.yml is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize the kit README to the shared section template and add the consumer TypeScript-config note</name>
  <files>packages/kit/README.md</files>
  <read_first>
    - packages/kit/README.md (full — this is the canonical README all others normalize toward)
    - 03-PATTERNS.md Pattern 2 / RESEARCH Pattern 2 (the shared section-order template)
    - tsconfig.base.json:4-5 (the two settings consumers using kit decorators must enable)
    - 03-RESEARCH.md Pitfall 3 (why the consumer note prevents a support ticket, satisfying DOCS-01)
  </read_first>
  <reversibility rating="reversible">Documentation-only heading reshuffle; revertible.</reversibility>
  <action>
    Normalize packages/kit/README.md heading order to the shared template used by all five packages (D-01 — kit is the canonical README the other four normalize toward): a level-1 title plus one-line description; an Install section carrying the npm install line with required peers (kit needs lit); a Quickstart section holding the single marked block from Task 1; a Core API section keeping the existing accurate reference tables and fragments as illustrative (unmarked) content; and a closing pointer line linking to the root README for the monorepo map and cross-package example. Keep the existing Core API content — only trim genuine drift, do not rewrite. Preserve the existing marker on the Quickstart block.

    Add a short "TypeScript config" note near the Quickstart telling consumers who use kit decorators (bind, watch, debounce, throttle) to enable experimentalDecorators true and useDefineForClassFields false in their tsconfig — these are the same settings the repo compiles under (tsconfig.base.json:4-5) and the reason the doc-check tsconfigs set them (Pitfall 3). This closes the DOCS-01 "install and build without a support ticket" gap for decorator users.

    Run the harness again after editing to confirm the marked Quickstart still compiles.
  </action>
  <verify>
    <automated>npm run doc-check:snippets</automated>
  </verify>
  <acceptance_criteria>
    - `npm run doc-check:snippets` exits 0 (kit Quickstart still compiles under both resolutions).
    - `grep -c '## Install' packages/kit/README.md` returns >=1 and `grep -c '## Quickstart' packages/kit/README.md` returns >=1.
    - `grep -c experimentalDecorators packages/kit/README.md` returns >=1 (consumer TypeScript-config note present).
    - `grep -c 'root README' packages/kit/README.md` returns >=1 (closing pointer to the monorepo map).
    - The Quickstart block retains its doc-check marker (`grep -c 'doc-check' packages/kit/README.md` returns >=1).
  </acceptance_criteria>
  <done>packages/kit/README.md follows the shared section template, documents the decorator tsconfig requirement for consumers, links to the root README, and its Quickstart still passes the doc-check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Authoring-time tooling + static docs; no runtime code path, no external input, no secret handled in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01a | Tampering | doc-check tsconfigs resolving internal src instead of dist | low | mitigate | Forbid allowImportingTsExtensions and workspace-src include (asserted by grep); resolution stays exports-map -> dist/*.d.ts. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs this phase (RESEARCH: zero new dependencies); package-legitimacy gate not applicable. |
</threat_model>

<verification>
- `npm run doc-check` exits 0 (build + extract + both tsc runs).
- Harness files present; .snippets/ gitignored; ci.yml unchanged (`git status .github/workflows/ci.yml` shows no modification).
- kit README normalized to the shared template with a marked, compiling Quickstart and the decorator tsconfig note.
</verification>

<success_criteria>
The authoring-time doc-check harness proves at least one README quickstart (@willram/kit) compiles against shipped dist/*.d.ts under both node16 and bundler, with the marker/extractor/tsconfig/npm-script path fully wired and ci.yml untouched — the tracer for DOCS-01.
</success_criteria>

<output>
Create `.planning/phases/03-docs/03-01-SUMMARY.md` when done.
</output>
