---
phase: 04-release-automation-publish
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - packages/kit/package.json
  - packages/router/package.json
  - packages/query/package.json
  - packages/forms/package.json
  - packages/store/package.json
  - .npmrc
autonomous: true
requirements: [RLS-02, RLS-03, RLS-06]

estimate:
  tokens: 54000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every one of the five `@willram/*` package.json declares `publishConfig.registry` = `https://npm.pkg.github.com` (RLS-02)"
    - "Every one of the five package.json `files` allowlist is exactly `[\"dist\", \"README.md\", \"LICENSE\", \"CHANGELOG.md\"]` (RLS-02, D-05)"
    - "Every one of the five package.json has `\"prepublishOnly\": \"npm run typecheck && npm run build\"` (RLS-06, D-06)"
    - "A committed root `.npmrc` maps the `@willram` scope to `https://npm.pkg.github.com` (RLS-03)"
  artifacts:
    - "packages/{kit,router,query,forms,store}/package.json — each with publishConfig, extended files, prepublishOnly"
    - ".npmrc (NEW, committed) — auth-free scope→registry line"
  key_links:
    - "`prepublishOnly` runs on `changeset publish` (npm lifecycle hook) — the build-before-publish guard for BOTH the manual 1.0.0 and CI 1.0.1+"
    - "The scoped `@willram:` registry line routes ONLY @willram/* to GitHub Packages; a global unscoped line would reroute lit/tanstack/vite/typescript and break `npm ci`"
  prohibitions:
    - "The committed `.npmrc` has NO unscoped global registry directive (would break `npm ci` for public deps)"
    - "The committed `.npmrc` has NO committed auth token — auth is runtime-only (local ~/.npmrc for the manual publish; actions/setup-node in CI)"
    - "Root package.json stays `private: true` and gets NO publishConfig/files edits — only the five workspace packages publish"
    - "Do NOT overwrite or edit the Phase-3 consumer `.npmrc.example` — the committed project `.npmrc` is a DISTINCT, auth-free file (D-07 seam)"
---

<objective>
Add the per-package publish metadata (RLS-02, RLS-06) and the committed auth-free root `.npmrc` (RLS-03) that make the five `@willram/*` packages publishable to GitHub Packages. All five package.json get an identical additive edit — a `publishConfig.registry`, an extended `files` allowlist, and a `prepublishOnly` guard. The new root `.npmrc` carries only the scoped `@willram` registry line, no auth.

Purpose: Encode the routing + tarball-contents + build-before-publish invariants that both the manual 1.0.0 publish (Plan 04-04) and steady-state CI releases depend on, with no secret ever committed.
Output: Five edited package.json files and a new committed `.npmrc`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-release-automation-publish/04-CONTEXT.md
@.planning/phases/04-release-automation-publish/04-RESEARCH.md
@.planning/phases/04-release-automation-publish/04-PATTERNS.md
@packages/kit/package.json
@.npmrc.example
</context>

## Artifacts this phase produces (this plan)

- `packages/kit/package.json`, `packages/router/package.json`, `packages/query/package.json`, `packages/forms/package.json`, `packages/store/package.json` — each gains a top-level `publishConfig.registry`, an extended `files` array `["dist", "README.md", "LICENSE", "CHANGELOG.md"]`, and a `prepublishOnly` script.
- `.npmrc` (NEW, committed at repo root) — a single auth-free line mapping the `@willram` scope to the GitHub Packages registry.

<tasks>

<task type="auto">
  <name>Task 1: Add publishConfig, extend files, and add prepublishOnly to all five packages</name>
  <files>packages/kit/package.json, packages/router/package.json, packages/query/package.json, packages/forms/package.json, packages/store/package.json</files>
  <read_first>
    - packages/kit/package.json (the canonical current shape — additive edit only; the other four are byte-identical in the fields this touches)
    - .planning/phases/04-release-automation-publish/04-PATTERNS.md (the three edits, applied identically to all five; do-not-touch list)
    - .planning/phases/04-release-automation-publish/04-CONTEXT.md (D-05 files allowlist, D-06 prepublishOnly string)
  </read_first>
  <action>Apply the identical additive edit to all five `packages/{kit,router,query,forms,store}/package.json`. Edit 1 (D-05, RLS-02): replace the `files` array value `["dist"]` with `["dist", "README.md", "LICENSE", "CHANGELOG.md"]` — CHANGELOG.md does not exist on disk yet and npm silently omits missing entries, so the 1.0.0 tarball ships dist+README+LICENSE and CHANGELOG.md first appears at 1.0.1; this is expected, not a gap. Edit 2 (RLS-02): add a new top-level key `publishConfig` with a single member `registry` set to `https://npm.pkg.github.com`. Edit 3 (D-06, RLS-06): add a `prepublishOnly` script whose value is exactly `npm run typecheck && npm run build` into the existing `scripts` object (each package already exposes `typecheck` and `build`, so the hook resolves). Preserve every other field exactly — `name`, `version` (stays `1.0.0`), `type`, `exports`, `main`, `module`, `types`, `peerDependencies`, `devDependencies`, and the per-package `repository.directory`. Do NOT edit the root `package.json`: it stays `private: true` and never gets publishConfig/files/prepublishOnly (only the five workspace packages publish).</action>
  <verify>
    <automated>node -e "for (const p of ['kit','router','query','forms','store']) { const j=require('./packages/'+p+'/package.json'); const ok = j.publishConfig && j.publishConfig.registry==='https://npm.pkg.github.com' && JSON.stringify(j.files)===JSON.stringify(['dist','README.md','LICENSE','CHANGELOG.md']) && j.scripts.prepublishOnly==='npm run typecheck && npm run build'; if(!ok){console.error('FAIL',p,JSON.stringify({pc:j.publishConfig,files:j.files,pp:j.scripts.prepublishOnly}));process.exit(1);} } const r=require('./package.json'); if(r.private!==true){console.error('root must stay private');process.exit(1);} console.log('all five OK; root private');"</automated>
  </verify>
  <acceptance_criteria>
    - For each of the five packages, `package.json` contains `"publishConfig": { "registry": "https://npm.pkg.github.com" }`.
    - For each of the five packages, `files` is exactly `["dist", "README.md", "LICENSE", "CHANGELOG.md"]`.
    - For each of the five packages, `scripts.prepublishOnly` is exactly `npm run typecheck && npm run build`.
    - `npm pack --dry-run -w @willram/kit` lists `dist/…`, `README.md`, and `LICENSE` (no CHANGELOG.md yet — expected at 1.0.0).
    - Root `package.json` still has `"private": true` and no `publishConfig`.
  </acceptance_criteria>
  <reversibility rating="reversible">Package metadata edits are trivially revertible before any publish.</reversibility>
  <done>All five package.json declare publishConfig.registry, the four-entry files allowlist, and the prepublishOnly guard; root stays private.</done>
</task>

<task type="auto">
  <name>Task 2: Create the committed auth-free root .npmrc</name>
  <files>.npmrc</files>
  <read_first>
    - .npmrc.example (Phase-3 consumer template — copy ONLY its scope→registry line; its auth line is what makes it consumer-side and must NOT be reproduced here)
    - .planning/phases/04-release-automation-publish/04-PATTERNS.md (`.npmrc` section — entire file is one line; critical constraints)
    - .planning/phases/04-release-automation-publish/04-RESEARCH.md (Pattern 3; Pitfall 3 — an unscoped global registry breaks npm ci)
  </read_first>
  <action>Create a NEW file `.npmrc` at the repo root whose only non-comment content is the single scoped line `@willram:registry=https://npm.pkg.github.com`. This is the one line SHARED with the Phase-3 `.npmrc.example`. Do NOT add an unscoped global registry directive — that would route every dependency (lit, @tanstack, vite, typescript) to GitHub Packages and break `npm ci`. Do NOT add any committed auth-token line — authentication is runtime-only: locally the maintainer supplies it via `~/.npmrc` for the manual 1.0.0 publish, and in CI `actions/setup-node` writes the runtime auth at publish time. Omit `always-auth` (not needed on npm 11.x). Leave the existing Phase-3 `.npmrc.example` completely untouched — it is a DISTINCT consumer-side file (D-07 seam).</action>
  <verify>
    <automated>test -f .npmrc && grep -q '@willram:registry=https://npm.pkg.github.com' .npmrc && test "$(grep -vE '^[[:space:]]*(#|$)' .npmrc | grep -cvE '^@willram:')" = "0" && git ls-files --error-unmatch .npmrc.example >/dev/null 2>&1 && echo "npmrc OK; example untouched-and-tracked"</automated>
  </verify>
  <acceptance_criteria>
    - `.npmrc` exists at repo root and contains `@willram:registry=https://npm.pkg.github.com`.
    - Every non-comment, non-blank line of `.npmrc` begins with `@willram:` — i.e. there is NO unscoped global registry directive and NO committed token line (structural proof via `grep -vE '^[[:space:]]*(#|$)' .npmrc | grep -cvE '^@willram:'` equals `0`).
    - `.npmrc.example` is unchanged from Phase 3 (still tracked, not overwritten).
  </acceptance_criteria>
  <reversibility rating="reversible">A new one-line config file; trivially revertible.</reversibility>
  <done>A committed root `.npmrc` routes only the `@willram` scope to GitHub Packages, carries no auth, and the Phase-3 consumer template is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed repo → npm registry | The committed `.npmrc` routes package resolution; it must never carry a credential |
| package.json `files` → published tarball | The allowlist decides exactly what bytes ship to consumers |
| root workspace → registry | The root package must never be published (it is `private`) |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Information Disclosure | committed `.npmrc` | high | mitigate | Committed `.npmrc` is auth-free (scope→registry only); the structural verify asserts every line begins with `@willram:`, so no token line can slip in — auth stays runtime-only |
| T-04-04 | Tampering | `.npmrc` routing | high | mitigate | Only the scoped `@willram:` line is present — no unscoped global registry directive that would reroute public deps to GitHub Packages and break `npm ci` |
| T-04-05 | Elevation of Privilege | root private package | medium | mitigate | Root `package.json` stays `private: true` with no publishConfig/files edits; the verify asserts `private===true`, so the root can never be accidentally published |
| T-04-SC | Tampering | supply chain | low | accept | This plan installs no packages and adds no third-party action — SHA-pin control lives in Plan 04-03 |
</threat_model>

<verification>
- `node` audit script confirms all five package.json carry publishConfig.registry, the four-entry files allowlist, and the prepublishOnly guard, and that root stays private.
- `.npmrc` exists, contains the scoped line, and every non-comment line begins with `@willram:` (no global registry, no token).
- `.npmrc.example` remains tracked and unchanged.
- `npm pack --dry-run -w @willram/kit` shows dist+README+LICENSE (CHANGELOG.md correctly absent at 1.0.0).
</verification>

<success_criteria>
- All five packages are publish-ready: registry routing, tarball allowlist, and build-before-publish guard in place (RLS-02, RLS-06).
- A committed, auth-free root `.npmrc` routes only `@willram` to GitHub Packages (RLS-03).
- No secret is committed; root package stays private; the Phase-3 consumer template is untouched.
</success_criteria>

<output>
Create `.planning/phases/04-release-automation-publish/04-02-SUMMARY.md` when done.
</output>
