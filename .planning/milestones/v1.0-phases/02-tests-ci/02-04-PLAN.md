---
phase: 02-tests-ci
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - package.json
  - package-lock.json
  - .changeset/config.json
  - .changeset/tests-ci-router-link-fix.md
  - vitest.config.ts
  - packages/query/vite.config.ts
  - packages/store/vite.config.ts
autonomous: false
requirements: [TEST-01, TEST-02, TEST-04, TEST-05, TEST-06]
user_setup: []
estimate:
  tokens: 72000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "@vitest/coverage-v8 is installed at 4.1.9 to exactly match the installed vitest 4.1.9 (no version drift)"
    - "npx changeset status reports no changed-package-without-changeset (exits 0) after the phase's covering changesets are authored; it exits non-zero when a package changes with no covering changeset"
    - "npx publint reports no publishing errors for all five packages and would flag a package whose exports place the types condition after import (condition-order correctness)"
    - "npx attw --pack --profile esm-only passes for all five ESM-only packages with no false CJS-resolution failures"
    - "npx vitest run --coverage produces a v8 coverage report (text + json-summary) with no threshold gate"
    - "query observer+mutation and store slice suites pass under the new setupFiles wiring (npm run test -w @willram/query and npm run test -w @willram/store are green)"
    - "packages/query/vite.config.ts and packages/store/vite.config.ts both reference setupFiles ['../../test-setup.ts']"
  artifacts:
    - .changeset/config.json
    - .changeset/tests-ci-router-link-fix.md
    - vitest.config.ts
    - package.json
    - packages/query/vite.config.ts
    - packages/store/vite.config.ts
  key_links:
    - ".changeset/config.json baseBranch: main → npx changeset status --since origin/main"
    - "vitest.config.ts projects ['packages/*'] + coverage.provider v8 → aggregated report-only coverage"
    - "root package.json devDependencies pin @vitest/coverage-v8 to the installed vitest version"
  prohibitions:
    - "coverage MUST be report-only — no percentage/minimum coverage gate configured (Out of Scope in REQUIREMENTS.md)"
    - "Phase 2 seeds a MINIMAL .changeset/config.json (baseBranch: main; access: restricted is harmless) — do NOT add a fixed/lockstep group or publish wiring; Phase 4 EXTENDS this file and it must NOT be recreated (D-05 seam)"
    - "@vitest/coverage-v8 version MUST NOT drift from the installed vitest version (both 4.1.9)"
    - "store's vite.config MUST NOT be forced onto jsdom just to load the setup — keep node env and rely on the guarded matchMedia stub (RESEARCH Pitfall 5)"
---

<objective>
Install and prove the read-only gate tooling (coverage-v8, publint, attw, changesets), seed the minimal changeset config (D-05), wire the remaining two packages (query, store) into the shared jsdom setup, and configure report-only aggregated coverage — everything the CI workflow (Plan 05) will run, proven locally first.

Purpose: TEST-04 (publint + attw), TEST-05 (changeset status gate + config, D-05: minimal changesets pulled forward), TEST-06 (v8 coverage report-only) — all per D-04 (single read-only ci-facing tooling); plus TEST-01/TEST-02 for query/store (D-03: wire the shared `test-setup.ts` via setupFiles; their suites already exist and this plan confirms green).
Output: pinned dev tools, `.changeset/config.json` + covering changesets, root `vitest.config.ts`, root scripts, query/store setupFiles wiring — all gate commands passing locally.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tests-ci/02-CONTEXT.md
@.planning/phases/02-tests-ci/02-RESEARCH.md
@.planning/phases/02-tests-ci/02-PATTERNS.md
@package.json
@packages/query/vite.config.ts
@packages/store/vite.config.ts
</context>

## Artifacts this phase produces (Plan 04 scope)

- Root `package.json` — EDIT. Add devDependencies `@vitest/coverage-v8@4.1.9`, `publint@^0.3`, `@arethetypeswrong/cli@^0.18`, `@changesets/cli@^3`; add scripts `coverage` (aggregated vitest run with coverage) and `changeset:status`.
- `package-lock.json` — updated by the install.
- `.changeset/config.json` — NEW. Minimal: `$schema`, `changelog`, `commit: false`, `access: restricted`, `baseBranch: main`, `updateInternalDependencies: patch`, `ignore: []` (shape at RESEARCH lines 335-347). NO `fixed`/lockstep group (Phase 4 seam).
- `.changeset/tests-ci-router-link-fix.md` — NEW. Patch changeset for `@willram/router` (the link.ts leak + dup-subscription fixes). Plus any additional covering changesets required so `changeset status` reports clean for this phase's package changes.
- `vitest.config.ts` (root) — NEW. `projects: ['packages/*']`, `coverage.provider: 'v8'`, report-only reporters (`text`, `json-summary`), no minimum gate.
- `packages/query/vite.config.ts` — EDIT. Add `setupFiles: ['../../test-setup.ts']`.
- `packages/store/vite.config.ts` — EDIT. Add a `test` block with `setupFiles: ['../../test-setup.ts']`, keeping node env (no `environment: 'jsdom'`).

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy confirmation before install (SUS-flagged deps)</name>
  <what-built>
    Nothing built yet — this gate precedes the `npm install` of four net-new dev tools. The RESEARCH Package Legitimacy Audit (02-RESEARCH.md lines 71-85) verdicts `@vitest/coverage-v8` and `@arethetypeswrong/cli` as OK/Approved, and flags `publint` and `@changesets/cli` as SUS strictly for `too-new` (their most recent patch was days old). Both SUS packages are long-established first-party tools (publint 1.0M/wk, @changesets/cli 4.3M/wk, canonical GitHub repos, `postinstall: null`). The researcher's disposition is "Approved — false positive."
  </what-built>
  <how-to-verify>
    Confirm the two SUS-flagged packages are legitimate before install:
    - Open https://www.npmjs.com/package/publint — confirm repo github.com/publint/publint, weekly downloads ~1M, no install scripts.
    - Open https://www.npmjs.com/package/@changesets/cli — confirm repo github.com/changesets/changesets, weekly downloads ~4M, no install scripts.
    - Optional belt-and-suspenders: pin either to a patch published >30 days ago.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install, or name a version to pin.</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: TRACER — install gate tooling + changeset config, proven by running the gates locally</name>
  <files>package.json, package-lock.json, .changeset/config.json, .changeset/tests-ci-router-link-fix.md</files>
  <read_first>
    - .planning/phases/02-tests-ci/02-RESEARCH.md lines 30-61 (stack + exact install command), 188-195 (attw esm-only), 204-214 (don't hand-roll), 334-347 (minimal changeset config), 238-242 (fetch-depth pitfall)
    - .planning/phases/02-tests-ci/02-CONTEXT.md D-04, D-05 (CI read-only + minimal changesets forward, cross-phase seam)
    - package.json (root — current scripts/deps)
  </read_first>
  <action>
    Install the four dev tools at the root workspace pinned per RESEARCH: `@vitest/coverage-v8@4.1.9` (exact match to the installed vitest 4.1.9), `publint@^0.3`, `@arethetypeswrong/cli@^0.18`, `@changesets/cli@^3`. Use the `npm install -D -w .` form from RESEARCH line 59.
    Create `.changeset/config.json` with the minimal shape from RESEARCH lines 335-347: `$schema`, `changelog: "@changesets/cli/changelog"`, `commit: false`, `access: restricted`, `baseBranch: main`, `updateInternalDependencies: patch`, `ignore: []`. Do not add a lockstep/pinned-version group or any publish wiring — Phase 4 extends this file (D-05 seam).
    Author `.changeset/tests-ci-router-link-fix.md` as a `@willram/router` patch changeset describing the link.ts event-listener-leak and duplicate-subscription fixes. Then run `npx changeset status` and, for any additional package it reports as changed-without-changeset in this phase, add a covering patch changeset (describe as test/CI hardening) until the command reports clean.
    Run the gate commands locally to prove they function: `npx changeset status` (ensure the `main` base ref is available; `git fetch origin main` if needed), `npx publint` over each of the five packages, and `npx attw --pack --profile esm-only` over each of the five built packages (run `npm run build` first so `dist/` exists).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; for d in packages/*; do npx publint "$d" || exit 1; done &amp;&amp; for d in packages/*; do npx attw --pack "$d" --profile esm-only || exit 1; done &amp;&amp; npx changeset status</automated>
  </verify>
  <acceptance_criteria>
    - `node -p "require('./package.json').devDependencies['@vitest/coverage-v8']"` prints a 4.1.9 specifier matching the installed vitest.
    - `.changeset/config.json` exists with `baseBranch` = `main`; `grep -c "fixed" .changeset/config.json` returns 0 (no lockstep group seeded).
    - `npx changeset status` exits 0 (all phase package changes covered).
    - `npx publint` exits 0 for all five packages; `npx attw --pack --profile esm-only` exits 0 for all five.
  </acceptance_criteria>
  <done>All four gate tools are installed and every gate command (publint, attw esm-only, changeset status) runs clean against the real repo — the exports/types + changeset gates are proven end-to-end locally.</done>
  <reversibility rating="reversible">Dev-dependency install + config files; removable.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Report-only coverage config + query/store setupFiles wiring + root scripts</name>
  <files>vitest.config.ts, package.json, packages/query/vite.config.ts, packages/store/vite.config.ts</files>
  <read_first>
    - .planning/phases/02-tests-ci/02-RESEARCH.md lines 362-371 (projects vs workspace), 384-388 (coverage shape recommendation), 255-259 (Pitfall 5: store node env + guarded matchMedia)
    - .planning/phases/02-tests-ci/02-PATTERNS.md lines 189-198 (vite.config test block edit; store has no test block today)
    - packages/query/vite.config.ts and packages/store/vite.config.ts (current)
    - package.json (root scripts)
  </read_first>
  <action>
    Create root `vitest.config.ts` with `test.projects: ['packages/*']`, `test.coverage.provider: 'v8'`, and reporters `['text', 'json-summary']`. Configure coverage as report-only — do not configure any coverage minimum. Set reasonable include/exclude globs (source under `packages/*/src`, exclude `*.test.ts` and `dist`).
    Edit `packages/query/vite.config.ts` to add `setupFiles: ['../../test-setup.ts']` to its existing `test` block.
    Edit `packages/store/vite.config.ts` to add a `test` block containing only `setupFiles: ['../../test-setup.ts']` — keep store on the default node test environment (do not set a browser DOM environment); the guarded matchMedia stub keeps node runs safe.
    Add root `package.json` scripts: `coverage` → `vitest run --coverage`, and `changeset:status` → `changeset status`.
    Run `npm run test` (all workspaces) and `npm run coverage` and confirm both are green and coverage prints a v8 summary.
  </action>
  <verify>
    <automated>npm run test &amp;&amp; npm run coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "setupFiles" packages/query/vite.config.ts packages/store/vite.config.ts` returns >= 1 for each file, values containing `../../test-setup.ts`.
    - `packages/store/vite.config.ts` does not force jsdom: `grep -c "jsdom" packages/store/vite.config.ts` returns 0.
    - `vitest.config.ts` configures no coverage minimum: `grep -cE "thresholds|lines:|branches:|statements:|functions:" vitest.config.ts` returns 0.
    - `npm run test` exits 0 (all five packages incl. query observer+mutation and store slice suites green); `npm run coverage` exits 0 and emits a v8 coverage summary.
  </acceptance_criteria>
  <done>Report-only aggregated coverage is configured, query/store are wired into the shared setup with their existing suites green, and the root has coverage + changeset scripts.</done>
</task>

</tasks>

<flagged_assumptions>
- Probe TEST-04 "empty/single/null input" — NOT APPLICABLE: publint/attw operate over each package's `package.json` + built `dist/` tarball, not element-wise input; there is no empty/single/null case for a packaging linter.
- Probe TEST-06 "precision/overflow/rounding/tie-breaking" — NOT APPLICABLE: coverage is report-only with no numeric threshold, so no comparison, rounding, or boundary arithmetic occurs.
- RESEARCH A3 (LOW): the exact `publint` invocation form (per-package dir arg vs a `for` loop) may need adjustment; the verify uses a `for` loop to be safe.
- RESEARCH A4 (LOW): root-aggregated coverage via `projects: ['packages/*']` assumes each package's vite config is discovered; verify each project is picked up in the coverage run.
- Local `changeset status` needs the `main` base ref available; `git fetch origin main` may be required locally (CI uses `fetch-depth: 0`).
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo | four net-new dev dependencies enter the dependency tree |
| dev tool install scripts → local/CI machine | postinstall hooks could execute arbitrary code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | npm installs (@vitest/coverage-v8, publint, @arethetypeswrong/cli, @changesets/cli) | high | mitigate | Package Legitimacy Audit run (RESEARCH lines 71-85): all four first-party, `postinstall: null`; two SUS-too-new false positives cleared via blocking human checkpoint (Task 1); versions pinned |
| T-02-09 | Tampering | version drift between @vitest/coverage-v8 and vitest | medium | mitigate | Pin coverage-v8 to the exact installed vitest (4.1.9); prohibition + acceptance criterion enforce it |
| T-02-10 | Elevation | changeset config seeding publish/access wiring too early | medium | accept | Seed only minimal config (baseBranch); access:restricted is harmless; no publish wiring; Phase 4 seam documented |

No high-severity threat is left unmitigated (ASVS L1, block_on: high).
</threat_model>

<verification>
- `npm run build` green, then `publint` + `attw --profile esm-only` green for all five packages.
- `npx changeset status` exits 0 with covering changesets present.
- `npm run test` + `npm run coverage` green; coverage is report-only (no threshold).
- query/store vite configs reference `../../test-setup.ts`; store stays node env.
</verification>

<success_criteria>
- TEST-04: publint + attw gate proven functional over every package's exports/types.
- TEST-05: changeset status gate real (minimal config + covering changesets; exits non-zero without a changeset).
- TEST-06: v8 coverage reported, report-only, no threshold gate.
- TEST-01/TEST-02 (query/store): suites green under shared setupFiles wiring.
</success_criteria>

<output>
Create `.planning/phases/02-tests-ci/02-04-SUMMARY.md` when done.
</output>
