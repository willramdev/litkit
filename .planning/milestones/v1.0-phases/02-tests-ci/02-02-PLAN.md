---
phase: 02-tests-ci
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - packages/forms/vite.config.ts
  - packages/forms/src/create-form.test.ts
  - packages/forms/src/field.test.ts
  - packages/forms/src/field-controller.test.ts
  - packages/forms/src/array-controller.test.ts
  - packages/forms/src/zod.test.ts
autonomous: true
requirements: [TEST-01, TEST-02]
estimate:
  tokens: 64000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "forms create-form, field, field-controller, array-controller, and zod suites pass (npm run test -w @willram/forms is green)"
    - "forms field-array (array-controller) preserves entry order across add/insert/remove/reorder operations"
    - "the @willram/forms/zod adapter returns undefined for valid input and an error string/messages for invalid input, matching the validator return contract"
    - "packages/forms/vite.config.ts test block references setupFiles ['../../test-setup.ts']"
  artifacts:
    - packages/forms/src/create-form.test.ts
    - packages/forms/src/field.test.ts
    - packages/forms/src/field-controller.test.ts
    - packages/forms/src/array-controller.test.ts
    - packages/forms/src/zod.test.ts
  key_links:
    - "forms tests construct a form via createForm(host, { initialValues }) then drive hostConnected/interact/hostDisconnected"
    - "packages/forms/vite.config.ts test.setupFiles → root test-setup.ts"
  prohibitions:
    - "forms tests MUST use explicit class fields in any helper, never constructor parameter properties (erasableSyntaxOnly)"
    - "@willram/forms tests MUST NOT import from sibling @willram/* packages other than the package under test"
---

<objective>
Close the HIGH-priority forms CONCERNS gaps with named critical-path suites for the field/array controllers, form creation, and the zod validator adapter, and wire the forms package into the shared jsdom setup.

Purpose: TEST-01 (D-01: named suites plus HIGH-priority CONCERNS gaps) requires named forms suites (field/array + zod) that pass; these files are genuinely net-new per the RESEARCH file inventory. TEST-02 (D-03) wires forms into the shared root `test-setup.ts` via setupFiles.
Output: Five forms test files + forms `vite.config.ts` setupFiles wiring, all green under `npm run test -w @willram/forms`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tests-ci/02-CONTEXT.md
@.planning/phases/02-tests-ci/02-RESEARCH.md
@.planning/phases/02-tests-ci/02-PATTERNS.md
@packages/forms/src/form-controller.test.ts
@packages/forms/src/group-controller.test.ts
@packages/forms/src/validators.test.ts
@packages/forms/src/create-form.ts
@packages/forms/src/field.ts
@packages/forms/src/field-controller.ts
@packages/forms/src/array-controller.ts
@packages/forms/src/zod.ts
@packages/forms/vite.config.ts
</context>

## Artifacts this phase produces (Plan 02 scope)

- `packages/forms/vite.config.ts` — EDIT. Add `setupFiles: ['../../test-setup.ts']` to the existing `test: { environment: 'jsdom' }` block.
- `packages/forms/src/create-form.test.ts` — NEW.
- `packages/forms/src/field.test.ts` — NEW.
- `packages/forms/src/field-controller.test.ts` — NEW.
- `packages/forms/src/array-controller.test.ts` — NEW.
- `packages/forms/src/zod.test.ts` — NEW.

No new dependencies (zod is already a dev dependency).

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TRACER — forms setupFiles wiring + create-form/field suites green</name>
  <files>packages/forms/vite.config.ts, packages/forms/src/create-form.test.ts, packages/forms/src/field.test.ts</files>
  <read_first>
    - packages/forms/src/form-controller.test.ts (lines 1-12 plain-object mock host; lines 44-102 lifecycle idiom; lines 132-149 async submit assertion)
    - .planning/phases/02-tests-ci/02-PATTERNS.md lines 110-140 (forms CRUD analog rules) and 189-198 (vite.config test block edit)
    - packages/forms/src/create-form.ts and packages/forms/src/field.ts (sources under test)
    - packages/forms/vite.config.ts (current test block)
  </read_first>
  <behavior>
    - create-form: createForm(host, { initialValues }) returns a controller; hostConnected registers it; setValue/reset mutate form state; handleSubmit invokes the onSubmit callback with the current values.
    - field: field access exposes value/touched/dirty/errors and setValue/setTouched mutators reflecting the underlying form state.
  </behavior>
  <action>
    Edit `packages/forms/vite.config.ts` to add `setupFiles: ['../../test-setup.ts']` to the existing `test` block.
    Write `create-form.test.ts` mirroring form-controller.test.ts: build the plain-object mock host, `createForm(host as any, { initialValues })`, call `hostConnected()`, exercise `setValue`/`reset`/`handleSubmit` (await the async submit with a short timeout as in form-controller.test.ts:132-149), assert `onSubmit` receives the expected values, then `hostDisconnected()`.
    Write `field.test.ts` asserting the `form.field(name)` factory/accessor surface: `.value`, `.touched`, `.dirty`, `.errors`, `.setValue`, `.setTouched`.
    Run `npm run test -w @willram/forms` and confirm both new suites pass alongside existing forms suites.
  </action>
  <verify>
    <automated>npm run test -w @willram/forms</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "setupFiles" packages/forms/vite.config.ts` returns >= 1 and the value contains `../../test-setup.ts`.
    - `npm run test -w @willram/forms` exits 0 with `create-form` and `field` present in passing output.
    - `grep -nE "constructor\\([^)]*(private|public|readonly|protected)" packages/forms/src/create-form.test.ts packages/forms/src/field.test.ts` returns nothing.
  </acceptance_criteria>
  <done>Forms is wired into the shared setup and the create-form/field idiom is proven green end-to-end.</done>
  <reversibility rating="reversible">Additive test + config; safe to trim.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: field-controller + array-controller suites</name>
  <files>packages/forms/src/field-controller.test.ts, packages/forms/src/array-controller.test.ts</files>
  <read_first>
    - packages/forms/src/group-controller.test.ts (lines 44-58 field-state assertions; lines 124-159 caching/membership assertions)
    - packages/forms/src/field-controller.ts and packages/forms/src/array-controller.ts (sources under test)
    - packages/forms/src/form-controller.test.ts (field access + lifecycle)
  </read_first>
  <behavior>
    - field-controller: mirrors group-controller field-state access (value/touched/dirty/errors) and reactive update on setValue.
    - array-controller: add/insert/remove/reorder mutate the field-array; entry ORDER is preserved and observable after each operation (adjacency/ordering edge).
  </behavior>
  <action>
    Write `field-controller.test.ts` mirroring the group-controller.test.ts field-state idiom.
    Write `array-controller.test.ts` mirroring group-controller.test.ts caching/membership assertions; explicitly assert that after `add`/`insert`/`remove`/reorder the array-field entries retain their expected order (author the ordering edge as concrete assertions, e.g. push two items then remove the first and assert the remaining item and index).
    Run `npm run test -w @willram/forms`.
  </action>
  <verify>
    <automated>npm run test -w @willram/forms</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -w @willram/forms` exits 0 with `field-controller` and `array-controller` in passing output.
    - `array-controller.test.ts` contains at least one assertion on entry order after a mutation: `grep -nE "toEqual|toBe|toMatchObject" packages/forms/src/array-controller.test.ts` returns >= 1.
  </acceptance_criteria>
  <done>Field and field-array critical paths covered, including order-preservation of array mutations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: zod adapter validator-contract suite</name>
  <files>packages/forms/src/zod.test.ts</files>
  <read_first>
    - packages/forms/src/validators.test.ts (validator return contract: undefined = success, string = error message)
    - packages/forms/src/zod.ts (adapter under test; structural typing works across zod v3/v4 per RESEARCH line 45)
    - .planning/phases/02-tests-ci/02-PATTERNS.md lines 136-140 (zod analog)
  </read_first>
  <behavior>
    - The @willram/forms/zod adapter wraps a zod schema and returns undefined for valid input and an error string (or messages) for invalid input.
  </behavior>
  <action>
    Write `zod.test.ts`: construct a small zod schema (e.g. an object with a required string / min-length rule), wrap it with the `@willram/forms/zod` adapter, and assert it returns `undefined` for a valid value and a non-empty error `string`/messages for an invalid value. Follow the assert-on-return-value style of validators.test.ts — no DOM, no host needed.
    Run `npm run test -w @willram/forms`.
  </action>
  <verify>
    <automated>npm run test -w @willram/forms</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -w @willram/forms` exits 0 with `zod` in passing output.
    - The suite asserts both the valid (undefined) and invalid (error) branches: `grep -cnE "toBeUndefined|toBe\\(undefined\\)" packages/forms/src/zod.test.ts` returns >= 1 AND an error-branch assertion exists.
  </acceptance_criteria>
  <done>The zod validator adapter's success/failure contract is covered, closing the forms TEST-01 bar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runtime → global scope | forms tests run under jsdom with the shared test-setup.ts stubs loaded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | zod schema input validation paths (V5 Input Validation) | low | accept | Tests exercise valid + invalid input branches; zod is a trusted dev dependency (see Plan 04 legitimacy audit) |
| T-02-06 | Information Disclosure | forms tests leaking cross-test state via shared host | low | mitigate | Each test constructs a fresh mock host; hostDisconnected cleanup asserted |

Supply-chain and CI token/permission threats are owned by Plan 04.
</threat_model>

<verification>
- `npm run test -w @willram/forms` green (existing + five new suites).
- forms `vite.config.ts` setupFiles references `../../test-setup.ts`.
</verification>

<success_criteria>
- TEST-01 (forms): field/array + zod named suites pass; HIGH-priority forms CONCERNS gaps closed.
- TEST-02: forms wired into the shared jsdom setup.
</success_criteria>

<output>
Create `.planning/phases/02-tests-ci/02-02-SUMMARY.md` when done.
</output>
