---
phase: 11-devtools-debugging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/devtools/package.json
  - packages/devtools/vite.config.ts
  - packages/devtools/tsconfig.json
  - packages/devtools/tsconfig.build.json
  - packages/devtools/README.md
  - packages/devtools/LICENSE
  - packages/devtools/CHANGELOG.md
  - packages/devtools/src/internal/dev.ts
  - packages/devtools/src/router-log.ts
  - packages/devtools/src/router-log.test.ts
  - packages/devtools/src/index.ts
  - .changeset/config.json
  - .changeset/add-devtools-package.md
  - scripts/check-devtools-leaf.mjs
  - .github/workflows/ci.yml
autonomous: true
requirements: [DTOOL-01, DTOOL-03, DTOOL-04]
user_setup: []

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Running `npm run build -w packages/devtools` produces dist/devtools.js (ESM) + dist/index.d.ts with lit, @tanstack/*, @willramdev/*, and esm-env all externalized (no peer code bundled)."
    - "`attachRouterLog(router)` (D-06) logs each navigation via console.groupCollapsed with a single `[litkit]` prefix, showing from→to, path, and params."
    - "Calling attachRouterLog when DEV is false, or in an environment with no console, returns a valid no-op teardown and never throws or logs (D-04 no-op ethos; DTOOL-01 idempotency edge)."
    - "The teardown returned by attachRouterLog unsubscribes from router.subscribe; calling the teardown twice is safe (no throw, DTOOL-01 idempotency edge)."
    - "On the first navigation, when the previous RouteMatch is null, the router log renders the from-label as `(initial)` (DTOOL-03 boundary edge)."
    - "Router and RouteChangeCallback import cleanly from `@willramdev/router` and drive attachRouterLog with NO change to packages/router/src/router-core/** (DTOOL-04 verify-only, D-07)."
    - "No core package (kit/router/query/forms/store) declares @willramdev/devtools in any dependency map — `node scripts/check-devtools-leaf.mjs` exits 0 (DTOOL-01 leaf rule, D-10)."
    - statement: "attachRouterLog performs no asynchronous work — its subscription and teardown are fully synchronous, so there is no interrupted/parallel-run window to guard (DTOOL-01 concurrency edge)."
      verification: backstop
  artifacts:
    - packages/devtools/package.json
    - packages/devtools/vite.config.ts
    - packages/devtools/tsconfig.json
    - packages/devtools/tsconfig.build.json
    - packages/devtools/src/internal/dev.ts
    - packages/devtools/src/router-log.ts
    - packages/devtools/src/router-log.test.ts
    - packages/devtools/src/index.ts
    - scripts/check-devtools-leaf.mjs
    - .changeset/config.json
  key_links:
    - "packages/devtools/src/router-log.ts → @willramdev/router (public router.subscribe / RouteChangeCallback) — the consumed hook (DTOOL-04, D-07)"
    - "packages/devtools/src/internal/dev.ts → esm-env DEV (externalized in vite.config) — the local dev-gate, never a sibling import"
    - ".changeset/config.json fixed array → includes @willramdev/devtools (D-08 lockstep)"
    - ".github/workflows/ci.yml gate job → node scripts/check-devtools-leaf.mjs (leaf-rule enforcement, D-10)"
  prohibitions:
    - "packages/devtools must never be added to any package's sideEffects allowlist — sideEffects stays literal false (DTOOL-01, success criterion #1)."
    - "The dev-gate must be a LOCAL esm-env import in packages/devtools/src/internal/dev.ts — never import DEV from a sibling package (@willramdev/kit etc.), which would break the acyclic leaf rule."
    - "packages/router/src/router-core/** must not be modified — DTOOL-04 is verify-only (D-07)."
    - "Do not add access:restricted to packages/devtools/package.json publishConfig — access lives once in .changeset/config.json."
---

<objective>
Scaffold the sixth, leaf package `@willramdev/devtools` end-to-end and prove its packaging / dev-gate / leaf / externalization contract with the single thinnest attach function — `attachRouterLog` (D-06), a pure `console.groupCollapsed` log over the already-public `router.subscribe`, needing no external peer. This is the tracer: it wires one path through every layer the phase touches (manifest → build config → local dev-gate → public-hook consumption → console output → committed changeset → CI leaf-rule gate) so the architecture is validated before the Redux (11-02) and query-devtools (11-03) attach functions expand from it.

Purpose: De-risk the whole phase on the agent's best early-context tokens — if the leaf/optional-peer/dev-gate/packaging contract is wrong, catch it after one attach fn, not three. Simultaneously discharges DTOOL-04 (verify-only, D-07) by importing `Router`/`RouteChangeCallback` from `@willramdev/router` with zero core change, and establishes DTOOL-01's leaf rule as an enforced CI gate.
Output: A building, typechecking, tree-shakeable `packages/devtools/` package exporting `attachRouterLog`; the `.changeset/config.json` `fixed` lockstep edit (D-08); a `scripts/check-devtools-leaf.mjs` gate wired into the read-only `ci.yml` (D-10).

## Artifacts this phase produces (source-grounding exclusion list)
New symbols/paths introduced by Phase 11 (exclude from drift verification — they do not exist before this phase):
- Symbols: `attachRouterLog` (this plan), `attachStoreDevtools` + `StoreDevtoolsOptions` (11-02), `attachQueryDevtools` (11-03). Each returns a teardown `() => void`.
- Package: `@willramdev/devtools` (new, 6th).
- New files: `packages/devtools/{package.json,vite.config.ts,tsconfig.json,tsconfig.build.json,README.md,LICENSE,CHANGELOG.md}`, `packages/devtools/src/{index.ts,internal/dev.ts,router-log.ts,store-devtools.ts,query-devtools.ts}` + co-located `*.test.ts`, `scripts/check-devtools-leaf.mjs`, `.changeset/add-devtools-package.md`.
- Config edit: `.changeset/config.json` `fixed` array gains `"@willramdev/devtools"` (D-08).

## Flagged assumption (specless fallback — DTOOL-04 unclassified edge)
DTOOL-04 is treated as **verify-only** per D-07 and RESEARCH.md §Router Observer Hook: `router-core` already exports a public `subscribe(callback: RouteChangeCallback): () => void` (types.ts:154, impl router.ts:135, exported router-core/index.ts:17-18, re-exported at package root). This plan verifies sufficiency by consuming it from `@willramdev/router`. IF, during implementation, the public hook proves insufficient for the D-06 match log (it will not — signature quoted verbatim in RESEARCH.md), that is a flagged deviation requiring escalation, NOT a silent core edit.
</objective>

<assumption_delta_decision>
The assumption-delta detector fired (advisory, non-blocking) on the single token "optional" in the ROADMAP line "Phase 10 optional (examples app is a convenient manual test surface)". This refers to a phase DEPENDENCY being optional, not a required→optional identity transition of any entity or API. The optional-peer design of `@willramdev/devtools` (peerDependenciesMeta.optional) is greenfield-by-construction — the peers are optional from creation, not a promotion of a previously-required dependency. No identity decision to record; no scope change. Resolution: no-op.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-devtools-debugging/11-CONTEXT.md
@.planning/phases/11-devtools-debugging/11-RESEARCH.md
@.planning/phases/11-devtools-debugging/11-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "router match log" slice — scaffold @willramdev/devtools + local dev-gate + attachRouterLog</name>
  <files>packages/devtools/package.json, packages/devtools/vite.config.ts, packages/devtools/tsconfig.json, packages/devtools/tsconfig.build.json, packages/devtools/src/internal/dev.ts, packages/devtools/src/router-log.ts, packages/devtools/src/router-log.test.ts, packages/devtools/src/index.ts</files>
  <read_first>
    - packages/store/package.json (manifest shape to mirror — D-09: type:module, sideEffects:false, files, main/module/types, exports, scripts, publishConfig.registry ONLY)
    - packages/store/vite.config.ts (build shape to mirror — publicDir:false, build.lib entry/fileName/formats, rollupOptions.external, sourcemap, test.setupFiles)
    - packages/store/tsconfig.json + packages/store/tsconfig.build.json (copy verbatim; build config emits emitDeclarationOnly .d.ts)
    - packages/kit/src/internal/dev.ts (the local `import { DEV } from 'esm-env'` pattern — copy the DEV re-export, DROP the devWarn/devWarnOnce Lit helpers devtools does not need)
    - packages/kit/package.json (confirms esm-env is a real `dependencies` entry `^1.2.2`, not a peer — Phase 7 D-02)
    - .planning/phases/11-devtools-debugging/11-RESEARCH.md §Code Examples (VERIFIED verbatim body for attachRouterLog, L371-389; internal/dev.ts L400-405) and §Package Scaffolding (L423-500)
    - .planning/phases/11-devtools-debugging/11-PATTERNS.md (file-by-file analog map; the barrel + import-convention rules)
    - packages/router/src/router-core/types.ts (VERIFY-ONLY, do NOT edit: RouteChangeCallback L136, Router.subscribe L154, RouteMatch pathname/params/name)
    - packages/router/src/index.ts (confirm Router, RouteChangeCallback, createMockRouter, mockMatch are re-exported from the package root @willramdev/router)
    - packages/store/src/store.test.ts (Vitest convention: describe/it/expect/vi, co-located .test.ts, .ts import extensions)
  </read_first>
  <action>
Create packages/devtools/ mirroring packages/store per D-09. package.json: name "@willramdev/devtools", version "1.0.0", type "module", sideEffects false (literal boolean), files ["dist","README.md","LICENSE","CHANGELOG.md"], main/module "./dist/devtools.js", types "./dist/index.d.ts", exports "." → { types:"./dist/index.d.ts", import:"./dist/devtools.js" }, scripts identical to store (dev/build/typecheck/test/prepublishOnly), publishConfig { registry:"https://npm.pkg.github.com" } — registry ONLY, NO access field. dependencies: { "esm-env":"^1.2.2" } (real dep, per D-02 ethos — the gate is always needed). peerDependencies: @willramdev/store ^1.0.0, @willramdev/query ^1.0.0, @willramdev/router ^1.0.0, @tanstack/query-core ^5.0.0, @tanstack/query-devtools ^5.91.0 — every one marked optional in peerDependenciesMeta. devDependencies: @willramdev/store, @willramdev/query, @willramdev/router each as "*" (workspace — NOT "workspace:*", npm 11 rejects it per Phase 10), @tanstack/query-core ^5.91.0, @tanstack/query-devtools ^5.91.0, typescript ^6.0.3, vite ^8.0.1, vitest ^4.1.9. Omit lit from peerDependencies (no attach fn imports Lit — RESEARCH A1); keep the exact JSON block from RESEARCH §Package Scaffolding.

@tanstack/query-devtools legitimacy: RESEARCH §Package Legitimacy Audit rated it SUS(too-new) — a documented FALSE POSITIVE (the too-new signal fires on the latest patch 5.102.0, not the pinned ^5.91.0). ~9.99M downloads/wk, official TanStack monorepo, deprecated:false, postinstall:null, and it is the exact package @tanstack/react-query-devtools@5.91.0 pins. Pin ^5.91.0 (aligns with the installed @tanstack/query-core@5.91.0). No blocking checkpoint per the researcher's explicit recommendation.

vite.config.ts: copy store's file, change fileName to "devtools" and WIDEN rollupOptions.external to ['lit', /^lit\//, /^@tanstack\//, /^@willramdev\//, 'esm-env'] (D-09 externalization); keep test.setupFiles ['../../test-setup.ts']. tsconfig.json + tsconfig.build.json: copy store's verbatim (no change).

src/internal/dev.ts: LOCAL `import { DEV } from 'esm-env'; export { DEV };` — a local import, never a sibling helper (preserves the acyclic leaf rule; Claude's-Discretion note in CONTEXT). Do not reproduce kit's devWarn/devWarnOnce.

src/router-log.ts: implement `attachRouterLog(router: Router): () => void` copying the VERIFIED body from RESEARCH.md §Code Examples (L371-389). Guard order: `if (!DEV || typeof console === 'undefined') return () => {}` (D-04 silent no-op). Return `router.subscribe((match, previous) => …)` so the teardown is the unsubscribe. Each log line carries the single `[litkit]` prefix (D-06, Phase 7 D-07 contract); use console.groupCollapsed for the header (`[litkit] router → <to>`) then console.log from→to, path (match?.pathname), params (match?.params ?? {}); from-label is `(initial)` when previous is null, to-label is match ? (match.name ?? match.pathname) : '(no match)'. No warn-once dedupe — every navigation logs (D-06). Import Router and RouteChangeCallback as types from '@willramdev/router'.

src/index.ts: barrel exporting ONLY `export { attachRouterLog } from './router-log.ts';` for now (store/query exports are added in 11-02/11-03). Value exports first, `.ts` extensions on every specifier (repo erasableSyntaxOnly + explicit-extension rule).

src/router-log.test.ts: use createMockRouter + mockMatch from '@willramdev/router' to drive a subscribe callback (mockRouter.setCurrentMatch(...) fires subscribers). Spy console.groupCollapsed/log/groupEnd with vi.spyOn. Assert: (a) a navigation with a non-null match logs a groupCollapsed header containing '[litkit]'; (b) the first navigation with previous null renders '(initial)' as the from-label; (c) the returned teardown unsubscribes (after teardown, a further setCurrentMatch fires no new groupCollapsed call); (d) calling the teardown a second time does not throw. Force DEV true for the test env if needed by asserting under the default vitest env (esm-env resolves DEV true in dev/test).
  </action>
  <verify>
    <automated>npm install && npm run typecheck -w packages/devtools && npm run build -w packages/devtools && npm run test -w packages/devtools</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build -w packages/devtools` exits 0 and writes packages/devtools/dist/devtools.js and packages/devtools/dist/index.d.ts.
    - `node -e "const p=require('./packages/devtools/package.json'); if(p.sideEffects!==false) process.exit(1); for(const k of ['@willramdev/store','@willramdev/query','@willramdev/router','@tanstack/query-core','@tanstack/query-devtools']){ if(!p.peerDependenciesMeta?.[k]?.optional) process.exit(1);} if(p.dependencies?.['esm-env']===undefined) process.exit(1); if(p.publishConfig?.access!==undefined) process.exit(1);"` exits 0.
    - Built dist/devtools.js contains no bundled Lit/@tanstack/@willramdev/esm-env source — `node -e "const s=require('fs').readFileSync('packages/devtools/dist/devtools.js','utf8'); if(/from ?['\"]esm-env['\"]/.test(s)===false) process.exit(1);"` exits 0 (esm-env stays an external import specifier).
    - `npm run test -w packages/devtools` passes, including the '(initial)' first-navigation case, the '[litkit]' prefix assertion, and the double-teardown safety case.
    - `npm run typecheck -w packages/devtools` exits 0, proving Router/RouteChangeCallback import from @willramdev/router with no router-core change (DTOOL-04, D-07).
    - `git status --porcelain packages/router/src/router-core/` prints nothing (router-core untouched).
  </acceptance_criteria>
  <reversibility rating="costly">The public function name `attachRouterLog` (D-01) is the published 1.x API surface — renaming later is a breaking change. Confirmed against CONTEXT D-01.</reversibility>
  <done>packages/devtools/ builds, typechecks, and tests green; attachRouterLog logs [litkit]-prefixed navigations over the public router.subscribe and returns a safe idempotent teardown; router-core is unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Release wiring — Changesets fixed-group edit (D-08) + package doc stubs + covering changeset</name>
  <files>.changeset/config.json, .changeset/add-devtools-package.md, packages/devtools/README.md, packages/devtools/LICENSE, packages/devtools/CHANGELOG.md</files>
  <read_first>
    - .changeset/config.json (VERIFIED current fixed array is a single nested array of the five core packages; access:"restricted"; ignore:["examples"])
    - packages/store/README.md and packages/store/LICENSE (doc-stub shape + license text to copy)
    - .planning/phases/11-devtools-debugging/11-RESEARCH.md §Changesets (L502-514 — the exact append edit)
  </read_first>
  <action>
Edit .changeset/config.json: append the string "@willramdev/devtools" as the sixth member of the inner fixed array so it becomes the lockstep group of six (D-08). Leave access, baseBranch, updateInternalDependencies, ignore, changelog, commit untouched. Create .changeset/add-devtools-package.md — a standard changeset front-matter block bumping "@willramdev/devtools": minor with a one-line summary ("Add opt-in @willramdev/devtools leaf package: store time-travel, query-cache inspection, router match log"); the fixed group carries the other five along automatically. Create packages/devtools/README.md (brief: what the package is, the three attach fns, opt-in/dev-gated/tree-shakeable note, install from GitHub Packages). Copy packages/store/LICENSE verbatim to packages/devtools/LICENSE. Create packages/devtools/CHANGELOG.md as an empty-but-present stub (Changesets populates it on version) matching the sibling shape.
  </action>
  <verify>
    <automated>node -e "const c=require('./.changeset/config.json'); const g=c.fixed[0]; if(!g.includes('@willramdev/devtools')) process.exit(1); if(c.access!=='restricted') process.exit(1); if(JSON.stringify(c.ignore)!==JSON.stringify(['examples'])) process.exit(1);" && npx changeset status --since origin/main</automated>
  </verify>
  <acceptance_criteria>
    - The node assertion exits 0: `.changeset/config.json` fixed[0] includes "@willramdev/devtools", access stays "restricted", ignore stays ["examples"].
    - `.changeset/add-devtools-package.md` exists with valid changeset front-matter bumping "@willramdev/devtools": minor.
    - `npx changeset status --since origin/main` exits 0 and reports @willramdev/devtools (plus the fixed-group siblings) as pending a version bump.
    - packages/devtools/README.md, LICENSE, and CHANGELOG.md all exist (the files allowlist references them for the published tarball).
  </acceptance_criteria>
  <reversibility rating="costly">Joining the Changesets `fixed` lockstep group (D-08) is costly to reverse — extracting @willramdev/devtools to an independent version line later means re-basing its published semver history and re-pinning peer ranges. Accepted per CONTEXT D-08.</reversibility>
  <done>@willramdev/devtools is the sixth member of the fixed lockstep group, a covering changeset exists, and the package carries the README/LICENSE/CHANGELOG its files allowlist ships.</done>
</task>

<task type="auto">
  <name>Task 3: Leaf-rule CI gate — scripts/check-devtools-leaf.mjs + read-only ci.yml step (DTOOL-01, D-10)</name>
  <files>scripts/check-devtools-leaf.mjs, .github/workflows/ci.yml</files>
  <read_first>
    - scripts/check-single-instance.mjs (the model: a standalone node script that reads the workspace, prints OK/FAIL, process.exit(1) on failure; cross-platform, no token)
    - .github/workflows/ci.yml (the gate job structure; existing steps run under `permissions: contents: read`; add the new step in the `gate` job, do NOT widen permissions, do NOT touch release.yml — D-10)
    - .planning/phases/11-devtools-debugging/11-RESEARCH.md §Open Questions #2 (the recommended non-vacuous leaf-rule assertion)
  </read_first>
  <action>
Create scripts/check-devtools-leaf.mjs (ESM, mirroring scripts/check-single-instance.mjs style): read the five CORE package manifests — packages/kit/package.json, packages/router/package.json, packages/query/package.json, packages/forms/package.json, packages/store/package.json — parse each, and assert that NONE lists the devtools package name in any of dependencies / peerDependencies / devDependencies / optionalDependencies. For each offending manifest print a FAIL line naming the file and the map; print an OK line per clean manifest; process.exit(1) if any offender is found, else exit 0. This proves DTOOL-01's leaf rule (`devtools → {store,query,router}`, nothing imports it) non-vacuously by construction — a dependency-graph assertion, not a build-fold. Build the devtools-package-name string from parts (e.g. `['@willramdev','devtools'].join('/')`) so the check script itself is not a self-referential match.

Edit .github/workflows/ci.yml: add ONE step to the existing `gate` job, after the single-instance check step, named "devtools leaf-rule check (DTOOL-01)" running `node scripts/check-devtools-leaf.mjs`. The existing `build-test` job already runs `npm run build` (--workspaces), which now compiles packages/devtools too, so no separate devtools build step is needed. Do NOT add write scopes; the top-level `permissions: contents: read` is sufficient (local file reads only). Do NOT modify release.yml (D-10). Add a brief comment mirroring the neighbouring steps' style explaining the leaf rule and that no token is needed.
  </action>
  <verify>
    <automated>node scripts/check-devtools-leaf.mjs && node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); if(!/check-devtools-leaf\.mjs/.test(y)) process.exit(1); if(!/permissions:\s*\n\s*contents:\s*read/.test(y)) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-devtools-leaf.mjs` exits 0 today (no core manifest references the devtools package) and would exit 1 if any of the five core package.json files gained a devtools reference in any dependency map.
    - ci.yml contains a `node scripts/check-devtools-leaf.mjs` step inside the `gate` job and the top-level `permissions: contents: read` is unchanged.
    - `git diff --exit-code .github/workflows/release.yml` prints nothing (release.yml untouched, D-10).
  </acceptance_criteria>
  <done>The leaf rule (DTOOL-01) is an enforced, non-vacuous read-only CI gate; core never depends on devtools; release.yml and CI permissions are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer bundler → shipped devtools dist | devtools code must be DEV-gated + tree-shakeable so it never survives into a production consumer bundle |
| Redux DevTools browser extension → devtools (11-02) | untrusted-ish message input crosses here at runtime (dev only) — modelled fully in 11-02 |
| npm registry → workspace install | @tanstack/query-devtools / @tanstack/query-core installed as devDeps when this package's manifest lands |

## STRIDE Threat Register (ASVS L1; block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Information Disclosure | devtools code leaking into a prod consumer bundle | medium | mitigate | LOCAL esm-env `DEV` gate (`if (!DEV) return () => {}`) + `sideEffects:false` + per-module split + leaf-rule CI gate (scripts/check-devtools-leaf.mjs); opt-in explicit import is the only entry path (DTOOL-01, D-04) |
| T-11-03 | Denial of Service | SSR / no-`console` reference in attachRouterLog | low | mitigate | Guard order `if (!DEV || typeof console === 'undefined') return () => {}` — silent no-op, never throws (D-04, Pitfall 4) |
| T-11-SC | Tampering | npm install of @tanstack/query-devtools / @tanstack/query-core | medium | mitigate | RESEARCH §Package Legitimacy Audit performed the verification: @tanstack/query-devtools is SUS(too-new) — a documented false-positive on the latest patch only; ~9.99M dl/wk, official TanStack monorepo, deprecated:false, postinstall:null, the exact package the official React adapter pins. Pinned `^5.91.0` (aligned to installed query-core). No blocking human checkpoint per the researcher's explicit recommendation. |

No threat is rated `high`; nothing blocks at ASVS L1.
</threat_model>

<verification>
- `npm install && npm run build -w packages/devtools && npm run typecheck -w packages/devtools && npm run test -w packages/devtools` all exit 0.
- `node scripts/check-devtools-leaf.mjs` exits 0 (leaf rule holds).
- `node -e` manifest assertions (sideEffects false, all peers optional, esm-env dep, no publishConfig.access) exit 0.
- `npx changeset status --since origin/main` reports the fixed-group bump.
- `git status --porcelain packages/router/src/router-core/ .github/workflows/release.yml` prints nothing (verify-only DTOOL-04; release.yml untouched).
</verification>

<success_criteria>
- @willramdev/devtools exists as a building, typechecking, tested ESM leaf package with all peers externalized and optional, sideEffects:false, and the sibling packaging contract (D-09).
- attachRouterLog (D-06) works end-to-end over the public router.subscribe with the [litkit] prefix, dev-gated, returning a safe idempotent teardown — DTOOL-03 (router log) and DTOOL-04 (verify-only, D-07) discharged.
- DTOOL-01's leaf rule is an enforced read-only CI gate; the changeset fixed group (D-08) includes devtools; release.yml/CI permissions unchanged (D-10).
</success_criteria>

<output>
Create `.planning/phases/11-devtools-debugging/11-01-SUMMARY.md` when done.
</output>