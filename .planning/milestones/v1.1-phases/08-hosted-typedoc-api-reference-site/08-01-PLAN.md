---
phase: 08-hosted-typedoc-api-reference-site
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - typedoc.json
  - .gitignore
  - packages/kit/typedoc.json
  - packages/query/typedoc.json
  - packages/store/typedoc.json
  - packages/forms/typedoc.json
  - packages/router/typedoc.json
autonomous: true
requirements: [DOCS-05]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Running `npx typedoc --emit none` exits 0 with no `[warning]` or `[error]` lines — all five @willramdev/* packages convert cleanly from source (DOCS-05, specless-edge-probe DOCS-05 'convert cleanly' resolved-explicit; verification: explicit — packageOptions.treatWarningsAsErrors makes any warning fail the exit code, so exit 0 IS the clean-convert gate)."
    - "TypeDoc's merged `--json` output covers all 8 entry points with no package silently missing — the five package names @willramdev/{kit,query,store,forms,router} plus the extra subpath markers zod, router-core, router-lit are all present (DOCS-05, specless-edge-probe DOCS-05 'empty/subpath' resolved-explicit; verification: explicit — Task 2 greps docs/api.json for all 8 markers)."
    - "`npx typedoc` produces a populated `docs/` directory containing `docs/index.html` (DOCS-05; verification: explicit — Task 2 asserts docs/index.html exists after conversion)."
    - "The generated `docs/` output is git-ignored and never tracked (DOCS-05; verification: explicit — `git check-ignore -q docs` succeeds)."
    - "Every per-package `entryPoints` value names the package's `src/*.ts` SOURCE (never `dist/*.d.ts`), aligned one-to-one with that package's `exports` subpaths — kit/query/store `.`->`src/index.ts`, forms `.`+`./zod`->`src/index.ts`+`src/zod.ts`, router `.`+`./core`+`./lit`->`src/index.ts`+`src/router-core/index.ts`+`src/router-lit/index.ts` (DOCS-05; verification: explicit — Task 2 8-entry-point coverage proof)."
  artifacts:
    - typedoc.json
    - packages/kit/typedoc.json
    - packages/query/typedoc.json
    - packages/store/typedoc.json
    - packages/forms/typedoc.json
    - packages/router/typedoc.json
    - package.json (modified — typedoc devDependency + docs script)
    - .gitignore (modified — /docs/ ignore entry)
  key_links:
    - "Root `typedoc.json` `entryPointStrategy: packages` + `entryPoints: [packages/*]` discovers each package directory, then EACH package's own `typedoc.json` supplies the source entry points (root-level conversion options are NOT copied to children — Pitfall 4)."
    - "Per-package `entryPoints` name `src/*.ts` source, so TypeDoc reads first-party TypeScript through each package's own tsconfig (allowImportingTsExtensions / experimentalDecorators / moduleResolution:bundler already set) and runs `git` for source links — no package build required."
    - "Root `typedoc.json` `out: docs` MUST equal the `/docs/` path added to `.gitignore` — the ignored directory and the emit directory are the same string."
  prohibitions:
    - "TypeDoc entry points must NOT point at built `dist/*.d.ts` or the `exports` dist targets — they must read `src/*.ts` source, or JSDoc and source links are lost and a prior build becomes required."
    - "The generated `docs/` output directory must NOT be committed to git — it is a rebuilt-in-CI artifact."
    - "Root-level conversion options must NOT be relied upon to propagate to child packages — packages-mode does not copy them, so each package's entry points live in that package's own `typedoc.json`."
    - "No TypeDoc base-path / `--base` flag may be set for the `/litkit/` subpath — TypeDoc links are relative and relocatable; `hostedBaseUrl` is for sitemap/canonical output only."
---

<objective>
Stand up the merged multi-package TypeDoc site as a production-quality LOCAL build that converts all five `@willramdev/*` packages from source, with per-package entry points aligned to each package's `exports` map. This is the phase's tracer slice: it wires TypeDoc end-to-end (install -> root packages-mode config -> per-package source entry points -> clean conversion -> emitted HTML + JSON), proven with a real runnable gate BEFORE Plan 08-03 expands it into the isolated GitHub Pages deploy workflow. If TypeDoc cannot convert this repo's `.ts`-extension imports or `@customElement` decorators, we discover it here on one commit, not after wiring CI.

Purpose: DOCS-05 — a hosted site covering all five packages via `entryPointStrategy: "packages"`, entry points aligned to each `exports` map (router `./core`/`./lit`, forms `./zod`). Delivered here as the locally-verified build; the deploy layer is Plan 08-03.
Output: root `typedoc.json`, five per-package `typedoc.json`, a `typedoc@0.28.20` exact-pinned root devDependency, a `docs` npm script, and a `/docs/` `.gitignore` entry.
</objective>

<flagged_assumptions>
Per the spec-less edge-probe contract, every probe edge is surfaced here — none silently dropped:
- **DOCS-05 "convert cleanly" edge** -> resolved as an explicit `must_haves.truth`: `npx typedoc --emit none` exits 0 with no `[warning]`/`[error]` (this plan).
- **DOCS-05 "empty/subpath" edge** (no package silently missing) -> resolved as an explicit `must_haves.truth`: all 8 entry points present in the merged `--json` output (this plan, Task 2).
- **DOCS-05 "adjacency" probe** ("when two things are exactly equal or just touch, do they merge/collide/separate?") -> **N/A for doc generation**: TypeDoc converts independent package reflections; there is no notion of two equal elements merging/colliding. No behavioral edge to gate. Flagged, not dropped.
- **DOCS-05 "ordering" probe** ("when elements compare equal, is output order specified and stable?") -> **N/A**: nav/member ordering is TypeDoc-internal presentation, not a litkit correctness contract; DOCS-05 specifies coverage (all 8 entry points), not ordering. Flagged, not dropped.
- **DOCS-06 probe** -> resolved as a truth in Plan 08-03 (docs.yml scope + siblings untouched).
- **DOCS-07 probe** -> resolved as a truth in Plan 08-02 (grep stale owner = 0).
</flagged_assumptions>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-hosted-typedoc-api-reference-site/08-RESEARCH.md
@.planning/phases/08-hosted-typedoc-api-reference-site/08-PATTERNS.md
@.planning/phases/08-hosted-typedoc-api-reference-site/08-VALIDATION.md
@package.json
@.gitignore
@packages/kit/package.json
@packages/query/package.json
@packages/store/package.json
@packages/forms/package.json
@packages/router/package.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TypeDoc packages-mode pipeline proven on the three single-entry packages</name>
  <files>package.json, typedoc.json, .gitignore, packages/kit/typedoc.json, packages/query/typedoc.json, packages/store/typedoc.json</files>
  <read_first>
    - package.json (root) — the `scripts` block (flat one-liner convention) and the exact-pin devDependency convention (`dts-bundle-generator: "9.5.1"`, `typescript: "6.0.3"` no-caret); the `docs` script and `typedoc` devDep follow these exactly
    - .gitignore — existing bare-path build-output ignores (`dist`, `coverage`, `tools/doc-check/.snippets/`) — follow the same convention for `/docs/`
    - packages/kit/package.json `exports` (`.`->`dist/index.d.ts`) — confirms kit is single-entry; source entry is `src/index.ts`
    - packages/query/package.json `exports` (`.` only) and packages/store/package.json `exports` (`.` only) — both single-entry; source entry `src/index.ts`
    - .planning/phases/08-hosted-typedoc-api-reference-site/08-RESEARCH.md §"Pattern 1" (root config skeleton + packageOptions Pitfall 4) and §"Common Pitfalls 3/4/5"
    - .planning/phases/08-hosted-typedoc-api-reference-site/08-PATTERNS.md §"typedoc.json (root)" and §"packages/<pkg>/typedoc.json"
  </read_first>
  <action>
    Install TypeDoc as a root devDependency, exact-pinned (no caret), matching the repo's tooling-pin convention: run `npm install -D -E typedoc@0.28.20` at the workspace root so it lands as `"typedoc": "0.28.20"` in the root `package.json` `devDependencies` and package-lock.json records it. TypeDoc's peer `typescript` range includes `6.0.x` (repo pins `6.0.3`) — do NOT bump typescript. TypeDoc runs no postinstall (Package Legitimacy Audit verdict OK).

    Add a `docs` script to the root `package.json` `scripts` block as a flat one-liner: `"docs": "typedoc"` (matching the existing script style; do NOT add flags — the root `typedoc.json` carries all options).

    Create the root `typedoc.json` with these keys and nothing else: `$schema` = `https://typedoc.org/schema.json`; `entryPointStrategy` = `packages`; `entryPoints` = the array of the THREE single-entry package directories `packages/kit`, `packages/query`, `packages/store` (this task's scope — Task 2 broadens it to `packages/*`); `out` = `docs`; `hostedBaseUrl` = `https://willramdev.github.io/litkit/`; `name` = `litkit API`; and a `packageOptions` object holding the shared per-package conversion options `excludeInternal: true` and `treatWarningsAsErrors: true`. Put `treatWarningsAsErrors` in `packageOptions` (NOT only at root) because it takes effect during each package's conversion and root-level conversion options are not copied to children (Pitfall 4). Do NOT put an `entryPoints` key inside `packageOptions` — each package declares its own (Open Question #1 resolution). Do NOT add any base-path flag (Pitfall 3): `hostedBaseUrl` is for sitemap/canonical only; TypeDoc links are relative. If a genuinely benign warning surfaces that cannot be fixed at the source, prefer a scoped `exclude`/filter option in `packageOptions` over disabling `treatWarningsAsErrors` — the clean-convert gate must stay non-vacuous.

    Create `packages/kit/typedoc.json`, `packages/query/typedoc.json`, and `packages/store/typedoc.json`, each a minimal object with a single key `entryPoints` set to `["src/index.ts"]` (source, not `dist/*.d.ts`). Keep them minimal — shared options live in the root `packageOptions`.

    Add a `/docs/` entry to `.gitignore` under a brief `# TypeDoc build output (rebuilt in docs.yml)` comment, following the existing bare-path convention. This path MUST equal the root `typedoc.json` `out` value (`docs`).
  </action>
  <verify>
    <automated>npx typedoc --emit none &amp;&amp; git check-ignore -q docs</automated>
  </verify>
  <acceptance_criteria>
    - Root `package.json` `devDependencies` contains `"typedoc": "0.28.20"` (exact, no caret); package-lock.json records typedoc; typescript remains `6.0.3`.
    - Root `package.json` `scripts` contains `"docs": "typedoc"`.
    - Root `typedoc.json` sets `entryPointStrategy: "packages"`, `entryPoints: ["packages/kit","packages/query","packages/store"]`, `out: "docs"`, `hostedBaseUrl: "https://willramdev.github.io/litkit/"`, `name: "litkit API"`, and `packageOptions` containing `excludeInternal: true` and `treatWarningsAsErrors: true`; `packageOptions` has NO `entryPoints` key; no base-path/`--base` flag exists anywhere.
    - `packages/{kit,query,store}/typedoc.json` each contain exactly one key `entryPoints` set to `["src/index.ts"]`.
    - `npx typedoc --emit none` exits 0 with no `[warning]` or `[error]` lines (the three single-entry packages convert cleanly from source).
    - `.gitignore` contains a `/docs/` entry; `git check-ignore -q docs` succeeds (docs/ is ignored).
  </acceptance_criteria>
  <reversibility rating="costly">Introduces the `typedoc` root devDependency + the packages-mode config tree (root + three per-package configs) and the `docs`/`out` directory contract. Reversible but costly: swapping doc generators or restructuring entry-point strategy later touches root deps, the lockfile, and every config file. Not a one-way door — flag only per REVERSIBILITY_GATES, no checkpoint.</reversibility>
  <done>TypeDoc's packages-mode pipeline is proven end-to-end on the three single-entry packages: install resolves, the root + per-package source-entry configs convert cleanly (exit 0, zero warnings), and the emit directory is git-ignored — the tracer for the whole phase.</done>
</task>

<task type="auto">
  <name>Task 2: Expand to router + forms subpaths and prove full 8-entry-point coverage</name>
  <files>typedoc.json, packages/forms/typedoc.json, packages/router/typedoc.json</files>
  <read_first>
    - packages/forms/package.json `exports` — `.`->`dist/index.d.ts` (source `src/index.ts`) and `./zod`->`dist/zod.d.ts` (source `src/zod.ts`); confirms the two forms entry points
    - packages/router/package.json `exports` — `.`->`dist/index.d.ts` (`src/index.ts`), `./core`->`dist/router-core/index.d.ts` (`src/router-core/index.ts`), `./lit`->`dist/router-lit/index.d.ts` (`src/router-lit/index.ts`); confirms the three router entry points
    - typedoc.json (root, from Task 1) — the `entryPoints` array to broaden from the three explicit dirs to `["packages/*"]`
    - .planning/phases/08-hosted-typedoc-api-reference-site/08-RESEARCH.md §"Pattern 1" exact entry-point->exports map (the 8-row table) and §"Validation Architecture" (the 8-entry-point coverage gate)
  </read_first>
  <action>
    Create `packages/forms/typedoc.json` with `entryPoints` set to `["src/index.ts", "src/zod.ts"]` — the two source entry points aligned to forms' `.` and `./zod` exports.

    Create `packages/router/typedoc.json` with `entryPoints` set to `["src/index.ts", "src/router-core/index.ts", "src/router-lit/index.ts"]` — the three source entry points aligned to router's `.`, `./core`, and `./lit` exports.

    Broaden the root `typedoc.json` `entryPoints` from the three explicit directories to `["packages/*"]` so packages-mode now discovers all five package directories (each of which now has its own `typedoc.json`). Change ONLY the `entryPoints` value; leave `entryPointStrategy`, `out`, `hostedBaseUrl`, `name`, and `packageOptions` exactly as Task 1 set them.

    Do not add any other keys and do not introduce a `packageOptions.entryPoints` default — all five packages now declare entry points explicitly (the unambiguous configuration recommended in Research Open Question #1).
  </action>
  <verify>
    <automated>npx typedoc --json docs/api.json &amp;&amp; test -f docs/index.html &amp;&amp; node -e "const s=require('fs').readFileSync('docs/api.json','utf8'); const need=['@willramdev/kit','@willramdev/query','@willramdev/store','@willramdev/forms','@willramdev/router','zod','router-core','router-lit']; const miss=need.filter(n=>!s.includes(n)); if(miss.length){console.error('MISSING ENTRY POINTS:',miss);process.exit(1);} console.log('ALL 8 ENTRY POINTS PRESENT');" &amp;&amp; git check-ignore -q docs</automated>
  </verify>
  <acceptance_criteria>
    - `packages/forms/typedoc.json` sets `entryPoints: ["src/index.ts","src/zod.ts"]`.
    - `packages/router/typedoc.json` sets `entryPoints: ["src/index.ts","src/router-core/index.ts","src/router-lit/index.ts"]`.
    - Root `typedoc.json` `entryPoints` is now `["packages/*"]`; all other root keys and `packageOptions` are unchanged from Task 1.
    - `npx typedoc --json docs/api.json` exits 0 (clean convert over all five packages) and writes `docs/api.json` + `docs/index.html`.
    - The 8-entry-point coverage node check passes: `docs/api.json` contains all five `@willramdev/*` package names plus the markers `zod`, `router-core`, and `router-lit` (proving no package or subpath is silently missing).
    - `git check-ignore -q docs` still succeeds (the generated `docs/api.json` and HTML are untracked).
  </acceptance_criteria>
  <reversibility rating="reversible">Adds two per-package configs and broadens one array value; deleting the configs and reverting the array fully undoes it with no shipped-package impact.</reversibility>
  <done>All five packages plus every subpath (forms `./zod`; router `./core`/`./lit`) convert cleanly and appear in the merged output — DOCS-05's full 8-entry-point coverage is proven locally, ready for the Plan 08-03 deploy layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> root install | The new `typedoc` root devDependency is fetched at install time; it runs no postinstall and is dev-only (never shipped in any package tarball) |
| first-party source -> TypeDoc | TypeDoc reads `packages/*/src/*.ts` (first-party TypeScript) and runs `git` locally for source links; there is no untrusted input surface |
| generated docs/ -> git | The emitted `docs/` directory is a build artifact that must stay untracked |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8-01 | Tampering | `typedoc` npm devDependency (supply-chain) | high | mitigate | RESEARCH Package Legitimacy Audit verdict OK/Approved (typedoc 0.28.20, ~4.15M/wk, github.com/TypeStrong/TypeDoc, no postinstall); exact-pin `0.28.20`; not [ASSUMED]/[SUS] so no blocking-human install checkpoint is required |
| T-8-02 | Information Disclosure | generated `docs/` committed to git | low | mitigate | `/docs/` added to `.gitignore`; the site is rebuilt in `docs.yml` (Plan 08-03) and never committed |
</threat_model>

<verification>
- `npx typedoc --emit none` exits 0 with no `[warning]`/`[error]` (clean convert over all five packages once Task 2 lands).
- `npx typedoc --json docs/api.json` writes `docs/api.json` + `docs/index.html`; the 8-entry-point node coverage check prints `ALL 8 ENTRY POINTS PRESENT`.
- `git check-ignore -q docs` succeeds; `git status --porcelain -- docs` is empty (docs/ untracked).
- Root `package.json` has `typedoc: "0.28.20"` (exact) + a `docs` script; typescript stays `6.0.3`.
- All six `typedoc.json` files exist with source entry points aligned to each package's `exports`.
</verification>

<success_criteria>
A single local TypeDoc packages-mode build converts all five `@willramdev/*` packages plus every `exports` subpath (forms `./zod`; router `./core`/`./lit`) cleanly from source — exit 0, zero warnings, all 8 entry points present in the merged output, HTML emitted to a git-ignored `docs/` — with the `typedoc@0.28.20` devDependency and `docs` script in place. This is the proven tracer the Plan 08-03 Pages workflow deploys.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Phase 8 — full set, for the plan-review source-grounding pass)

Created/modified in THIS plan (08-01) — DOCS-05:
- `typedoc.json` (root) — new; `entryPointStrategy: packages`, `entryPoints: [packages/*]`, `out: docs`, `hostedBaseUrl`, `packageOptions` (excludeInternal + treatWarningsAsErrors)
- `packages/kit/typedoc.json` — new; `entryPoints: [src/index.ts]`
- `packages/query/typedoc.json` — new; `entryPoints: [src/index.ts]`
- `packages/store/typedoc.json` — new; `entryPoints: [src/index.ts]`
- `packages/forms/typedoc.json` — new; `entryPoints: [src/index.ts, src/zod.ts]`
- `packages/router/typedoc.json` — new; `entryPoints: [src/index.ts, src/router-core/index.ts, src/router-lit/index.ts]`
- `package.json` (root) — modified; new `devDependencies.typedoc: 0.28.20` + `scripts.docs: typedoc`
- `.gitignore` — modified; new `/docs/` ignore entry

Created in sibling plans (listed so this plan's scope is unambiguous):
- `.github/workflows/docs.yml` — new; isolated Pages deploy workflow (08-03, DOCS-06)
- `packages/{kit,router,query,forms,store}/package.json` — modified; `repository.url` owner correction (08-02, DOCS-07)
- `COVERAGE.md` — already written by the orchestrator ("No external API integration" declaration)

The optional root `repository` field (Research Open Question #2, Claude's discretion) is intentionally DECLINED — it is not required by any DOCS-05/06/07 success criterion, the root manifest is `private` and never published, and adding it would create a cross-plan edit contention on the root `package.json`. DOCS-07's owner correction is scoped to the five published package manifests (Plan 08-02).
</artifacts_produced>

<output>
Create `.planning/phases/08-hosted-typedoc-api-reference-site/08-01-SUMMARY.md` when done.
</output>
