---
phase: 08-hosted-typedoc-api-reference-site
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/kit/package.json
  - packages/router/package.json
  - packages/query/package.json
  - packages/forms/package.json
  - packages/store/package.json
autonomous: true
requirements: [DOCS-07]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "`git grep -n \"willram/litkit\" -- '*package.json'` returns no matches (exit 1) — zero stale-owner occurrences remain in any manifest (DOCS-07, specless-edge-probe DOCS-07 resolved-explicit; verification: explicit — the negated grep is the pass condition)."
    - "All five `packages/*/package.json` `repository.url` values read `https://github.com/willramdev/litkit.git` and each file remains valid, parseable JSON (DOCS-07; verification: explicit — node JSON.parse of each manifest asserts the corrected url)."
    - "Each manifest's `repository.directory` value (`packages/<pkg>`) is unchanged — only the `url` line's owner segment changed (DOCS-07; verification: explicit — the five directory values still read packages/kit, packages/router, packages/query, packages/forms, packages/store)."
  artifacts:
    - packages/kit/package.json (modified)
    - packages/router/package.json (modified)
    - packages/query/package.json (modified)
    - packages/forms/package.json (modified)
    - packages/store/package.json (modified)
  key_links:
    - "The `repository.url` owner correction feeds the published-tarball GitHub Packages 'Repository' link and Phase 9's CEM repo association — it does NOT feed TypeDoc source links, which derive from the git remote origin (already the correct owner)."
  prohibitions:
    - "The `repository.url` correction must NOT be verified by asserting 'TypeDoc source links now resolve' — source links derive from `git remote get-url origin` (already correct), not from `package.json`; verify ONLY by grep-for-zero stale owner (Pitfall 1)."
    - "Only the `url` line's owner segment changes — `repository.directory` (`packages/<pkg>`), `repository.type`, and every other manifest field must NOT be altered."
    - "No manifest's `exports`, `files`, `sideEffects`, `version`, `dependencies`, or `peerDependencies` may change — this is a metadata-only owner correction, additive and non-breaking to the v1.0 published contract."
---

<objective>
Correct the stale owner segment in `repository.url` across the five published package manifests so the URL points at the shipped `@willramdev` GitHub owner. The current value carries an old owner segment that predates the `willramdev` rename; the five manifests each read the wrong owner while `repository.directory` is already correct. This is a metadata-only correctness change.

Purpose: DOCS-07 — correct `repository.url` to the shipped `@willramdev` owner across package manifests so the published-tarball repository link and Phase 9 CEM repo association resolve. Note (Pitfall 1): TypeDoc source links are NOT affected by this change — they come from the git remote origin, which is already `willramdev`. This plan is verified purely by grepping the manifests to zero stale-owner occurrences.
Output: five corrected `packages/*/package.json` manifests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-hosted-typedoc-api-reference-site/08-RESEARCH.md
@.planning/phases/08-hosted-typedoc-api-reference-site/08-PATTERNS.md
@packages/kit/package.json
@packages/router/package.json
@packages/query/package.json
@packages/forms/package.json
@packages/store/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the repository.url owner across all five package manifests</name>
  <files>packages/kit/package.json, packages/router/package.json, packages/query/package.json, packages/forms/package.json, packages/store/package.json</files>
  <read_first>
    - packages/kit/package.json line 15 — the `repository.url` line with the stale owner segment (the exact current string is at this line; do not transcribe it into edits, edit in place)
    - packages/router/package.json lines 14-18 — the `repository` block (`type`/`url`/`directory`); `directory` is `packages/router` and is already correct
    - packages/query/package.json line 16, packages/forms/package.json line 16, packages/store/package.json line 16 — the `repository.url` lines (same stale owner segment)
    - .planning/phases/08-hosted-typedoc-api-reference-site/08-RESEARCH.md §"Runtime State Inventory" (the exact 5-file/line table + `git remote -v` confirming the correct owner is `willramdev`) and §"Pitfall 1" (why this does NOT drive source links)
    - .planning/phases/08-hosted-typedoc-api-reference-site/08-PATTERNS.md §"packages/{...}/package.json (×5)" (edit the `url` line only; leave `directory`)
  </read_first>
  <action>
    In each of the five manifests (`packages/kit/package.json`, `packages/router/package.json`, `packages/query/package.json`, `packages/forms/package.json`, `packages/store/package.json`), change the `repository.url` value so its owner segment is `willramdev`. The corrected value for every manifest is exactly `https://github.com/willramdev/litkit.git`. Edit ONLY the `url` line: the owner segment sits between `github.com/` and the `/litkit.git` suffix — replace the old owner token with `willramdev`, leaving the `https://github.com/` prefix and the `/litkit.git` suffix intact.

    Do NOT touch `repository.type` (`git`), `repository.directory` (already `packages/kit`, `packages/router`, `packages/query`, `packages/forms`, `packages/store` respectively — each correct), or any other field in any manifest. Do not reformat the files, do not reorder keys, and do not change `exports`, `files`, `sideEffects`, `version`, `dependencies`, or `peerDependencies` — this is a metadata-only owner correction that must stay additive and non-breaking to the v1.0 published contract. Each file must remain valid JSON after the edit.
  </action>
  <verify>
    <automated>! git grep -n "willram/litkit" -- '*package.json' &amp;&amp; node -e "['kit','router','query','forms','store'].forEach(p=>{const m=require('./packages/'+p+'/package.json'); if(m.repository.url!=='https://github.com/willramdev/litkit.git'){console.error('BAD url in',p,m.repository.url);process.exit(1);} if(m.repository.directory!==('packages/'+p)){console.error('BAD directory in',p,m.repository.directory);process.exit(1);}}); console.log('ALL 5 MANIFESTS CORRECTED');"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "willram/litkit" -- '*package.json'` returns no matches (exit 1) — zero stale-owner occurrences remain in any tracked manifest.
    - Each of the five `packages/*/package.json` has `repository.url === "https://github.com/willramdev/litkit.git"` (verified by JSON.parse, not string search).
    - Each of the five manifests still has `repository.directory === "packages/<pkg>"` (kit/router/query/forms/store) and `repository.type === "git"`.
    - Each manifest remains valid, parseable JSON with no other field changed (no diff outside the single `url` line in each file).
  </acceptance_criteria>
  <reversibility rating="reversible">A five-line string correction across manifests; fully reversible by reverting the `url` values, with no shipped-code or API impact.</reversibility>
  <done>All five package manifests carry the correct `willramdev` owner in `repository.url`, the stale-owner grep returns zero, and `repository.directory` plus every other field is untouched — the GitHub Packages repository link and Phase 9 CEM association will resolve to the right repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer edit -> package manifest | Five reviewed string edits to `repository.url`; no runtime code, no token, no external input |
| published tarball -> GitHub Packages page | The corrected `repository.url` is consumed downstream by the registry's "Repository" link and Phase 9 CEM repo association |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8-03 | Tampering | `package.json` `repository.url` metadata | low | accept | First-party reviewed edit; grep-verified to zero stale-owner occurrences and JSON.parse-verified corrected value; owner-string correction only, no runtime or security surface; `repository.directory` and all other fields untouched |
| T-8-SC | Tampering | npm/package installs in this plan | n/a | accept | None — this plan installs no packages and adds no dependency |
</threat_model>

<verification>
- `git grep -n "willram/litkit" -- '*package.json'` returns no matches (exit 1).
- The node assertion prints `ALL 5 MANIFESTS CORRECTED` — every manifest's `repository.url` is `https://github.com/willramdev/litkit.git` and `repository.directory` is unchanged.
- `git diff` on each manifest shows a single changed line (the `url`), nothing else.
</verification>

<success_criteria>
Every published package manifest's `repository.url` points at the correct `willramdev` owner, the stale-owner grep returns zero matches, and no other manifest field changed — DOCS-07 is satisfied as a manifest-correctness change (independent of TypeDoc source links, which already resolve from git origin).
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (08-02) — DOCS-07

- `packages/kit/package.json` — modified; `repository.url` owner corrected to `willramdev`
- `packages/router/package.json` — modified; `repository.url` owner corrected
- `packages/query/package.json` — modified; `repository.url` owner corrected
- `packages/forms/package.json` — modified; `repository.url` owner corrected
- `packages/store/package.json` — modified; `repository.url` owner corrected

The root `package.json` is intentionally NOT touched here (it has no `repository` field; adding one is the declined discretionary item noted in Plan 08-01). Full phase artifact roll-up: see Plan 08-01 `<artifacts_produced>`.
</artifacts_produced>

<output>
Create `.planning/phases/08-hosted-typedoc-api-reference-site/08-02-SUMMARY.md` when done.
</output>
