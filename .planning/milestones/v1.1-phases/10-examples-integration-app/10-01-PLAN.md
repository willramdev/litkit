---
phase: 10-examples-integration-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - examples/package.json
  - examples/tsconfig.json
  - examples/vite.config.ts
  - examples/index.html
  - examples/src/main.ts
  - examples/src/app.ts
  - examples/src/router.ts
  - examples/src/views/home-view.ts
  - .changeset/config.json
  - scripts/check-single-instance.mjs
  - .github/workflows/ci.yml
autonomous: true
requirements: [EXPL-01, EXPL-02, EXPL-03]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # EXPL-01 idempotency (specless-probe, resolved explicit)
    - "Re-running `npm install` then `npm run build` (root, all workspaces including `examples`) a second time in succession exits 0 with no errors and reproduces the same `examples/dist/` structure — the workspace wiring and app build are idempotent."
    # EXPL-02 adjacency + empty (specless-probe, resolved explicit)
    - "`scripts/check-single-instance.mjs` asserts exactly one distinct resolved version per package and exits non-zero the moment any of `lit`, `@tanstack/query-core`, `@tanstack/form-core` resolves to two or more distinct versions in the tree — no silent pass on divergence."
    - "If a package is absent from the resolved tree (`npm ls <pkg> --all --json` finds zero matches), `check-single-instance.mjs` reports a version-set size of 0, which fails the `size !== 1` gate exactly like a duplicate — a missing package is never a vacuous pass."
    # phase truths
    - "The examples app resolves `@willramdev/{kit,router,query,forms,store}` to their local, `npm run build`-produced `dist/` via npm workspace symlinks (`node_modules/@willramdev/*` realpaths into `packages/*`), never against the GitHub Packages registry."
    - "`examples/vite.config.ts` bundles `lit` and `@tanstack/*` (no `build.lib`, no `rollupOptions.external`) and deduplicates them via `resolve.dedupe` — the inverse of every package's own library `vite.config.ts`."
    - "Navigating to `/` renders `<home-view>`, which subscribes to a `@willramdev/store` slice via `storeSlice`/`.value` and re-renders on `update()` — the store seam and the router shell (provider + outlet + link) both prove out end-to-end in one path."
    - "`examples/package.json` has `\"private\": true` and `.changeset/config.json`'s `ignore` array contains `\"examples\"` — both release-exclusion mechanisms are in place before any changeset could ever reference the app."
  prohibitions:
    - "The examples app package must never be publishable nor trigger a Changesets version bump (EXPL-03) — `private:true` blocks publish; `ignore` blocks version bump; `examples` must never be added to the `fixed` group or share a changeset with a `fixed`-group package."
    - "MUST NOT externalize `lit`/`@tanstack/*` in `examples/vite.config.ts` — externalizing would defeat the EXPL-02 dedup canary (it is the inverse config of the package builds by design)."
  artifacts:
    - package.json
    - examples/package.json
    - examples/tsconfig.json
    - examples/vite.config.ts
    - examples/index.html
    - examples/src/main.ts
    - examples/src/app.ts
    - examples/src/router.ts
    - examples/src/views/home-view.ts
    - scripts/check-single-instance.mjs
    - .changeset/config.json
    - .github/workflows/ci.yml
  key_links:
    - "root `package.json` `workspaces` MUST include `\"examples\"` (not just `\"packages/*\"`) or the `\"*\"` deps in `examples/package.json` resolve against the registry instead of the local symlink, silently defeating EXPL-01/EXPL-02."
    - "`examples/package.json` deps use plain `\"*\"`, never `\"workspace:*\"` — npm 11 throws `EUNSUPPORTEDPROTOCOL` on install with the pnpm/yarn-only protocol."
    - "Every route-target custom element (`home-view` now, `data-view`/`form-view` in 10-02) MUST be side-effect-imported in `main.ts` BEFORE `<router-outlet>` first navigates, or the outlet silently `console.warn`s and renders nothing (`router-outlet.ts:177-188`) — no build-time error."
    - "`<router-link>` navigates via its `to` attribute, not `href`; it auto-resolves its `Router` from the ancestor `<router-provider>` DOM context (no explicit `.router` binding needed on the link itself) — `router-link.ts:35-36,61-63`."
    - "`.changeset/config.json`'s `ignore` entry (`\"examples\"`) must exactly match `examples/package.json`'s `\"name\"` field or the ignore is silently ineffective."
---

<objective>
Wire the private `examples/` workspace app into the monorepo and prove, end-to-end against the real built packages, the thinnest working path: the navigation shell (`<router-provider>` + `<router-outlet>` + `<router-link>`) rendering one seam (store). Stand up the release-exclusion (`private:true` + Changesets `ignore`) and externalization-canary (`resolve.dedupe` + `npm ls` single-version script) infrastructure the rest of the phase reuses unchanged.

Purpose: de-risk the whole phase on its hardest, most novel wiring first — the npm workspace `"*"` dependency form, consuming `dist/` (not `src/`), the app-mode Vite config that is the deliberate *inverse* of every package's library config, and the router's tag-registration-before-navigation contract — before 10-02 expands out to the query and forms seams, which are lower-risk copies of existing in-repo demos.
Output: a booting `examples/` app (one route, one seam) + `scripts/check-single-instance.mjs` + the release-exclusion config, all green in a read-only CI gate.
</objective>

## Artifacts this phase produces (created by this plan)

New, this plan:
- `examples/package.json`, `examples/tsconfig.json`, `examples/vite.config.ts`, `examples/index.html` — the workspace app skeleton.
- `examples/src/main.ts`, `examples/src/app.ts`, `examples/src/router.ts`, `examples/src/views/home-view.ts` — the tracer path (router shell + store seam).
- `scripts/check-single-instance.mjs` — the EXPL-02 tree-level single-version hard gate.
- Edits: root `package.json` (`workspaces` gains `"examples"`), `.changeset/config.json` (`ignore` gains `"examples"`), `.github/workflows/ci.yml` (two new read-only `gate`-job steps).

10-02 (wave 2) adds `examples/src/views/data-view.ts` and `examples/src/views/form-view.ts` and wires them into the same `router.ts`/`main.ts`/`app.ts` this plan creates.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-examples-integration-app/10-RESEARCH.md
@.planning/phases/10-examples-integration-app/10-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Workspace wiring + tracer route — router shell rendering the store seam</name>
  <files>package.json, examples/package.json, examples/tsconfig.json, examples/vite.config.ts, examples/index.html, examples/src/main.ts, examples/src/app.ts, examples/src/router.ts, examples/src/views/home-view.ts</files>
  <read_first>
    - package.json (root) — current `"workspaces": ["packages/*"]` and `"build": "npm run build --workspaces"` has NO `--if-present`, so the new `examples` member MUST define its own `build` script or the command errors.
    - packages/kit/package.json — `"exports"` maps `"."` to `"./dist/kit.js"` — confirms the `"*"` workspace dep resolves through `exports` into `dist/`, not `src/`.
    - packages/kit/tsconfig.json — `"extends": "../../tsconfig.base.json"` + `"types": ["vite/client"]` pattern; `examples/` sits ONE level under root (not two, like `packages/*`), so its `extends` path must be `"../tsconfig.base.json"`.
    - tsconfig.base.json — `target: ES2023`, `erasableSyntaxOnly: true` (line 19), `moduleResolution: "bundler"`, `noEmit: true`.
    - packages/query/vite.config.ts — the LIBRARY config that externalizes `lit`/`@tanstack/query-core` (`rollupOptions.external`) — `examples/vite.config.ts` must do the OPPOSITE (bundle, `resolve.dedupe`, no `build.lib`, no `external`).
    - packages/router/src/router-core/types.ts — `RouteDefinition.component?: string` (line 25) — a route names its target by kebab-case tag string.
    - packages/router/src/router-core/routes.ts — `defineRoutes()` (lines 10-20) compiles `RouteDefinition[]` into `CompiledRoute[]`; `createRouter({ routes })` requires the COMPILED form, not raw definitions.
    - packages/router/src/router-lit/router-provider.ts — `.router` is a required, non-attribute property (lines 25-27); throws if unset (line 35); registered via `define("router-provider", ...)` at module scope (line 58, always runs on import).
    - packages/router/src/router-lit/router-link.ts — navigates via the `to` attribute (lines 34-36), NOT `href`; auto-resolves its router from the ancestor `<router-provider>` context when no explicit `.router` is set (lines 59-63).
    - packages/router/src/router-lit/router-outlet.ts — `renderComponent()` (lines 170-192): if `customElements.get(tagName)` is falsy it `console.warn`s and renders `nothing` (lines 177-188) — every route-target tag MUST already be `define()`d before the first navigation.
    - packages/kit/src/kit-element.ts — `use()` (lines 26-32) accepts a controller instance OR a `(host) => controller` factory.
    - packages/kit/src/index.ts — exports `KitElement` (line 2) AND the idempotent `define` wrapper (line 4) — use `define()` from `@willramdev/kit`, not raw `customElements.define`, for the app's own elements.
    - packages/store/src/store.ts — `createStore<T>(initialState)` returns `Store<T>` with `get/set/update/subscribe` (lines 30-92).
    - packages/store/src/store-slice.ts — `storeSlice(host, store, selector, options?)` (lines 58-66); the controller's read accessor is the `.value` getter (lines 38-40).
    - .planning/phases/10-examples-integration-app/10-RESEARCH.md — §Pattern 1 (workspace dep form), §Pattern 2 (app-mode vite config), §Common Pitfalls 1/2/3.
    - .planning/phases/10-examples-integration-app/10-PATTERNS.md — the `router.ts`/`main.ts`/`app.ts`/`home-view.ts` pattern assignments. NOTE: PATTERNS.md's own `app.ts` excerpt uses `<router-link href="/data">` — that is WRONG against the real source (`router-link.ts:35-36` — the property is `to`, not `href`). Use `to=`.
  </read_first>
  <action>
    Edit root `package.json`: change `"workspaces": ["packages/*"]` to `"workspaces": ["packages/*", "examples"]`. Without this the `"*"` deps below resolve against the registry, not the local packages.

    Create `examples/package.json`: `"name": "examples"`, `"private": true`, `"version": "0.0.0"`, `"type": "module"`; `dependencies` = `@willramdev/kit`, `@willramdev/router`, `@willramdev/query`, `@willramdev/forms`, `@willramdev/store` each at plain `"*"` (never `"workspace:*"` — npm 11 throws `EUNSUPPORTEDPROTOCOL`), plus `lit@^3.3.2`, `@tanstack/query-core@^5.91.0`, `@tanstack/form-core@^1.28.5`; `devDependencies` = `vite@^8.0.1`, `typescript@6.0.3`; `scripts`: `"dev": "vite"`, `"build": "vite build"`, `"typecheck": "tsc --noEmit"` (the `build` script is REQUIRED — root's `npm run build --workspaces` has no `--if-present` and errors on any workspace lacking it).

    Create `examples/tsconfig.json` extending `"../tsconfig.base.json"` (ONE `../`), `compilerOptions.types: ["vite/client"]`, `include: ["src"]`.

    Create `examples/vite.config.ts`: `defineConfig({ resolve: { dedupe: ['lit', '@lit/reactive-element', 'lit-html', 'lit-element', '@tanstack/query-core', '@tanstack/form-core'] } })` — no `build.lib`, no `rollupOptions.external`.

    Create `examples/index.html`: standard Vite app shell with `<script type="module" src="/src/main.ts"></script>` in `<body>`.

    Create `examples/src/router.ts`: `import { createRouter, defineRoutes } from '@willramdev/router'`; `export const router = createRouter({ routes: defineRoutes([{ path: '/', component: 'home-view' }]), mode: 'history' })`.

    Create `examples/src/app.ts`: `import { KitElement, define } from '@willramdev/kit'`; a class `ExamplesApp extends KitElement` whose `render()` returns a `<router-provider .router=${router}>` wrapping a `<nav>` with `<router-link to="/">Home</router-link>` and a `<router-outlet></router-outlet>` (property-bind `.router`; `to=` on the link — not `href=`); register with `define('examples-app', ExamplesApp)`.

    Create `examples/src/views/home-view.ts`: module-level `const counter = createStore({ count: 0 })`; class `HomeView extends KitElement` with a private field `#count = this.use(storeSlice(this, counter, (s) => s.count))`, rendering `${this.#count.value}` and a button calling `counter.update((s) => ({ count: s.count + 1 }))`; register with `define('home-view', HomeView)`.

    Create `examples/src/main.ts`: side-effect import `'@willramdev/router'` (registers `<router-provider>`/`<router-outlet>`/`<router-link>`), then `'./app.ts'`, then `'./views/home-view.ts'` (registration BEFORE navigation), then `document.body.appendChild(document.createElement('examples-app'))`.

    Run `npm install` from repo root once (links the new `examples` workspace member and hoists `lit`/`@tanstack/*`/`vite`/`typescript`), then `npm run build` (root, builds all five packages), then `npm run build -w examples` (packages must build before the app).
  </action>
  <verify>
    <automated>npm install && npm run build && npm run build -w examples && node -e "const fs=require('fs');const path=require('path');if(!fs.existsSync('examples/dist/index.html'))throw new Error('examples app did not build');const real=fs.realpathSync('node_modules/@willramdev/kit');if(!real.split(path.sep).includes('kit'))throw new Error('kit not resolved to local workspace symlink: '+real);const dir='examples/dist/assets';const bundle=fs.readdirSync(dir).filter(f=>f.endsWith('.js')).map(f=>fs.readFileSync(dir+'/'+f,'utf8')).join('');if(!bundle.includes('home-view'))throw new Error('home-view tag missing from bundle');console.log('tracer OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Root `package.json` `workspaces` array is `["packages/*", "examples"]`.
    - `node -e "console.log(require('fs').realpathSync('node_modules/@willramdev/kit'))"` prints a path inside `packages/kit` (local symlink, not a registry-fetched copy).
    - `npm run build` (root) then `npm run build -w examples` both exit 0; `examples/dist/index.html` exists.
    - The built `examples/dist/assets/*.js` bundle contains the string `home-view` (the route-target element's registration made it into the shipped bundle, not tree-shaken away).
    - `examples/vite.config.ts` contains `resolve.dedupe` and contains neither `build: { lib` nor `external:`.
    - `examples/src/app.ts` uses `<router-link to=` (not `href=`).
  </acceptance_criteria>
  <reversibility rating="reversible">Adding `examples` to workspaces plus the plain `"*"` dependency form is fully reversible (remove the directory + the `workspaces` entry, `npm install` again) with no data, schema, or external-service footprint — no decision checkpoint needed.</reversibility>
  <done>The `examples/` workspace app is linked via npm workspaces, builds successfully against the real built `dist/` of all five packages, and one navigable route (`/`) renders `<home-view>` exercising the store seam through the router shell (provider + outlet + link) end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Externalization canary script + release exclusion + CI wiring</name>
  <files>scripts/check-single-instance.mjs, .changeset/config.json, .github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/10-examples-integration-app/10-RESEARCH.md — §Code Examples "npm ls single-version check" (the full script to copy) and §Common Pitfalls 3 (vacuous dedup proof) and 4 (`ignore` alone doesn't stop publish).
    - scripts/verify-consumer.mjs (lines 439-455) — the existing `npm ls`/`shell:true`/version-`Set` parsing precedent this script upgrades from a WARNING into a hard gate.
    - .changeset/config.json — current state: `"fixed"` lists the 5 packages, `"ignore": []`. Change ONLY `ignore`, never touch `fixed`.
    - .github/workflows/ci.yml — the `gate` job's top-level `permissions: contents: read`; the final existing step `coverage (report-only)` (`run: npx vitest run --coverage`) — append the two new steps immediately after it.
  </read_first>
  <action>
    Create `scripts/check-single-instance.mjs`: copy the implementation from 10-RESEARCH.md's "npm ls single-version check" code block — `execFileSync('npm', ['ls', pkg, '--all', '--json'], { encoding: 'utf8', shell: true })` (try/catch reading `e.stdout` on npm's non-zero exit for unmet deps, per the `verify-consumer.mjs` precedent), a recursive `walk()` over `node.dependencies` collecting `dep.version` into a `Set` per package, and a hard `process.exit(1)` if `versions.size !== 1` for ANY of `['lit', '@tanstack/query-core', '@tanstack/form-core']` — zero matches (package absent) fails identically to two-or-more matches (no vacuous pass). No `cwd` override needed — `process.cwd()` is repo root when invoked as `node scripts/check-single-instance.mjs`, and npm workspaces hoist to root `node_modules`.

    Edit `.changeset/config.json`: change `"ignore": []` to `"ignore": ["examples"]` (must exactly match `examples/package.json`'s `"name"` field from Task 1). Do NOT touch the `"fixed"` array — never share a changeset between an ignored and a `fixed`-group package.

    Append two new steps to the `gate` job in `.github/workflows/ci.yml`, immediately after the existing `coverage (report-only)` step, each with an explanatory comment matching the surrounding gate-step voice: `examples app build (EXPL-01)` running `npm run build -w examples`, and `single-instance check (EXPL-02)` running `node scripts/check-single-instance.mjs`. Both are local-only (no registry token needed) — leave the top-level `permissions: contents: read` untouched and do not touch `.github/workflows/release.yml`.
  </action>
  <verify>
    <automated>node scripts/check-single-instance.mjs && node -e "const j=JSON.parse(require('fs').readFileSync('.changeset/config.json','utf8'));if(!j.ignore.includes('examples'))throw new Error('ignore missing examples');if(j.fixed.length!==1||j.fixed[0].length!==5)throw new Error('fixed group mutated');const p=require('./examples/package.json');if(p.private!==true)throw new Error('examples not private');const c=require('fs').readFileSync('.github/workflows/ci.yml','utf8');if(!c.includes('check-single-instance.mjs')||!c.includes('build -w examples'))throw new Error('ci.yml missing new steps');if(!/permissions:\s*\n\s*contents:\s*read/.test(c))throw new Error('permissions widened');console.log('release-exclusion + canary OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-single-instance.mjs` exits 0 and prints `OK: lit single version …`, `OK: @tanstack/query-core single version …`, `OK: @tanstack/form-core single version …`.
    - `.changeset/config.json` `ignore` array contains exactly `"examples"`; the `fixed` array is unchanged (still the 5-package group).
    - `examples/package.json` has `"private": true`.
    - `.github/workflows/ci.yml` `gate` job contains an `examples app build` step (`npm run build -w examples`) and a `single-instance check` step (`node scripts/check-single-instance.mjs`), both after the existing `coverage (report-only)` step; `permissions: contents: read` at the top of the file is unchanged; `.github/workflows/release.yml` is byte-for-byte untouched.
    - Negative check: temporarily editing `check-single-instance.mjs`'s `pkgs` array to include a package name absent from the tree and re-running makes the script exit 1 with a `FAIL:` line reporting 0 versions — proves the gate is not vacuous.
  </acceptance_criteria>
  <reversibility rating="reversible">Both `private:true` and the `ignore` entry are simple, fully reversible config flags — flipping them back later is a one-line edit each, no data migration.</reversibility>
  <done>`scripts/check-single-instance.mjs` hard-fails (exit 1) on any non-single-version resolution of `lit`/`@tanstack/query-core`/`@tanstack/form-core` and currently passes; the examples app is doubly excluded from releases (`private:true` + Changesets `ignore`); both checks are wired into the existing read-only `ci.yml` gate job with no token-scope change.</done>
</task>

</tasks>

## Flagged Assumptions (edge probe — no silent drops)

- **[FLAGGED — EXPL-01 concurrency edge probe]** "What is guaranteed if interrupted or run in parallel?" does not apply to this phase: `npm install`/`npm run build`/`vite build` are standard build-tool invocations with no phase-specific concurrency contract to prove — not a build requirement of this phase, not machine-verified here.
- **[FLAGGED — EXPL-02 ordering edge probe]** "Is output order specified and stable when elements compare equal?" does not apply: `check-single-instance.mjs` compares a `Set`'s cardinality (`size !== 1`), never its iteration order — order is irrelevant to correctness.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| examples app (local dev only) ↔ developer's browser | No network-exposed service; never deployed; single local user |
| npm workspace `"*"` dependency resolution ↔ registry | Trust boundary only if workspaces are misconfigured — mitigated by explicit membership |
| CI runner (`ci.yml` `gate` job) ↔ repo source | Read-only checkout, no secrets, no publish |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | root `package.json` workspaces / `examples`'s `"*"` deps | medium | mitigate | `examples` explicitly added to `workspaces`; `realpathSync('node_modules/@willramdev/kit')` proves local symlink resolution, not a registry fetch — verified in Task 1 acceptance criteria |
| T-10-02 | Information Disclosure | accidental publish of the never-meant-for-release `examples` app | medium | mitigate | `private:true` (authoritative publish block) + `.changeset/config.json` `ignore` entry (belt-and-suspenders); verified in Task 2 acceptance criteria |
| T-10-03 | Elevation of Privilege | new `ci.yml` gate steps | low | mitigate | Both new steps run under the existing `permissions: contents: read`; no new secrets, no registry token, no widened scope |
| T-10-SC | Tampering | package-manager installs | n/a | accept | No new external packages installed this phase (Package Legitimacy Audit: none) — `examples` depends only on the five in-repo workspace packages plus `lit`/`@tanstack/*`/`vite`/`typescript`, all already pinned in the existing lockfile |

No high-severity threat present (block-on = high) — nothing in this plan blocks.
</threat_model>

<verification>
- `npm run build` (root) + `npm run build -w examples` both exit 0.
- `node scripts/check-single-instance.mjs` exits 0.
- `examples/package.json` has `private:true`; `.changeset/config.json` `ignore` contains `"examples"`.
- `.github/workflows/ci.yml` carries both new gate steps; `permissions: contents: read` and `.github/workflows/release.yml` unchanged.
</verification>

<success_criteria>
- EXPL-01 (partial — full 4-seam coverage completes in 10-02): the app builds and boots one route/seam against the real built `dist/`.
- EXPL-02: `resolve.dedupe` configured + `check-single-instance.mjs` proves single-instance, wired into the read-only CI gate.
- EXPL-03: `private:true` + Changesets `ignore` both in place.
</success_criteria>

<output>
Create `.planning/phases/10-examples-integration-app/10-01-SUMMARY.md` when done.
</output>
