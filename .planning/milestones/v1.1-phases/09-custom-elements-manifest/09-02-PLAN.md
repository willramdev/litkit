---
phase: 09-custom-elements-manifest
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - packages/query/custom-elements-manifest.config.mjs
  - packages/query/src/query-client-provider.ts
  - packages/query/package.json
  - packages/query/custom-elements.json
  - packages/query/vscode.html-custom-data.json
  - packages/query/vscode.css-custom-data.json
  - packages/query/web-types.json
  - tools/cem-check/known-tags.json
autonomous: true
requirements: [CEM-01, CEM-02, CEM-03, CEM-04]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Building `@willramdev/query` regenerates `packages/query/custom-elements.json` whose `tagName` set equals exactly `{lit-query-client-provider}` (CEM-01/CEM-03)."
    - "The query manifest carries the enriched `client` property (the QueryClient provided to descendants) and a default slot from additive JSDoc (D-04 richness)."
    - "`packages/query/src/demo.ts` (`lit-query-demo-app`, `lit-query-demo-surface`) is glob-excluded from the query analyzer config, so no demo tag enters the query manifest (D-10)."
    - "`npm pack --dry-run -w @willramdev/query` lists `custom-elements.json`, `vscode.html-custom-data.json`, `vscode.css-custom-data.json`, and `web-types.json` alongside `dist/` — the `files` allowlist and `customElements`/`web-types` fields agree (CEM-02)."
    - "After appending the `query` row, `node tools/cem-check/assert-tags.mjs` enforces EQUALITY across both `forms` and `query`, and the freshness gate stays byte-stable (LF-pinned) on a rebuild (CEM-03)."
  prohibitions:
    - "MUST NOT change any runtime behavior of `lit-query-client-provider` or the v1.0 public API — additive JSDoc + config + generated JSON only."
    - "MUST NOT let the query demo tags (lit-query-demo-app, lit-query-demo-surface) enter the shipped query manifest."
  artifacts:
    - packages/query/custom-elements-manifest.config.mjs
    - packages/query/custom-elements.json
    - packages/query/vscode.html-custom-data.json
    - packages/query/vscode.css-custom-data.json
    - packages/query/web-types.json
  key_links:
    - "query `custom-elements-manifest.config.mjs` `exclude` MUST list `src/demo.ts` — else `lit-query-demo-app`/`lit-query-demo-surface` leak and the equality gate fails."
    - "query `customElements`/`web-types` fields ↔ `files` entries ↔ committed artifact filenames must agree (Anti-Pattern 5)."
    - "the appended `query` row in `tools/cem-check/known-tags.json` is the shared file that serializes this plan before 09-03 (router)."
---

<objective>
Expand CEM to `@willramdev/query` by copying the proven forms slice from 09-01: a per-package analyzer config (with the `src/demo.ts` exclude), additive JSDoc on `lit-query-client-provider`, `cem` chained into `build`, committed manifest + editor-data, the `customElements`/`web-types` fields + `files` entries, and the `query` row appended to the shared known-tag contract.

Purpose: `query` is a near-exact mirror of `forms` — one element (`lit-query-client-provider`) registered via the real `@customElement` decorator, so `tagName` resolves automatically. The only query-specific wrinkle is excluding `src/demo.ts` (which registers two demo tags via the real decorator). Proven pattern, low risk — done before the harder router work in 09-03.
Output: a green query manifest + VS Code custom-data + web-types shipped in the tarball; `assert-tags.mjs` now enforces both forms and query.
</objective>

## Artifacts this phase produces (created by this plan)

New, this plan (excluded from source-grounding drift verification — all net-new):
- `packages/query/custom-elements-manifest.config.mjs` — per-package analyzer config with `src/demo.ts` excluded (D-02/D-10).
- `packages/query/custom-elements.json` — generated manifest, committed (D-05).
- `packages/query/vscode.html-custom-data.json`, `packages/query/vscode.css-custom-data.json`, `packages/query/web-types.json` — editor-data (D-07).
- New package.json fields on `@willramdev/query`: `customElements`, `web-types`, four `files` entries, a `cem` script, `cem` chained into `build`, three new devDeps.
- Appended `query` row in `tools/cem-check/known-tags.json`.
- Additive JSDoc (`@prop`/`@slot`) on `packages/query/src/query-client-provider.ts`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-custom-elements-manifest/09-CONTEXT.md
@.planning/phases/09-custom-elements-manifest/09-RESEARCH.md
@.planning/phases/09-custom-elements-manifest/09-PATTERNS.md
@.planning/phases/09-custom-elements-manifest/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CEM config + enriched JSDoc + build/packaging wiring for @willramdev/query</name>
  <files>packages/query/custom-elements-manifest.config.mjs, packages/query/src/query-client-provider.ts, packages/query/package.json, packages/query/custom-elements.json, packages/query/vscode.html-custom-data.json, packages/query/vscode.css-custom-data.json, packages/query/web-types.json</files>
  <read_first>
    - packages/forms/custom-elements-manifest.config.mjs (the proven 09-01 config to mirror — same keys, only the `exclude` differs)
    - packages/forms/package.json (the 09-01 modified `files`/`scripts`/`customElements`/`web-types`/`devDependencies` shape to copy)
    - packages/query/src/query-client-provider.ts (the element to annotate — real `@customElement('lit-query-client-provider')` at line 9; `client: QueryClient` `@property({ attribute: false })` at lines 11-12; default `<slot>` at line 28)
    - packages/query/src/demo.ts (registers `lit-query-demo-app` + `lit-query-demo-surface` via the real decorator — MUST be excluded, D-10)
    - packages/query/package.json (current `files` at lines 23-28, `build` at line 40, `devDependencies` at lines 50-56)
    - .planning/phases/09-custom-elements-manifest/09-RESEARCH.md (§Pattern 1 per-package `exclude` for query = `['src/**/*.test.ts', 'src/demo.ts']`; §Code Examples the verbatim query JSDoc; §Installation versions)
  </read_first>
  <action>
    Install the three pinned devDeps into `@willramdev/query` only: `@custom-elements-manifest/analyzer@0.11.0`, `custom-element-vs-code-integration@1.5.0`, `custom-element-jet-brains-integration@1.7.0` (D-01/D-03). Add to `devDependencies` in existing alphabetized order.

    Create `packages/query/custom-elements-manifest.config.mjs` (D-02) by mirroring the 09-01 forms config exactly, changing only `exclude` to `['src/**/*.test.ts', 'src/demo.ts']` (D-10 — `src/demo.ts` registers the two `lit-query-demo-*` tags). Keep `globs: ['src/**/*.ts']`, `outdir: '.'`, `litelement: true`, `packagejson: false`, and both editor plugins (`customElementVsCodePlugin({ outdir: '.' })`, `customElementJetBrainsPlugin({ outdir: '.', packageJson: false })`).

    Add additive JSDoc ONLY to `packages/query/src/query-client-provider.ts` (D-04) — real decorator, NO `@tag` needed. Use RESEARCH §Code Examples verbatim: `@prop {QueryClient} client` (the QueryClient provided to descendants, defaults to `createQueryClient()`) and `@slot` for the default slot. Comments only.

    Modify `packages/query/package.json` (D-03/D-05): add `"cem": "cem analyze"`; append `&& npm run cem` to `"build": "vite build && tsc -p tsconfig.build.json"`; add `"customElements": "./custom-elements.json"` and `"web-types": "./web-types.json"`; extend `files` with the four artifact filenames. Set fields by hand (`packagejson`/`packageJson` are `false`).

    Run `npm run build -w @willramdev/query` and commit the four generated artifacts. (`.gitattributes` LF pins already cover `packages/*/...` from 09-01, so the artifacts land LF-normalized.)
  </action>
  <verify>
    <automated>npm run build -w @willramdev/query && node -e "const m=require('./packages/query/custom-elements.json');const t=(m.modules||[]).flatMap(x=>x.declarations||[]).filter(d=>d.customElement&&d.tagName).map(d=>d.tagName).sort();if(JSON.stringify(t)!==JSON.stringify(['lit-query-client-provider']))throw new Error('tagName set '+JSON.stringify(t));const fs=require('fs');for(const f of ['vscode.html-custom-data.json','vscode.css-custom-data.json','web-types.json'])if(!fs.statSync('packages/query/'+f).size)throw new Error('empty '+f);console.log('query CEM OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/query/custom-elements.json` exists and its `customElement` `tagName` set equals exactly `["lit-query-client-provider"]` — no `lit-query-demo-app`/`lit-query-demo-surface` present (demo exclude worked).
    - `packages/query/vscode.html-custom-data.json`, `packages/query/vscode.css-custom-data.json`, and `packages/query/web-types.json` exist and are non-empty.
    - `npm pack --dry-run -w @willramdev/query` lists all four JSON artifacts plus `dist/`.
    - `packages/query/package.json` has the `customElements`/`web-types` fields, four new `files` entries, a `cem` script, and `&& npm run cem` on `build`; `query-client-provider.ts` diff is comment-only.
    - A second `npm run build -w @willramdev/query` leaves `git status --porcelain packages/query/*.json` clean (byte-stable).
  </acceptance_criteria>
  <done>Building `@willramdev/query` produces a committed, enriched, demo-free `custom-elements.json` (`tagName` == `{lit-query-client-provider}`) plus editor-data, shipped via agreeing discovery fields and `files` entries.</done>
</task>

<task type="auto">
  <name>Task 2: Register query in the shared tag-set completeness contract</name>
  <files>tools/cem-check/known-tags.json</files>
  <read_first>
    - tools/cem-check/known-tags.json (the 09-01 contract — currently `{ "forms": ["lit-form"] }`; append the query row, preserve JSON shape/formatting)
    - tools/cem-check/assert-tags.mjs (the gate that reads this file — confirm the row key must match the `packages/<pkg>` directory name)
    - .planning/phases/09-custom-elements-manifest/09-RESEARCH.md (§Pattern 6 the known-tags contract shape)
  </read_first>
  <action>
    Append the `query` row to `tools/cem-check/known-tags.json` so it reads `{ "forms": ["lit-form"], "query": ["lit-query-client-provider"] }` (D-09). The key must equal the `packages/query` directory name; the value is the sorted known-tag list for query. This makes the shared `assert-tags.mjs` enforce EQUALITY across both forms and query. No script change is needed — `assert-tags.mjs` iterates the contract.
  </action>
  <verify>
    <automated>npm run build -w @willramdev/forms && npm run build -w @willramdev/query && node tools/cem-check/assert-tags.mjs && git add -A -- 'packages/*/custom-elements.json' 'packages/*/vscode.*-custom-data.json' 'packages/*/web-types.json' && git diff --cached --exit-code -- 'packages/*/custom-elements.json' 'packages/*/vscode.*-custom-data.json' 'packages/*/web-types.json' && echo "forms+query gates OK"</automated>
  </verify>
  <acceptance_criteria>
    - `tools/cem-check/known-tags.json` contains both a `forms` and a `query` row with their correct tag lists.
    - `node tools/cem-check/assert-tags.mjs` exits 0 with both forms and query manifests present (equality holds for both).
    - The freshness gate (`git add -A` + `git diff --cached --exit-code`, artifact-globbed) exits 0 after rebuilding both packages.
  </acceptance_criteria>
  <done>`assert-tags.mjs` now enforces tag-set equality for forms AND query via the shared committed contract, with the freshness gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → devDependency install | The same three analyzer/editor plugins enter the query package's toolchain |
| Committed static JSON → published tarball | query manifest/editor-data shipped to consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-SC | Tampering | 3 devDeps added to `@willramdev/query` | medium | mitigate | Same pinned, [VERIFIED], zero-postinstall packages as 09-01 (RESEARCH Package Legitimacy Audit) — no blocking human checkpoint required |
| T-09-03 | Tampering | committed query manifest/editor-data JSON | low | mitigate | Regenerated by `build` + `git diff --cached --exit-code` freshness gate (D-06) |
| T-09-05 | Spoofing / Tampering | leaked demo tag in shipped manifest | low | mitigate | `src/demo.ts` glob-excluded (D-10) + tag-set EQUALITY gate fails on any stray tag (D-09) |

No high-severity threat present (block-on = high) — nothing in this plan blocks.
</threat_model>

<verification>
- `npm run build -w @willramdev/query` regenerates all four query artifacts deterministically.
- `node tools/cem-check/assert-tags.mjs` exits 0 across forms + query.
- freshness gate (artifact-globbed `git diff --cached --exit-code`) exits 0.
- `npm pack --dry-run -w @willramdev/query` lists the four artifacts.
</verification>

<success_criteria>
- CEM-01: `packages/query/custom-elements.json` generated via the analyzer, chained into `build`.
- CEM-02: query declares `customElements` + `web-types` fields and ships the manifest via `files`.
- CEM-03: query tag-set equality green (`{lit-query-client-provider}`, no demo leak) in the shared gate.
- CEM-04: VS Code custom-data + web-types emitted for query (referenced via the 09-01 `.vscode/settings.json` + `web-types` field).
</success_criteria>

<output>
Create `.planning/phases/09-custom-elements-manifest/09-02-SUMMARY.md` when done.
</output>
