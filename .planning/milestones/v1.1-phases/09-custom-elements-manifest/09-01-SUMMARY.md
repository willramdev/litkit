---
phase: 09-custom-elements-manifest
plan: 01
subsystem: infra
tags: [custom-elements-manifest, cem, lit, vscode-custom-data, web-types, ci-gate, editor-autocomplete]

# Dependency graph
requires:
  - phase: 06-sharper-types-plain-js-ergonomics-type-semver-gate
    provides: committed-artifact + git-diff --cached --exit-code freshness gate pattern (ci.yml) and .gitattributes eol=lf pin convention
provides:
  - "@willramdev/forms Custom Elements Manifest (custom-elements.json) generated by @custom-elements-manifest/analyzer, chained into build, committed at package root"
  - "VS Code custom-data (vscode.html/css-custom-data.json) + JetBrains web-types (web-types.json) for forms, shipped via files allowlist + customElements/web-types package.json fields"
  - "Shared tools/cem-check/assert-tags.mjs tag-set EQUALITY gate + tools/cem-check/known-tags.json contract (reused unchanged by 09-02 query and 09-03 router)"
  - "Two ci.yml gate-job steps: CEM freshness gate + CEM completeness gate (read-only, permissions unchanged)"
  - ".gitattributes eol=lf pins covering all three element packages' CEM artifacts"
affects: [09-02-query-cem, 09-03-router-cem]

# Actuals
actuals:
  tokens: 18705
  tasks: 2
  commits: 3

# Tech tracking
tech-stack:
  added:
    - "@custom-elements-manifest/analyzer@0.11.0 (devDep, forms only)"
    - "custom-element-vs-code-integration@1.5.0 (devDep, forms only)"
    - "custom-element-jet-brains-integration@1.7.0 (devDep, forms only)"
  patterns:
    - "Per-package custom-elements-manifest.config.mjs (litelement:true, packagejson:false, two editor plugins)"
    - "cem analyze chained into build; artifacts committed at package root, LF-pinned, byte-stable"
    - "Tag-set EQUALITY gate (assert-tags.mjs) + committed known-tags.json contract that grows per package"

key-files:
  created:
    - packages/forms/custom-elements-manifest.config.mjs
    - packages/forms/custom-elements.json
    - packages/forms/vscode.html-custom-data.json
    - packages/forms/vscode.css-custom-data.json
    - packages/forms/web-types.json
    - tools/cem-check/assert-tags.mjs
    - tools/cem-check/known-tags.json
    - .vscode/settings.json (uncommitted — gitignored, repo-local dogfooding)
  modified:
    - packages/forms/package.json
    - packages/forms/src/lit-form.ts
    - .gitattributes
    - .github/workflows/ci.yml

key-decisions:
  - "Two editor plugins, not one — corrected D-07's single-plugin premise (RESEARCH A1): custom-element-vs-code-integration emits VS Code custom-data, custom-element-jet-brains-integration emits web-types; both run in one config."
  - "devDeps landed as caret ranges (^0.11.0/^1.5.0/^1.7.0) matching repo-wide convention; lockfile pins exact resolved versions (satisfies threat T-09-SC)."
  - ".vscode/settings.json created but left uncommitted — repo .gitignore ignores .vscode/*; matches D-08's 'repo-local, not committed-artifact, not gated' framing."

patterns-established:
  - "CEM per-package config + committed root artifacts + LF pin + freshness/completeness gate — the spine 09-02/09-03 reuse verbatim, swapping only the tag rows and excludes."

requirements-completed: [CEM-01, CEM-02, CEM-03, CEM-04]

coverage:
  - id: D1
    description: "custom-elements.json generated for @willramdev/forms via analyzer, chained into build; tagName set == {lit-form}"
    requirement: CEM-01
    verification:
      - kind: automated
        ref: "npm run build -w @willramdev/forms && node -e (tagName set == ['lit-form'] + native-validation present)"
        status: pass
    human_judgment: false
  - id: D2
    description: "forms ships manifest + editor-data via files allowlist and customElements/web-types fields"
    requirement: CEM-02
    verification:
      - kind: automated
        ref: "npm pack --dry-run -w @willramdev/forms lists custom-elements.json, vscode.*-custom-data.json, web-types.json, dist/"
        status: pass
    human_judgment: false
  - id: D3
    description: "tag-set EQUALITY completeness gate + git-diff freshness gate green for forms; shared assert-tags.mjs + ci.yml infra exists"
    requirement: CEM-03
    verification:
      - kind: automated
        ref: "node tools/cem-check/assert-tags.mjs (exit 0) + git add -A/git diff --cached --exit-code (exit 0) + negative check (hand-edit -> non-zero)"
        status: pass
    human_judgment: false
  - id: D4
    description: "VS Code custom-data + JetBrains web-types emitted for forms and referenced via .vscode/settings.json + web-types field"
    requirement: CEM-04
    verification:
      - kind: automated
        ref: "vscode.html/css-custom-data.json + web-types.json exist non-empty; .vscode/settings.json html.customData references forms path"
        status: pass
    human_judgment: true
    rationale: "Whether every attribute/event/slot renders live in a real VS Code / JetBrains session is not machine-verifiable in-phase (FLAGGED CEM-04 edge probe); accepted as manually spot-checkable via .vscode dogfooding (D-08)."

# Metrics
duration: 22min
completed: 2026-08-22
status: complete
---

# Phase 9 Plan 01: End-to-end CEM for @willramdev/forms Summary

**Custom Elements Manifest generation proven end-to-end on @willramdev/forms — analyzer-generated custom-elements.json + VS Code custom-data + JetBrains web-types shipped in the tarball, plus the shared tag-set-equality and byte-stable freshness gates all three element packages reuse.**

## Performance

- **Duration:** ~22 min
- **Completed:** 2026-08-22
- **Tasks:** 2
- **Files modified/created:** 13 (12 committed + 1 gitignored dogfood file)

## Accomplishments
- `@willramdev/forms` builds `custom-elements.json` via `@custom-elements-manifest/analyzer@0.11.0` (LitElement flavor) chained into `npm run build`; `tagName` set equals exactly `{lit-form}` and the manifest carries the enriched `native-validation` attribute + default slot from additive JSDoc.
- Both editor-data artifacts ship: VS Code custom-data (`vscode.html/css-custom-data.json`) via `customElementVsCodePlugin` and JetBrains `web-types.json` via `customElementJetBrainsPlugin` — corrected the single-plugin premise (D-07) to the two-plugin reality (RESEARCH A1).
- Packaging contract verified: `customElements`/`web-types` fields + four `files` entries agree; `npm pack --dry-run` lists all four artifacts alongside `dist/`.
- Byte-stability proven: a second `npm run build` produced `git hash-object`-identical artifacts; `.gitattributes` `eol=lf` pins (covering all three element packages) keep Windows-author == Ubuntu-CI.
- Shared gate infrastructure stood up: `tools/cem-check/assert-tags.mjs` (order-independent tag-set EQUALITY, throws on missing manifest) + committed `known-tags.json` contract; two `ci.yml` gate-job steps (freshness + completeness) after `npm run build`, `permissions: contents: read` unchanged, `release.yml` untouched.
- Negative check confirmed: a hand-edit to `custom-elements.json` makes the freshness gate exit non-zero; the completeness EQUALITY logic detects a leaked/extra tag.

## Task Commits

1. **Task 1: End-to-end CEM for @willramdev/forms (tracer)** - `aaa3e70` (feat)
2. **Task 2: Shared completeness + freshness gates, CI wiring, dogfooding** - `49cf3a8` (feat)

**Plan metadata:** this commit (docs: complete plan)

## Files Created/Modified
- `packages/forms/custom-elements-manifest.config.mjs` - Analyzer config: `litelement:true`, `packagejson:false`, both editor plugins, `outdir:'.'`.
- `packages/forms/custom-elements.json` - Generated manifest, committed at package root.
- `packages/forms/vscode.html-custom-data.json`, `vscode.css-custom-data.json` - VS Code custom-data.
- `packages/forms/web-types.json` - JetBrains web-types.
- `packages/forms/package.json` - Added `cem` script, chained into `build`; `customElements`/`web-types` fields; four `files` entries; three devDeps.
- `packages/forms/src/lit-form.ts` - Additive `@attr native-validation`/`@prop form`/`@slot` JSDoc (comment-only).
- `.gitattributes` - `eol=lf` pins for the three CEM artifact globs (all element packages).
- `tools/cem-check/assert-tags.mjs` - Tag-set EQUALITY gate.
- `tools/cem-check/known-tags.json` - Committed known-tag contract (forms row only in this plan).
- `.github/workflows/ci.yml` - "CEM freshness gate" + "CEM completeness gate" steps in the `gate` job.
- `.vscode/settings.json` - `html.customData` for all three packages (created, uncommitted — gitignored, repo-local dogfooding).

## Decisions Made
- **Two editor plugins, not one.** D-07 assumed a single plugin emits both artifacts; RESEARCH A1 corrected this. Verified installed exports (`customElementVsCodePlugin`, `customElementJetBrainsPlugin`) and default filenames against the packages' READMEs before relying on them (Assumption A2).
- **Caret devDep ranges.** `npm install -D` produced `^0.11.0`/`^1.5.0`/`^1.7.0`, matching every existing devDep in the repo; the lockfile pins exact resolved versions, satisfying threat T-09-SC's pinning intent.
- **`.vscode/settings.json` left uncommitted.** The repo `.gitignore` ignores `.vscode/*`; leaving it untracked matches D-08's explicit "repo-local dogfooding only — not committed-artifact, not gated" framing and respects the established ignore convention (no force-add).

## Deviations from Plan

### Auto-fixed / adjustments

**1. [Rule 3 - adjustment] `.vscode/settings.json` is gitignored — committed-file contract satisfied on disk, not in git**
- **Found during:** Task 2 staging.
- **Issue:** `.gitignore:20` (`.vscode/*`) ignores the dogfood settings file; `git add` refused it.
- **Resolution:** Left the file on disk (it serves its repo-local autocomplete purpose there) rather than force-adding against the ignore rule. Consistent with the plan's own "not committed-artifact" framing for D-08. No functional impact — the file is not gated and 09-02/09-03 need not touch it.
- **Files:** `.vscode/settings.json` (present, untracked).
- **Logged:** WINDOWS ledger (deviation) + this SUMMARY.

**Total deviations:** 1 adjustment (no code auto-fixes needed). **Impact:** none on scope; the acceptance criterion (file exists with all three package paths) is met on disk.

## Known Stubs
None. All acceptance criteria met with real, generated artifacts.

## Issues Encountered
- The tracer negative-check's first-pass `.replace('lit-form', ...)` hit a module-path string rather than the `tagName`, so the completeness gate appeared not to fire; re-ran with a direct tag-set comparison confirming the EQUALITY logic detects a leaked/extra tag. No code change needed — test artifact only.

## Flagged Assumptions (surfaced, not dropped)
- **[FLAGGED CEM-04 edge probe]** Live editor rendering of every attribute/event/slot in a real VS Code / JetBrains session is not machine-verifiable in-phase. Verified in-phase by asserting the three editor-data files exist and are non-empty; accepted as manually spot-checkable via the `.vscode/settings.json` dogfooding (D-08). Recorded as `human_judgment: true` on coverage D4 and in the WINDOWS ledger.

## Self-Check: PASSED
- Files verified present: `packages/forms/custom-elements.json`, `packages/forms/web-types.json`, `packages/forms/vscode.html-custom-data.json`, `packages/forms/vscode.css-custom-data.json`, `packages/forms/custom-elements-manifest.config.mjs`, `tools/cem-check/assert-tags.mjs`, `tools/cem-check/known-tags.json` — all exist.
- Commits verified in `git log`: `aaa3e70`, `49cf3a8`.

## Next Phase Readiness
- The CEM spine is proven and reusable: 09-02 (query) and 09-03 (router) add their own `custom-elements-manifest.config.mjs` + build chain + append their `known-tags.json` rows; the `ci.yml` gates and `.gitattributes` globs already cover them. Router (09-03) carries the harder `@tag` `define()`-wrapper work.
- No blockers.

---
*Phase: 09-custom-elements-manifest*
*Completed: 2026-08-22*
