---
phase: 09-custom-elements-manifest
plan: 02
subsystem: infra
tags: [custom-elements-manifest, cem, lit, vscode-custom-data, web-types, ci-gate, editor-autocomplete, query]

# Dependency graph
requires:
  - phase: 09-custom-elements-manifest
    provides: "09-01 CEM spine — per-package config shape, cem-into-build chain, shared assert-tags.mjs EQUALITY gate + known-tags.json contract, .gitattributes LF pins, ci.yml gate steps"
provides:
  - "@willramdev/query Custom Elements Manifest (custom-elements.json) generated by @custom-elements-manifest/analyzer, chained into build, committed at package root; tagName set == {lit-query-client-provider}, demo tags excluded"
  - "VS Code custom-data (vscode.html/css-custom-data.json) + JetBrains web-types (web-types.json) for query, shipped via files allowlist + customElements/web-types package.json fields"
  - "query row appended to tools/cem-check/known-tags.json — assert-tags.mjs now enforces tag-set EQUALITY across forms AND query"
affects: [09-03-router-cem]

# Actuals
actuals:
  tokens: 8338
  tasks: 2
  commits: 3

# Tech tracking
tech-stack:
  added:
    - "@custom-elements-manifest/analyzer@^0.11.0 (devDep, query — caret range, lockfile pins exact)"
    - "custom-element-vs-code-integration@^1.5.0 (devDep, query)"
    - "custom-element-jet-brains-integration@^1.7.0 (devDep, query)"
  patterns:
    - "09-01 CEM spine reused verbatim for query — swapped only the demo exclude (src/demo.ts) and the known-tags row"

key-files:
  created:
    - packages/query/custom-elements-manifest.config.mjs
    - packages/query/custom-elements.json
    - packages/query/vscode.html-custom-data.json
    - packages/query/vscode.css-custom-data.json
    - packages/query/web-types.json
  modified:
    - packages/query/package.json
    - packages/query/src/query-client-provider.ts
    - tools/cem-check/known-tags.json
    - package-lock.json

key-decisions:
  - "query exclude adds src/demo.ts (not needed for forms, whose demo lives outside src/) — src/demo.ts registers lit-query-demo-app + lit-query-demo-surface via the real decorator (D-10); without the exclude those tags leak and the EQUALITY gate fails."
  - "devDeps landed as caret ranges (^0.11.0/^1.5.0/^1.7.0) matching repo convention; lockfile pins exact resolved versions (satisfies T-09-SC). The three packages were already resolved in the workspace from 09-01, so npm reported 'up to date' while adding them to query's devDependencies."
  - "customElements/web-types fields set by hand (packagejson:false / packageJson:false) so the freshness gate never trips on a tooling key-reorder."

patterns-established:
  - "The CEM slice is now proven on two packages via one shared gate; 09-03 (router) appends its known-tags row and adds @tag JSDoc for the define()-wrapper tags."

requirements-completed: [CEM-01, CEM-02, CEM-03, CEM-04]

coverage:
  - id: D1
    description: "custom-elements.json generated for @willramdev/query via analyzer, chained into build; tagName set == {lit-query-client-provider}, no demo leak"
    requirement: CEM-01
    verification:
      - kind: automated
        ref: "npm run build -w @willramdev/query && node -e (tagName set == ['lit-query-client-provider']; demo tags absent)"
        status: pass
    human_judgment: false
  - id: D2
    description: "query ships manifest + editor-data via files allowlist and customElements/web-types fields"
    requirement: CEM-02
    verification:
      - kind: automated
        ref: "npm pack --dry-run -w @willramdev/query lists custom-elements.json, vscode.*-custom-data.json, web-types.json, dist/"
        status: pass
    human_judgment: false
  - id: D3
    description: "tag-set EQUALITY completeness gate + git-diff freshness gate green for forms AND query via shared assert-tags.mjs + known-tags.json contract"
    requirement: CEM-03
    verification:
      - kind: automated
        ref: "node tools/cem-check/assert-tags.mjs (exit 0) + git add -A/git diff --cached --exit-code artifact-globbed (exit 0) after rebuilding both packages"
        status: pass
    human_judgment: false
  - id: D4
    description: "VS Code custom-data + JetBrains web-types emitted for query and referenced via .vscode/settings.json (09-01) + web-types field"
    requirement: CEM-04
    verification:
      - kind: automated
        ref: "vscode.html/css-custom-data.json + web-types.json exist non-empty; .vscode/settings.json (09-01) already references the query path"
        status: pass
    human_judgment: true
    rationale: "Whether every attribute/property/slot renders live in a real VS Code / JetBrains session is not machine-verifiable in-phase (inherited FLAGGED CEM-04 edge probe from 09-01); accepted as manually spot-checkable via .vscode dogfooding."

# Metrics
duration: 3min
completed: 2026-08-22
status: complete
---

# Phase 9 Plan 02: End-to-end CEM for @willramdev/query Summary

**CEM expanded to @willramdev/query by copying the proven 09-01 forms slice — analyzer-generated custom-elements.json (demo tags glob-excluded) + VS Code custom-data + JetBrains web-types shipped in the tarball, and the shared tag-set-equality gate now enforces both forms and query.**

## Performance

- **Duration:** ~3 min
- **Completed:** 2026-08-22
- **Tasks:** 2
- **Files modified/created:** 9 (5 created + 4 modified)

## Accomplishments
- `@willramdev/query` builds `custom-elements.json` via `@custom-elements-manifest/analyzer@0.11.0` (LitElement flavor) chained into `npm run build`; `tagName` set equals exactly `{lit-query-client-provider}` and the manifest carries the enriched `client` property plus a default slot from additive JSDoc.
- `src/demo.ts` (`lit-query-demo-app`, `lit-query-demo-surface`) is glob-excluded — verified absent from the shipped manifest (D-10).
- Both editor-data artifacts ship: VS Code custom-data (`vscode.html/css-custom-data.json`) and JetBrains `web-types.json`.
- Packaging contract verified: `customElements`/`web-types` fields + four `files` entries agree; `npm pack --dry-run` lists all four artifacts alongside `dist/query.js`.
- Byte-stability proven: a second `npm run build` left the artifact-globbed `git diff --cached --exit-code` clean; `.gitattributes` LF pins (already covering `packages/*/` from 09-01) keep the artifacts LF-normalized.
- Shared completeness contract grown: appended the `query` row to `known-tags.json`; `assert-tags.mjs` now enforces tag-set EQUALITY for forms AND query — no script change needed.

## Task Commits

1. **Task 1: End-to-end CEM for @willramdev/query** - `d8fe7f6` (feat)
2. **Task 2: Register query in the shared tag-set completeness contract** - `32b2bbe` (feat)

**Plan metadata:** docs commit (this SUMMARY + STATE + ROADMAP).

## Files Created/Modified
- `packages/query/custom-elements-manifest.config.mjs` - Analyzer config mirroring forms; only `exclude` differs (`['src/**/*.test.ts', 'src/demo.ts']`, D-10).
- `packages/query/custom-elements.json` - Generated manifest, committed at package root.
- `packages/query/vscode.html-custom-data.json`, `vscode.css-custom-data.json` - VS Code custom-data.
- `packages/query/web-types.json` - JetBrains web-types.
- `packages/query/package.json` - Added `cem` script, chained into `build`; `customElements`/`web-types` fields; four `files` entries; three CEM devDeps.
- `packages/query/src/query-client-provider.ts` - Additive `@prop {QueryClient} client`/`@slot` JSDoc (comment-only).
- `tools/cem-check/known-tags.json` - Appended `query` row (forms row preserved).
- `package-lock.json` - Records the three query devDeps.

## Decisions Made
- **query needs the `src/demo.ts` exclude; forms did not.** forms' demo lives outside `src/` and was never matched by the `src/**/*.ts` glob, so its config excluded only tests. query's demo is `src/demo.ts` and registers two `lit-query-demo-*` tags via the real decorator — excluding it is what keeps the manifest demo-free and the EQUALITY gate green (D-10).
- **devDeps as caret ranges.** Matches repo convention; the three packages were already resolved in the workspace from 09-01 (npm printed "up to date"), so this only added them to query's `devDependencies` with the lockfile pinning exact versions (T-09-SC).
- **Hand-set discovery fields.** `customElements`/`web-types` set manually with `packagejson`/`packageJson` false, so the freshness gate never red-lines on a tooling-authored key reorder.

## Deviations from Plan
None — plan executed exactly as written. Rules 1-3 not triggered; no auto-fixes needed.

## Known Stubs
None. All acceptance criteria met with real, generated artifacts.

## Issues Encountered
None.

## Self-Check: PASSED
- Files verified present: `packages/query/custom-elements.json`, `packages/query/web-types.json`, `packages/query/vscode.html-custom-data.json`, `packages/query/vscode.css-custom-data.json`, `packages/query/custom-elements-manifest.config.mjs` — all exist.
- `tools/cem-check/known-tags.json` contains both `forms` and `query` rows.
- Commits verified in `git log`: `d8fe7f6`, `32b2bbe`.

## Next Phase Readiness
- 09-03 (router) is the last CEM wave: add `packages/router/custom-elements-manifest.config.mjs` with the router excludes (`src/example/**`, `src/my-element.ts`), `@tag` JSDoc on `router-outlet`/`router-provider`/`router-link` (they register via the `define()` wrapper the analyzer can't statically resolve), chain `cem` into router's build, and append the `router` row to `known-tags.json`. The ci.yml gates and `.gitattributes` globs already cover it.
- No blockers.

---
*Phase: 09-custom-elements-manifest*
*Completed: 2026-08-22*
