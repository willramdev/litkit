No external API integration: Phase 9 wires the build-time `@custom-elements-manifest/analyzer` CLI plus two editor-data generator plugins (`custom-element-vs-code-integration`, `custom-element-jet-brains-integration`) and emits static JSON artifacts (`custom-elements.json`, VS Code custom-data, `web-types.json`). These are dev/build tooling and shipped static files — there is no external runtime API, SDK, or service to build a coverage matrix against.
