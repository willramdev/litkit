---
phase: 01-build-typecheck-hardening
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/query/package.json
  - packages/forms/package.json
  - packages/forms/src/internal/engine.ts
autonomous: true
requirements: [BUILD-04, BUILD-03, BUILD-01]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "`@tanstack/query-core` is under `peerDependencies` (^5.0.0) in @willram/query and NOT under `dependencies`"
    - "`@tanstack/form-core` is under `peerDependencies` (^1.0.0) in @willram/forms and NOT under `dependencies`"
    - "both TanStack cores remain `devDependencies` at a working pinned version so local build/typecheck/test resolve"
    - "query `sideEffects` allowlists dist/query.js; forms `sideEffects` allowlists dist/forms.js (zod.js stays tree-shakeable)"
    - "`npm run build -w @willram/query` and `-w @willram/forms` exit 0 and emit dist"
    - "recoverable `any` in packages/forms/src/internal/engine.ts is reduced within erasableSyntaxOnly limits with no public/exported type changed"
  artifacts:
    - packages/query/package.json
    - packages/forms/package.json
    - packages/forms/src/internal/engine.ts
  key_links:
    - "peerDependencies range ↔ devDependencies pin — the dev pin must satisfy the broad peer range so in-workspace resolution stays green"
    - "sideEffects allowlist path ↔ the single Vite-emitted entry that bundles the @customElement provider/lit-form registration"
---

<objective>
Expand the proven hardening pattern from the router tracer to the two TanStack-backed packages: reclassify the TanStack cores as required peer dependencies (D-02/BUILD-04), allowlist each package's element-registering built entry in `sideEffects` (D-03/BUILD-03), and finish the bounded internal-`any` cleanup in the forms engine (D-04/BUILD-01).

Purpose: Prevent the duplicate-instance breakage a consumer hits when litkit ships its own `@tanstack/*` copy (Pitfall 6), and prevent tree-shaking from dropping the query/forms element registration — while keeping local build/typecheck/test green.
Output: query + forms package.json with TanStack cores as peers (kept as devDeps), precise `sideEffects` allowlists, and a lower-`any` `engine.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-typecheck-hardening/01-CONTEXT.md
@.claude/CLAUDE.md
@packages/query/package.json
@packages/forms/package.json
@packages/forms/src/internal/engine.ts
</context>

## Artifacts this phase produces (this plan)

- `packages/query/package.json` — `@tanstack/query-core` moved from `dependencies` to `peerDependencies` (`^5.0.0`), added to `devDependencies` (pinned `^5.91.0`); `sideEffects` changed from `false` to `["dist/query.js"]`.
- `packages/forms/package.json` — `@tanstack/form-core` moved from `dependencies` to `peerDependencies` (`^1.0.0`), added to `devDependencies` (pinned `^1.28.5`); `sideEffects` changed from `false` to `["dist/forms.js"]`.
- `packages/forms/src/internal/engine.ts` — recoverable `any` reduced (D-04); no exported/public type or API signature changed.

<tasks>

<task type="auto">
  <name>Task 1: TanStack cores → required peerDependencies (query + forms)</name>
  <files>packages/query/package.json, packages/forms/package.json</files>
  <read_first>
    - packages/query/package.json (`@tanstack/query-core` currently under `dependencies: ^5.91.0`)
    - packages/forms/package.json (`@tanstack/form-core` currently under `dependencies: ^1.28.5`; note existing `peerDependenciesMeta.zod.optional`)
    - .planning/research/PITFALLS.md section "Pitfall 6" (duplicate-instance breakage rationale)
    - .planning/phases/01-build-typecheck-hardening/01-CONTEXT.md decisions D-02 and D-02a (broad required range; keep as devDeps; do NOT mark optional; confirm no form-core API shifted within ^1.x)
  </read_first>
  <action>
    Per D-02, move `@tanstack/query-core` out of `dependencies` in packages/query/package.json and declare it under `peerDependencies` with range `^5.0.0`; also add it to `devDependencies` pinned at `^5.91.0` so local build/typecheck/test resolve it. Do the same in packages/forms/package.json for `@tanstack/form-core`: remove from `dependencies`, add to `peerDependencies` at `^1.0.0`, add to `devDependencies` at `^1.28.5`. These are REQUIRED peers — do NOT add a `peerDependenciesMeta.optional` entry for either core (leave the existing forms `peerDependenciesMeta.zod.optional` untouched). Leave `lit` peer/dev entries as-is. After editing, run `npm install` from the repo root to refresh the lockfile, then confirm both packages still typecheck against the devDep-resolved cores. Per D-02a, keep the broad `^1.0.0` form-core range: the codebase already builds and typechecks green against 1.28.5, so no incompatibility warrants tightening to `^1.28.0`; record that finding in the SUMMARY.
  </action>
  <verify>
    <automated>npm install && node -e "const q=require('./packages/query/package.json');const f=require('./packages/forms/package.json');if(q.dependencies&&q.dependencies['@tanstack/query-core'])throw new Error('query-core still a dependency');if(q.peerDependencies['@tanstack/query-core']!=='^5.0.0')throw new Error('query peer range wrong');if(!q.devDependencies['@tanstack/query-core'])throw new Error('query-core missing from devDependencies');if(f.dependencies&&f.dependencies['@tanstack/form-core'])throw new Error('form-core still a dependency');if(f.peerDependencies['@tanstack/form-core']!=='^1.0.0')throw new Error('forms peer range wrong');if(!f.devDependencies['@tanstack/form-core'])throw new Error('form-core missing from devDependencies');if(f.peerDependenciesMeta&&f.peerDependenciesMeta['@tanstack/form-core'])throw new Error('form-core must not be optional');console.log('OK')" && npm run typecheck -w @willram/query && npm run typecheck -w @willram/forms</automated>
  </verify>
  <acceptance_criteria>
    - `packages/query/package.json`: `@tanstack/query-core` present under `peerDependencies` as `^5.0.0` and under `devDependencies`, absent from `dependencies`.
    - `packages/forms/package.json`: `@tanstack/form-core` present under `peerDependencies` as `^1.0.0` and under `devDependencies`, absent from `dependencies`; no `peerDependenciesMeta` entry for `@tanstack/form-core`.
    - `npm run typecheck -w @willram/query` and `npm run typecheck -w @willram/forms` both exit 0 (cores resolve via devDeps).
    - `npm install` completes without an unmet-peer error that breaks the workspace install.
  </acceptance_criteria>
  <done>Both TanStack cores are required peer dependencies with broad ranges, retained as devDeps, and both packages still typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: sideEffects allowlist for the query + forms element entries</name>
  <files>packages/query/package.json, packages/forms/package.json</files>
  <read_first>
    - packages/query/src/query-client-provider.ts (`@customElement('lit-query-client-provider')` — registers at import time; bundled into the single `dist/query.js` entry)
    - packages/query/src/index.ts (re-exports `LitQueryClientProvider`, so the registration is part of the main entry)
    - packages/forms/src/lit-form.ts (`@customElement` for `<lit-form>`; bundled into `dist/forms.js`)
    - packages/forms/src/index.ts (re-exports `LitForm`; note `./zod` is a separate pure entry `dist/zod.js`)
    - .planning/phases/01-build-typecheck-hardening/01-CONTEXT.md decision D-03 / D-03a (precise allowlist of BUILT entry files; confirmed dist chunk shape: query = single `dist/query.js`, forms = `dist/forms.js` + pure `dist/zod.js`)
  </read_first>
  <action>
    Per D-03/D-03a, replace `"sideEffects": false` with a precise allowlist of the built entries that carry `customElements.define` registrations. In packages/query/package.json set `sideEffects` to `["dist/query.js"]` — query is a single-bundle package whose one entry contains the `lit-query-client-provider` registration, so the whole entry must be treated as side-effectful. In packages/forms/package.json set `sideEffects` to `["dist/forms.js"]` — this is the entry that bundles the `<lit-form>` registration; do NOT list `dist/zod.js`, which is a pure validators entry that must stay tree-shakeable. Use the built `dist/*.js` filenames confirmed from the actual Vite output, not `src` paths. After editing, rebuild both packages and confirm the element tag string is present in the allowlisted entry.
  </action>
  <verify>
    <automated>npm run build -w @willram/query && npm run build -w @willram/forms && node -e "const q=require('./packages/query/package.json');const f=require('./packages/forms/package.json');if(JSON.stringify(q.sideEffects)!=='[\"dist/query.js\"]')throw new Error('query sideEffects wrong');if(JSON.stringify(f.sideEffects)!=='[\"dist/forms.js\"]')throw new Error('forms sideEffects wrong');const fs=require('fs');if(!fs.readFileSync('packages/query/dist/query.js','utf8').includes('lit-query-client-provider'))throw new Error('query registration missing from allowlisted entry');if(!fs.readFileSync('packages/forms/dist/forms.js','utf8').includes('lit-form'))throw new Error('forms registration missing from allowlisted entry');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/query/package.json` `sideEffects` equals `["dist/query.js"]`.
    - `packages/forms/package.json` `sideEffects` equals `["dist/forms.js"]`.
    - `npm run build -w @willram/query` and `-w @willram/forms` exit 0 and emit `dist/query.js` / `dist/forms.js`.
    - `grep -q 'lit-query-client-provider' packages/query/dist/query.js` and `grep -q 'lit-form' packages/forms/dist/forms.js` both succeed.
  </acceptance_criteria>
  <done>query and forms allowlist exactly their element-carrying built entries; the forms zod entry stays tree-shakeable; both builds are green and the registrations live in the allowlisted files.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Bounded internal-`any` reduction in forms engine (D-04)</name>
  <files>packages/forms/src/internal/engine.ts</files>
  <read_first>
    - packages/forms/src/internal/engine.ts (the `FormApi`/`FieldApi` generic `any` args and the `as any` casts at the field/form boundary)
    - packages/forms/src/index.ts and packages/forms/src/form-controller.ts (to confirm which types are PUBLIC/exported — these signatures must NOT change)
    - .planning/codebase/CONCERNS.md (documents the intentional `engine.ts` adapter coupling and which `any`s are load-bearing)
    - .planning/phases/01-build-typecheck-hardening/01-CONTEXT.md decision D-04 (bounded: reduce where a real type is recoverable; no architecture refactor; no public/exported type change; documented `any` acceptable where `erasableSyntaxOnly` generic-expansion blocks a precise type)
  </read_first>
  <action>
    Reduce `any` in packages/forms/src/internal/engine.ts only where a real type is recoverable without changing behavior or any exported signature (D-04, hardening-only). Concrete targets: replace the `({ value }: { value: any })` validator-callback parameter shapes with the form's value type where it is in scope; type the `setMeta((prev: any) => ...)` callback parameter with the field-meta type instead of `any`; type the `_ensureField`/field-map values against the recoverable field type rather than a wall of `any`. Leave the TanStack `FormApi<T, any, any, ...>` / `FieldApi<...>` positional generic slots as `any` where TanStack's 12-to-23-parameter generic signatures cannot be expressed under `erasableSyntaxOnly` without contortion — add a single one-line comment at each such retained `any` noting it is a TanStack generic-arity limit (do not attempt to reconstruct those generics). Do NOT touch the JSON deep-clone helper or the single-caller `subscribe` (explicitly deferred). Stay within `erasableSyntaxOnly`: explicit class fields only, no constructor parameter properties, no enums/namespaces. Every exported type and public API signature in forms must be byte-identical after this change — verified by the emitted public `.d.ts` diffing empty.
  </action>
  <verify>
    <automated>npm run typecheck -w @willram/forms && npm run build -w @willram/forms && npm run test -w @willram/forms</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck -w @willram/forms` exits 0.
    - `npm run test -w @willram/forms` exits 0 (behavior unchanged).
    - The count of `any` tokens in `packages/forms/src/internal/engine.ts` (via `grep -o` for `\bany\b` excluding comment lines with `grep -v '^\s*//'`) is strictly lower than the pre-change count recorded in the SUMMARY.
    - Public surface unchanged: `git diff -- packages/forms/dist/index.d.ts packages/forms/dist/form-controller.d.ts` after rebuild shows no change to any exported signature (only internal `.d.ts` may differ).
  </acceptance_criteria>
  <done>Recoverable `any` in engine.ts is typed precisely; unavoidable TanStack-generic `any`s are documented one-liners; forms still typechecks, builds, and tests green with no public-type change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| litkit `@tanstack/*` copy → consumer's `@tanstack/*` copy | Live `QueryClient`/form instances cross the boundary by identity; two installed copies break `instanceof`/context/cache sharing |
| litkit `sideEffects` field → consumer bundler | Consumer tree-shaking decision for `customElements.define` in query/forms entries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering | `@tanstack/query-core` / `@tanstack/form-core` declared as `dependencies` | high | mitigate | Move both to required `peerDependencies` (broad ranges) so the consumer owns the single instance; keep as devDeps for local resolution (D-02) |
| T-01-04 | Tampering | query/forms `sideEffects: false` | high | mitigate | Allowlist `dist/query.js` / `dist/forms.js` so the provider/`lit-form` registration survives a consumer prod build; grep-verify the tag is in the allowlisted entry |
| T-01-SC | Tampering | package installs | low | accept | No new packages introduced — the TanStack cores were already present and vetted; this plan only re-classifies them (dependencies → peer+dev). No package-legitimacy gate required. |
</threat_model>

<verification>
- `npm run typecheck -w @willram/query` and `-w @willram/forms` exit 0.
- `npm run build -w @willram/query` and `-w @willram/forms` exit 0 with dist emitted.
- `npm run test -w @willram/forms` exits 0.
- package.json assertions (peer/dev classification, sideEffects arrays) pass as scripted in the task verifies.
</verification>

<success_criteria>
- TanStack cores are required peers (kept as devDeps), matching how `lit` is already handled (BUILD-04/D-02).
- query and forms allowlist exactly their element-carrying built entries; forms zod stays tree-shakeable (BUILD-03/D-03).
- engine.ts internal `any` is reduced within the bounded D-04 scope with no public-type change (BUILD-01/D-04).
</success_criteria>

<output>
Create `.planning/phases/01-build-typecheck-hardening/01-02-SUMMARY.md` when done.
</output>
