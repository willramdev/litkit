---
phase: 12-dependency-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/dependabot.yml
autonomous: true
requirements: [DEPS-01, DEPS-02]
user_setup:
  - service: github-dependabot
    why: "Dependabot version updates activate on detection of .github/dependabot.yml; if the repo has org-level Dependabot disabled, a maintainer must enable it (Settings -> Code security and analysis -> Dependabot version updates). No secret or token is required."
estimate:
  tokens: 18000
  raw_tokens: 12000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "`.github/dependabot.yml` is a valid Dependabot v2 config (`version: 2`) with exactly two `updates:` entries — one `package-ecosystem: \"npm\"` and one `package-ecosystem: \"github-actions\"`, both `directory: \"/\"` and both `schedule.interval: \"weekly\"` (DEPS-01, D-06)"
    - "Each ecosystem groups minor+patch into one weekly PR via a `groups` block whose `update-types` is limited to `[\"minor\",\"patch\"]`, so a major-version bump never rides with safe bumps — majors open as their own separate PR (DEPS-01, D-05; resolves the DEPS-02 adjacency edge: minor/patch touch and merge, majors separate)"
    - "The npm entry's `ignore` block is non-empty and lists both `lit` and `@tanstack/*` with no `versions`/`update-types` narrowing, so Dependabot can never open a PR that narrows an externalized peer range (DEPS-02, D-07; resolves the DEPS-02 empty edge: the ignore block is non-empty)"
    - "No PR-automation is configured anywhere in `.github` — every Dependabot PR (npm or github-actions, grouped or major, including a future `changesets/action` SHA bump) is surfaced for manual human review and merge (DEPS-02, D-08/D-09)"
  artifacts:
    - ".github/dependabot.yml (new — the DEPS-01/DEPS-02 deliverable)"
  key_links:
    - "Dependabot `github-actions` ecosystem entry -> the `changesets/action` SHA-pin in release.yml: the updater bumps the pinned SHA plus its trailing `# vX.Y.Z` comment and opens an ordinary reviewable PR, never auto-merged (D-08)"
    - "npm `ignore` globs -> the v1.0 externalization peer-range contract (`lit`, `@tanstack/*`): the ignore is the mechanism that makes a range-narrowing bot PR impossible (D-07)"
  prohibitions:
    - "MUST NOT configure any PR-automation (no automated-merge action, no auto-approval step, no merge-on-green CLI) — every bump is human-reviewed (D-09)"
    - "MUST NOT omit or narrow the `lit` / `@tanstack/*` ignore globs — the ignore must stay wide (all updates for those names) so no range-narrowing PR can be proposed (D-07)"
    - "MUST NOT include majors in any group — each `groups` block's `update-types` must be limited to minor+patch so majors split into their own PRs (D-05)"
    - "MUST NOT add any new npm dependency or new third-party marketplace action — this phase adds zero supply-chain surface (D-01)"
    - "MUST NOT write the hyphenated automated-merge term in any comment in the file (it would break the repo-wide `.github` negative-grep gate); phrase review comments as 'manually reviewed / no automatic merging'"
  assumptions:
    - "FLAGGED (DEPS-02 empty/null-input edge): N/A at runtime — `dependabot.yml` is static declarative config with no runtime input; the checkable interpretation is authored as a truth (the `ignore` block is non-empty and lists both peers)."
    - "FLAGGED (DEPS-02 ordering/stable-sort edge): N/A — the intra-group ordering of bumped dependencies inside a grouped PR is Dependabot-service-managed, not a litkit config surface; the defensible adjacency assertion (majors never absorbed into the minor/patch group) is authored as a truth."
---

<objective>
Create `.github/dependabot.yml` (new file) so dependency and GitHub-action updates arrive as grouped, safe, weekly, human-reviewed PRs — and so the `lit`/`@tanstack/*` externalized peer ranges can never be narrowed by a bot.

Purpose: Deliver DEPS-01 (grouped weekly Dependabot for npm + github-actions) and the DEPS-02 safety policy (ignore the externalized peers; surface `changesets/action` SHA bumps for manual review, never auto-merged). This is the config-only deliverable that hardens the supply chain without adding any surface.
Output: `.github/dependabot.yml` with two grouped weekly `updates:` entries.

Note (config-only phase — tracer degenerates): There is no layered architecture to prove end-to-end here; the phase touches only declarative `.github/` config. Per TRACER_MODE guidance, artificial slicing is not forced — this plan and its Wave-1 sibling (`12-02`) are two zero-overlap parallel plans, not a tracer + expansion pair. Flagged as a deliberate planning decision.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-dependency-hygiene/12-CONTEXT.md
@.planning/phases/12-dependency-hygiene/12-RESEARCH.md
@.planning/phases/12-dependency-hygiene/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/dependabot.yml — two grouped weekly updates entries (npm + github-actions)</name>
  <files>.github/dependabot.yml</files>
  <read_first>
    - .planning/phases/12-dependency-hygiene/12-RESEARCH.md — §Code Examples "Complete .github/dependabot.yml skeleton" (the v2 schema template) and §Architecture Patterns Pattern 1 (group minor+patch, split majors) + Pattern 2 (ignore peer-range bumps by glob)
    - .planning/phases/12-dependency-hygiene/12-PATTERNS.md — §".github/dependabot.yml (config, NEW — DEPS-01/02)" (structure to write; the release.yml:32 SHA-pin convention D-08 preserves)
    - .planning/phases/12-dependency-hygiene/12-CONTEXT.md — D-05 (group minor+patch, majors split), D-06 (weekly), D-07 (ignore lit/@tanstack), D-08 (changesets surfaced for manual review), D-09 (no auto-merge anywhere), and "Claude's Discretion" (labels, limit, commit-message prefix, schedule.day/time)
    - .github/workflows/release.yml — the SHA-pinned `changesets/action@198f833dd7d863100ea6e28967bc9a9fdefadb0a  # v2.1.0` (line 32): the github-actions updater bumps exactly this SHA + comment into a reviewable PR
  </read_first>
  <action>
    Create the new file `.github/dependabot.yml`. It does not exist yet — this is a net-new file, use Write.

    Top line `version: 2`, then an `updates:` list with exactly two entries.

    ENTRY 1 — npm ecosystem: `package-ecosystem: "npm"`, `directory: "/"` (the root workspace lockfile covers the whole `packages/*` + `examples` tree), `schedule:` with `interval: "weekly"` (D-06). Add a `groups:` block with one named group (e.g. `npm-minor-patch`) whose `update-types:` lists only `minor` and `patch` — this is D-05: routine bumps batch into one weekly PR while any major falls out of the group and opens standalone. Add an `ignore:` block (D-07) with two entries: `dependency-name: "lit"` and `dependency-name: "@tanstack/*"` (glob). Do NOT add `versions:` or `update-types:` under those ignore entries — omitting them ignores ALL updates for those names, which is the wide guard D-07 wants so no range-narrowing PR is ever proposed. Cosmetic discretion (RESEARCH A4): set `open-pull-requests-limit` (e.g. 10), a `commit-message.prefix` (e.g. "deps"), and `labels` (e.g. "dependencies") if desired.

    ENTRY 2 — github-actions ecosystem: `package-ecosystem: "github-actions"`, `directory: "/"` (Dependabot always scans `.github/workflows/` regardless of the directory value; `/` is conventional), `schedule.interval: "weekly"` (D-06), and a `groups:` block (e.g. `actions-minor-patch`) with `update-types:` limited to `minor` and `patch` (D-05). This entry is what will later open the `changesets/action` SHA-bump PR (D-08) — no extra config is needed for that; a major SHA bump simply opens standalone. Cosmetic discretion: `commit-message.prefix` (e.g. "ci") and `labels`.

    Add NO PR-automation of any kind (no automated-merge configuration, no auto-approval, no merge-on-green): D-09 is uniform manual review. If you write explanatory YAML comments, phrase them as "manually reviewed / no automatic merging" — do NOT write the hyphenated automated-merge term, because a repo-wide negative-grep gate over `.github` asserts zero occurrences of it.

    Do NOT add any new npm dependency or third-party marketplace action (D-01) — this file is pure GitHub-native config.
  </action>
  <verify>
    <automated>npx --yes yaml-lint .github/dependabot.yml</automated>
    <automated>grep -c 'package-ecosystem: "npm"' .github/dependabot.yml && grep -c 'package-ecosystem: "github-actions"' .github/dependabot.yml</automated>
    <automated>grep -q 'dependency-name: "lit"' .github/dependabot.yml && grep -q 'dependency-name: "@tanstack/\*"' .github/dependabot.yml && echo IGNORE_OK</automated>
    <automated>grep -R "auto-merge\|gh pr merge --auto" .github; test $? -ne 0 && echo NO_AUTOMERGE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.github/dependabot.yml` exists and `npx --yes yaml-lint .github/dependabot.yml` exits 0.
    - The file contains `version: 2` and exactly two `package-ecosystem:` values: `"npm"` and `"github-actions"`, each with `schedule:` `interval: "weekly"`.
    - Both ecosystems have a `groups` block whose `update-types` is exactly the two values `minor` and `patch` (no `major`) — a `grep -A3 'update-types' .github/dependabot.yml` shows only minor/patch under each group.
    - The npm entry has an `ignore` block that lists both `dependency-name: "lit"` and `dependency-name: "@tanstack/*"`, and neither ignore entry adds a `versions:` or `update-types:` narrowing key.
    - `grep -R "auto-merge\|gh pr merge --auto" .github` returns 0 matches (no PR-automation anywhere, and no comment reintroduces the hyphenated term).
    - The file adds no npm dependency and no `uses:` marketplace action (it is Dependabot config, not a workflow).
  </acceptance_criteria>
  <done>`.github/dependabot.yml` is valid Dependabot v2 config: two grouped weekly entries (npm + github-actions), majors split out of both groups, npm ignoring `lit` and `@tanstack/*` (all updates), and zero PR-automation — DEPS-01 and the DEPS-02 safety policy are satisfied at the config level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dependabot service -> repo | Bot-authored update PRs cross into the repo; they must be reviewable, never auto-applied |
| upstream registry (npm / GitHub Actions) -> dependency graph | New upstream versions enter via Dependabot proposals |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering / Elevation | `changesets/action` version (github-actions ecosystem) | high | mitigate | Dependabot surfaces the SHA bump as an ordinary reviewable PR; the action stays SHA-pinned in release.yml and every bump gets manual human review (D-08/D-09) |
| T-12-02 | Tampering | `lit` / `@tanstack/*` peer ranges (npm ecosystem) | medium | mitigate | npm `ignore` globs on `lit` and `@tanstack/*` make a range-narrowing bot PR impossible (D-07) |
| T-12-04 | Tampering | any Dependabot PR (auto-merge path) | high | mitigate | No PR-automation configured anywhere in `.github` — uniform manual review (D-09) |
| T-12-SC | Tampering | npm / actions installs | low | accept | Config-only plan: zero package installs and zero new third-party actions added. No `[ASSUMED]`/`[SUS]` packages -> no package-legitimacy checkpoint required (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `npx --yes yaml-lint .github/dependabot.yml` exits 0 (schema/syntax).
- `grep` assertions above confirm: two ecosystems, weekly, groups limited to minor+patch, ignore lists both peers, and zero auto-merge automation in `.github`.
- Post-merge (observational, non-blocking — GitHub-native, cannot run in PR CI): after the first weekly Dependabot cycle, the repo's Dependabot PR list shows grouped npm and github-actions PRs, and NO PR proposes a `lit` or `@tanstack/*` bump.
</verification>

<success_criteria>
- DEPS-01: `.github/dependabot.yml` configures npm + github-actions, grouped, weekly. MET when the two-entry file lints clean and both groups are minor+patch-only.
- DEPS-02: Dependabot ignores `lit`/`@tanstack/*` and surfaces `changesets/action` SHA bumps for manual review, never auto-merged. MET when the npm ignore lists both peers, the github-actions entry exists to open the SHA-bump PR, and no auto-merge automation exists anywhere in `.github`.
</success_criteria>

<output>
Create `.planning/phases/12-dependency-hygiene/12-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

This plan (12-01) creates:
- **`.github/dependabot.yml`** (new file) — two grouped weekly `updates:` entries (npm + github-actions), majors split out, npm ignoring `lit` and `@tanstack/*`, zero PR-automation.

The sibling plan (12-02) modifies the four workflow files, adds the `npm audit` CI step, and bumps the `@v5` action tags. See 12-02-PLAN.md for that artifact list.

No new symbols, decorators, classes, or CLI flags are produced anywhere in this phase — it is config-only (`.github/` YAML). Stated explicitly per the downstream-consumer contract.
