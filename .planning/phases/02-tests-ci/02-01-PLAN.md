---
phase: 02-tests-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test-setup.ts
  - packages/kit/vite.config.ts
  - packages/kit/src/controllers/media-query.test.ts
  - packages/kit/src/controllers/resize-observer.test.ts
  - packages/kit/src/controllers/intersection-observer.test.ts
  - packages/kit/src/kit-element.test.ts
autonomous: true
requirements: [TEST-01, TEST-02]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "test-setup.ts installs inert ResizeObserver, IntersectionObserver, and guarded matchMedia stubs on the global scope before any package test runs"
    - "kit media-query, resize-observer, and intersection-observer controllers construct, register on the host, observe/subscribe, and disconnect against the jsdom mocks without throwing (npm run test -w @willram/kit is green)"
    - "packages/kit/vite.config.ts test block references setupFiles ['../../test-setup.ts']"
    - "KitElement base class exercises use()/controller registration and emit() event dispatch in a jsdom-registered test element (kit-element.test.ts passes)"
  artifacts:
    - test-setup.ts
    - packages/kit/src/controllers/media-query.test.ts
    - packages/kit/src/controllers/resize-observer.test.ts
    - packages/kit/src/controllers/intersection-observer.test.ts
    - packages/kit/src/kit-element.test.ts
  key_links:
    - "packages/kit/vite.config.ts test.setupFiles → root test-setup.ts (relative ../../ path)"
    - "kit controller constructors → global ResizeObserver/IntersectionObserver/matchMedia provided by test-setup.ts"
  prohibitions:
    - "test-setup.ts mocks are inert — tests MUST NOT assert that ResizeObserver/IntersectionObserver callbacks fire via the global stub; capture a constructed instance with a per-test spy to assert callback behavior"
    - "the matchMedia stub assignment MUST be guarded (if (!window.matchMedia)) so a non-jsdom (node) package does not crash on the shared setup"
    - "kit test helpers MUST use explicit class fields, never constructor parameter properties (erasableSyntaxOnly)"
    - "@willram/kit tests MUST NOT import from sibling @willram/* packages"
---

<objective>
Create the shared jsdom mock infrastructure and prove it end-to-end by exercising the kit browser controllers CONCERNS.md flags untested. This plan is the phase tracer: it wires one global mock → setupFiles → a controller test that constructs against the mock → a green suite, proving the whole test-infra layer before the other packages expand on it.

Purpose: TEST-02 requires jsdom to mock ResizeObserver/IntersectionObserver/matchMedia so the kit controllers are actually exercised (D-03: one shared root `test-setup.ts` referenced via setupFiles in each package); TEST-01 requires named critical-path kit suites (D-01: named suites PLUS the HIGH-priority CONCERNS gaps — factories/emit/decorators already exist; the browser controllers + kit-element are the net-new gap).
Output: Root `test-setup.ts`, kit `vite.config.ts` setupFiles wiring, and four kit test files, all green under `npm run test -w @willram/kit`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tests-ci/02-CONTEXT.md
@.planning/phases/02-tests-ci/02-RESEARCH.md
@.planning/phases/02-tests-ci/02-PATTERNS.md
@packages/kit/src/controllers/media-query.ts
@packages/kit/src/controllers/resize-observer.ts
@packages/kit/src/controllers/intersection-observer.ts
@packages/kit/src/controllers/click-outside.test.ts
@packages/kit/src/controllers/listen.test.ts
@packages/kit/vite.config.ts
</context>

## Artifacts this phase produces (Plan 01 scope)

- `test-setup.ts` (repo root) — NEW. Inert `MockResizeObserver`, `MockIntersectionObserver`, guarded `window.matchMedia` stubs assigned to `globalThis`. Consumed by every package's `test.setupFiles` (this plan wires kit; Plans 02/03/04 wire the rest).
- `packages/kit/vite.config.ts` — EDIT. Add `setupFiles: ['../../test-setup.ts']` to the existing `test` block.
- `packages/kit/src/controllers/media-query.test.ts` — NEW test file.
- `packages/kit/src/controllers/resize-observer.test.ts` — NEW test file.
- `packages/kit/src/controllers/intersection-observer.test.ts` — NEW test file.
- `packages/kit/src/kit-element.test.ts` — NEW test file.

No new dependencies are added in this plan (vitest is already installed). Tooling installs happen in Plan 04.

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — shared jsdom mocks wired into kit, proven by the media-query suite</name>
  <files>test-setup.ts, packages/kit/vite.config.ts, packages/kit/src/controllers/media-query.test.ts</files>
  <read_first>
    - .planning/phases/02-tests-ci/02-RESEARCH.md lines 143-179 (Pattern 1: shared jsdom mocks + inert-stub note) and lines 255-259 (Pitfall 5: store has no jsdom env — guard matchMedia)
    - .planning/phases/02-tests-ci/02-PATTERNS.md lines 60-86 (media-query.test analog) and 184-198 (jsdom global stubs + vite.config test block edit rule)
    - packages/kit/src/controllers/media-query.ts (source under test — matchMedia called in the constructor, change listener added in hostConnected, removed in hostDisconnected)
    - packages/kit/src/controllers/listen.test.ts (plain-object mock host idiom + addController assertion)
    - packages/kit/vite.config.ts (current test block to edit)
  </read_first>
  <action>
    Create root `test-setup.ts` with inert stub classes `MockResizeObserver` and `MockIntersectionObserver` (explicit class fields only, no constructor parameter properties) exposing no-op `observe`/`unobserve`/`disconnect` (and `takeRecords` returning `[]` plus `root`/`rootMargin`/`thresholds` fields for IntersectionObserver). Assign both to `globalThis.ResizeObserver`/`globalThis.IntersectionObserver` via `as unknown as` casts. Install a `window.matchMedia` stub returning `{ matches: false, media: query, onchange: null, addEventListener, removeEventListener, addListener, removeListener, dispatchEvent }` ONLY inside an `if (!window.matchMedia)` guard so a node-environment package importing this setup does not crash. Shape reference: RESEARCH lines 147-171.
    Edit `packages/kit/vite.config.ts` to add `setupFiles: ['../../test-setup.ts']` to the existing `test: { environment: 'jsdom' }` block.
    Write `packages/kit/src/controllers/media-query.test.ts` following the listen.test.ts plain-object mock-host idiom: construct via the `mediaQuery(query)` factory, invoke `factory(host)`, assert the returned controller calls `host.addController(ctrl)`, assert `.matches` reflects the stub default (`false`), drive `hostConnected()`/`hostDisconnected()` and assert change-listener add/remove wiring occurs without throwing. Do not assert a media-query change callback fires from the global stub.
    Run `npm run test -w @willram/kit` and confirm the new media-query suite passes alongside all existing kit suites.
  </action>
  <verify>
    <automated>npm run test -w @willram/kit</automated>
  </verify>
  <acceptance_criteria>
    - `test-setup.ts` exists at repo root and `grep -c "if (!window.matchMedia)" test-setup.ts` returns >= 1 (guarded matchMedia).
    - `grep -c "setupFiles" packages/kit/vite.config.ts` returns >= 1 and the value contains `../../test-setup.ts`.
    - `test-setup.ts` contains no constructor parameter properties: `grep -nE "constructor\\([^)]*(private|public|readonly|protected)" test-setup.ts` returns nothing.
    - `npm run test -w @willram/kit` exits 0 with `media-query` in the passing test output.
  </acceptance_criteria>
  <done>Shared inert mocks exist, kit is wired to them via setupFiles, and the media-query controller suite proves a controller constructs/observes/disconnects against the mock — the test-infra layer works end-to-end.</done>
  <reversibility rating="reversible">Adding a setup file, a config line, and a test file are all additive and trivially removable.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand kit browser-controller + KitElement suites</name>
  <files>packages/kit/src/controllers/resize-observer.test.ts, packages/kit/src/controllers/intersection-observer.test.ts, packages/kit/src/kit-element.test.ts</files>
  <read_first>
    - .planning/phases/02-tests-ci/02-PATTERNS.md lines 31-56 (resize/intersection analog: real-DOM mock host) and 88-92 (kit-element analog)
    - packages/kit/src/controllers/click-outside.test.ts (real-DOM mock host variant: document.createElement + appendChild, .remove() cleanup)
    - packages/kit/src/controllers/resize-observer.ts and packages/kit/src/controllers/intersection-observer.ts (sources under test)
    - packages/kit/src/kit-element.ts, packages/kit/src/define.ts, packages/kit/src/emit.test.ts (KitElement use()/emit()/props() surface + idempotent define guard)
  </read_first>
  <behavior>
    - resize-observer: factory returns a function; calling it registers the controller (host.addController called) and observes the host element; hostDisconnected calls observer.disconnect() with no throw.
    - intersection-observer: same lifecycle contract against the inert IntersectionObserver stub.
    - kit-element: registering a KitElement subclass (via the define.ts idempotent guard) and attaching to document.body allows use() controller registration and emit() to dispatch a CustomEvent that a listener receives.
  </behavior>
  <action>
    Write `resize-observer.test.ts` and `intersection-observer.test.ts` using the click-outside.test.ts real-DOM mock-host variant (a controller that observes a node needs a real element). Assert construction/lifecycle only — the global stubs are inert; to assert any callback behavior, spy on the constructed observer instance rather than driving the global stub.
    Write `kit-element.test.ts` exercising the KitElement base class: register a minimal subclass through the `define.ts` idempotent guard, append an instance to `document.body`, assert `use()` registers a controller and `emit(name, detail)` dispatches a `CustomEvent` captured by an `addEventListener` spy; clean up with `.remove()`. Cross-reference emit.test.ts for the event-dispatch idiom.
    Run `npm run test -w @willram/kit` and confirm all four new files pass.
  </action>
  <verify>
    <automated>npm run test -w @willram/kit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -w @willram/kit` exits 0 with `resize-observer`, `intersection-observer`, and `kit-element` present in the passing output.
    - No test asserts a ResizeObserver/IntersectionObserver callback fired via the global stub: `grep -rnE "stub.*callback|globalThis.(Resize|Intersection)Observer.*=>.*expect" packages/kit/src/controllers/resize-observer.test.ts packages/kit/src/controllers/intersection-observer.test.ts` returns nothing.
    - `grep -nE "constructor\\([^)]*(private|public|readonly|protected)" packages/kit/src/controllers/resize-observer.test.ts packages/kit/src/controllers/intersection-observer.test.ts packages/kit/src/kit-element.test.ts` returns nothing.
  </acceptance_criteria>
  <done>kit's HIGH-priority CONCERNS controller gaps (resize/intersection/kit-element) have named passing suites; TEST-01 kit bar and TEST-02 exercise both satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runtime → global scope | `test-setup.ts` mutates `globalThis`/`window` before tests run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | test-setup.ts global stubs | low | accept | Stubs are inert no-ops scoped to the vitest run; guarded matchMedia prevents node-env crashes; no production code path touches them |
| T-02-02 | Denial of Service | inert stub asserted to fire callbacks | low | mitigate | Prohibition: assert lifecycle only, spy on instances for callbacks (avoids hangs/false passes) |

Supply-chain threats (new dev-dependency installs, CI token/permissions) are enumerated and mitigated in Plan 04, which owns all tooling installs and the workflow; this plan adds no dependencies.
</threat_model>

<verification>
- `npm run test -w @willram/kit` green (all existing + four new suites).
- `test-setup.ts` present with guarded matchMedia and no ctor param properties.
- kit `vite.config.ts` setupFiles points at `../../test-setup.ts`.
</verification>

<success_criteria>
- TEST-02: kit browser controllers construct/observe/disconnect against jsdom mocks (media-query, resize-observer, intersection-observer suites green).
- TEST-01 (kit): factories/emit/decorators (existing) + browser controllers + kit-element all covered and passing.
- The shared `test-setup.ts` exists for Plans 02/03/04 to wire into their packages.
</success_criteria>

<output>
Create `.planning/phases/02-tests-ci/02-01-SUMMARY.md` when done.
</output>
