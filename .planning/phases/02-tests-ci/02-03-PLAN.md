---
phase: 02-tests-ci
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - packages/router/vite.config.ts
  - packages/router/src/test/matcher.test.ts
  - packages/router/src/router-lit/link.ts
  - packages/router/src/test/link.test.ts
autonomous: true
requirements: [TEST-01, TEST-02]
estimate:
  tokens: 56000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "router-core matcher .test()/.exec() suite passes for static, root, param, wildcard, and catch-all routes (npm run test -w @willram/router is green)"
    - "router matcher resolves overlapping/adjacent route patterns to a single deterministic match by declaration precedence"
    - "router matcher handles the root path '/' and unknown/empty paths (returns no match) without throwing"
    - "the link() directive removes the previous element's click listener when the directive moves to a new element, and does not accumulate duplicate router subscriptions across disconnect→reconnect (D-02 regression tests pass)"
    - "packages/router/vite.config.ts test block references setupFiles ['../../test-setup.ts']"
  artifacts:
    - packages/router/src/test/matcher.test.ts
    - packages/router/src/router-lit/link.ts
    - packages/router/src/test/link.test.ts
  key_links:
    - "link.ts update() removes the old element's click listener before re-pointing _element/_clickHandler"
    - "link.ts reconnected() guards re-subscription with !this._unsubscribe"
    - "packages/router/vite.config.ts test.setupFiles → root test-setup.ts"
  prohibitions:
    - "the link.ts edits are the two surgical patches only (element-listener leak fix + duplicate-subscription guard); do NOT refactor unrelated directive logic"
    - "router test helpers MUST use explicit class fields, never constructor parameter properties (erasableSyntaxOnly)"
    - "@willram/router source MUST NOT import from sibling @willram/* packages"
---

<objective>
Close the HIGH-priority router-core matcher gap with a named suite, and fold Phase 1's two parked `router-lit/link.ts` bug fixes into this phase so the fix and its regression tests land together (D-02).

Purpose: TEST-01 (D-01: named suites plus HIGH-priority CONCERNS gaps) requires named router suites (matcher/guards); `matcher.ts` (router-core) is the genuinely-untested sibling of the already-covered `compiled-matcher`. D-02 requires the two link.ts fixes plus regression tests so the bugs cannot silently return. TEST-02 (D-03) wires router into the shared root `test-setup.ts` via setupFiles.
Output: `matcher.test.ts`, two surgical `link.ts` patches, and two new `link.test.ts` regression cases — all green under `npm run test -w @willram/router`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tests-ci/02-CONTEXT.md
@.planning/phases/02-tests-ci/02-RESEARCH.md
@.planning/phases/02-tests-ci/02-PATTERNS.md
@packages/router/src/test/compiled-matcher.test.ts
@packages/router/src/router-core/matcher.ts
@packages/router/src/router-core/testing.ts
@packages/router/src/router-lit/link.ts
@packages/router/src/test/link.test.ts
@packages/router/vite.config.ts
</context>

## Artifacts this phase produces (Plan 03 scope)

- `packages/router/vite.config.ts` — EDIT. Add `setupFiles: ['../../test-setup.ts']` to the existing `test` block.
- `packages/router/src/test/matcher.test.ts` — NEW.
- `packages/router/src/router-lit/link.ts` — EDIT. Two surgical patches (D-02): remove old element's click listener before re-pointing in `update()`; guard `reconnected()` re-subscription with `!this._unsubscribe`.
- `packages/router/src/test/link.test.ts` — EXTEND. Two regression tests for the fixes above.

No new dependencies. The `@willram/router` source change (link.ts) requires a changeset for the CI `changeset status` gate — that changeset is authored in Plan 04.

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — router setupFiles wiring + router-core matcher suite green</name>
  <files>packages/router/vite.config.ts, packages/router/src/test/matcher.test.ts</files>
  <read_first>
    - packages/router/src/test/compiled-matcher.test.ts (exact analog: sibling router-core matcher, no DOM, nested describe per route category asserting .test() boolean and .exec() { params })
    - packages/router/src/router-core/matcher.ts (API under test — instantiate with a pattern string as compiled-matcher.test.ts does)
    - packages/router/src/router-core/testing.ts (createMockRouter/mockMatch/defineRoutes fixtures if route context is needed)
    - packages/router/vite.config.ts (current test block; note double-quote string style and describe/expect/it import ordering in router tests)
  </read_first>
  <action>
    Edit `packages/router/vite.config.ts` to add `setupFiles: ['../../test-setup.ts']` to the existing `test` block (preserve the existing `onConsoleLog` filter).
    Write `packages/router/src/test/matcher.test.ts` mirroring compiled-matcher.test.ts structure: nested `describe` per route category (static / root '/' / param / wildcard / catch-all). Assert `.test()` boolean and `.exec()` `{ params }` shape. Explicitly cover: the root path '/', an unknown/non-matching path (returns no match / false without throwing), a param route extracting params, and overlapping patterns resolving to a single deterministic match by declaration order (precedence). Use the double-quote + `describe, expect, it` router idiom.
    Run `npm run test -w @willram/router`.
  </action>
  <verify>
    <automated>npm run test -w @willram/router</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "setupFiles" packages/router/vite.config.ts` returns >= 1 and the value contains `../../test-setup.ts`.
    - `npm run test -w @willram/router` exits 0 with `matcher` present in passing output.
    - matcher.test.ts asserts the root path and a no-match path: `grep -nE "\"/\"|'/'|no ?match|toBe\\(false\\)|toBeNull|toBeUndefined" packages/router/src/test/matcher.test.ts` returns >= 1.
    - `grep -nE "constructor\\([^)]*(private|public|readonly|protected)" packages/router/src/test/matcher.test.ts` returns nothing.
  </acceptance_criteria>
  <done>Router is wired into the shared setup and the router-core matcher critical path (including empty/root/unknown + precedence edges) has a named passing suite.</done>
  <reversibility rating="reversible">Additive test + config line.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: link.ts bug fixes (D-02) + regression tests</name>
  <files>packages/router/src/router-lit/link.ts, packages/router/src/test/link.test.ts</files>
  <read_first>
    - .planning/phases/02-tests-ci/02-RESEARCH.md lines 267-332 (exact BEFORE/AFTER patches for both bugs + regression-test intent)
    - packages/router/src/router-lit/link.ts (current source: update() at lines 61-66, reconnected() at lines 108-119)
    - packages/router/src/test/link.test.ts (existing harness lines 1-46: lit render(html`<a ${link(...)}>`, container) + createMockRouter/mockMatch + real MouseEvent dispatch)
    - packages/router/src/router-core/testing.ts (createMockRouter subscription bookkeeping used to assert subscribe counts)
  </read_first>
  <behavior>
    - Bug (1) listener leak on directive move: after the directive re-points to a new anchor B, a click on the old anchor A no longer navigates (old listener removed).
    - Bug (2) duplicate subscription on disconnect→reconnect: reconnecting does not create a second router subscription without an intervening unsubscribe.
  </behavior>
  <action>
    Apply the two surgical patches to `link.ts` exactly as specified in RESEARCH lines 269-317: (1) in `update()`, before re-pointing `_element`/`_clickHandler`, remove the previous element's `click` listener when `_element` exists and differs from the new element; (2) in `reconnected()`, only re-subscribe when `!this._unsubscribe`. Do not alter any other directive logic. Keep the edits erasableSyntaxOnly-clean.
    Extend `link.test.ts` with two regression tests reusing the existing beforeEach/afterEach lit-render harness and createMockRouter/mockMatch: (1) render link on anchor A, re-render the same directive expression on anchor B, dispatch a real click on A, assert A no longer navigates (router navigation history unchanged); (2) drive disconnect then reconnect and assert router.subscribe was not called twice without an intervening unsubscribe, using the mock router's subscription bookkeeping.
    Run `npm run test -w @willram/router` and `npm run typecheck -w @willram/router`.
  </action>
  <verify>
    <automated>npm run test -w @willram/router &amp;&amp; npm run typecheck -w @willram/router</automated>
  </verify>
  <acceptance_criteria>
    - `link.ts` update() removes the prior listener before re-pointing: `grep -nE "removeEventListener\\(\"click\"" packages/router/src/router-lit/link.ts` returns >= 2 (existing _cleanup + new guard in update).
    - `link.ts` reconnected() guards re-subscription: `grep -nE "if \\(this._router && !this._unsubscribe\\)" packages/router/src/router-lit/link.ts` returns >= 1.
    - `npm run test -w @willram/router` exits 0; link.test.ts contains both new regression cases (grep for two new `it(`/`test(` blocks referencing move/reconnect).
    - `npm run typecheck -w @willram/router` exits 0.
  </acceptance_criteria>
  <done>Both link.ts bugs are fixed and locked in by regression tests that fail without the patch; D-02 satisfied.</done>
  <reversibility rating="reversible">Surgical source patch + tests; revertable independently.</reversibility>
</task>

</tasks>

<flagged_assumptions>
- RESEARCH A2 (MEDIUM): the exact mechanism to drive a Lit AsyncDirective's `disconnected()`/`reconnected()` from jsdom is unverified. The regression intent is unambiguous; the executor resolves the wiring via `createMockRouter`'s subscription bookkeeping and/or part-level manipulation (clearing then re-rendering the template). If direct lifecycle driving proves infeasible in jsdom, assert the subscription-count invariant through the mock router's bookkeeping instead of the DOM lifecycle.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runtime → global scope | router tests run under jsdom with shared test-setup.ts stubs loaded |
| directive → DOM element | link() attaches/removes click listeners on anchor elements (leak surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Denial of Service | link() event-listener/subscription leak (memory growth) | medium | mitigate | The two D-02 patches remove stale listeners and guard duplicate subscriptions; regression tests prevent reintroduction |
| T-02-08 | Tampering | matcher precedence ambiguity for overlapping routes | low | accept | Matcher suite asserts deterministic declaration-order precedence |

Supply-chain and CI token/permission threats are owned by Plan 04.
</threat_model>

<verification>
- `npm run test -w @willram/router` green (existing + matcher suite + two link regressions).
- `npm run typecheck -w @willram/router` green.
- router `vite.config.ts` setupFiles references `../../test-setup.ts`.
</verification>

<success_criteria>
- TEST-01 (router): matcher/guards named suites pass, including router-core matcher.
- D-02: both link.ts bugs fixed with regression tests landing together.
- TEST-02: router wired into the shared jsdom setup.
</success_criteria>

<output>
Create `.planning/phases/02-tests-ci/02-03-SUMMARY.md` when done.
</output>
