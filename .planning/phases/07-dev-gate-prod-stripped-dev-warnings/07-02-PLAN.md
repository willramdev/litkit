---
phase: 07-dev-gate-prod-stripped-dev-warnings
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - packages/router/src/internal/dev.ts
  - packages/router/src/define.ts
  - packages/router/src/define.test.ts
  - packages/router/scripts/build.js
  - packages/router/package.json
  - packages/router/src/router-core/routes.ts
  - packages/router/src/test/routes.test.ts
  - .changeset/dev-gate-router.md
autonomous: true
requirements: [WARN-01, WARN-02]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "router's dev-gate mechanism duplicates kit's exactly (D-03: NOT imported from kit — a separate, verbatim copy) — packages/router/src/internal/dev.ts has zero imports from @willramdev/kit or any file under packages/kit (verification: explicit)."
    - "esm-env is externalized in packages/router/scripts/build.js's shared external array (alongside 'lit', /^lit\\//) so router's three per-entry builds (router-core.js, router-lit.js, router.js) all leave the bare esm-env import unresolved (verification: explicit — each of the three dist files still contains the bare import after `npm run build -w @willramdev/router`)."
    - "router's define() warns exactly once, [litkit]-prefixed, on a tag COLLISION only (a different constructor) — identical collision-only semantics to kit's define() (verification: explicit — define.test.ts)."
    - "defineRoutes() warns once per route definition for each of: no path and no children; a duplicate route name; redirectTo set together with component/render — all framework-neutral (router-core has zero Lit imports, confirmed unchanged) and evaluated once at defineRoutes() config-load time, never per-navigation (verification: explicit — routes.test.ts)."
    - "Each router warning fires at most once per stable key even under repeated triggering of the same condition (module-level Set dedupe, D-06 — the same specless-edge-probe WARN-02 concurrency resolution proven in Plan 07-01, now proven on router's own sites; verification: explicit)."
    - "The Phase 6 type-SemVer shape gate stays green for all three router snapshots (router.d.ts, router-core.d.ts, router-lit.d.ts) — no public signature changed by this plan (verification: explicit — `npm run type-snapshot && git diff --exit-code` against the three router snapshot files)."
  artifacts:
    - packages/router/src/internal/dev.ts
    - packages/router/src/define.ts (modified)
    - packages/router/src/router-core/routes.ts (modified)
    - .changeset/dev-gate-router.md
  key_links:
    - "packages/router/src/define.ts -> ./internal/dev.ts (devWarnOnce) -> the same esm-env DEV mechanism as kit, duplicated per D-03 (a separate module-level dedupe Set, independent of kit's)"
    - "packages/router/src/router-core/routes.ts -> ../internal/dev.ts -> devWarnOnce (router-core stays Lit-free; internal/dev.ts has zero Lit imports so core may call it, the D-03 layering rule)"
    - "packages/router/scripts/build.js's shared external const feeds ALL THREE per-entry Vite builds -> esm-env stays unresolved in every router dist output"
  prohibitions:
    - "No dev-warning may fire more than once for the same stable condition/key — a repeated navigation or a repeated defineRoutes() call over the same route tree must never flood the console."
    - "Every dev-warning message uses the single [litkit] prefix, never a new per-site or per-package tag."
---

<objective>
Duplicate the Plan 07-01 tracer pattern into `@willramdev/router`'s foundational layer: router's own `internal/dev.ts` helper (a separate copy, not an import from kit), router's own duplicate-registration warning, `esm-env` externalized across all three of router's per-entry builds, and the framework-neutral invalid-route-config warnings in `router-core`. Plan 07-03 then fills the remaining four router-lit missing-router gaps that depend on this plan's `internal/dev.ts`.

Purpose: WARN-01 (the same dev-gate mechanism, proven to survive a SECOND package's build with a different per-entry build script than kit's) + WARN-02 (fill router's duplicate-registration gap and the invalid-route-config gap, both real silent gaps confirmed in RESEARCH's Silent-Gap Audit).
Output: `packages/router/src/internal/dev.ts`, a modified `packages/router/src/define.ts`, three new warn-once checks in `packages/router/src/router-core/routes.ts`, `packages/router/scripts/build.js` externalizing `esm-env`, `packages/router/package.json` with the new dependency, and `.changeset/dev-gate-router.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-dev-gate-prod-stripped-dev-warnings/07-CONTEXT.md
@.planning/phases/07-dev-gate-prod-stripped-dev-warnings/07-RESEARCH.md
@.planning/phases/07-dev-gate-prod-stripped-dev-warnings/07-PATTERNS.md
@packages/kit/src/internal/dev.ts
@packages/kit/src/define.ts
@packages/router/src/define.ts
@packages/router/src/router-core/routes.ts
@packages/router/src/router-core/types.ts
@packages/router/src/test/routes.test.ts
@packages/router/scripts/build.js
@packages/router/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Duplicate the dev-gate to @willramdev/router + duplicate-registration warning</name>
  <files>packages/router/src/internal/dev.ts, packages/router/src/define.ts, packages/router/src/define.test.ts, packages/router/scripts/build.js, packages/router/package.json</files>
  <read_first>
    - packages/kit/src/internal/dev.ts (the just-proven Plan 07-01 helper — copy verbatim, D-03 duplication, NOT an import)
    - packages/kit/src/define.ts and packages/kit/src/define.test.ts (the just-proven Plan 07-01 collision-only pattern and test shape)
    - packages/router/src/define.ts (current swallow site — byte-identical twin of kit's original, pre-Plan-07-01)
    - packages/router/scripts/build.js line 9 (`const external = ["lit", /^lit\//];` — a single shared const feeding all three per-entry builds)
    - packages/router/package.json (no existing dependencies block)
    - .planning/phases/07-dev-gate-prod-stripped-dev-warnings/07-PATTERNS.md §"packages/kit/src/define.ts + packages/router/src/define.ts"
  </read_first>
  <action>
    Create packages/router/src/internal/dev.ts as a VERBATIM duplicate of packages/kit/src/internal/dev.ts's content (D-03 — copy, do NOT import from @willramdev/kit; router must have zero import from any sibling package, preserving the acyclic dependency graph, Anti-Pattern 1). Same exports (DEV, devWarn, devWarnOnce), same module-level Set<string> dedupe shape — this Set is a SEPARATE instance from kit's, independent dedupe state.

    Modify packages/router/src/define.ts identically to how Plan 07-01 modified kit's: import devWarnOnce from ./internal/dev.ts; on a truthy customElements.get(tag), call devWarnOnce with key `dup:${tag}`, the same "a different element is already registered for this tag" message wording, and `when: existing !== ctor`, then return; otherwise register as before. Keep the exported signature byte-identical.

    Create packages/router/src/define.test.ts (new file — none exists for router today). Mirror packages/kit/src/define.test.ts's structure and the Plan 07-01 WARN-02 cases: registers, idempotent-no-throw, keeps-first-registration (the 3 pre-existing kit-style cases) plus the collision-warns-once and no-warn-on-same-ctor cases, using vi.spyOn(console, 'warn'), one distinct tag per case.

    Externalize esm-env in packages/router/scripts/build.js line 9: extend the shared `external` const to also include "esm-env". Because this ONE const feeds all three per-entry build passes (router-core, router-lit, router), a single edit covers all three dist outputs.

    Add esm-env to packages/router/package.json as a new top-level dependencies block (router currently has none), pinned to ^1.2.2 — real (non-dev, non-peer) per D-02. Do NOT touch router's sideEffects array (["dist/router.js", "dist/router-lit.js"] stays exactly as-is). Run `npm install` at the repo root afterward.
  </action>
  <verify>
    <automated>npm test -w @willramdev/router -- define.test.ts &amp;&amp; npm run build -w @willramdev/router</automated>
  </verify>
  <acceptance_criteria>
    - packages/router/src/internal/dev.ts exists and is a verbatim duplicate of kit's version (same exports, same dedupe-Set shape); it does not import from @willramdev/kit or any file under packages/kit.
    - packages/router/src/define.ts warns on tag-collision only, identical semantics to kit's; its exported signature is unchanged.
    - packages/router/src/define.test.ts exists with fire-once/idempotent/collision test coverage; `npm test -w @willramdev/router -- define.test.ts` passes.
    - packages/router/scripts/build.js's shared external const includes esm-env.
    - packages/router/package.json has a dependencies block pinning esm-env to ^1.2.2; its sideEffects array is byte-identical to before this task.
    - After `npm run build -w @willramdev/router`, all three of dist/router-core.js, dist/router-lit.js, and dist/router.js still contain a bare, unresolved esm-env import.
  </acceptance_criteria>
  <reversibility rating="costly">Same D-01/D-02/D-03/D-07 rationale as Plan 07-01, now applied to a second package — undoing it is a coordinated multi-package change. Not one-way — flag only.</reversibility>
  <done>router has its own duplicated dev-gate helper and duplicate-registration warning, proven by a green router test suite and externalized across all three per-entry builds.</done>
</task>

<task type="auto">
  <name>Task 2: Invalid route-config warnings in router-core (framework-neutral)</name>
  <files>packages/router/src/router-core/routes.ts, packages/router/src/test/routes.test.ts</files>
  <read_first>
    - packages/router/src/router-core/routes.ts (current defineRoutes/flattenRoutes, lines 1-41 — no validation exists today)
    - packages/router/src/router-core/types.ts (RouteDefinition shape: path is required by TS but a plain-JS caller can omit it; name/children/redirectTo/component/render fields)
    - packages/router/src/test/routes.test.ts (existing test conventions and fixtures using compiledMatcherFactory)
    - packages/router/src/internal/dev.ts (created in Task 1 of this plan; import path is ../internal/dev.ts from router-core)
    - .planning/phases/07-dev-gate-prod-stripped-dev-warnings/07-RESEARCH.md §"Pattern 2: Invalid route config" and its Open Question on which checks to ship
  </read_first>
  <action>
    Modify packages/router/src/router-core/routes.ts: import devWarnOnce from ../internal/dev.ts (router-core stays Lit-free — internal/dev.ts has zero Lit imports, so this does not violate the framework-neutral layering rule). Inside flattenRoutes's per-definition loop, add THREE cheap O(n) checks, each firing at most once at defineRoutes()/config-load time (never per-navigation):
    1. No path and no children: warn once, keyed on the route's name (or a placeholder for unnamed routes), when the definition's path is strictly undefined AND it has no children — this route can never match.
    2. Duplicate route name: track a Set of names seen so far ACROSS the whole defineRoutes() call (create this Set fresh inside defineRoutes() itself, then thread it through the flattenRoutes recursion — module scope is wrong here since names are scoped to one route tree, not shared globally across separate defineRoutes() calls); when a definition's name is already in the set, warn once keyed on that name, noting only the first-registered route with that name will be resolvable by findRouteByName()/href({name}); otherwise add the name to the set.
    3. redirectTo set together with component or render: warn once, keyed on the definition's name or path, when redirectTo is truthy AND either component or render is also truthy — note that a route cannot both redirect and render, without asserting which one wins at runtime (do not claim a precedence you have not verified).
    Keep defineRoutes's and flattenRoutes's signatures unchanged — this is purely additive.

    Extend packages/router/src/test/routes.test.ts with a new describe block (spy on console.warn): (a) a route with no path and no children warns once, with the [litkit] prefix; (b) two sibling routes sharing the same name warn once (on the second occurrence, not the first); (c) a route with both redirectTo and component set warns once; (d) the file's existing valid-route-tree fixtures trigger ZERO warnings — a regression guard proving the new checks never false-positive on legitimate configs.
  </action>
  <verify>
    <automated>npm test -w @willramdev/router -- routes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/router/src/router-core/routes.ts imports devWarnOnce from ../internal/dev.ts and has zero Lit imports (unchanged framework-neutral status).
    - Three new warn-once checks exist in flattenRoutes: no-path-no-children, duplicate-name, redirectTo-with-component-or-render — each fires exactly once per condition and never on a valid config.
    - defineRoutes's exported signature is unchanged.
    - `npm test -w @willramdev/router -- routes.test.ts` passes, including the 4 new cases and every pre-existing case.
  </acceptance_criteria>
  <done>Invalid route configs now warn once per issue at config-load time, in router-core, without becoming a schema validator or changing defineRoutes's public contract.</done>
</task>

<task type="auto">
  <name>Task 3: router changeset + full router regression proof</name>
  <files>.changeset/dev-gate-router.md</files>
  <read_first>
    - .changeset/dev-gate-kit.md (created in Plan 07-01 Task 3 — match its frontmatter/prose format)
    - .changeset/config.json
  </read_first>
  <action>
    Create .changeset/dev-gate-router.md with frontmatter listing `@willramdev/router` as a `minor` bump, and a changelog description covering the two warnings added this plan (duplicate registration, invalid route config), noting the same esm-env DEV gate as kit is reused (duplicated per-package, not shared).

    Run the full regression sweep: build router, run router's test suite, and confirm the Phase 6 type-SemVer regression guard stays clean for all three router snapshot files (router's public API is unchanged by this plan). Confirm the scope boundary still holds: the esm-env dependency does not appear in packages/query/package.json, packages/forms/package.json, or packages/store/package.json.
  </action>
  <verify>
    <automated>npm run build -w @willramdev/router &amp;&amp; npm test -w @willramdev/router &amp;&amp; npm run type-snapshot &amp;&amp; git diff --exit-code -- tools/type-snapshots/router.d.ts tools/type-snapshots/router-core.d.ts tools/type-snapshots/router-lit.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - .changeset/dev-gate-router.md exists with `@willramdev/router: minor` frontmatter and a non-empty changelog description.
    - `npm run build -w @willramdev/router && npm test -w @willramdev/router` exit 0.
    - All three router type-snapshot files show zero drift.
    - A scan of packages/query/package.json, packages/forms/package.json, and packages/store/package.json for the new dependency finds nothing.
  </acceptance_criteria>
  <reversibility rating="reversible">A changeset file only affects the next `npx changeset version` run; deleting it is a no-op revert.</reversibility>
  <done>router's foundational dev-gate layer (helper, dup-registration, route-config validation) is fully green and covered by its own changeset — ready for Plan 07-03 to fill the remaining router-lit missing-router gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry -> router build | The new real `dependencies` entry `esm-env` is fetched at a consumer's install time and its export condition resolved at the consumer's build time — the second and final package to gain this dependency |
| litkit source -> dev console | Dev-warning message strings are static prose; route-config warnings interpolate only source-literal `name`/`path` values already present in the developer's own route definitions, never runtime navigation/user data |
| acyclic internal dependency graph | packages/router/src/internal/dev.ts is a verbatim duplicate, never an import from @willramdev/kit — router must not become the first sibling to depend on kit at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-07-SC2 | Tampering | esm-env dependency (router, second package) | low | mitigate | Same vetted esm-env@^1.2.2 and legitimacy verdict as Plan 07-01; router's own changeset lands in the same PR (D-02) |
| T-07-ID2 | Information Disclosure | route-config warning messages | low | mitigate | Messages interpolate only source-literal route name/path values already present in the developer's own route config, never runtime navigation/user data |
| T-07-AC | Tampering (acyclic-graph integrity) | packages/router/src/internal/dev.ts | low | mitigate | Verbatim duplicate, zero import from @willramdev/kit or any sibling — preserves the acyclic dependency graph (Anti-Pattern 1) |
</threat_model>

<verification>
- `npm test -w @willramdev/router -- define.test.ts routes.test.ts` passes.
- `npm run build -w @willramdev/router` succeeds; all three dist files still contain an unresolved esm-env import.
- `npm run type-snapshot && git diff --exit-code` against all three router snapshots exits 0.
- `.changeset/dev-gate-router.md` exists with a minor bump for @willramdev/router.
- The esm-env dependency does not appear in query/forms/store package.json files.
</verification>

<success_criteria>
router's foundational dev-gate layer duplicates kit's proven mechanism exactly: its own internal/dev.ts, esm-env externalized across all three per-entry builds, a collision-only duplicate-registration warning, and three framework-neutral invalid-route-config warnings — all green, all covered by a changeset, and the Phase 6 type gate untouched. Plan 07-03 can now build on this package's internal/dev.ts to fill the remaining router-lit missing-router gaps.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Phase 7 — cross-reference; full set listed in 07-01-PLAN.md)

Newly created/modified in THIS plan (07-02):
- `packages/router/src/internal/dev.ts` — new; verbatim duplicate of kit's helper (D-03)
- `packages/router/src/define.ts` — modified; tag-collision-only duplicate-registration warning
- `packages/router/src/define.test.ts` — new; mirrors kit's define.test.ts shape
- `packages/router/scripts/build.js` — modified; `esm-env` added to the shared `external` const
- `packages/router/package.json` — modified; new `dependencies.esm-env: ^1.2.2`
- `packages/router/src/router-core/routes.ts` — modified; three invalid-route-config warn-once checks
- `packages/router/src/test/routes.test.ts` — modified; new dev-warning test coverage
- `.changeset/dev-gate-router.md` — new; @willramdev/router minor bump

Created in prior/sibling plans (listed so drift verification excludes them):
- `packages/kit/src/internal/dev.ts`, `packages/kit/src/define.ts` (modified), `packages/kit/src/define.test.ts`, `packages/kit/vite.config.ts` (modified), `packages/kit/package.json` (modified), `tools/dev-warning-strip/`, `scripts/dev-warning-strip.mjs`, `.github/workflows/ci.yml` (modified), `.changeset/dev-gate-kit.md` (07-01)
- `packages/router/src/router-lit/{route-controller,search-params-controller,router-outlet,router-link}.ts` (modified) + their test files (07-03)
- `tools/dev-warning-strip/src/warn-entry.ts` expanded to cover router; `scripts/dev-warning-strip.mjs` expanded with a negative control + phase-wide scope guard (07-04)

No changes are made to `@willramdev/query`, `@willramdev/forms`, or `@willramdev/store` — RESEARCH's Silent-Gap Audit found no silent misuse surface in any of the three (D-04).
</artifacts_produced>

<output>
Create `.planning/phases/07-dev-gate-prod-stripped-dev-warnings/07-02-SUMMARY.md` when done.
</output>