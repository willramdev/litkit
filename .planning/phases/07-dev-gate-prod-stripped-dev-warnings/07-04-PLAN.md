---
phase: 07-dev-gate-prod-stripped-dev-warnings
plan: 04
type: execute
wave: 4
depends_on: [07-03]
files_modified:
  - tools/dev-warning-strip/src/warn-entry.ts
  - scripts/dev-warning-strip.mjs
autonomous: true
requirements: [WARN-01, WARN-02, WARN-03]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The strip harness's warn-entry.ts re-exports enough of @willramdev/kit and @willramdev/router's public surface (define, RouterOutlet, RouterLink, RouteController, SearchParamsController, defineRoutes) that ALL SEVEN warning call sites (kit dup-registration, router dup-registration, RouteController/SearchParamsController/RouterOutlet/RouterLink missing-router, and route-config validation) are retained in the pre-minify bundle, and a mode:'production' build strips every one of them — grep count zero across the whole set (verification: explicit)."
    - "A mode:'development' variant of the SAME harness build (negative control, Pitfall 5) leaves the grep count greater than zero, proving the production strip is a real DEV-gate effect and not a vacuous or coincidental pass (verification: explicit)."
    - "The no-process sandbox probe covers BOTH @willramdev/kit's and @willramdev/router's raw dist output — importing either in a Node process with globalThis.process unset never throws process is not defined (WARN-03 #4; verification: explicit)."
    - "The new esm-env dependency is present ONLY in packages/kit/package.json and packages/router/package.json — packages/query, packages/forms, and packages/store have zero reference to it anywhere, package.json included (D-02/D-04 scope boundary, verified once at the phase-wide level rather than per-plan; verification: explicit — final scope sweep)."
    - "The three pre-Phase-7 existing throws (query's MISSING_QUERY_CLIENT_MESSAGE, forms bind()'s and field()'s missing-form-context throws) are byte-identical to their pre-Phase-7 source, and their existing test suites are still green — D-05 held for the entire phase, not just individually per plan (verification: explicit — full `npm test` run)."
  artifacts:
    - tools/dev-warning-strip/src/warn-entry.ts (final, all seven sites)
    - scripts/dev-warning-strip.mjs (final, with negative control + phase-wide scope guard)
  key_links:
    - "warn-entry.ts's re-exports -> Rollup retains full function bodies (router's sideEffects allowlist keeps router-lit/router-core code even when only some of its exports are used) -> mode:'production' constant-folds DEV=false -> grep proof"
    - "the SAME warn-entry.ts, built with mode:'development' -> DEV resolves truthy -> grep proof shows a count greater than zero (the negative control that closes the loop opened in Plan 07-01 Task 2)"
  prohibitions:
    - "Dev-warning strings must never survive a real minified production consumer build across ANY of the seven call sites, not just the one proven in Plan 07-01."
    - "Importing litkit — kit or router — must never throw ReferenceError: process is not defined in a no-process environment."
    - "The esm-env dependency, internal/dev.ts, and any dev-warning call site must never be added to packages/query, packages/forms, or packages/store — the Silent-Gap Audit found no misuse surface in any of the three (D-04)."
---

<objective>
Close Phase 7: expand the `tools/dev-warning-strip/` harness's `warn-entry.ts` to exercise ALL SEVEN warning call sites (kit's one plus router's six from Plans 07-02/07-03), add the negative control that proves the strip proof is not vacuous (Pitfall 5), extend the no-process sandbox probe to cover both packages, and run a final phase-wide scope-guard sweep confirming `@willramdev/query`, `@willramdev/forms`, and `@willramdev/store` remain completely untouched.

Purpose: WARN-03 — the full, non-vacuous strip + no-process proof across the whole phase's warning surface, not just the Plan 07-01 tracer's one site.
Output: an expanded `tools/dev-warning-strip/src/warn-entry.ts` and `scripts/dev-warning-strip.mjs` (negative control + expanded no-process probe + scope guard), with all four ROADMAP Phase 7 success criteria independently proven.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-dev-gate-prod-stripped-dev-warnings/07-RESEARCH.md
@.planning/phases/07-dev-gate-prod-stripped-dev-warnings/07-PATTERNS.md
@tools/dev-warning-strip/src/warn-entry.ts
@tools/dev-warning-strip/vite.config.ts
@scripts/dev-warning-strip.mjs
@packages/router/src/index.ts
@packages/router/package.json
@packages/query/src/query-controller.ts
@packages/forms/src/bind.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the strip harness to cover all six router warning sites</name>
  <files>tools/dev-warning-strip/src/warn-entry.ts</files>
  <read_first>
    - tools/dev-warning-strip/src/warn-entry.ts (current kit-only version from Plan 07-01)
    - packages/router/src/index.ts (confirms RouterOutlet, RouterLink, RouteController, SearchParamsController, and defineRoutes are ALL public exports — no internal reach-around needed)
    - packages/router/package.json's sideEffects array (["dist/router.js", "dist/router-lit.js"] — confirms Rollup retains the FULL contents of these files, including devWarnOnce call sites inside unexported helper functions like flattenRoutes, once ANY export from them is used)
    - .planning/phases/07-dev-gate-prod-stripped-dev-warnings/07-RESEARCH.md §"Silent-Gap Audit" table rows 1-7 (the full list of seven sites this harness must now cover)
  </read_first>
  <action>
    Modify tools/dev-warning-strip/src/warn-entry.ts to ALSO re-export from @willramdev/router: RouterOutlet, RouterLink, RouteController, SearchParamsController, defineRoutes. This stays re-export-only (no calls, no DOM construction needed) — importing RouterOutlet/RouterLink from @willramdev/router's main entry (which resolves to router's sideEffects-allowlisted bundled dist file) causes Rollup to retain that file's full top-level code, including router's define() collision-check call (covering the router dup-registration site), and defineRoutes's export retains flattenRoutes's body (covering the route-config-validation site) via static reachability — regardless of whether these exports are ever invoked by warn-entry.ts. Update the file's header comment to list all seven sites now covered: kit dup-registration; router dup-registration; RouteController/SearchParamsController/RouterOutlet/RouterLink missing-router; route-config validation.

    Re-run the strip harness (after a full workspace build, since it now needs BOTH kit's and router's dist output) and confirm the strip proof still reports a zero occurrence count — now proving zero warning-prefix occurrences across all seven call sites in one minified bundle, not just kit's original one.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/dev-warning-strip.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tools/dev-warning-strip/src/warn-entry.ts re-exports define (kit) plus RouterOutlet, RouterLink, RouteController, SearchParamsController, defineRoutes (router); still contains zero top-level function calls.
    - `node scripts/dev-warning-strip.mjs` (after a full `npm run build`) reports the strip proof still passing with a zero count, now covering both packages.
    - The emitted tools/dev-warning-strip/dist/warn-entry.js is meaningfully larger than the kit-only version (a sanity check confirming router's code was actually included pre-minify, not silently dropped — not a hard gate).
  </acceptance_criteria>
  <done>The strip harness now exercises all seven WARN-02 call sites across both kit and router, and the production build still strips every occurrence of the warning prefix.</done>
</task>

<task type="auto">
  <name>Task 2: Negative control, expanded no-process proof, and phase-wide scope-guard closure</name>
  <files>scripts/dev-warning-strip.mjs</files>
  <read_first>
    - scripts/dev-warning-strip.mjs (current version from Plan 07-01 Task 2)
    - tools/dev-warning-strip/vite.config.ts (mode:'production' — the negative control needs a mode:'development' variant of the same build)
    - .planning/phases/07-dev-gate-prod-stripped-dev-warnings/07-RESEARCH.md §"Pitfall 5" (vacuous-proof guard) and §"No-process import smoke"
    - packages/query/src/query-controller.ts lines 220-238, packages/forms/src/bind.ts lines 69-73 (the existing throws — final D-05 confirmation reads)
  </read_first>
  <action>
    Modify scripts/dev-warning-strip.mjs:
    1. Add a NEGATIVE-CONTROL build step: invoke Vite's build() a second time against the SAME tools/dev-warning-strip/vite.config.ts, overriding mode to 'development' (Vite's build() JS API accepts an inline config object whose fields override the loaded config file) and emitting to a SEPARATE output directory so it never clobbers the production artifact. Read the emitted dev-mode bundle and assert its warning-prefix occurrence count is greater than zero — fail loudly with a clear message if it is zero (that would mean the whole strip mechanism is not actually gated by DEV, Pitfall 5).
    2. Expand the no-process proof to import BOTH @willramdev/kit and @willramdev/router (sequentially, in the same child-process probe, both after globalThis.process is unset), asserting both resolve without a process-related throw. Reuse the jsdom-fallback approach from Plan 07-01 Task 2 if the plain-Node attempt fails for an unrelated missing-browser-global reason.
    3. Add a final scope-guard step: read packages/query/package.json, packages/forms/package.json, and packages/store/package.json, and assert none of the three contain the new dependency's package name anywhere in their contents; fail with a clear message naming which file if any do.

    Run the FULL phase-wide regression sweep and fix anything red: a full workspace build, the full workspace test suite (confirms query/forms/store's existing throw-based tests are untouched, D-05), the expanded dev-warning-strip harness (now running all four proofs — strip, negative control, no-process, scope guard — in one command), and the Phase 6 type-SemVer gate across the full 8-snapshot set.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test &amp;&amp; node scripts/dev-warning-strip.mjs &amp;&amp; npm run type-snapshot &amp;&amp; git diff --exit-code -- tools/type-snapshots/</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/dev-warning-strip.mjs` runs four proofs in order and all pass: the strip proof (production build, zero occurrences), the negative control (development build, more than zero occurrences), the no-process proof (both kit and router), and the scope-guard proof (no reference to the new dependency in query/forms/store).
    - `npm run test` (full workspace) passes, including query/forms/store's pre-existing test suites, unmodified.
    - `npm run type-snapshot && git diff --exit-code -- tools/type-snapshots/` exits 0 across all 8 committed snapshots (the Phase 6 gate stays green through the end of Phase 7).
    - A final repository-wide check confirms the new dependency's package name appears in exactly two package.json files: packages/kit/package.json and packages/router/package.json.
  </acceptance_criteria>
  <done>Phase 7 is fully closed: all four ROADMAP success criteria are independently proven (dev-gate mechanism, all seven warning sites, a non-vacuous strip proof with negative control, and the no-process guarantee on both touched packages), with zero scope leakage into query/forms/store and the Phase 6 type gate still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| the full production strip proof | Now spans two packages and seven call sites — the single most consequential check in the phase, since it is the entire basis for WARN-03's "provably stripped" claim |
| phase-wide scope integrity | The final confirmation that query/forms/store received zero code, dependency, or warning-surface changes across all four plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-07-FIN | Tampering | full-phase strip proof | medium | mitigate | The negative control (development-mode build shows a nonzero count) proves the WHOLE seven-site proof is non-vacuous, closing Pitfall 5 for the complete surface, not just Plan 07-01's kit-only tracer site |
| T-07-SCOPE | Elevation of Privilege (scope creep) | esm-env / warning surface leaking into query/forms/store | low | mitigate | Final grep sweep across all three untouched packages' package.json, confirmed as the LAST step of the phase so no later plan could have silently reintroduced it |
</threat_model>

<verification>
- `node scripts/dev-warning-strip.mjs` passes all four proofs (strip, negative control, no-process x2, scope guard).
- `npm run test` (full workspace) is green.
- `npm run type-snapshot && git diff --exit-code -- tools/type-snapshots/` exits 0 for all 8 snapshots.
- The new dependency appears in exactly two package.json files in the whole repo.
</verification>

<success_criteria>
All four ROADMAP Phase 7 success criteria are met and independently proven: (1) a single dev-gate mechanism chosen once and surviving litkit's own build; (2) dev-only warnings covering the top misuse cases across kit and router; (3) a minified consumer production build containing zero dev-warning strings, proven non-vacuous; (4) no process-is-not-defined crash in a no-process sandbox for either touched package. query/forms/store remain byte-identical to their pre-Phase-7 state.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Phase 7 — full set, final cross-reference)

Newly created/modified in THIS plan (07-04):
- `tools/dev-warning-strip/src/warn-entry.ts` — final version; re-exports all seven warning call sites across kit + router
- `scripts/dev-warning-strip.mjs` — final version; strip proof + negative control + dual-package no-process proof + phase-wide scope guard

Created in prior plans (full phase inventory):
- `packages/kit/src/internal/dev.ts`, `packages/kit/src/define.ts` (modified), `packages/kit/src/define.test.ts` (modified), `packages/kit/vite.config.ts` (modified), `packages/kit/package.json` (modified), `tools/dev-warning-strip/vite.config.ts`, `package.json` root script, `.github/workflows/ci.yml` (modified), `.changeset/dev-gate-kit.md` (07-01)
- `packages/router/src/internal/dev.ts`, `packages/router/src/define.ts` (modified), `packages/router/src/define.test.ts`, `packages/router/scripts/build.js` (modified), `packages/router/package.json` (modified), `packages/router/src/router-core/routes.ts` (modified), `packages/router/src/test/routes.test.ts` (modified), `.changeset/dev-gate-router.md` (07-02)
- `packages/router/src/router-lit/{route-controller,search-params-controller,router-outlet,router-link}.ts` (modified) + their test files (07-03)

No changes were made anywhere in this phase to `@willramdev/query`, `@willramdev/forms`, or `@willramdev/store` — confirmed at both the per-plan and phase-wide level.
</artifacts_produced>

<output>
Create `.planning/phases/07-dev-gate-prod-stripped-dev-warnings/07-04-SUMMARY.md` when done.
</output>
