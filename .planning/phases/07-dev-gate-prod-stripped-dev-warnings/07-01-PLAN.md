---
phase: 07-dev-gate-prod-stripped-dev-warnings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/kit/src/internal/dev.ts
  - packages/kit/src/define.ts
  - packages/kit/src/define.test.ts
  - packages/kit/vite.config.ts
  - packages/kit/package.json
  - tools/dev-warning-strip/src/warn-entry.ts
  - tools/dev-warning-strip/vite.config.ts
  - scripts/dev-warning-strip.mjs
  - package.json
  - .github/workflows/ci.yml
  - .changeset/dev-gate-kit.md
autonomous: true
requirements: [WARN-01, WARN-02, WARN-03]
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The dev-gate mechanism is esm-env's DEV export (D-01) — not import.meta.env.DEV, not a build-time define — and esm-env is externalized in packages/kit/vite.config.ts (alongside lit) so litkit's own build leaves `import { DEV } from 'esm-env'` unresolved in dist/kit.js (verification: explicit — the bare esm-env import is confirmed present in dist/kit.js after `npm run build -w @willramdev/kit`)."
    - "kit's define() warns exactly once, with the [litkit] prefix, when a second, DIFFERENT constructor is registered under an already-taken tag — and stays silent on a same-constructor idempotent re-call (D-04, RESEARCH audit row 1; verification: explicit — define.test.ts fire-once + no-warn-on-idempotent-recall assertions)."
    - "A minified `vite build --mode production` of a mini consumer that imports kit's define contains zero [litkit] occurrences (WARN-03 #3; verification: explicit — node scripts/dev-warning-strip.mjs reports STRIP PASS with a zero grep count)."
    - "Importing @willramdev/kit's built dist in a Node process with globalThis.process unset never throws process is not defined (WARN-03 #4, satisfied by construction via esm-env's globalThis.process?.env?.NODE_ENV optional chaining; verification: explicit — node scripts/dev-warning-strip.mjs reports NO-PROCESS PASS)."
    - "Each dev-warning fires at most once per stable key even when the same misuse condition is triggered repeatedly (module-level Set<string> dedupe, D-06) — no warning can flood the console under repeated triggering of the same key (specless-edge-probe WARN-02 concurrency resolution; verification: explicit — define.test.ts asserts a third colliding define() call adds no further console.warn calls beyond the first)."
    - "The Phase 6 type-SemVer shape gate stays green — kit's public API, including define()'s signature, is unchanged, so tools/type-snapshots/kit.d.ts shows zero drift (verification: explicit — `npm run build && npm run type-snapshot && git diff --exit-code -- tools/type-snapshots/kit.d.ts`)."
  artifacts:
    - packages/kit/src/internal/dev.ts
    - packages/kit/src/define.ts (modified)
    - tools/dev-warning-strip/src/warn-entry.ts
    - tools/dev-warning-strip/vite.config.ts
    - scripts/dev-warning-strip.mjs
    - .changeset/dev-gate-kit.md
  key_links:
    - "packages/kit/src/define.ts -> ./internal/dev.ts (devWarnOnce) -> esm-env DEV -> Vite production condition -> stripped from a consumer's minified bundle (the whole phase's mechanism, proven end-to-end on one site)"
    - "packages/kit/vite.config.ts external: [..., 'esm-env'] -> dist/kit.js keeps the bare esm-env import unresolved -> the CONSUMER's own bundler resolves DEV, not litkit's own build (Pattern 3, the load-bearing config change)"
    - "scripts/dev-warning-strip.mjs -> tools/dev-warning-strip/vite.config.ts (mode:'production', minify:true) -> warn-entry.ts re-exports -> grep [litkit] == 0"
  prohibitions:
    - "Dev-warning strings must never survive a real minified production consumer build — the strip harness's mode:'production' + minify:true config must not be a vacuous check (Pitfall 5)."
    - "Importing litkit must never throw ReferenceError: process is not defined in an environment without a process global — no bare process reference is permitted anywhere in the dev-gate helper or its call sites."
    - "No dev-warning may fire more than once for the same stable condition/key — a component re-render or repeated misuse trigger must never flood the console."
    - "Every dev-warning message uses the single [litkit] prefix, never a per-package or per-site tag — the WARN-03 strip-verification grep contract depends on exactly one stable prefix string."
---

<objective>
Wire the dev-gate mechanism (esm-env's `DEV` export) and prove it end-to-end on ONE real, currently-silent misuse case in `@willramdev/kit` — duplicate custom-element registration — as a production-quality tracer covering every layer this phase touches: the dev-gate helper, a real warning call site, litkit's own build config, and a real consumer-side strip + no-`process`-sandbox proof. Plans 07-02/03 duplicate this exact pattern into `@willramdev/router`'s remaining five silent gaps; Plan 07-04 closes the loop with a non-vacuous negative control.

Purpose: WARN-01 (choose the dev-gate mechanism once, survive litkit's own build) + WARN-03 (prove stripped + no-`process`-safe) proven on the smallest possible real surface before fanning out. WARN-02 (fill kit's one silent gap: duplicate registration) is delivered alongside it since the tracer needs a real call site to prove the mechanism against.
Output: `packages/kit/src/internal/dev.ts`, a modified `packages/kit/src/define.ts` that warns on tag collision, `packages/kit/vite.config.ts` externalizing `esm-env`, `packages/kit/package.json` with the new `esm-env` dependency, the new `tools/dev-warning-strip/` harness + `scripts/dev-warning-strip.mjs` runner wired into the read-only `ci.yml` `gate` job, and `.changeset/dev-gate-kit.md`.
</objective>

<assumption_delta_decision>
Primary noun: "the single dev-gate mechanism"
Decision: no-change
Rationale: The assumption-delta detector flagged the word "chosen" in the ROADMAP goal line ("A single dev-gate mechanism ... is chosen once and survives litkit's own build ..."). This is a false positive — WARN-01 deliberately picks ONE dev-gate (esm-env's `DEV`) and this is a locked decision (D-01), not a derived-to-chosen or singular-to-plural generalization the detector normally flags. The gate is intentionally NOT consumer-configurable: there is exactly one mechanism, duplicated per-package as source (D-03) but never pluralized into multiple selectable gates. No question raised to the user.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dev-gate-prod-stripped-dev-warnings/07-CONTEXT.md
@.planning/phases/07-dev-gate-prod-stripped-dev-warnings/07-RESEARCH.md
@.planning/phases/07-dev-gate-prod-stripped-dev-warnings/07-PATTERNS.md
@packages/kit/src/define.ts
@packages/kit/src/define.test.ts
@packages/kit/vite.config.ts
@packages/kit/package.json
@packages/forms/src/internal/engine.ts
@scripts/verify-consumer.mjs
@tools/verify-consumer/vite.config.ts
@.github/workflows/ci.yml
@package.json
@.changeset/config.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Dev-gate mechanism + one real warning site — @willramdev/kit duplicate registration</name>
  <files>packages/kit/src/internal/dev.ts, packages/kit/src/define.ts, packages/kit/src/define.test.ts, packages/kit/vite.config.ts, packages/kit/package.json</files>
  <read_first>
    - packages/kit/src/define.ts (the swallow site — 10 lines total; `if (!customElements.get(tag)) { customElements.define(tag, ctor, options); }`, no message on a dup tag)
    - packages/kit/src/define.test.ts (3 existing tests: registers, does-not-throw-on-dup-tag, keeps-first-registration — extend, do not remove)
    - packages/kit/vite.config.ts line 12 (`external: ['lit', /^lit\//]`)
    - packages/kit/package.json (no existing `dependencies` block)
    - packages/forms/src/internal/engine.ts (the repo's only existing `internal/` module — header-comment convention + confirms `internal/` files are never re-exported from index.ts)
    - .planning/phases/07-dev-gate-prod-stripped-dev-warnings/07-RESEARCH.md §"Pattern 1: The internal/dev.ts helper" (verbatim helper body to copy) and §"Duplicate-registration warning — critical nuance" (collision-only rule)
  </read_first>
  <action>
    Create packages/kit/src/internal/dev.ts by copying RESEARCH.md's Pattern 1 helper verbatim: `import { DEV } from 'esm-env'`, a re-export of `DEV`, a module-level `const warnedKeys = new Set<string>()`, `export function devWarn(message: string): void` that gates on `DEV` as the OUTERMOST condition and calls `console.warn` with the message prefixed exactly `[litkit] `, and `export function devWarnOnce(key: string, message: string, when = true): void` that guards on `DEV && when && !warnedKeys.has(key)`, adds the key to the Set, then warns. Zero Lit imports (framework-neutral, D-03). Do NOT export this file from packages/kit/src/index.ts — it stays a private implementation detail, matching the packages/forms/src/internal/engine.ts precedent.

    Modify packages/kit/src/define.ts: import `devWarnOnce` from `./internal/dev.ts`. Replace the current swallow with: read `existing = customElements.get(tag)`; if `existing` is truthy, call `devWarnOnce` with key `dup:${tag}`, a message describing that a different element is already registered for this tag and the new registration was ignored, and a `when` argument of `existing !== ctor` — then `return` without registering; otherwise call `customElements.define(tag, ctor, options)` as before. The `when: existing !== ctor` condition means a same-constructor idempotent re-call (SSR/HMR/repeat-import — the designed, correct usage per the RESEARCH nuance section) stays completely silent; only a genuine tag COLLISION (a different constructor) warns. Keep the function's exported signature byte-identical (`tag: string, ctor: CustomElementConstructor, options?: ElementDefinitionOptions): void`) so the Phase 6 kit.d.ts snapshot stays clean.

    Extend packages/kit/src/define.test.ts (keep the 3 existing tests) with a new describe block using `vi.spyOn(console, 'warn').mockImplementation(() => {})`, restored per-test: (a) registering a SECOND, DIFFERENT constructor under an already-used tag calls console.warn exactly once, and the call's first argument starts with `[litkit]`; (b) registering the SAME constructor twice under a fresh tag calls console.warn zero times (idempotent, silent); (c) a THIRD collision attempt on the SAME already-warned tag from case (a) does not add a second console.warn call (warn-once, D-06). Use a distinct tag per test case so the module-level dedupe Set (shared across the whole test file) never causes one test's prior trigger to silently suppress another test's expected first warning.

    Externalize esm-env in packages/kit/vite.config.ts line 12: extend the `external` array to also include `'esm-env'` (Pattern 3 — the load-bearing config change; without this, litkit's OWN build resolves the esm-env export condition and bakes a fixed DEV value, defeating the whole consumer-strip mechanism, Anti-Pattern 3).

    Add `esm-env` to packages/kit/package.json as a new top-level `dependencies` block (kit currently has none) pinned to `^1.2.2` — real (non-dev, non-peer) per D-02. Do NOT add it to `devDependencies`, `peerDependencies`, or any `sideEffects` field (kit's `sideEffects` stays `false`, unchanged). Run `npm install` at the repo root afterward so package-lock.json picks up the new dependency.
  </action>
  <verify>
    <automated>npm test -w @willramdev/kit -- define.test.ts &amp;&amp; npm run build -w @willramdev/kit</automated>
  </verify>
  <acceptance_criteria>
    - packages/kit/src/internal/dev.ts exists, exports DEV, devWarn, devWarnOnce, and imports only from esm-env (zero Lit imports).
    - packages/kit/src/internal/dev.ts is not imported or re-exported from packages/kit/src/index.ts.
    - packages/kit/src/define.ts warns on tag-collision (different constructor) with a [litkit]-prefixed message and stays silent on a same-constructor idempotent re-call; define()'s exported signature is unchanged.
    - `npm test -w @willramdev/kit -- define.test.ts` passes, including the 3 pre-existing cases plus the new collision/idempotent/warn-once cases.
    - packages/kit/vite.config.ts's external array includes esm-env alongside lit and /^lit\//.
    - packages/kit/package.json has a dependencies block pinning esm-env to ^1.2.2; esm-env does not appear in devDependencies, peerDependencies, or any sideEffects field.
    - After `npm run build -w @willramdev/kit`, packages/kit/dist/kit.js still contains a bare, unresolved esm-env import statement (externalization confirmed — Pitfall 1 guard).
  </acceptance_criteria>
  <reversibility rating="costly">Introduces the esm-env DEV-gate + duplicated internal/dev.ts architecture (D-01/D-02/D-03) and the [litkit] grep contract (D-07). Reversible but costly: swapping the gate mechanism, consolidating the helper into a shared module, or changing the prefix all require coordinated edits across every warning call site plus a changeset. Not a one-way-door migration — no checkpoint, flag only per REVERSIBILITY_GATES.</reversibility>
  <done>kit's dev-gate mechanism is proven on one real site: DEV resolves via esm-env, esm-env survives kit's own build unresolved, and the duplicate-registration warning fires once (collision only) with the [litkit] prefix — all covered by a green kit test suite.</done>
</task>

<task type="auto">
  <name>Task 2: Strip + no-process sandbox harness proving kit's warning site is stripped and safe</name>
  <files>tools/dev-warning-strip/src/warn-entry.ts, tools/dev-warning-strip/vite.config.ts, scripts/dev-warning-strip.mjs, package.json, .github/workflows/ci.yml</files>
  <read_first>
    - scripts/verify-consumer.mjs lines 285-402 (checkTreeshake — the direct clone target: a production+minify Vite build, then a static read of the emitted bundle) and lines 362-386 (the jsdom global-exposure probe pattern for importing a bundle that touches browser APIs)
    - tools/verify-consumer/vite.config.ts (the production+minify+external:[] config shape to clone)
    - packages/router/scripts/build.js line 1 (`import { build } from "vite";` — the repo's own convention for calling Vite's JS API directly instead of spawning the CLI)
    - .github/workflows/ci.yml lines 39-91 (the gate job — insert the new step after "shape gate" ~line 76, before "publint" ~line 78; top-level permissions: contents: read lines 12-14 must stay unchanged)
    - package.json root scripts block (the `verify:consumer` -> `node scripts/verify-consumer.mjs` convention to match)
    - .planning/phases/07-dev-gate-prod-stripped-dev-warnings/07-RESEARCH.md §"Strip + sandbox harness (WARN-03 detail)" and §"Pitfall 5" (vacuous-proof guard)
  </read_first>
  <action>
    Create tools/dev-warning-strip/src/warn-entry.ts: a single re-export line, `export { define } from '@willramdev/kit';`. This does NOT need to CALL define() — Rollup retains a re-exported function's full body regardless of whether it is invoked, and the DEV-gated console.warn branch inside it is removed by CONSTANT FOLDING once Vite resolves DEV to the literal `false` at build time (production mode), independent of any runtime call graph. Add a header comment stating this re-export-only rationale so Plan 07-04 can extend the file the same way for router's exports without re-deriving the reasoning.

    Create tools/dev-warning-strip/vite.config.ts, cloned from tools/verify-consumer/vite.config.ts's shape: `mode: 'production'`, `build: { minify: true, outDir: 'dist', emptyOutDir: true, lib: { entry: 'src/warn-entry.ts', formats: ['es'], fileName: () => 'warn-entry.js' }, rollupOptions: { external: [] } }` — bundle @willramdev/kit and esm-env IN (nothing externalized) so the emitted bundle is self-contained and the esm-env export condition resolves DURING this build, mirroring what a real consumer's `vite build` does.

    Create scripts/dev-warning-strip.mjs (repo-root anchored, pure Node ESM using only node:fs/path/child_process/url — no bash, cross-platform, matching scripts/verify-consumer.mjs's own convention since the dev box is win32):
    1. Import Vite's JS `build()` API directly (matching packages/router/scripts/build.js's convention) and run it once against tools/dev-warning-strip/vite.config.ts with root set to tools/dev-warning-strip.
    2. STRIP PROOF: read the emitted tools/dev-warning-strip/dist/warn-entry.js, count occurrences of the litkit warning prefix, fail with a clear stderr message and exit 1 if the count is not zero, otherwise log "STRIP PASS".
    3. NO-PROCESS PROOF (targets kit's OWN raw dist directly, not the harness bundle — the harness's production build has already had esm-env's export condition resolved and inlined by Vite, so it no longer exercises esm-env's runtime fallback at all; the real no-process risk lives in litkit's unbundled dist/kit.js, which Task 1's externalization keeps as a bare, unresolved esm-env import). Build the probe string entirely in the PARENT process — do not rely on `process.argv` inside the child probe after nulling process, since that is self-contradicting. Spawn a child `node --input-type=module --eval <probe>` (cwd: repo root so the workspace symlink resolves @willramdev/kit) where `<probe>` sets `globalThis.process = undefined;`, then `await import('@willramdev/kit')`, then prints a success marker. If this plain-Node attempt throws for a reason UNRELATED to process (e.g. a missing browser global from Lit, such as HTMLElement is not defined), retry the SAME probe but first expose the jsdom window's global surface onto globalThis (clone the technique from scripts/verify-consumer.mjs's jsdomProbe, lines 362-380 — construct a JSDOM instance, copy Object.getOwnPropertyNames(window) onto globalThis excluding the globalThis/window/self/top/parent/frames aliases) BEFORE nulling process, then re-attempt the import. Either path's success criterion is the same: the import must not throw an error referencing `process is not defined`; log "NO-PROCESS PASS" on success.
    4. A `main()` that runs build -> strip proof -> no-process proof in order, exiting 1 with a clear message on any failure.

    Add a root `dev-warning-strip` npm script running `node scripts/dev-warning-strip.mjs`, matching the existing `verify:consumer` convention.

    Add a new step to .github/workflows/ci.yml's `gate` job, positioned after the existing "shape gate" step (~line 76) and before "publint" (~line 78), running the new `dev-warning-strip` npm script under a clear step name referencing WARN-03. Do not touch the top-level `permissions: contents: read` or release.yml (D-09).
  </action>
  <verify>
    <automated>npm run build -w @willramdev/kit &amp;&amp; node scripts/dev-warning-strip.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tools/dev-warning-strip/src/warn-entry.ts re-exports define from @willramdev/kit and contains no top-level function calls (re-export only).
    - tools/dev-warning-strip/vite.config.ts sets mode:'production', build.minify:true, and rollupOptions.external:[].
    - Running `node scripts/dev-warning-strip.mjs` after `npm run build -w @willramdev/kit` prints both a STRIP PASS and a NO-PROCESS PASS marker and exits 0.
    - Temporarily forcing the harness's own build config to mode:'development' (manual, throwaway check during this task — do not commit) makes the strip proof's occurrence count greater than zero, confirming the production-mode proof is not vacuous (Pitfall 5); revert before finishing this task.
    - package.json (root) scripts block contains a `dev-warning-strip` entry running `node scripts/dev-warning-strip.mjs`.
    - .github/workflows/ci.yml's gate job contains the new step positioned after "shape gate" and before "publint"; top-level permissions: contents: read is unchanged; release.yml is untouched.
  </acceptance_criteria>
  <reversibility rating="reversible">A self-contained tools/ dir + one CI step (D-08/D-09); removing it later does not affect any shipped package.</reversibility>
  <done>kit's one real warning site is proven stripped from a real minified production consumer build and importing kit's dist never throws process is not defined, wired as a read-only CI step — closing the tracer's end-to-end loop.</done>
</task>

<task type="auto">
  <name>Task 3: kit changeset + full tracer regression proof</name>
  <files>.changeset/dev-gate-kit.md</files>
  <read_first>
    - .changeset/config.json (the fixed group covers all 5 packages; access restricted; baseBranch main)
    - .planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-01-PLAN.md (the type-snapshot regression command shape used at the end of a Phase 6 tracer plan)
    - packages/query/src/query-controller.ts lines 220-238 (the existing MISSING_QUERY_CLIENT_MESSAGE throw — D-05, confirm this stays byte-identical)
  </read_first>
  <action>
    Create .changeset/dev-gate-kit.md with frontmatter listing `@willramdev/kit` as a `minor` bump, followed by a short changelog description of the new dev-only, prod-stripped duplicate-registration warning: mention it is gated by esm-env's DEV export, uses a single stable message prefix, and is verified stripped from production consumer builds.

    Run the full regression sweep for this plan and fix anything red: build kit, run kit's test suite, run the dev-warning-strip harness, and confirm the Phase 6 type-SemVer regression guard for kit's snapshot stays clean (kit's public API, including define()'s signature, must be byte-identical to before this plan). Also confirm the scope boundary described in RESEARCH's Silent-Gap Audit: the new dependency added in Task 1 must not appear in packages/query/package.json, packages/forms/package.json, or packages/store/package.json (none of those three packages gain any change this phase), and packages/query/src/query-controller.ts's existing missing-client throw is unchanged (D-05).
  </action>
  <verify>
    <automated>npm run build -w @willramdev/kit &amp;&amp; npm test -w @willramdev/kit &amp;&amp; node scripts/dev-warning-strip.mjs &amp;&amp; npm run type-snapshot &amp;&amp; git diff --exit-code -- tools/type-snapshots/kit.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - .changeset/dev-gate-kit.md exists with `@willramdev/kit: minor` frontmatter and a non-empty changelog description.
    - The full verify command chain above exits 0.
    - A directory listing of packages/query, packages/forms, and packages/store shows no file modified by this plan; a scan of their package.json files for the dependency added in Task 1 finds nothing.
  </acceptance_criteria>
  <reversibility rating="reversible">A changeset file only affects the next `npx changeset version` run; deleting it is a no-op revert.</reversibility>
  <done>Plan 07-01 is fully green end-to-end (kit build, tests, strip harness, and the Phase 6 type gate) with a covering changeset — ready for Plan 07-02 to duplicate the proven pattern into router.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry -> kit build | The new real `dependencies` entry `esm-env` is fetched at a consumer's install time and its export condition resolved at the consumer's build time |
| PR author -> read-only CI gate | The new dev-warning-strip step runs in the existing `ci.yml` `gate` job under `contents: read`, no auth token |
| litkit source -> dev console | Dev-warning message strings are static prose with only source-literal tag names interpolated — never runtime/user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-07-SC | Tampering | esm-env dependency (kit) | low | mitigate | RESEARCH's Package Legitimacy Audit verdict OK (esm-env@1.2.2, 6.29M/wk, no postinstall, github.com/benmccann/esm-env); pin ^1.2.2; changeset lands in the same PR (D-02). Not [ASSUMED]/[SUS] — no blocking human-verify checkpoint required. |
| T-07-ID | Information Disclosure | dev-warning message content | low | mitigate | Messages are static strings with only source-literal identifiers (tag names) interpolated; no user/form/route runtime values ever appear (Security Domain rule from RESEARCH.md) |
| T-07-EP | Elevation of Privilege | new ci.yml gate step | low | mitigate | Attached to the existing read-only `gate` job only; no new permissions requested; release.yml untouched (D-09) |
| T-07-BY | Tampering | strip-proof bypass (a vacuous check that always passes) | medium | mitigate | Task 2's manual dev-mode throwaway build proves the check is non-vacuous on this tracer site; Plan 07-04 repeats this as a committed, permanent negative control once all seven sites are wired (Pitfall 5) |
</threat_model>

<verification>
- `npm test -w @willramdev/kit -- define.test.ts` passes, including collision/idempotent/warn-once cases.
- `npm run build -w @willramdev/kit` succeeds and packages/kit/dist/kit.js still contains an unresolved esm-env import.
- `node scripts/dev-warning-strip.mjs` reports both STRIP PASS and NO-PROCESS PASS.
- `.github/workflows/ci.yml` gate job runs the new step; permissions and release.yml unchanged.
- `npm run type-snapshot && git diff --exit-code -- tools/type-snapshots/kit.d.ts` exits 0.
- `.changeset/dev-gate-kit.md` exists with a minor bump for @willramdev/kit.
</verification>

<success_criteria>
The dev-gate mechanism (esm-env DEV) is chosen, wired, and proven end-to-end on one real kit warning site: it survives kit's own build unresolved, the duplicate-registration warning fires once with the [litkit] prefix, a real minified consumer build strips it to zero occurrences, importing kit never throws process is not defined, and the whole chain is committed with a covering changeset — ready for Plan 07-02 to duplicate the pattern into router.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Phase 7 — full set, for the plan-review source-grounding pass)

Newly created/modified in THIS plan (07-01):
- `packages/kit/src/internal/dev.ts` — new; DEV re-export + devWarn + devWarnOnce (framework-neutral, zero Lit imports)
- `packages/kit/src/define.ts` — modified; tag-collision-only duplicate-registration warning
- `packages/kit/src/define.test.ts` — modified; fire-once/idempotent/collision test coverage
- `packages/kit/vite.config.ts` — modified; `esm-env` added to `external`
- `packages/kit/package.json` — modified; new `dependencies.esm-env: ^1.2.2`
- `tools/dev-warning-strip/src/warn-entry.ts` — new; kit-only re-export (expanded in 07-04)
- `tools/dev-warning-strip/vite.config.ts` — new; mode:'production', minify:true, external:[]
- `scripts/dev-warning-strip.mjs` — new; build -> strip grep -> no-process sandbox runner
- `package.json` (root) — modified; new `dev-warning-strip` script
- `.github/workflows/ci.yml` — modified; new read-only WARN-03 step in the `gate` job
- `.changeset/dev-gate-kit.md` — new; @willramdev/kit minor bump

Created in sibling plans (listed so drift verification excludes them):
- `packages/router/src/internal/dev.ts`, `packages/router/src/define.ts` (modified), `packages/router/src/define.test.ts`, `packages/router/scripts/build.js` (modified), `packages/router/package.json` (modified), `packages/router/src/router-core/routes.ts` (modified), `packages/router/src/test/routes.test.ts` (modified), `.changeset/dev-gate-router.md` (07-02)
- `packages/router/src/router-lit/{route-controller,search-params-controller,router-outlet,router-link}.ts` (modified) + their test files (07-03)
- `tools/dev-warning-strip/src/warn-entry.ts` expanded to cover router; `scripts/dev-warning-strip.mjs` expanded with a negative control + phase-wide scope guard (07-04)

No changes are made to `@willramdev/query`, `@willramdev/forms`, or `@willramdev/store` anywhere in this phase — RESEARCH's Silent-Gap Audit found no silent misuse surface in any of the three (D-04).
</artifacts_produced>

<output>
Create `.planning/phases/07-dev-gate-prod-stripped-dev-warnings/07-01-SUMMARY.md` when done.
</output>
