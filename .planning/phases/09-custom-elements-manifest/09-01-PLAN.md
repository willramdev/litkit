---
phase: 09-custom-elements-manifest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/forms/custom-elements-manifest.config.mjs
  - packages/forms/src/lit-form.ts
  - packages/forms/package.json
  - packages/forms/custom-elements.json
  - packages/forms/vscode.html-custom-data.json
  - packages/forms/vscode.css-custom-data.json
  - packages/forms/web-types.json
  - .gitattributes
  - tools/cem-check/known-tags.json
  - tools/cem-check/assert-tags.mjs
  - .github/workflows/ci.yml
  - .vscode/settings.json
autonomous: true
requirements: [CEM-01, CEM-02, CEM-03, CEM-04]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # CEM-01 edge coverage (idempotency, concurrency) — resolved explicit
    - "Re-running `npm run build` in an element package regenerates a byte-identical `custom-elements.json` (deterministic, source-derived), so `git diff --exit-code` stays green on a no-source-change rebuild (CEM-01 idempotency)."
    - "Each package's `cem analyze` reads only that package's own `src/**` and writes only that package's root artifacts — no shared mutable state across packages, so per-package builds are independent whether run serially or in parallel (CEM-01 concurrency)."
    # CEM-02 edge coverage (adjacency, empty, ordering, concurrency) — resolved explicit
    - "`npm pack --dry-run -w @willramdev/forms` lists `custom-elements.json`, `vscode.html-custom-data.json`, `vscode.css-custom-data.json`, and `web-types.json` alongside `dist/` — the `files` allowlist and the `customElements`/`web-types` fields agree, no missing or colliding entry (CEM-02 adjacency)."
    - "A single-element package (forms → `lit-form`) still produces a valid, non-empty manifest with exactly one `customElement` declaration that ships in the tarball (CEM-02 empty/single-element input)."
    - "The analyzer emits declarations in a stable, deterministic order across runs, which is what lets the committed manifest serve as a byte-stable freshness baseline (CEM-02 ordering)."
    - "The `files`/`customElements`/`web-types` packaging contract is evaluated per package at pack time and does not depend on any other package's build completing (CEM-02 concurrency)."
    # CEM-03 edge coverage (adjacency, empty, ordering, idempotency, concurrency) — resolved explicit
    - "The tag-set gate asserts EQUALITY (not subset): a leaked demo/example tag fails CI exactly as loudly as a missing real tag (CEM-03 adjacency/equality)."
    - "A hollow declaration (empty `tagName`) is absent from the extracted tag set, so `assert-tags.mjs` reports `got` shorter than `want` and fails (CEM-03 empty/hollow)."
    - "`assert-tags.mjs` sorts both the generated and the known tag lists before comparing, so the equality check is order-independent and stable (CEM-03 ordering)."
    - "Re-running the build leaves each manifest's `tagName` set unchanged, so the completeness gate is stable across repeated runs (CEM-03 idempotency)."
    - "The completeness gate reads each committed manifest from disk after the build and makes no cross-package assumption beyond the committed `known-tags.json` contract (CEM-03 concurrency)."
    # phase truths
    - "Building `@willramdev/forms` regenerates `packages/forms/custom-elements.json` whose `tagName` set equals exactly `{lit-form}` (CEM-01/CEM-03)."
    - "The forms manifest carries the enriched `native-validation` attribute and a default slot from additive JSDoc (D-04 richness)."
    - "`.gitattributes` LF pins keep the committed JSON artifacts byte-identical between the Windows author and the ubuntu-latest CI runner, so the freshness gate never false-fails on CRLF."
    - "The freshness gate (`git add -A` + `git diff --cached --exit-code`) and the tag-set completeness gate (`node tools/cem-check/assert-tags.mjs`) both run in the read-only `ci.yml` gate job with `permissions: contents: read` unchanged (D-06/D-09/D-12)."
  prohibitions:
    - "MUST NOT change any runtime element behavior or the v1.0 public API — every CEM change is additive JSDoc, config, generated JSON, or build/CI wiring (no signature, field, decorator, or render change)."
    - "MUST NOT let any demo/example element tag enter a shipped manifest (demo-form, lit-query-demo-app, lit-query-demo-surface, example-app, page-*, my-element)."
  artifacts:
    - packages/forms/custom-elements-manifest.config.mjs
    - packages/forms/custom-elements.json
    - packages/forms/vscode.html-custom-data.json
    - packages/forms/vscode.css-custom-data.json
    - packages/forms/web-types.json
    - tools/cem-check/assert-tags.mjs
    - tools/cem-check/known-tags.json
    - .vscode/settings.json
  key_links:
    - "forms `customElements`/`web-types` package.json fields ↔ `files` allowlist entries ↔ committed artifact filenames (mismatch ships an autocomplete-less success — Anti-Pattern 5)."
    - "`.gitattributes` `eol=lf` pins MUST land before the first artifact commit (CRLF drift would false-fail the freshness gate on Windows→Ubuntu)."
    - "analyzer `packagejson: false` + JetBrains plugin `packageJson: false` — else the analyzer rewrites package.json mid-CI and trips the freshness gate."
---

<objective>
Establish Custom Elements Manifest generation end-to-end for the simplest element package (`@willramdev/forms`, one real-decorator element `lit-form`) and stand up the shared freshness + completeness gate infrastructure that all three element packages will reuse.

This is the phase TRACER: one element package taken through every layer — additive JSDoc on the element source, a per-package analyzer config, `cem analyze` chained into `build`, committed manifest + editor-data artifacts, the `customElements`/`web-types` discovery fields + `files` allowlist, LF pins, the tag-set equality gate, and the read-only CI wiring — proven working before `query` (09-02) and `router` (09-03) expand out from it.

Purpose: de-risk the whole phase on the easiest package first. `forms` uses the real `@customElement('lit-form')` decorator, so `tagName` resolves automatically (no `define()`-wrapper gap) — the only novel risks here are the two-plugin editor-data config, the packaging contract, and the LF-stable freshness gate. Router's harder `@tag` work is deliberately deferred to 09-03.
Output: a green forms manifest + VS Code custom-data + JetBrains web-types shipped in the tarball, plus `tools/cem-check/*`, `.gitattributes` pins, `.vscode/settings.json`, and two new `ci.yml` gate steps.
</objective>

## Artifacts this phase produces (created by this plan)

New, this plan (excluded from source-grounding drift verification — all net-new):
- `packages/forms/custom-elements-manifest.config.mjs` — per-package analyzer config (D-02).
- `packages/forms/custom-elements.json` — generated CEM manifest, committed (D-05).
- `packages/forms/vscode.html-custom-data.json`, `packages/forms/vscode.css-custom-data.json` — VS Code custom-data (D-07).
- `packages/forms/web-types.json` — JetBrains web-types (D-07).
- `tools/cem-check/assert-tags.mjs` — tag-set EQUALITY gate script (D-09).
- `tools/cem-check/known-tags.json` — committed known-tag contract (D-09), forms row only in this plan.
- `.vscode/settings.json` — repo-local `html.customData` dogfood config (D-08).
- New package.json fields on `@willramdev/forms`: `customElements`, `web-types`, four `files` entries, a `cem` script, `cem` chained into `build`, three new devDeps.
- New `.gitattributes` `eol=lf` pins for the three artifact globs.
- New `ci.yml` steps: CEM freshness gate + CEM completeness gate.
- Additive JSDoc (`@attr`/`@prop`/`@slot`) on `packages/forms/src/lit-form.ts`.

## Flagged Assumptions (edge probe — no silent drops)

- **[FLAGGED — CEM-04 edge probe returned category `unclassified`]** Editor-autocomplete coverage is verified in-phase only by asserting each package's `vscode.html-custom-data.json`, `vscode.css-custom-data.json`, and `web-types.json` exist and are non-empty after build. Whether every attribute/event/slot renders live in a real VS Code / JetBrains session is NOT machine-verifiable here; it is accepted as manually spot-checkable via the `.vscode/settings.json` in-repo dogfooding (D-08). Surfaced, not dropped.

<assumption_delta_decision>
- **Primary noun:** editor-data plugin (the tool that turns the manifest into IDE autocomplete data).
- **Transition:** singular → plural. D-07/CONTEXT assumed ONE plugin (`custom-element-vs-code-integration`) emits both VS Code custom-data AND JetBrains web-types; RESEARCH Assumption A1 corrected this — two separate plugins are required.
- **Decision:** `add-alongside`. Add `custom-element-jet-brains-integration` (`customElementJetBrainsPlugin` → `web-types.json`) alongside `custom-element-vs-code-integration` (`customElementVsCodePlugin` → `vscode.*-custom-data.json`); both run in the same per-package `custom-elements-manifest.config.mjs`. D-07's two-artifact OUTCOME is preserved; only the mechanism (two devDeps, not one) changed.
- **Rationale:** verified against npm + the packages' repos (RESEARCH Package Legitimacy Audit / Pattern 4). Advisory, non-blocking.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-custom-elements-manifest/09-CONTEXT.md
@.planning/phases/09-custom-elements-manifest/09-RESEARCH.md
@.planning/phases/09-custom-elements-manifest/09-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end CEM for @willramdev/forms — one package through every layer</name>
  <files>packages/forms/custom-elements-manifest.config.mjs, packages/forms/src/lit-form.ts, packages/forms/package.json, packages/forms/custom-elements.json, packages/forms/vscode.html-custom-data.json, packages/forms/vscode.css-custom-data.json, packages/forms/web-types.json, .gitattributes</files>
  <read_first>
    - packages/forms/src/lit-form.ts (the element to annotate — real `@customElement('lit-form')` at line 14; `static properties = { form, nativeValidation: { type: Boolean, attribute: 'native-validation' } }` at lines 16-19; default `<slot>` at line 94)
    - packages/forms/package.json (current `files` at lines 36-41, `build` at line 44, `devDependencies` at lines 60-67 — match the existing alphabetized order)
    - .gitattributes (the single existing `tools/type-snapshots/** text eol=lf` pin — mirror its shape and rationale)
    - .planning/phases/09-custom-elements-manifest/09-RESEARCH.md (§Installation exact devDep versions; §Pattern 1 config shape; §Pattern 2 build chain; §Pattern 4 package.json fields; §Code Examples the verbatim lit-form JSDoc; §Common Pitfalls 3 & 4)
    - .planning/phases/09-custom-elements-manifest/09-PATTERNS.md (the config.mjs analog `tools/type-snapshots.config.mjs` header-comment discipline; the package.json MODIFY block; the lit-form JSDoc analog)
    - tools/type-snapshots.config.mjs (the `.mjs` tooling header-comment + LF-only convention to match)
  </read_first>
  <action>
    Install the three devDeps into `@willramdev/forms` only (pinned): `@custom-elements-manifest/analyzer@0.11.0`, `custom-element-vs-code-integration@1.5.0`, `custom-element-jet-brains-integration@1.7.0` (D-01/D-03, RESEARCH Installation block). Add them to the package's `devDependencies` in existing alphabetized order.

    Create `packages/forms/custom-elements-manifest.config.mjs` (D-02) as an ESM `export default {}` object matching RESEARCH §Pattern 1: `globs: ['src/**/*.ts']`, `exclude: ['src/**/*.test.ts']` (forms' `demo/demo-form.ts` is OUTSIDE `src/`, so it is never matched — no extra exclude needed, D-10), `outdir: '.'` (package root, D-05), `litelement: true` (reads the `@customElement` decorator AND `static properties` — Pitfall 4), `packagejson: false` (D-07/Open Q4 — do NOT let the analyzer rewrite package.json). Import and register both editor plugins in `plugins`: `customElementVsCodePlugin({ outdir: '.' })` and `customElementJetBrainsPlugin({ outdir: '.', packageJson: false })`. Add a header comment mirroring the `tools/type-snapshots.config.mjs` discipline (what/why + the LF-pin note). After `npm i`, confirm the two plugins' exported names and default output filenames against their installed READMEs (RESEARCH Assumption A2) before relying on them.

    Add additive JSDoc ONLY to `packages/forms/src/lit-form.ts` (D-04) — the class already uses the real decorator so NO `@tag` is needed. Use the verbatim facts from RESEARCH §Code Examples: `@attr` for the `native-validation` boolean attribute (default false), `@prop form` (the FormInstance driving submit/reset), and `@slot` for the default slot wrapping the user-authored `<form>`. Comments only — no signature/field/decorator/render change (`erasableSyntaxOnly` unaffected).

    Modify `packages/forms/package.json` (D-03/D-05, RESEARCH §Pattern 2 + §Pattern 4): add a `"cem": "cem analyze"` script; append `&& npm run cem` to the existing `"build": "vite build && tsc -p tsconfig.build.json"`; add `"customElements": "./custom-elements.json"` and `"web-types": "./web-types.json"` top-level fields; extend `files` with `custom-elements.json`, `vscode.html-custom-data.json`, `vscode.css-custom-data.json`, `web-types.json`. Set the fields BY HAND (the analyzer/plugin will not, because both `packagejson`/`packageJson` are `false`).

    Extend `.gitattributes` with three `text eol=lf` pins (mirroring the existing `tools/type-snapshots/**` pin — Pitfall 3): `packages/*/custom-elements.json`, `packages/*/vscode.*-custom-data.json`, `packages/*/web-types.json`. These globs cover all three element packages from the start so 09-02/09-03 need not touch `.gitattributes`. Add the pins BEFORE running the build so the first committed artifacts are LF-normalized.

    Run `npm run build -w @willramdev/forms` to generate the four artifacts at the forms package root, then commit them.
  </action>
  <verify>
    <automated>npm run build -w @willramdev/forms && node -e "const m=require('./packages/forms/custom-elements.json');const t=(m.modules||[]).flatMap(x=>x.declarations||[]).filter(d=>d.customElement&&d.tagName).map(d=>d.tagName).sort();if(JSON.stringify(t)!==JSON.stringify(['lit-form']))throw new Error('tagName set '+JSON.stringify(t));const s=JSON.stringify(m);if(!s.includes('native-validation'))throw new Error('missing native-validation attr');for(const f of ['vscode.html-custom-data.json','vscode.css-custom-data.json','web-types.json']){const fs=require('fs');if(!fs.statSync('packages/forms/'+f).size)throw new Error('empty '+f)}console.log('forms CEM OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/forms/custom-elements.json` exists and its `customElement` `tagName` set equals exactly `["lit-form"]`.
    - The forms manifest JSON contains the string `native-validation` (D-04 richness present, Pitfall 4 confirmed — `static properties` was read).
    - `packages/forms/vscode.html-custom-data.json`, `packages/forms/vscode.css-custom-data.json`, and `packages/forms/web-types.json` all exist and are non-empty.
    - `npm pack --dry-run -w @willramdev/forms` output lists all four JSON artifacts plus `dist/`.
    - `git status --porcelain packages/forms/*.json` is clean immediately after a second `npm run build -w @willramdev/forms` (byte-stable, LF-pinned).
    - `packages/forms/package.json` has `"customElements": "./custom-elements.json"`, `"web-types": "./web-types.json"`, the four new `files` entries, a `cem` script, and `&& npm run cem` on `build`; `lit-form.ts` diff is comment-only.
  </acceptance_criteria>
  <reversibility rating="costly">D-05: the `customElements`/`web-types` fields + `files` entries + root output location are the tooling discovery contract. Moving to `dist/` later means changing the fields, the `files` entries, editor expectations, and re-verifying `npm pack` together — costly but fully reversible (no data loss), so no decision checkpoint.</reversibility>
  <done>Building `@willramdev/forms` produces a committed, LF-stable, enriched `custom-elements.json` (`tagName` == `{lit-form}`) plus VS Code custom-data + web-types, all shipped in the tarball via agreeing `customElements`/`web-types` fields and `files` entries.</done>
</task>

<task type="auto">
  <name>Task 2: Shared completeness + freshness gates, CI wiring, and repo-local dogfooding</name>
  <files>tools/cem-check/known-tags.json, tools/cem-check/assert-tags.mjs, .github/workflows/ci.yml, .vscode/settings.json</files>
  <read_first>
    - tools/type-snapshots.config.mjs (the `node:fs` `readFileSync` + `process.exit` + repo-root-anchored + header-comment convention for `tools/*.mjs` gate scripts)
    - .github/workflows/ci.yml (the `gate` job at lines 39-98; the existing type-snapshot gate at lines 65-76 — copy the `git add -A` + `git diff --cached --exit-code` shape exactly; the `permissions: contents: read` at lines 13-14 that MUST stay unchanged; `npm run build` at line 52)
    - .planning/phases/09-custom-elements-manifest/09-RESEARCH.md (§Pattern 5 the exact ci.yml step yaml; §Pattern 6 the `assert-tags.mjs` script + `known-tags.json` shape; §Architecture diagram the discovery contracts)
    - .planning/phases/09-custom-elements-manifest/09-PATTERNS.md (the `assert-tags.mjs` + `known-tags.json` + `ci.yml` + `.vscode/settings.json` analog assignments)
  </read_first>
  <action>
    Create `tools/cem-check/known-tags.json` (D-09) as the committed known-tag contract. In THIS plan it lists ONLY the forms row: `{ "forms": ["lit-form"] }`. 09-02 appends the `query` row and 09-03 appends the `router` row — the file grows per package (this is why those plans serialize on it).

    Create `tools/cem-check/assert-tags.mjs` (D-09) per RESEARCH §Pattern 6, matching the `tools/type-snapshots.config.mjs` node-script convention (`import { readFileSync } from 'node:fs'`, repo-root reads, exit-code signaling, header comment). For each `[pkg, tags]` in `known-tags.json`: read `packages/${pkg}/custom-elements.json`, extract the sorted `tagName` set of declarations where `d.customElement && d.tagName`, and compare with `JSON.stringify(got) === JSON.stringify([...tags].sort())` (sort both sides — order-independent EQUALITY, not subset). On any mismatch print `[cem-check] ${pkg}: expected … got …` and `process.exit(1)`; else `process.exit(0)`. Do NOT tolerate a missing manifest file — a throw there is a real failure. Equality catches both a missing real tag (hollow) and a leaked demo/example tag.

    Add two steps to the `ci.yml` `gate` job AFTER the existing `npm run build` (RESEARCH §Pattern 5), pathspec-scoped to the three artifact globs so unrelated build output never enters the gate:
    - "CEM freshness gate": `git add -A -- 'packages/*/custom-elements.json' 'packages/*/vscode.*-custom-data.json' 'packages/*/web-types.json'` then `git diff --cached --exit-code --` the same pathspec (staging first so a new UNTRACKED artifact also fails, mirroring the WR-01 hardening on the type-snapshot gate).
    - "CEM completeness gate": `node tools/cem-check/assert-tags.mjs`.
    Keep the top-level `permissions: contents: read` unchanged; do NOT touch `release.yml` (D-12). Add explanatory comments in the same voice as the surrounding type-snapshot gate comments.

    Create `.vscode/settings.json` (D-08, no in-repo analog) with `html.customData` referencing all three packages' VS Code HTML custom-data files: `packages/forms/vscode.html-custom-data.json`, `packages/query/vscode.html-custom-data.json`, `packages/router/vscode.html-custom-data.json`. Listing all three now (a missing file is silently ignored by VS Code and this file is NOT under the freshness gate) means 09-02/09-03 need not edit it. This is repo-local dogfooding only — not committed-artifact, not gated.
  </action>
  <verify>
    <automated>npm run build -w @willramdev/forms && node tools/cem-check/assert-tags.mjs && git add -A -- 'packages/*/custom-elements.json' 'packages/*/vscode.*-custom-data.json' 'packages/*/web-types.json' && git diff --cached --exit-code -- 'packages/*/custom-elements.json' 'packages/*/vscode.*-custom-data.json' 'packages/*/web-types.json' && node -e "const c=require('fs').readFileSync('.github/workflows/ci.yml','utf8');if(!c.includes('CEM freshness gate')||!c.includes('assert-tags.mjs'))throw new Error('ci steps missing');if(!/permissions:\s*\n\s*contents:\s*read/.test(c))throw new Error('permissions widened');const s=require('./.vscode/settings.json');if(!(s['html.customData']||[]).some(p=>p.includes('forms/vscode.html-custom-data.json')))throw new Error('vscode dogfood missing');console.log('gate infra OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/cem-check/assert-tags.mjs` exits 0 against the forms manifest (tag-set EQUALITY holds for `{lit-form}`).
    - After `npm run build`, `git add -A -- <artifact globs>` + `git diff --cached --exit-code -- <artifact globs>` exits 0 (no drift).
    - `.github/workflows/ci.yml` contains a "CEM freshness gate" step (`git add -A` + `git diff --cached --exit-code`, pathspec-scoped) and a "CEM completeness gate" step (`node tools/cem-check/assert-tags.mjs`), both in the `gate` job after `npm run build`.
    - `ci.yml` still declares `permissions: contents: read` (unchanged, not widened); `.github/workflows/release.yml` is byte-for-byte untouched.
    - `.vscode/settings.json` `html.customData` references all three `packages/*/vscode.html-custom-data.json` paths.
    - Negative check: after manually editing `packages/forms/custom-elements.json` by hand, the freshness gate command exits non-zero (proves the gate detects tampering/drift).
  </acceptance_criteria>
  <done>The forms slice is green in the read-only CI gate: `assert-tags.mjs` enforces tag-set equality, the `git diff` freshness gate enforces byte-stability, and `.vscode/settings.json` wires in-repo autocomplete — all reusable unchanged by 09-02/09-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → devDependency install | Third-party analyzer + two editor plugins enter the build toolchain |
| Committed static JSON → published tarball | Generated manifest/editor-data shipped to consumers; hand-tampering is possible |
| CI workflow token scope | Gate steps run under the read-only `contents: read` token |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-SC | Tampering | 3 new devDeps (analyzer + VS Code + JetBrains plugins) | medium | mitigate | Pin exact versions `@custom-elements-manifest/analyzer@0.11.0`, `custom-element-vs-code-integration@1.5.0`, `custom-element-jet-brains-integration@1.7.0`; RESEARCH Package Legitimacy Audit passed (real repos, healthy downloads, zero `postinstall`) — all [VERIFIED], no blocking human checkpoint required |
| T-09-01 | Tampering | `ci.yml` gate steps | low | mitigate | Keep freshness + completeness gates in read-only `ci.yml`; never widen `permissions: contents: read`; `release.yml` untouched (D-12) |
| T-09-02 | Information disclosure | committed `custom-elements.json` | low | accept | Manifest describes litkit's own elements only; externalized peer types (`lit`/`@tanstack`) stay opaque type strings, never inlined (PITFALLS §6) |
| T-09-03 | Tampering | committed manifest/editor-data JSON | low | mitigate | `build` regenerates artifacts + `git diff --cached --exit-code` fails on any hand-edit drift (D-06) |
| T-09-04 | Elevation of privilege | analyzer/plugin postinstall | low | mitigate | RESEARCH verified none of the three declare a `postinstall` script (`npm view … scripts.postinstall` empty for all) |

No high-severity threat present (block-on = high) — nothing in this plan blocks.
</threat_model>

<verification>
- `npm run build -w @willramdev/forms` regenerates all four forms artifacts deterministically.
- `node tools/cem-check/assert-tags.mjs` exits 0 (forms tag-set equality).
- `git add -A -- <artifact globs>` + `git diff --cached --exit-code -- <artifact globs>` exits 0 (freshness).
- `npm pack --dry-run -w @willramdev/forms` lists the four artifacts.
- `ci.yml` carries both gate steps with `permissions: contents: read` intact; `release.yml` untouched.
</verification>

<success_criteria>
- CEM-01: `packages/forms/custom-elements.json` generated via the analyzer, chained into `build`.
- CEM-02: forms declares `customElements` + `web-types` fields and ships the manifest via `files` (npm pack confirms).
- CEM-03: tag-set equality gate green for `{lit-form}`; freshness gate green — the shared `assert-tags.mjs` + `ci.yml` infra exists.
- CEM-04: VS Code custom-data + web-types emitted for forms and referenced via `.vscode/settings.json` + `web-types` field.
</success_criteria>

<output>
Create `.planning/phases/09-custom-elements-manifest/09-01-SUMMARY.md` when done.
</output>
