---
phase: 10-examples-integration-app
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - examples/src/views/data-view.ts
  - examples/src/views/form-view.ts
  - examples/src/router.ts
  - examples/src/main.ts
  - examples/src/app.ts
autonomous: true
requirements: [EXPL-01, EXPL-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Navigating to `/data` renders `<data-view>`, which provides one `QueryClient` via `<lit-query-client-provider>` and reads it back through a nested `QueryController` (`.result.data`) — the query seam proves out end-to-end (EXPL-01)."
    - "Navigating to `/form` renders `<form-view>`, whose `createForm`-backed `<lit-form>` binds inputs via `bind()`/`field()` and validates with `required()`/`email()`/`minLength()` — the forms seam proves out end-to-end (EXPL-01)."
    - "All three routes (`/`, `/data`, `/form`) resolve because `home-view`, `data-view`, and `form-view` are all side-effect-imported in `main.ts` BEFORE `document.body.appendChild(document.createElement('examples-app'))` runs — no route silently renders nothing."
    - "With all four seams' dependencies bundled (`@tanstack/query-core`, `@tanstack/form-core`, `lit`), `node scripts/check-single-instance.mjs` still reports exactly one version each — the dedup canary holds under full load, not just the tracer's single-seam case (EXPL-02)."
  prohibitions:
    - "MUST NOT reintroduce `build.lib`/`external` into `examples/vite.config.ts` while wiring the new seams — would defeat the EXPL-02 dedup canary established in 10-01."
  artifacts:
    - examples/src/views/data-view.ts
    - examples/src/views/form-view.ts
  key_links:
    - "`main.ts` MUST import `'@willramdev/query'` and `'@willramdev/forms'` for their bare-import side effects (registers `<lit-query-client-provider>` and `<lit-form>`) — without them the new views fail to render even though `data-view`/`form-view` themselves are defined."
    - "`router.ts`'s two new `defineRoutes` entries (`/data` → `data-view`, `/form` → `form-view`) must use the exact tag strings the two new files pass to `define()` — a mismatch renders nothing with only a console warning, no build error."
    - "`app.ts`'s two new `<router-link to=\"/data\">`/`<router-link to=\"/form\">` use `to`, not `href` (same `router-link.ts:35-36` contract as 10-01)."
---

<objective>
Expand the examples app from 10-01's one-route tracer to full four-seam coverage by copying the query and forms wiring straight from their proven in-repo demos: `<data-view>` (query seam, provider + `QueryController`) and `<form-view>` (forms seam, `createForm` + `<lit-form>` + `bind()`/`field()`), then wiring both into the same `router.ts`/`main.ts`/`app.ts` navigation shell 10-01 created.

Purpose: query and forms are lower-risk copies of `packages/query/src/demo.ts` and `packages/forms/demo/demo-form.ts` — both APIs are already exercised working code in-repo. The remaining risk is purely wiring (route table, side-effect import order, nav links), not API discovery, so this plan can safely reuse 10-01's proven shell.
Output: EXPL-01 fully satisfied — all four seams (router, query, forms, store) reachable and rendering through the shared shell; EXPL-02's dedup canary re-confirmed with the full dependency surface bundled in.
</objective>

## Artifacts this phase produces (created by this plan)

New, this plan:
- `examples/src/views/data-view.ts` — query seam (`<lit-query-client-provider>` + `QueryController`).
- `examples/src/views/form-view.ts` — forms seam (`createForm` + `<lit-form>` + `bind()`/`field()`).

Edits (extend 10-01's files, no new files): `examples/src/router.ts` (two more routes), `examples/src/main.ts` (two more side-effect imports + view imports), `examples/src/app.ts` (two more nav links).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-examples-integration-app/10-RESEARCH.md
@.planning/phases/10-examples-integration-app/10-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Query + forms seam view components</name>
  <files>examples/src/views/data-view.ts, examples/src/views/form-view.ts</files>
  <read_first>
    - packages/query/src/demo.ts (lines 1-32, 74-93) — the provider/consumer split pattern (`createQueryClient`, `<lit-query-client-provider .client=>`, `QueryController` direct instantiation) and the mock async data-source shape this view mirrors.
    - packages/query/src/index.ts (lines 22-29, 56-73) — `createQueryClient(config?)`, `QueryController` export.
    - packages/query/src/query-client-provider.ts (lines 14-24) — `<lit-query-client-provider>`'s `.client` property (attribute:false, defaults to `createQueryClient()`), registered via `@customElement` decorator (bare-import side effect).
    - packages/forms/demo/demo-form.ts (lines 1-73) — `createForm(host, config)`, `<lit-form .form=>`, `bind()`/`field()` usage, validator shapes.
    - packages/forms/src/index.ts (lines 6-32) — `createForm`, `bind`, `field`, `required`/`email`/`minLength` exports; `LitForm` export (registers `<lit-form>`).
    - .planning/phases/10-examples-integration-app/10-PATTERNS.md — the `data-view.ts`/`form-view.ts` pattern assignments (provider/consumer split; forms core pattern).
    - examples/src/views/home-view.ts (from 10-01) — the `KitElement`/`define()` conventions this file's sibling views follow.
  </read_first>
  <action>
    Create `examples/src/views/data-view.ts`: a raw-`LitElement` inner class `DataSurface` (mirrors `demo.ts`'s direct `QueryController` instantiation, not a factory) with a private field `private readonly todosQuery = new QueryController(this, () => ({ queryKey: ['todos'] as const, queryFn: fetchTodos }))`, rendering `this.todosQuery.result.status` and `.data`; a local `fetchTodos()` returning a small in-memory array (mirrors `demo.ts`'s mock `todoStore` — no real network call, matching this phase's `COVERAGE.md` no-external-API scope); register via `define('data-surface', DataSurface)`. A module-level `const client = createQueryClient({ defaultOptions: { queries: { staleTime: 15_000 } } })`. An outer `KitElement` route-target class `DataView` whose `render()` returns `<lit-query-client-provider .client=${client}><data-surface></data-surface></lit-query-client-provider>`; register via `define('data-view', DataView)`.

    Create `examples/src/views/form-view.ts`: a `KitElement` route-target class `FormView` with a `form = createForm(this, { initialValues: { email: '', password: '' }, validators: { email: [required(), email()], password: [required(), minLength(8)] }, onSubmit: async ({ value }) => console.log('Submitted:', value) })` field; `render()` returns `<lit-form .form=${this.form}>` wrapping a `<form>` with two `bind()`-directive inputs and `field()`-rendered error text (copy the exact directive-usage shape from `demo-form.ts:39-58` — attribute-position `${bind('email')}` on the `<input>`, `field('email', (f) => f.error ? html\`…\` : '')` immediately after); register via `define('form-view', FormView)`.

    Do NOT wire these into `router.ts`/`main.ts`/`app.ts` yet — that is Task 2, so this task's files stay isolated and independently typecheckable.
  </action>
  <verify>
    <automated>npm run typecheck -w examples</automated>
  </verify>
  <acceptance_criteria>
    - `examples/src/views/data-view.ts` defines both `data-view` and `data-surface` via `define()` imported from `@willramdev/kit`.
    - `examples/src/views/form-view.ts` defines `form-view` via `define()`.
    - `npm run typecheck -w examples` (runs `tsc --noEmit` over all of `examples/src`) exits 0 — both new files type-check cleanly against the built `dist/index.d.ts` of `@willramdev/query` and `@willramdev/forms`.
    - Neither file uses a constructor parameter property (`erasableSyntaxOnly` compliance — explicit class fields only, assigned as field initializers).
  </acceptance_criteria>
  <done>Both the query-seam (`data-view`/`data-surface`) and forms-seam (`form-view`) components exist, type-check cleanly against the real package `dist/` types, and are ready to be wired into navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Wire query and forms seams into navigation, full-suite re-verify</name>
  <files>examples/src/router.ts, examples/src/main.ts, examples/src/app.ts</files>
  <read_first>
    - examples/src/router.ts, examples/src/main.ts, examples/src/app.ts (as created in 10-01 — current single-route state).
    - packages/router/src/router-core/routes.ts (lines 10-20) — `defineRoutes()` accepts an array; adding two more `{ path, component }` entries is purely additive.
    - packages/router/src/router-lit/router-outlet.ts (lines 177-188) — the silent-warn-and-render-nothing contract this task must not trip for the two new routes.
    - scripts/check-single-instance.mjs (from 10-01) — re-run after bundling two more upstream deps (`@tanstack/query-core`, `@tanstack/form-core`) to prove dedupe still holds under full load.
  </read_first>
  <action>
    Edit `examples/src/router.ts`: extend the `defineRoutes([...])` array with two more entries — `{ path: '/data', component: 'data-view' }` and `{ path: '/form', component: 'form-view' }` — alongside the existing `/` → `home-view` entry.

    Edit `examples/src/main.ts`: add side-effect imports `'@willramdev/query'` (registers `<lit-query-client-provider>`) and `'@willramdev/forms'` (registers `<lit-form>`) alongside the existing `'@willramdev/router'` import, and add `'./views/data-view.ts'` + `'./views/form-view.ts'` alongside the existing `'./views/home-view.ts'` import — ALL view imports must run before the `document.body.appendChild(document.createElement('examples-app'))` call at the bottom.

    Edit `examples/src/app.ts`: add `<router-link to="/data">Data</router-link>` and `<router-link to="/form">Form</router-link>` inside the existing `<nav>`, alongside the `/` link (same `to=` attribute contract, not `href=`).

    Run the full build + canary chain: `npm run build` (root), `npm run build -w examples`, `node scripts/check-single-instance.mjs`.
  </action>
  <verify>
    <automated>npm run build && npm run build -w examples && node scripts/check-single-instance.mjs && node -e "const fs=require('fs');const dir='examples/dist/assets';const bundle=fs.readdirSync(dir).filter(f=>f.endsWith('.js')).map(f=>fs.readFileSync(dir+'/'+f,'utf8')).join('');for(const tag of ['home-view','data-view','data-surface','form-view']){if(!bundle.includes(tag))throw new Error('missing tag in bundle: '+tag)}console.log('full-suite OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `examples/src/router.ts`'s `defineRoutes([...])` array has exactly three entries: `/`→`home-view`, `/data`→`data-view`, `/form`→`form-view`.
    - `examples/src/main.ts` imports `'@willramdev/router'`, `'@willramdev/query'`, `'@willramdev/forms'`, and all three `./views/*.ts` files, with every import preceding the `appendChild` call.
    - `examples/src/app.ts` contains three `<router-link to=` entries (`/`, `/data`, `/form`) — none uses `href=`.
    - `npm run build` then `npm run build -w examples` both exit 0.
    - `node scripts/check-single-instance.mjs` exits 0 (single-instance canary holds with all four seams' dependencies bundled).
    - The built `examples/dist/assets/*.js` bundle contains all four tag-name strings: `home-view`, `data-view`, `data-surface`, `form-view`.
  </acceptance_criteria>
  <reversibility rating="reversible">The route table, import, and nav-link additions are pure additive wiring — trivially removable with no data or schema footprint.</reversibility>
  <done>All three routes (`/`, `/data`, `/form`) are reachable and render their respective seam (store, query, forms) through the shared router shell; the app builds cleanly against the real built packages; the single-instance dedup canary still holds with the full four-seam dependency surface bundled in — EXPL-01 is fully satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| examples app (local dev only) ↔ developer's browser | Unchanged from 10-01 — no network-exposed service, never deployed |
| form input fields ↔ `@willramdev/forms` validators | Demonstration-only local input, no server round-trip |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02 | Information Disclosure | accidental publish of the app | medium | mitigate | Same `private:true` + `.changeset/config.json` `ignore` from 10-01, unmodified by this plan |
| T-10-04 | Tampering | duplicate-version drift from newly-bundled `@tanstack/form-core` | low | mitigate | `check-single-instance.mjs` re-run in Task 2 verify explicitly covers the newly-bundled package |
| T-10-05 | Information Disclosure | form demo logs submitted values to `console.log` | low | accept | Local dev-only demo data typed by the developer testing the app; never persisted, never sent over the network, app never deployed |

No high-severity threat present (block-on = high) — nothing in this plan blocks.
</threat_model>

<verification>
- All three routes render their respective seam (store, query, forms).
- `npm run build` + `npm run build -w examples` exit 0.
- `node scripts/check-single-instance.mjs` exits 0 with the full four-seam dependency surface bundled.
- The built bundle contains all four view/element tag strings.
</verification>

<success_criteria>
- EXPL-01: fully satisfied — all four seams (router, query, forms, store) exercised end-to-end.
- EXPL-02: re-confirmed under full dependency load.
</success_criteria>

<output>
Create `.planning/phases/10-examples-integration-app/10-02-SUMMARY.md` when done.
</output>