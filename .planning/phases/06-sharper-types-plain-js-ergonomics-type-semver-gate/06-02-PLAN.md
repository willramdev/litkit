---
phase: 06-sharper-types-plain-js-ergonomics-type-semver-gate
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - tools/type-snapshots.config.mjs
  - tools/type-snapshots/store.d.ts
  - tools/type-snapshots/query.d.ts
  - tools/type-snapshots/forms.d.ts
  - tools/type-snapshots/forms-zod.d.ts
  - tools/type-snapshots/router.d.ts
  - tools/type-snapshots/router-core.d.ts
  - tools/type-snapshots/router-lit.d.ts
autonomous: true
requirements: [TYPE-02]
estimate:
  tokens: 31000
  raw_tokens: 19000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The committed snapshot covers all five packages and every subpath export — 8 flattened .d.ts files total: kit, store, query, forms, forms-zod, router, router-core, router-lit (D-11 subpath coverage)."
    - "Each generated snapshot's symbol set matches its package's src/index.ts (and subpath entry) re-exports — no known export is missing and no sibling-package src is resolved by mistake (Pitfall 3)."
    - "On an unchanged source tree, `npm run type-snapshot` leaves all 8 files with a clean `git diff --exit-code tools/type-snapshots/` (exit 0) after the full expansion."
  artifacts:
    - tools/type-snapshots/store.d.ts
    - tools/type-snapshots/query.d.ts
    - tools/type-snapshots/forms.d.ts
    - tools/type-snapshots/forms-zod.d.ts
    - tools/type-snapshots/router.d.ts
    - tools/type-snapshots/router-core.d.ts
    - tools/type-snapshots/router-lit.d.ts
    - tools/type-snapshots.config.mjs (expanded to 8 entries)
  key_links:
    - "forms ./zod subpath -> packages/forms/src/zod.ts -> tools/type-snapshots/forms-zod.d.ts"
    - "router ./core -> packages/router/src/router-core/index.ts -> router-core.d.ts; router ./lit -> packages/router/src/router-lit/index.ts -> router-lit.d.ts"
  prohibitions:
    - "Do not modify ci.yml — the directory-wide git diff gate from 06-01 already covers every new snapshot file."
    - "Do not commit the whole per-module dist/**/*.d.ts tree — one flattened file per public entry only (D-09)."
    - "Do not edit any public factory signature in store/forms/query/router — snapshot generation is verify-only for shape (TYPE-01 is a separate verify-only plan)."
    - "Do not commit a snapshot regenerated with a different TypeScript version than the lockfile-pinned 6.0.3 (emit-format drift, Pitfall 5)."
---

<objective>
Expand the proven type-SemVer gate from kit (Plan 01) to the four sibling packages and every subpath export, producing the remaining 7 committed flattened `.d.ts` snapshots so the shape gate covers the entire public type surface.

Purpose: TYPE-02 full coverage — the D-11 subpath requirement (router `./core` + `./lit`, forms `./zod`) means 8 snapshot files, not 5. This plan is horizontal expansion from the tracer: the config, script, `.gitattributes`, and ci.yml step already exist from Plan 01 and are reused unchanged.
Output: 7 new committed snapshots (store, query, forms, forms-zod, router, router-core, router-lit) and the expanded 8-entry `tools/type-snapshots.config.mjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-CONTEXT.md
@.planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-RESEARCH.md
@.planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-PATTERNS.md
@tools/type-snapshots.config.mjs
@packages/store/src/index.ts
@packages/forms/src/index.ts
@packages/router/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add store, query, forms, and forms/zod snapshot entries</name>
  <files>tools/type-snapshots.config.mjs, tools/type-snapshots/store.d.ts, tools/type-snapshots/query.d.ts, tools/type-snapshots/forms.d.ts, tools/type-snapshots/forms-zod.d.ts</files>
  <read_first>
    - tools/type-snapshots.config.mjs (the kit-only config from Plan 01 — the schema/CLI-form already resolved there; add entries in the same shape)
    - packages/store/src/index.ts (store public re-exports — expected snapshot symbols)
    - packages/query/src/index.ts (query public re-exports — the already-defaulted-generic reference surface)
    - packages/forms/src/index.ts (forms main entry re-exports) and packages/forms/src/zod.ts (the ./zod subpath source, confirmed to exist)
    - .planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-RESEARCH.md (§Pattern 1 entry map; §Pitfall 3 wrong-entry-path warning)
  </read_first>
  <action>
    Extend `tools/type-snapshots.config.mjs` with four entries in the exact shape Plan 01 established: `packages/store/src/index.ts` -> `tools/type-snapshots/store.d.ts`; `packages/query/src/index.ts` -> `tools/type-snapshots/query.d.ts`; `packages/forms/src/index.ts` -> `tools/type-snapshots/forms.d.ts`; and the forms subpath `packages/forms/src/zod.ts` -> `tools/type-snapshots/forms-zod.d.ts` (D-11). Keep the entry list stable-ordered.

    Run `npm run build` then `npm run type-snapshot` to generate the four files. Cross-check each generated snapshot's exported symbol set against the corresponding `src/index.ts` (and `src/zod.ts`) re-exports so no known export is missing and the generator did not resolve a sibling package's workspace src by mistake (Pitfall 3). Confirm the query snapshot reflects the already-defaulted query/mutation generics (D-06 reference — no edits).

    Commit the four snapshots (the `.gitattributes` LF pin from Plan 01 already governs this directory).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run type-snapshot &amp;&amp; test -f tools/type-snapshots/store.d.ts &amp;&amp; test -f tools/type-snapshots/query.d.ts &amp;&amp; test -f tools/type-snapshots/forms.d.ts &amp;&amp; test -f tools/type-snapshots/forms-zod.d.ts &amp;&amp; git diff --exit-code tools/type-snapshots/</automated>
  </verify>
  <acceptance_criteria>
    - `tools/type-snapshots.config.mjs` contains store, query, forms, and forms-zod entries mapping to the four target files.
    - `tools/type-snapshots/store.d.ts`, `query.d.ts`, `forms.d.ts`, and `forms-zod.d.ts` all exist as single flattened .d.ts files.
    - `forms-zod.d.ts` contains the zod-adapter surface (e.g. zodValidator) from `packages/forms/src/zod.ts` — the subpath is covered, not just the main forms entry.
    - Each snapshot's symbol set matches its package `src/index.ts` re-exports (spot-checked: no missing known export, no sibling-src bleed).
    - After generation on a clean tree, `git diff --exit-code tools/type-snapshots/` exits 0.
  </acceptance_criteria>
  <done>store, query, forms, and forms/zod snapshots are committed and regenerate byte-identically on a clean tree.</done>
</task>

<task type="auto">
  <name>Task 2: Add router main + ./core + ./lit snapshot entries (subpath coverage)</name>
  <files>tools/type-snapshots.config.mjs, tools/type-snapshots/router.d.ts, tools/type-snapshots/router-core.d.ts, tools/type-snapshots/router-lit.d.ts</files>
  <read_first>
    - tools/type-snapshots.config.mjs (now holds 5 entries; add the final 3)
    - packages/router/src/index.ts (router main entry re-exports)
    - packages/router/src/router-core/index.ts (the ./core subpath entry — framework-neutral surface)
    - packages/router/src/router-lit/index.ts (the ./lit subpath entry — Lit element surface)
    - .planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-RESEARCH.md (§Pattern 1 router entries; §Pitfall 3)
  </read_first>
  <action>
    Add the final three entries to `tools/type-snapshots.config.mjs` (D-11 subpath coverage): `packages/router/src/index.ts` -> `tools/type-snapshots/router.d.ts`; `packages/router/src/router-core/index.ts` -> `tools/type-snapshots/router-core.d.ts`; `packages/router/src/router-lit/index.ts` -> `tools/type-snapshots/router-lit.d.ts`. The router-core/index.ts and router-lit/index.ts files are the exports-map `types` targets and are confirmed to exist.

    Run `npm run build` then `npm run type-snapshot` to generate all three. Cross-check that `router-core.d.ts` carries a core-only symbol (e.g. CompiledPathMatcher) and `router-lit.d.ts` carries a Lit-only element (e.g. RouterOutlet), proving the two subpaths are snapshotted separately from the main router entry (Pitfall 3). Confirm the full snapshot directory now holds exactly 8 files (kit from Plan 01 + these 7).

    Commit the three router snapshots.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run type-snapshot &amp;&amp; test -f tools/type-snapshots/router.d.ts &amp;&amp; test -f tools/type-snapshots/router-core.d.ts &amp;&amp; test -f tools/type-snapshots/router-lit.d.ts &amp;&amp; git diff --exit-code tools/type-snapshots/</automated>
  </verify>
  <acceptance_criteria>
    - `tools/type-snapshots.config.mjs` declares all 8 entries (kit, store, query, forms, forms-zod, router, router-core, router-lit).
    - `tools/type-snapshots/` contains exactly 8 committed `.d.ts` files.
    - `router-core.d.ts` contains a core-only symbol and `router-lit.d.ts` contains a Lit-only element — the `./core` and `./lit` subpaths are covered distinctly.
    - After generation on a clean tree, `git diff --exit-code tools/type-snapshots/` exits 0 across all 8 files.
  </acceptance_criteria>
  <done>All five packages and every subpath export have a committed flattened snapshot; the shape gate now covers the entire public type surface and regenerates byte-identically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author -> read-only CI gate | The directory-wide shape-diff step (added in 06-01) now guards 8 snapshot files; no new CI surface is added here. |

This plan adds only committed generated `.d.ts` artifacts and config entries — no runtime code, no network, no auth, no input handling.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02-WE | Tampering | wrong-entry-path snapshots the wrong/sibling surface | low | mitigate | Cross-check each generated snapshot symbol set against the package `src/index.ts` re-exports (Pitfall 3); the router `./core`/`./lit` distinct-symbol checks confirm subpath separation. |
| T-06-02-TS | Tampering | TS-version emit drift red-lines the gate with no source change | low | accept | Snapshots are generated with the lockfile-pinned typescript 6.0.3; a future TS bump is treated as an intended regeneration (noted for the Phase 12 Dependabot planner). |
</threat_model>

<verification>
- `npm run build && npm run type-snapshot && git diff --exit-code tools/type-snapshots/` exits 0 with all 8 files present.
- forms-zod, router-core, router-lit snapshots each carry their subpath-distinct symbols (D-11 coverage proven).
- ci.yml is unmodified (the 06-01 directory-wide gate already covers the new files).
</verification>

<success_criteria>
The type-SemVer shape gate covers the complete public surface of all five packages including every subpath export (8 committed flattened snapshots), regenerates byte-identically on a clean tree, and needs no further CI wiring — the gate established in Plan 01 now protects the whole library.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (06-02)
- `tools/type-snapshots/store.d.ts` — new committed snapshot
- `tools/type-snapshots/query.d.ts` — new committed snapshot
- `tools/type-snapshots/forms.d.ts` — new committed snapshot
- `tools/type-snapshots/forms-zod.d.ts` — new committed snapshot (./zod subpath, D-11)
- `tools/type-snapshots/router.d.ts` — new committed snapshot
- `tools/type-snapshots/router-core.d.ts` — new committed snapshot (./core subpath, D-11)
- `tools/type-snapshots/router-lit.d.ts` — new committed snapshot (./lit subpath, D-11)
- `tools/type-snapshots.config.mjs` — expanded from 1 to 8 entries

No public package signatures are edited (verify-only). No new runtime symbols. Full phase artifact list is in 06-01-PLAN.md.
</artifacts_produced>

<output>
Create `.planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-02-SUMMARY.md` when done.
</output>
