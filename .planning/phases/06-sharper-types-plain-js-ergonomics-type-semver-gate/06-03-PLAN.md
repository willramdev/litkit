---
phase: 06-sharper-types-plain-js-ergonomics-type-semver-gate
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - tools/typecheck-smoke/TYPE-01-audit.md
  - tools/typecheck-smoke/tsconfig.checkjs.json
  - tools/typecheck-smoke/js-kit.js
  - tools/typecheck-smoke/js-store.js
  - tools/typecheck-smoke/js-query.js
  - tools/typecheck-smoke/js-forms.js
  - tools/typecheck-smoke/js-router.js
  - package.json
autonomous: true
requirements: [TYPE-01, TYPE-03]
estimate:
  tokens: 40000
  raw_tokens: 25000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The TYPE-01 audit enumerates every generic-bearing public symbol in kit/store/forms/router and shows each infers its type parameter from a required value argument at a zero-generic call site — no public API forces an explicit generic (TYPE-01 edge, verification: explicit)."
    - "A `tsc --checkJs` smoke consumer per package compiles with zero explicit generics at every call site, proving plain-JS callers never hit a forced generic (TYPE-03 edge, verification: explicit)."
    - "The checkJs leg is wired into the root typecheck:smoke script so it runs in the existing CI build-test/gate flow."
  artifacts:
    - tools/typecheck-smoke/TYPE-01-audit.md
    - tools/typecheck-smoke/tsconfig.checkjs.json
    - tools/typecheck-smoke/js-kit.js
    - tools/typecheck-smoke/js-store.js
    - tools/typecheck-smoke/js-query.js
    - tools/typecheck-smoke/js-forms.js
    - tools/typecheck-smoke/js-router.js
    - package.json (typecheck:smoke checkJs leg)
  key_links:
    - "js-*.js call factories at zero-generic sites -> tsc -p tsconfig.checkjs.json (checkJs+allowJs) -> a clean compile IS the TYPE-03 proof"
    - "@willramdev/* imports in the .js consumers resolve via exports map into dist (npm run build must precede)"
  prohibitions:
    - "Do not edit any public generic factory signature in store/forms/kit/router — TYPE-01 is verify-only; the literal reading of D-05 (add defaults only where a public API forces a generic today) yields an empty change set."
    - "Do not add expectType / tsd / @ts-expect-error inference assertions — the smoke is compile-only; type-level inference tests are deferred TYPE-F1 / P3 (D-08)."
    - "Do not add allowImportingTsExtensions to tsconfig.checkjs.json — it defeats the exports-map resolution the harness exists to verify."
    - "Do not build one combined cross-package .js app — one .js consumer per package (D-07/D-08); a combined app drifts toward the Phase 10 examples app."
    - "Do not import from a package's relative src/*.ts in any consumer — import only from the published @willramdev/* specifier."
---

<objective>
Prove and document the plain-JS ergonomics floor: an audit artifact showing every public generic already infers from a value argument (TYPE-01, verify-only), and a `tsc --checkJs` smoke consumer per package that compiles with zero explicit generics (TYPE-03).

Purpose: TYPE-01 is satisfied by verification, not signature edits — the research audit is [VERIFIED] that no public API in store/forms/kit/router forces an explicit generic today, and query already ships full defaulted generics (D-06). This plan turns that finding into a committed audit artifact plus an objective, CI-run compile proof.
Output: `tools/typecheck-smoke/TYPE-01-audit.md`, `tsconfig.checkjs.json`, five `js-*.js` consumers, and the `typecheck:smoke` checkJs leg.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-CONTEXT.md
@.planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-RESEARCH.md
@.planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-PATTERNS.md
@tools/typecheck-smoke/tsconfig.node16.json
@tools/typecheck-smoke/consumer-rest.ts
@tools/typecheck-smoke/consumer-router.ts
@package.json
@packages/query/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TYPE-01 verify-only audit artifact (no signature edits)</name>
  <files>tools/typecheck-smoke/TYPE-01-audit.md</files>
  <read_first>
    - .planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-RESEARCH.md (§Required-Generic Audit — the [VERIFIED] per-symbol table with file:line and signatures)
    - packages/store/src/store.ts, packages/store/src/store-slice.ts, packages/store/src/derived.ts (createStore, storeSlice, derived signatures)
    - packages/forms/src/index.ts, packages/forms/src/create-form.ts, packages/forms/src/field.ts, packages/forms/src/bind.ts (form, createForm, field, bind)
    - packages/kit/src/computed.ts, packages/kit/src/persisted-state.ts, packages/kit/src/query-state.ts (computed, persistedState, queryState)
    - packages/router/src/router-core/router.ts, packages/router/src/router-lit/index.ts, packages/router/src/route-decorator.ts (createRouter, routeState, searchParams, route — non-generic)
    - packages/query/src/index.ts (the D-06 reference: query/mutation already fully defaulted — reference only, no work)
  </read_first>
  <action>
    Produce `tools/typecheck-smoke/TYPE-01-audit.md` — the TYPE-01 deliverable under the maintainer VERIFY-ONLY decision. Re-verify each generic-bearing public symbol against the LIVE source files (do not copy the research table blind; confirm file:line and the current signature). Author a per-symbol table with columns: symbol, file:line, current signature (verbatim), the required value argument the type param binds to, and the zero-generic JS call site that infers it (e.g. createStore(0) infers T=number from initialState).

    Cover: store (createStore, storeSlice, derived single + multi), forms (form, createForm, field, bind), kit (computed 1-arg + deps, persistedState, queryState), router (createRouter, routeState, searchParams, route — record these as non-generic). Note query/mutation as the D-06 reference pattern (already fully defaulted `<T = unknown>` style) that is explicitly out of scope for edits.

    State the conclusion explicitly: every public generic infers from a required value argument, so no public API forces an explicit generic today; TYPE-01's no-required-generic floor is met structurally and this phase makes ZERO signature edits. Record that the consistency-alignment sweep (aligning store/forms/kit to query's explicit-default style) was REJECTED for this phase, and that verify-only means no one-way defaulted-generic commitment is made (D-05). Reference the js-*.js smoke consumers (Task 2/3) as the executable proof backing each row.
  </action>
  <verify>
    <automated>test -f tools/typecheck-smoke/TYPE-01-audit.md &amp;&amp; git diff --exit-code -- packages/</automated>
  </verify>
  <acceptance_criteria>
    - `tools/typecheck-smoke/TYPE-01-audit.md` exists with a per-symbol table covering createStore, storeSlice, derived (single+multi), form, createForm, field, bind, computed (both overloads), persistedState, queryState, createRouter, routeState, searchParams, route.
    - Every generic-bearing row names the required value argument its type param binds to and a zero-generic JS call site that infers it.
    - The audit states the verify-only conclusion (no forced generic; zero signature edits; consistency sweep rejected).
    - `git diff --exit-code -- packages/` exits 0 — no source signature was changed (verify-only honored).
  </acceptance_criteria>
  <done>The TYPE-01 no-required-generic guarantee is documented per-symbol against live source, with zero signature edits, and cross-referenced to the executable smoke proof.</done>
</task>

<task type="auto">
  <name>Task 2: checkJs harness + kit and store plain-JS consumers</name>
  <files>tools/typecheck-smoke/tsconfig.checkjs.json, tools/typecheck-smoke/js-kit.js, tools/typecheck-smoke/js-store.js, package.json</files>
  <precondition>packages/*/dist/*.d.ts exist (run `npm run build`) — the .js consumers resolve @willramdev/* through each package exports map into dist, so a missing dist fails resolution.</precondition>
  <read_first>
    - tools/typecheck-smoke/tsconfig.node16.json (copy verbatim, then add allowJs+checkJs, switch include to *.js)
    - tools/typecheck-smoke/consumer-rest.ts (header-comment convention + import/reference pattern to mirror in JS)
    - package.json (typecheck:smoke line 13 — the &&-chained tsc style to extend)
    - packages/kit/src/index.ts, packages/store/src/index.ts (the exact value bindings to import and call)
    - .planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-RESEARCH.md (§Plain-JS Smoke Consumer — verbatim zero-generic call sites)
  </read_first>
  <action>
    Create `tools/typecheck-smoke/tsconfig.checkjs.json`: copy tsconfig.node16.json (module nodenext / moduleResolution node16 / target ES2023 / strict / skipLibCheck / types [] / noEmit), then set allowJs true and checkJs true and change include to `["*.js"]`. Do NOT add allowImportingTsExtensions (mirror the load-bearing warning from consumer-rest.ts lines 12-14).

    Create `tools/typecheck-smoke/js-kit.js` and `tools/typecheck-smoke/js-store.js`, each opening with a header comment (mirroring the analog's intent block) stating that a clean checkJs compile with no explicit generic IS the TYPE-03 proof. Import only value bindings from the published specifier and CALL each factory at a zero-generic call site: js-store.js -> createStore(0), storeSlice(host, s, x => x.n), derived(s, v => v*2); js-kit.js -> computed(host, () => 1), persistedState(host, 'k', { default: 0 }), queryState(host, 'q', { default: '' }). Provide a minimal host (a ReactiveElement-shaped object or a KitElement subclass instance) as needed to satisfy the call signatures. No type-only imports, no expectType, no @ts-expect-error.

    Append the checkJs leg to the root `typecheck:smoke` script: `&& tsc -p tools/typecheck-smoke/tsconfig.checkjs.json` (matching the existing &&-chained tsc style).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; tsc -p tools/typecheck-smoke/tsconfig.checkjs.json</automated>
  </verify>
  <acceptance_criteria>
    - `tools/typecheck-smoke/tsconfig.checkjs.json` exists with allowJs:true, checkJs:true, include ["*.js"], and does NOT contain allowImportingTsExtensions.
    - `js-kit.js` calls computed, persistedState, queryState; `js-store.js` calls createStore, storeSlice, derived — all with no explicit `<...>` generic.
    - `tsc -p tools/typecheck-smoke/tsconfig.checkjs.json` exits 0 (clean compile with zero explicit generics).
    - root `typecheck:smoke` script ends with `&& tsc -p tools/typecheck-smoke/tsconfig.checkjs.json`.
  </acceptance_criteria>
  <done>The checkJs harness compiles kit and store plain-JS consumers clean with no forced generics, and the checkJs leg is wired into typecheck:smoke.</done>
</task>

<task type="auto">
  <name>Task 3: query, forms, and router plain-JS consumers</name>
  <files>tools/typecheck-smoke/js-query.js, tools/typecheck-smoke/js-forms.js, tools/typecheck-smoke/js-router.js</files>
  <precondition>packages/*/dist/*.d.ts exist (run `npm run build`) — the .js consumers resolve @willramdev/* through each package exports map into dist.</precondition>
  <read_first>
    - tools/typecheck-smoke/js-store.js (the JS-consumer shape established in Task 2 — mirror it)
    - tools/typecheck-smoke/tsconfig.checkjs.json (the include ["*.js"] config that auto-picks-up these new files)
    - tools/typecheck-smoke/consumer-router.ts (router import shape + subpath specifiers)
    - packages/query/src/index.ts, packages/forms/src/index.ts, packages/router/src/index.ts (exact value bindings + call shapes)
    - .planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-RESEARCH.md (§Plain-JS Smoke Consumer — verbatim zero-generic call sites for query/forms/router)
  </read_first>
  <action>
    Create the three remaining consumers, each mirroring the Task-2 shape (header comment + value-only imports from the published specifier + zero-generic calls; no inference assertions): js-query.js -> query({...}), mutation({...}), createQueryClient(); js-forms.js -> form({ initialValues: {...}, onSubmit }), field(...), bind(...); js-router.js -> createRouter({...}), routeState(), searchParams(). Supply the minimal config objects / host each call needs to satisfy its signature. The existing `include: ["*.js"]` in tsconfig.checkjs.json auto-includes these files — no config change.

    Run the full checkJs leg to confirm all five consumers compile clean together, and run the whole `typecheck:smoke` script to confirm the checkJs leg composes with the existing node16 + bundler legs.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run typecheck:smoke</automated>
  </verify>
  <acceptance_criteria>
    - `js-query.js`, `js-forms.js`, `js-router.js` each exist and call their package's factories at zero-generic sites with no explicit `<...>`.
    - `tsc -p tools/typecheck-smoke/tsconfig.checkjs.json` compiles all five js-*.js consumers with exit 0.
    - `npm run typecheck:smoke` (node16 + bundler + checkJs legs) exits 0.
    - No consumer imports from a relative src/*.ts path; all imports use @willramdev/* specifiers (including router `./core`/`./lit` and forms `./zod` where exercised).
  </acceptance_criteria>
  <done>All five packages have a compile-clean plain-JS smoke consumer proving no forced generic; the checkJs leg runs in the standard typecheck:smoke flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author -> read-only CI | The checkJs leg runs under the existing read-only typecheck:smoke/CI flow; no new CI permissions or credentials. |

This plan adds only a doc artifact, a tsconfig, and compile-only .js consumers — no runtime code, no network, no auth, no user input.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03-RB | Tampering | consumer bypasses the exports map (resolves workspace src) and hides a forced-generic regression | low | mitigate | Omit allowImportingTsExtensions from tsconfig.checkjs.json and import only @willramdev/* specifiers (mirrors the .ts smoke's load-bearing warning) so resolution goes through dist, not src. |
| T-06-03-SC | Information Disclosure | none — no secrets, network, or runtime surface introduced | low | accept | Compile-only smoke + a markdown audit; nothing to disclose. |
</threat_model>

<verification>
- `git diff --exit-code -- packages/` exits 0 (TYPE-01 verify-only: no signature edits).
- `npm run build && npm run typecheck:smoke` exits 0 (node16 + bundler + new checkJs legs).
- `tsc -p tools/typecheck-smoke/tsconfig.checkjs.json` compiles all five plain-JS consumers with zero explicit generics.
- `tools/typecheck-smoke/TYPE-01-audit.md` documents every generic-bearing symbol inferring from a value argument.
</verification>

<success_criteria>
TYPE-01 is documented as met with zero signature edits (verify-only audit), and TYPE-03 is objectively proven: a per-package plain-JS `checkJs` consumer compiles clean with no explicit generics, wired into the standard `typecheck:smoke` CI flow.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (06-03)
- `tools/typecheck-smoke/TYPE-01-audit.md` — new; verify-only per-symbol audit (TYPE-01 deliverable)
- `tools/typecheck-smoke/tsconfig.checkjs.json` — new; allowJs+checkJs, no allowImportingTsExtensions
- `tools/typecheck-smoke/js-kit.js` — new; plain-JS consumer
- `tools/typecheck-smoke/js-store.js` — new; plain-JS consumer
- `tools/typecheck-smoke/js-query.js` — new; plain-JS consumer
- `tools/typecheck-smoke/js-forms.js` — new; plain-JS consumer
- `tools/typecheck-smoke/js-router.js` — new; plain-JS consumer
- `package.json` — typecheck:smoke script gains the checkJs leg

No public package signatures are edited (TYPE-01 verify-only). No new runtime symbols. Full phase artifact list is in 06-01-PLAN.md.
</artifacts_produced>

<output>
Create `.planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-03-SUMMARY.md` when done.
</output>
