---
phase: 06-sharper-types-plain-js-ergonomics-type-semver-gate
plan: 04
type: execute
wave: 1
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - .github/workflows/ci.yml
  - package.json
  - package-lock.json
autonomous: true
gap_closure: true
requirements: [TYPE-02, TYPE-03]
user_setup: []
estimate:
  tokens: 35000
  raw_tokens: 22000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The `.github/workflows/ci.yml` `gate` job contains a `run: npm run typecheck:smoke` step positioned AFTER the gate job's `npm run build` step, so the five `js-*.js` checkJs plain-JS consumers (plus the node16 + bundler legs) run on every push/PR — the TYPE-03 plain-JS floor is an enforced CI gate, not a local-only proof (SC3/TYPE-03; restates and makes TRUE the falsified 06-03 must_have truth 'checkJs leg is wired ... so it runs in the existing CI build-test/gate flow')."
    - "Forcing an explicit generic on a public factory (temporary probe) makes `npm run typecheck:smoke` exit non-zero (the corresponding `js-*.js` zero-generic call site fails checkJs); reverting the probe restores exit 0 and a clean `git diff --exit-code -- packages/` — proving the wired gate red-lines a plain-JS ergonomics regression rather than merely 'the step exists' (SC3/TYPE-03)."
    - "The ci.yml shape-gate step fails on an UNTRACKED new file under `tools/type-snapshots/` (not only on modified tracked snapshots), so a future un-baselined subpath snapshot cannot ship CI-green (TYPE-02 hardening, WR-01; D-01/D-03/D-04)."
    - "`typescript` is exact-pinned (`6.0.3`, no caret) in `package.json` and `package-lock.json` so a within-caret TS bump cannot silently reorder `.d.ts` emit and spuriously red-line (or silently rewrite) the committed type snapshots (WR-03)."
  artifacts:
    - .github/workflows/ci.yml
    - package.json
    - package-lock.json
  key_links:
    - "ci.yml gate job: `npm run build` -> `npm run typecheck:smoke` -> the five js-*.js checkJs consumers resolve @willramdev/* through the exports map into dist and type-check on every CI run"
    - "ci.yml shape gate: `git add -A -- tools/type-snapshots/` + `git diff --cached --exit-code -- tools/type-snapshots/` catches untracked AND modified snapshot drift (closes the WR-01 blind spot)"
    - "package.json `typescript: 6.0.3` (exact) -> package-lock.json pinned resolution -> byte-stable .d.ts emit that the shape gate depends on"
  prohibitions:
    - "Do NOT widen `ci.yml` permissions beyond the existing top-level `permissions: contents: read`, and do NOT touch `release.yml` or `verify-consumer.yml` (D-10 — preserve the token-safe two-workflow split)."
    - "Do NOT commit the temporary forced-generic probe. It exists only to prove the gate red-lines; revert it before committing. `git diff --exit-code -- packages/` MUST exit 0 at task end (TYPE-01 verify-only stays honored; D-05)."
    - "Do NOT move `typescript` out of `dependencies` into `devDependencies` — that is IN-01 (info-only, out of scope). Only remove the caret from its version range in place."
    - "Do NOT edit the five `js-*.js` consumers, `tsconfig.checkjs.json`, the `typecheck:smoke` script body, or the `tools/type-snapshots/*.d.ts` snapshots — plans 06-01/06-02/06-03 own those; this plan only wires and hardens the CI gate."
    - "Do NOT install any NEW package. WR-03 re-pins an already-installed `typescript@6.0.3`; `npm install` only re-syncs the lockfile spec (no new dependency, no `allowScripts` change)."
---

<objective>
Close the Phase 06 verification gaps so the plain-JS type floor and the type-SemVer shape gate are enforced in CI, not just provable locally. Plans 06-01/06-02/06-03 built the mechanisms; this gap-closure plan wires and hardens them:

1. **CR-01 (BLOCKER, SC3/TYPE-03):** wire `npm run typecheck:smoke` into the `ci.yml` `gate` job after `npm run build`, and prove behaviorally that the wired gate turns red when a public factory forces an explicit generic (temporary probe, reverted) — the falsified 06-03 CI-wiring truth becomes TRUE.
2. **WR-02 (ties to CR-01):** guarantee CI step ordering — the `typecheck:smoke` step is placed after `npm run build` with an inline ordering-invariant comment (the js-*.js consumers resolve `@willramdev/*` through the exports map into `dist/`).
3. **WR-01 (TYPE-02 hardening):** close the shape-gate untracked-file blind spot so a new-but-uncommitted subpath snapshot fails CI (D-01/D-03/D-04).
4. **WR-03:** exact-pin `typescript` so a within-caret bump cannot desync byte-stable `.d.ts` emit from the committed snapshots.

Purpose: The phase goal — "these type improvements can never ship as a stealth breaking change" — is only truly met once the gate runs on every CI build AND is future-proof against forced generics, untracked snapshots, and emit-format drift. All changes stay in the read-only `ci.yml` (D-10) and are trivially reversible.
Output: a hardened `.github/workflows/ci.yml`, an exact `typescript` pin in `package.json` + `package-lock.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-CONTEXT.md
@.planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-VERIFICATION.md
@.planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-03-SUMMARY.md
@.github/workflows/ci.yml
@package.json
@tools/typecheck-smoke/js-store.js
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — wire typecheck:smoke into the ci.yml gate job after build (CR-01 + WR-02) and prove it red/greens on a forced generic</name>
  <files>.github/workflows/ci.yml</files>
  <precondition>packages/*/dist/*.d.ts exist after `npm run build` — the five js-*.js consumers invoked by `typecheck:smoke` resolve `@willramdev/*` through each package exports map into `dist/`, so `typecheck:smoke` fails resolution on a tree that was never built. Prior plans 06-01/06-02/06-03 already created the `typecheck:smoke` script (package.json:13), the five `js-*.js` consumers, and `tsconfig.checkjs.json`; assert they are present before wiring.</precondition>
  <read_first>
    - .github/workflows/ci.yml (the live file being modified — note the `gate` job starts at line 39 `name: gate`, `needs: build-test`; its `- run: npm run build` step is at line 52; the shape-gate steps are lines 57-60; top-level `permissions: contents: read` is lines 12-14)
    - package.json (line 13 `typecheck:smoke` = the node16 + bundler + checkjs tsc chain this step invokes; line 16 `doc-check` = the self-contained `npm run build && ...` precedent, for contrast on the WR-02 decision)
    - .planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-VERIFICATION.md (gaps frontmatter CR-01 + WR-02; Key Link Verification row "CI workflow -> typecheck:smoke = NOT_WIRED")
    - .planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-03-SUMMARY.md (the false "runs in the existing CI build-test/gate flow" claim this task makes true)
    - tools/typecheck-smoke/js-store.js (the zero-generic call sites — createStore(0), storeSlice(host, s, x => x.n), derived(s, v => v*2) — that the forced-generic probe will red-line)
    - packages/store/src/store.ts (the `createStore<T>(initialState: T): Store<T>` signature at line ~30 used as the probe target)
  </read_first>
  <action>
    Edit `.github/workflows/ci.yml` in the `gate` job (the single-Node job `name: gate`, `needs: build-test`). Insert a NEW step immediately AFTER the existing `- run: npm run build` step (line 52) and BEFORE the `type-snapshot (regenerate flattened public .d.ts)` step (line 57). The new step is a `run: npm run typecheck:smoke` step with a descriptive `name:` such as "plain-JS ergonomics gate (checkJs smoke — TYPE-03)". Immediately above it, add an inline YAML comment recording the WR-02 ordering invariant in prose: this step MUST run after `npm run build` because the five js-*.js consumers resolve `@willramdev/*` through the exports map into dist — do not reorder it before the build step. Placing it in the already-built `gate` job (which runs `npm run build` at line 52) satisfies WR-02 by ordering guarantee and avoids a redundant second build, so `typecheck:smoke` itself stays unchanged (no `npm run build &&` prefix added to the script — that is the deliberate WR-02 choice: ordering guarantee, not self-containment).

    Honor D-10: do NOT add or widen a `permissions:` block; the step inherits the top-level `contents: read`. Do NOT touch `release.yml` or `verify-consumer.yml`. Do NOT edit the `build-test` matrix job — the smoke runs once in `gate`, not per Node matrix leg (the verifier's recommendation).

    Behavioral red/green proof (the acceptance criterion is a live red-line, not "the step exists"): after the wiring edit, temporarily force an explicit generic on ONE public factory — recommended instance: in `packages/store/src/store.ts`, decouple the type parameter from the value argument so `T` is no longer inferable (e.g. type `createStore`'s `initialState` parameter as `unknown` while the return stays `Store<T>`, making `T` resolvable only via an explicit `createStore<...>()`). Run `npm run build && npm run typecheck:smoke` and confirm it exits NON-ZERO because `tools/typecheck-smoke/js-store.js`'s zero-generic call sites (`storeSlice`/`derived` selectors reading a property off the now-`unknown` state) fail under checkJs. Then REVERT the probe with `git checkout -- packages/store/src/store.ts` (the probe is never committed), re-run `npm run build && npm run typecheck:smoke` and confirm exit 0, and confirm `git diff --exit-code -- packages/` exits 0 (TYPE-01 verify-only stays honored; D-05). If the recommended `createStore` instance does not red-line, use any other public factory edit that genuinely forces an explicit generic (a forced-generic checkJs error, not an arity/syntax error) and record which factory was used and the observed exit codes.
  </action>
  <verify>
    <automated>grep -q "run: npm run typecheck:smoke" .github/workflows/ci.yml &amp;&amp; npm run build &amp;&amp; npm run typecheck:smoke</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` `gate` job contains a step whose `run:` value is exactly `npm run typecheck:smoke`.
    - That step's position in the `gate` job steps list is AFTER the `- run: npm run build` step (line 52) and it is preceded by an inline comment stating the build -> typecheck:smoke ordering dependency (WR-02).
    - No `permissions:` block is added or widened in `ci.yml`; `git diff .github/workflows/` shows changes ONLY inside the `gate` job (D-10 preserved); `release.yml` and `verify-consumer.yml` are unmodified.
    - Green baseline: `npm run build && npm run typecheck:smoke` exits 0.
    - Red proof: with the temporary forced-generic probe applied, `npm run build && npm run typecheck:smoke` exits non-zero (recorded in SUMMARY with the factory used and the exit code).
    - Green-restored proof: after `git checkout -- packages/store/src/store.ts` (or the probed file), `npm run typecheck:smoke` exits 0 AND `git diff --exit-code -- packages/` exits 0 — the probe left zero residue.
  </acceptance_criteria>
  <reversibility rating="reversible">A CI-workflow step addition; the source probe is reverted with `git checkout` and never committed. Trivially revertible.</reversibility>
  <done>The checkJs plain-JS smoke runs on every CI build via the gate job (after build, per WR-02 ordering), and the gate is proven to red-line a forced explicit generic and green on revert — CR-01 closed, the falsified 06-03 CI-wiring truth is now TRUE.</done>
</task>

<task type="auto">
  <name>Task 2: Harden the shape gate against untracked snapshots (WR-01)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the shape-gate step: `name: shape gate (git diff --exit-code tools/type-snapshots/)` line 59, `run: git diff --exit-code tools/type-snapshots/` line 60 — the exact step to modify; the `type-snapshot (regenerate ...)` step at 57-58 that precedes it stays unchanged)
    - .planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-VERIFICATION.md (Behavioral Spot-Checks row "Shape gate ignores untracked new snapshot ... exit 0 (green) — WR-01 ... FAIL (blind spot)"; Anti-Patterns row ci.yml 57-60; §Review-Finding Adjudication WR-01 CONFIRMED)
    - .planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-CONTEXT.md (D-01/D-03/D-04 — committed-snapshot baseline, block on ANY diff, no branch-vs-main fetch)
    - tools/type-snapshots.config.mjs (confirm the 8 committed snapshots live under tools/type-snapshots/ so the pathspec is correct)
  </read_first>
  <action>
    Modify ONLY the shape-gate step in the `gate` job (currently `run: git diff --exit-code tools/type-snapshots/`, line 60). Change its `run:` to first stage the snapshot directory and then diff the staged tree against HEAD, scoped to the snapshot path — `git add -A -- tools/type-snapshots/` followed by `git diff --cached --exit-code -- tools/type-snapshots/` (chained with `&&`). This catches all three drift classes for `tools/type-snapshots/`: modified tracked snapshots, DELETED tracked snapshots, and — the WR-01 fix — UNTRACKED new snapshot files (a `git add -A` stages an untracked `.d.ts`, so `git diff --cached` red-lines it), while still printing the human-readable diff CI reviewers rely on (D-03). Update the step `name:` to reflect the new behavior (e.g. "shape gate (fail on any tracked-or-untracked snapshot drift)"), and add an inline comment noting the WR-01 rationale: plain `git diff --exit-code` ignores untracked files, so an un-baselined new subpath snapshot could otherwise ship CI-green.

    Keep the preceding `npm run type-snapshot` regeneration step (57-58) unchanged — the ordering `build -> type-snapshot -> shape gate` (D-04 self-contained committed baseline) is preserved. Do NOT widen permissions (D-10); `git add`/`git diff` are local index operations that need no token. Do NOT scope the staging or diff beyond `tools/type-snapshots/` (the `-- tools/type-snapshots/` pathspec on BOTH commands keeps unrelated build output out of the gate).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run type-snapshot &amp;&amp; git add -A -- tools/type-snapshots/ &amp;&amp; git diff --cached --exit-code -- tools/type-snapshots/</automated>
  </verify>
  <acceptance_criteria>
    - The `gate` job shape-gate step's `run:` value is `git add -A -- tools/type-snapshots/ && git diff --cached --exit-code -- tools/type-snapshots/` (both commands pathspec-scoped to `tools/type-snapshots/`).
    - Clean-tree behavior: after `npm run build && npm run type-snapshot` regenerates byte-identical snapshots, the new gate command exits 0 (no drift).
    - Untracked-blind-spot closed (behavioral proof, recorded in SUMMARY): creating an untracked `tools/type-snapshots/newpkg.d.ts` makes the new gate command exit NON-ZERO (the old `git diff --exit-code tools/type-snapshots/` exited 0 on the same tree); after removing that probe file the command exits 0 again.
    - `git diff .github/workflows/ci.yml` shows the change confined to the shape-gate step; no `permissions:` change; `release.yml`/`verify-consumer.yml` untouched.
  </acceptance_criteria>
  <reversibility rating="reversible">A one-line CI command change; revertible by restoring the prior `git diff --exit-code` step.</reversibility>
  <done>The type-SemVer shape gate fails on untracked snapshot files as well as modified/deleted tracked ones, so a future un-committed subpath surface can no longer ship CI-green — WR-01 closed, TYPE-02 gate future-proofed.</done>
</task>

<task type="auto">
  <name>Task 3: Exact-pin typescript to protect byte-stable .d.ts emit (WR-03)</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (line 31 `"typescript": "^6.0.3"` in the `dependencies` block — the caret to remove; note line 38 `"dts-bundle-generator": "9.5.1"` is already exact-pinned, the precedent to match; the `allowScripts` block lines 27-29 stays unchanged)
    - .planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-VERIFICATION.md (Anti-Patterns row package.json:31 WR-03 — "A within-caret TS bump can reorder .d.ts emit and spuriously red-line (or silently rewrite) snapshots. dts-bundle-generator is exact-pinned; TS is not.")
    - .planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-RESEARCH.md (§Common Pitfalls "Pitfall 5: TS version drift changes emit format" — the byte-stable-emit dependency this pin protects)
  </read_first>
  <action>
    In `package.json`, change the `typescript` version range from `^6.0.3` to the exact `6.0.3` (remove the caret only). Leave `typescript` in the `dependencies` block where it currently sits — do NOT relocate it to `devDependencies` (that is IN-01, explicitly out of scope). Do NOT change `vitest` (unrelated to `.d.ts` emit determinism). Match the existing exact-pin style of `dts-bundle-generator: "9.5.1"`.

    Re-sync the lockfile: run `npm install` so `package-lock.json` records the exact `6.0.3` spec for the root `typescript` dependency. Because `typescript@6.0.3` is already the installed/resolved version, this adds NO new package and changes no transitive resolution — it only tightens the recorded range (no `allowScripts` entry needed; `typescript` has no postinstall). After the lockfile sync, confirm a clean install still works (`npm ci`) and the gate chain still passes (`npm run build && npm run type-snapshot && git diff --exit-code -- tools/type-snapshots/` and `npm run typecheck:smoke`) so the pin did not perturb emit.
  </action>
  <verify>
    <automated>grep -q '"typescript": "6.0.3"' package.json &amp;&amp; npm ci &amp;&amp; npm run build &amp;&amp; npm run typecheck:smoke</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies.typescript` is exactly `6.0.3` (no `^`, no `~`); `typescript` remains under `dependencies` (not moved to `devDependencies`).
    - `package-lock.json` records the `6.0.3` (no-caret) spec for the root `typescript` dependency and still resolves `typescript` to `6.0.3` (no new packages added; `git diff package-lock.json` shows only the typescript spec tighten, not a mass re-resolution).
    - `npm ci` succeeds against the updated lockfile.
    - Emit-stability regression check: `npm run build && npm run type-snapshot` followed by `git diff --exit-code -- tools/type-snapshots/` exits 0 (the pin did not change `.d.ts` emit), and `npm run typecheck:smoke` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">A version-range pin in package.json + lockfile sync; revertible by restoring the caret and re-running `npm install`.</reversibility>
  <done>`typescript` is exact-pinned in both `package.json` and `package-lock.json`, so a within-caret TS bump can no longer silently reorder `.d.ts` emit and desync the committed snapshots — WR-03 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author -> read-only CI (`ci.yml` gate job) | The new `typecheck:smoke` step and the hardened shape-gate step run under the existing top-level `permissions: contents: read`. No new credentials, no write scopes, no publish. `release.yml` (auth-bearing) is untouched — the v1.0 token-safe two-workflow split (D-10) is preserved. |
| Dependency spec -> lockfile resolution | The `typescript` exact-pin tightens the recorded range; `npm install` re-syncs `package-lock.json` without adding a new package. |

This gap plan only edits CI YAML and a `package.json` version range (plus its lockfile). It adds no runtime code, no network surface, no auth, no user input, and no new dependency. Every change HARDENS an existing gate.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04-EOP | Elevation of Privilege | New `typecheck:smoke` CI step could request elevated scopes | low | mitigate | Step inherits top-level `permissions: contents: read`; no `permissions:` block added or widened; change confined to the `gate` job; `release.yml`/`verify-consumer.yml` untouched (D-10). |
| T-06-04-TMP | Tampering | `typecheck:smoke` step runs after `npm run build` and consumes `dist/` output | low | accept | The step invokes `tsc` in type-check/`noEmit` mode over the js-*.js consumers — it does not execute build output. Runs in the same ephemeral read-only gate job that already builds and diffs; no privileged action introduced. |
| T-06-04-REP | Tampering (reproducibility) | Caret-ranged `typescript` allowed a within-range bump to alter `.d.ts` emit and mask/introduce snapshot drift | low | mitigate | Exact-pin `typescript@6.0.3` in `package.json` + lockfile; treat any future TS bump as an intended snapshot regeneration (Pitfall 5), flagged for the Phase 12 Dependabot planner. |
| T-06-04-SC | Tampering (supply chain) | `npm install` during the WR-03 pin | low | accept | No NEW package installed — `typescript@6.0.3` is already resolved; only the recorded range tightens. `allowScripts` posture unchanged; `typescript` has no postinstall. |

No `high`-severity threats introduced (ASVS L1, block-on-high) — every change reduces attack/regression surface rather than expanding it.
</threat_model>

<verification>
Phase-level checks after all three tasks (run from repo root; CI runs the same on ubuntu-latest with POSIX shell):

- `grep -q "run: npm run typecheck:smoke" .github/workflows/ci.yml` — CR-01 step present.
- The `typecheck:smoke` step appears after the gate job's `npm run build` step (manual position check / `git diff .github/workflows/ci.yml`) — WR-02 ordering.
- `npm run build && npm run typecheck:smoke` exits 0 — green baseline (node16 + bundler + checkJs legs).
- Forced-generic probe applied -> `npm run typecheck:smoke` exits non-zero; probe reverted -> exits 0 and `git diff --exit-code -- packages/` exits 0 — CR-01 red/green proof.
- Shape-gate step `run:` is `git add -A -- tools/type-snapshots/ && git diff --cached --exit-code -- tools/type-snapshots/`; an untracked `tools/type-snapshots/newpkg.d.ts` makes it exit non-zero; removing the probe restores exit 0 — WR-01.
- `grep -q '"typescript": "6.0.3"' package.json` and `npm ci` succeeds; `package-lock.json` shows only the typescript spec tighten — WR-03.
- No `permissions:` widening in `ci.yml`; `release.yml`/`verify-consumer.yml` unmodified — D-10 preserved.
</verification>

<success_criteria>
Phase 06 SC3 (TYPE-03) is enforced, not merely provable: the checkJs plain-JS smoke runs on every CI build via the `ci.yml` gate job (after build), and the wired gate is proven to red-line a forced explicit generic and green on revert. The type-SemVer shape gate (SC2/TYPE-02) is future-proofed against untracked snapshots (WR-01) and against `.d.ts`-emit drift from a caret TS bump (WR-03) — all within the read-only `ci.yml`, with no new dependency and no permission widening (D-10).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (06-04, gap closure)

This gap plan EDITS existing files (created by 06-01/06-02/06-03) — it introduces no new files, no new npm scripts, and no new packages.

- `.github/workflows/ci.yml` — MODIFIED:
  - `gate` job: NEW step `run: npm run typecheck:smoke` (named e.g. "plain-JS ergonomics gate (checkJs smoke — TYPE-03)"), placed after `npm run build` with an inline ordering-invariant comment (CR-01 + WR-02).
  - `gate` job: shape-gate step `run:` changed from `git diff --exit-code tools/type-snapshots/` to `git add -A -- tools/type-snapshots/ && git diff --cached --exit-code -- tools/type-snapshots/` (WR-01).
- `package.json` — MODIFIED: `dependencies.typescript` re-pinned from `^6.0.3` to exact `6.0.3` (WR-03); no relocation, no other dep change.
- `package-lock.json` — MODIFIED: re-synced via `npm install` to record the exact `typescript@6.0.3` spec (no new packages).

No new symbols, no new script names, no new dependencies. The `typecheck:smoke` script body, the five `js-*.js` consumers, `tsconfig.checkjs.json`, and the eight `tools/type-snapshots/*.d.ts` snapshots are unchanged (owned by 06-01/06-02/06-03). No package signatures edited (TYPE-01 verify-only stays honored; D-05).
</artifacts_produced>

<output>
Create `.planning/phases/06-sharper-types-plain-js-ergonomics-type-semver-gate/06-04-SUMMARY.md` when done. Record: the exact factory used for the forced-generic probe and the observed red/green exit codes; the untracked-snapshot WR-01 proof exit codes; and the `package-lock.json` diff scope for the WR-03 pin.
</output>
