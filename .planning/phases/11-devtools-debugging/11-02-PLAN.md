---
phase: 11-devtools-debugging
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - packages/devtools/src/store-devtools.ts
  - packages/devtools/src/store-devtools.test.ts
  - packages/devtools/src/index.ts
autonomous: true
requirements: [DTOOL-02]
user_setup: []

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`attachStoreDevtools(store, options?)` (D-01) connects to window.__REDUX_DEVTOOLS_EXTENSION__, calls connection.init(store.get()), and sends every store.set/update as a distinct sequential action (D-02 record path)."
    - "Dragging the Redux slider (a DISPATCH message with payload.type JUMP_TO_STATE / JUMP_TO_ACTION / ROLLBACK) restores state via store.set(JSON.parse(msg.state)) — full bidirectional time-travel (D-02)."
    - "A JUMP-triggered store.set does NOT re-broadcast as a new action — the isTimeTravel closure flag suppresses re-record during restore, so no feedback loop occurs (DTOOL-02 idempotency edge, Pitfall 1)."
    - "History is bounded: connect is called with maxAge (default 50, configurable via options.maxAge, D-03); at the cap the extension drops the oldest snapshot (DTOOL-02 concurrency/bound edge)."
    - "When DEV is false, or window is undefined (SSR), or window.__REDUX_DEVTOOLS_EXTENSION__ is absent, attachStoreDevtools does nothing and returns a valid no-op teardown — never throws, never logs (D-04; DTOOL-01 idempotency/no-op edge)."
    - "The returned teardown unsubscribes the store listener, unsubscribes the connection listener, and calls connection.unsubscribe(); calling the teardown twice is safe (DTOOL-01 idempotency edge)."
    - "malformed msg.state (not a JSON string, or JSON.parse throws) does not crash attachStoreDevtools or corrupt store state (ASVS V5 — JSON.parse guarded)."
    - statement: "store.subscribe yields only (state, prev) and cannot distinguish set from update, so the action label is a generic monotonic counter rather than an origin-tagged name — cosmetic only for Redux time-travel (DTOOL-02 precision edge, Pitfall 2)."
      verification: backstop
  artifacts:
    - packages/devtools/src/store-devtools.ts
    - packages/devtools/src/store-devtools.test.ts
  key_links:
    - "packages/devtools/src/store-devtools.ts → @willramdev/store (Store<T> get/set/subscribe) — the consumed public surface, no core change"
    - "packages/devtools/src/store-devtools.ts → window.__REDUX_DEVTOOLS_EXTENSION__.connect (the extension bridge)"
    - "packages/devtools/src/index.ts → re-exports attachStoreDevtools + StoreDevtoolsOptions (barrel; per-module split preserves tree-shaking)"
  prohibitions:
    - "Do not monkey-patch or wrap store.set/store.update to distinguish set from update — keep the generic monotonic action label (Pitfall 2)."
    - "Do not call connection.send inside the JUMP/ROLLBACK/RESET handler while isTimeTravel is true — that is the feedback loop (Pitfall 1)."
    - "Do not pass msg.state to store.set without JSON.parse — msg.state is a JSON string (Pitfall 3)."
    - "packages/store/src/** must not be modified — the time-travel surface is already public (D-02, D-07 ethos)."
---

<objective>
Implement `attachStoreDevtools(store, options?)` (D-01, D-02) — full bidirectional store ↔ Redux DevTools time-travel — as the first expansion slice on the tracer package from 11-01. This delivers DTOOL-02: record each store mutation as a Redux action and restore state when the user drives the extension's time-travel slider, with bounded history (D-03) and a silent no-op when the environment or extension is unavailable (D-04).

Purpose: DTOOL-02 is the trickiest logic in the phase (a bidirectional message protocol with a feedback-loop hazard); it earns a dedicated, focused plan and a thorough test so the record path, the restore path, the isTimeTravel guard, the bound, and the no-op guards are each proven.
Output: `packages/devtools/src/store-devtools.ts` (+ its test) exporting `attachStoreDevtools` and `StoreDevtoolsOptions`, re-exported from the package barrel.

## Artifacts this phase produces (source-grounding exclusion list)
New symbols introduced here (do not exist before Phase 11): `attachStoreDevtools`, `StoreDevtoolsOptions`. New files: `packages/devtools/src/store-devtools.ts`, `packages/devtools/src/store-devtools.test.ts`. Modified: `packages/devtools/src/index.ts` (adds the store-devtools re-export). The full phase artifact list is in 11-01's objective.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-devtools-debugging/11-CONTEXT.md
@.planning/phases/11-devtools-debugging/11-RESEARCH.md
@.planning/phases/11-devtools-debugging/11-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: attachStoreDevtools — Redux bidirectional time-travel (RED → GREEN)</name>
  <files>packages/devtools/src/store-devtools.test.ts, packages/devtools/src/store-devtools.ts</files>
  <read_first>
    - packages/devtools/src/internal/dev.ts (the local DEV re-export from 11-01 — the gate)
    - packages/store/src/store.ts (VERIFY-ONLY: Store<T> get():T L58, set(state:T):void L62, update L72, subscribe(listener:(state,prev)=>void):()=>void L82 — the record + restore surface; no core change)
    - packages/store/src/index.ts (confirm Store type is exported from @willramdev/store)
    - .planning/phases/11-devtools-debugging/11-RESEARCH.md §Code Examples "attachStoreDevtools" (L293-368 — VERIFIED verbatim body incl. the ReduxConnection/ReduxMessage/ReduxExtension type shapes + declare global Window) and §Common Pitfalls 1-4 (L198-214)
    - packages/store/src/store.test.ts (Vitest convention + createStore usage for driving real store mutations in the test)
  </read_first>
  <behavior>
    - Record: after attach, store.set(next) sends one connection.send({ type: `${name}/set #<n>` }, next) with a monotonically increasing n; connection.init was called once with the initial state.
    - Restore (JUMP_TO_STATE): a connection listener message { type:'DISPATCH', payload:{type:'JUMP_TO_STATE'}, state: JSON.stringify(snapshot) } calls store.set(snapshot); JUMP_TO_ACTION and ROLLBACK behave identically.
    - No feedback loop: the store.set performed during a JUMP does NOT trigger a further connection.send (isTimeTravel guard) — connection.send call count is unchanged by a restore.
    - RESET: restores store to the captured initial state and re-inits the connection. COMMIT: re-inits the connection with store.get(). (Both from the VERIFIED body.)
    - Bound: connect is invoked with { name, maxAge } where maxAge defaults to 50 and honors options.maxAge.
    - No-op guards: DEV false → no connect call, teardown is a no-op; window undefined → no-op; window.__REDUX_DEVTOOLS_EXTENSION__ absent → no-op; each returns a valid () => void.
    - Teardown: calls the store unsubscribe, the connection listener unsubscribe, and connection.unsubscribe(); a second teardown call does not throw.
    - Malformed state: a JUMP message whose state is not a valid JSON string does not throw out of attachStoreDevtools and leaves store state intact (JSON.parse guarded).
  </behavior>
  <action>
Write packages/devtools/src/store-devtools.test.ts FIRST (RED), mocking window.__REDUX_DEVTOOLS_EXTENSION__ with a fake extension whose connect() returns a controllable connection object: init (spy), send (spy), subscribe (captures the listener so the test can push DISPATCH messages), unsubscribe (spy). Drive a real createStore from @willramdev/store. Cover every bullet in <behavior> including the feedback-loop assertion (send call count constant across a restore), the maxAge default+override (assert connect received it), all no-op guard paths (mock window/DEV absent), double-teardown safety, and the malformed-state case. Then implement packages/devtools/src/store-devtools.ts (GREEN) by copying the VERIFIED body from RESEARCH.md §Code Examples: export interface StoreDevtoolsOptions { name?: string; maxAge?: number }; export function attachStoreDevtools<T>(store: Store<T>, options: StoreDevtoolsOptions = {}): () => void. Guard order exactly: `if (!DEV || typeof window === 'undefined') return () => {}` → read ext, `if (!ext) return () => {}`. name defaults to 'store'; connect({ name, maxAge: options.maxAge ?? 50 }); connection.init(store.get()). Record via store.subscribe with an isTimeTravel early-return and a monotonic counter label (Pitfall 2 — do NOT try to distinguish set/update). Restore via connection.subscribe handling msg.type==='DISPATCH' && msg.payload, switching on payload.type: JUMP_TO_STATE / JUMP_TO_ACTION / ROLLBACK → set isTimeTravel true, store.set(JSON.parse(msg.state) as T) inside try/finally (finally resets isTimeTravel), wrapping the JSON.parse in try/catch so malformed state cannot throw or corrupt (ASVS V5); RESET → restore initial + re-init; COMMIT → re-init with store.get(). The ReduxConnection/ReduxMessage/ReduxExtension interfaces and `declare global { interface Window { __REDUX_DEVTOOLS_EXTENSION__?: ReduxExtension } }` are type-only (erased under erasableSyntaxOnly). Teardown returns () => { off(); unsub(); connection.unsubscribe(); }. Use closure `let` flags (isTimeTravel, n) — no class fields. Import Store as a type from '@willramdev/store'; import DEV from './internal/dev.ts'.
  </action>
  <verify>
    <automated>npm run test -w packages/devtools -- store-devtools && npm run typecheck -w packages/devtools</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -w packages/devtools -- store-devtools` passes, including: record sends a sequential action; JUMP_TO_STATE restores via store.set(JSON.parse(state)); the restore does NOT increment connection.send (isTimeTravel guard); maxAge default 50 and options.maxAge override both reach connect; all three no-op guard paths return a valid teardown with no connect call; double-teardown does not throw; malformed state does not throw.
    - `npm run typecheck -w packages/devtools` exits 0 with the type-only Redux interfaces and the Store<T> import from @willramdev/store.
    - `git status --porcelain packages/store/src/` prints nothing (store core untouched).
  </acceptance_criteria>
  <reversibility rating="costly">The public name `attachStoreDevtools` and its `StoreDevtoolsOptions` shape (D-01) are the published 1.x API — renaming/re-shaping later is breaking. The time-travel wiring itself (D-02/D-03/D-04) is reversible (self-contained in the devtools package).</reversibility>
  <done>attachStoreDevtools records and restores store state through the Redux extension with a working isTimeTravel guard, bounded history, and silent no-op guards, all proven by store-devtools.test.ts; store core is unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Wire attachStoreDevtools into the barrel + re-verify the package builds</name>
  <files>packages/devtools/src/index.ts</files>
  <read_first>
    - packages/devtools/src/index.ts (the barrel from 11-01 — currently exports only attachRouterLog; preserve that line)
    - packages/store/src/index.ts (barrel convention: value exports first, then `export type`, `.ts` extensions)
  </read_first>
  <action>
Add to packages/devtools/src/index.ts: `export { attachStoreDevtools, type StoreDevtoolsOptions } from './store-devtools.ts';` (RESEARCH.md §index.ts barrel L394-398). Keep the existing attachRouterLog export line. Do not touch anything else. Then rebuild to confirm the added optional-peer path (@willramdev/store) resolves and the ESM bundle + .d.ts still emit cleanly with store externalized.
  </action>
  <verify>
    <automated>npm run build -w packages/devtools && npm run typecheck -w packages/devtools && npm run test -w packages/devtools</automated>
  </verify>
  <acceptance_criteria>
    - packages/devtools/src/index.ts exports both attachRouterLog and attachStoreDevtools (+ the StoreDevtoolsOptions type).
    - `npm run build -w packages/devtools` exits 0; dist/index.d.ts declares attachStoreDevtools and StoreDevtoolsOptions.
    - `node scripts/check-devtools-leaf.mjs` still exits 0 (leaf rule intact after the store edge is added).
  </acceptance_criteria>
  <done>The barrel exports attachRouterLog + attachStoreDevtools; the package builds/typechecks/tests green with the store optional-peer path added; the leaf rule still holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redux DevTools browser extension → attachStoreDevtools | the extension's DISPATCH messages (incl. msg.state as a JSON string) are consumed here; the extension is a trusted, user-installed dev tool present only in DEV |
| consumer bundler → shipped devtools dist | store-devtools.ts must be DEV-gated + tree-shakeable so it never survives into a production consumer bundle |

## STRIDE Threat Register (ASVS L1; block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-02 | Tampering / DoS | malformed msg.state from the extension throwing or corrupting store state | low | mitigate | JSON.parse wrapped in try/catch; restore only on DISPATCH+JUMP/ROLLBACK/RESET/COMMIT; isTimeTravel flag prevents the record↔restore feedback loop (Pitfall 1, Pitfall 3) |
| T-11-01b | Information Disclosure | store-devtools code leaking into a prod consumer bundle | medium | mitigate | LOCAL esm-env `DEV` gate (`if (!DEV || typeof window === 'undefined') return () => {}`) + `sideEffects:false` + per-module split; opt-in explicit import only (DTOOL-01, D-04) |
| T-11-04 | Denial of Service | SSR / no-`window` reference | low | mitigate | Guard order DEV → typeof window → ext presence, each returning a silent no-op teardown (D-04, Pitfall 4) |

No threat is rated `high`; nothing blocks at ASVS L1.
</threat_model>

<verification>
- `npm run test -w packages/devtools` passes (store-devtools + router-log suites).
- `npm run build -w packages/devtools && npm run typecheck -w packages/devtools` exit 0.
- `node scripts/check-devtools-leaf.mjs` exits 0.
- `git status --porcelain packages/store/src/` prints nothing (store core untouched, D-02/D-07 ethos).
</verification>

<success_criteria>
- DTOOL-02 delivered: store state time-travels bidirectionally through the Redux DevTools extension — opt-in, dev-gated (D-04), bounded (D-03), with no feedback loop.
- attachStoreDevtools + StoreDevtoolsOptions are exported from the package barrel and covered by a thorough unit test.
- The store core (packages/store/src/**) is unchanged; the leaf rule still holds.
</success_criteria>

<output>
Create `.planning/phases/11-devtools-debugging/11-02-SUMMARY.md` when done.
</output>
