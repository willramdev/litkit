---
phase: 03-docs
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - packages/query/README.md
  - packages/forms/README.md
autonomous: true
requirements: [DOCS-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "`npm run doc-check` exits 0 with the @willram/query and @willram/forms Quickstarts compiling against their shipped dist/*.d.ts under BOTH node16 and bundler (DOCS-01)"
    - "No marked block imports html from @willram/kit; KitElement quickstarts import html from lit — proven because a kit html import would be a hard TS error the doc-check catches (DOCS-01, Pitfall 1)"
    - "The forms README carries a marked, self-contained block importing from the @willram/forms/zod subpath, so the doc-check exercises that published subpath (DOCS-01, D-03)"
    - "Each of packages/query/README.md and packages/forms/README.md carries exactly one marked, self-contained Quickstart and follows the shared section template (DOCS-01)"
  artifacts:
    - "packages/query/README.md normalized + marked Quickstart, html drift fixed"
    - "packages/forms/README.md normalized + marked Quickstart + marked /zod subpath block, html drift fixed"
  key_links:
    - "marked blocks MUST be self-contained: undefined identifiers (login, fetchUsers, User) are replaced with inline stubs or literals so the block compiles standalone (Pitfall 2)"
    - "query/forms controllers need a consumer-supplied client/instance; install lines list the TanStack core peers (@tanstack/query-core, @tanstack/form-core) plus lit"
---

<objective>
Expand the DOCS-01 guarantee proven by the 03-01 tracer to the @willram/query and @willram/forms READMEs: normalize both to the shared section template, fix the confirmed html-import drift, and author one marked self-contained Quickstart each (plus a marked /zod subpath block for forms) that the doc-check compiles against the shipped types.

Purpose: two of the four remaining package quickstarts become copy-pasteable-and-verified.
Output: normalized, drift-free query and forms READMEs whose marked blocks pass `npm run doc-check`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-docs/03-CONTEXT.md
@.planning/phases/03-docs/03-RESEARCH.md
@.planning/phases/03-docs/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize the query README and fix the html-import drift with a marked, self-contained Quickstart</name>
  <files>packages/query/README.md</files>
  <read_first>
    - packages/query/README.md (full — note the KitElement Quickstart around line 44-58 whose combined kit import is the drift to fix)
    - packages/kit/README.md (the canonical template + the correct two-line html-from-lit import pattern at kit README:14-15)
    - tools/typecheck-smoke/consumer-rest.ts:21 (verified real exports: createQueryClient, QueryControllerConfig from @willram/query)
    - packages/query/src/index.ts (query's real export surface: query, QueryController, MutationController, createQueryClient, queryOptions, mutationOptions)
    - 03-RESEARCH.md Pitfall 1 (the html drift) and Pitfall 2 (self-contained requirement)
    - 03-PATTERNS.md "Peers-in-install-line" shared pattern
  </read_first>
  <reversibility rating="reversible">Documentation-only; revertible.</reversibility>
  <action>
    Normalize packages/query/README.md heading order to the shared template (D-01), matching the section order of the canonical kit README (title + description; Install with peers; Quickstart; Core API; closing root-README pointer). The Install line must list the required peers: the package, @tanstack/query-core, and lit.

    Fix the confirmed import drift in the KitElement quickstart: split the single combined import so that KitElement is imported from @willram/kit and html is imported from the lit package on its own separate import line. kit exports no html (verified against kit/src/index.ts), so a kit html import fails to type-check — the doc-check will flag it. Do NOT add an html re-export to kit (out-of-scope API change).

    Choose ONE quickstart block to mark with the doc-check HTML-comment marker and make it fully self-contained: all imports present, and any undefined identifiers replaced. In particular the fetch/result usage must type-check on its own (annotate or shape the fetched data so member access compiles under strict). Leave the alternate LitElement/provider blocks as unmarked illustrative fragments. Keep the existing Core API reference tables, trimming only genuine drift. Add the closing pointer line to the root README.

    Run the harness after editing to confirm the marked query Quickstart compiles under both resolutions.
  </action>
  <verify>
    <automated>npm run doc-check:snippets</automated>
  </verify>
  <acceptance_criteria>
    - `npm run doc-check:snippets` exits 0 (query marked Quickstart compiles under both resolutions — this fails if any marked block still imports html from kit, so green proves the drift is fixed).
    - `grep -c 'doc-check' packages/query/README.md` returns >=1 (exactly one marked Quickstart).
    - `grep -c '## Install' packages/query/README.md` and `grep -c '## Quickstart' packages/query/README.md` each return >=1.
    - `grep -c 'query-core' packages/query/README.md` returns >=1 (peer listed in the install line).
    - `grep -c 'root README' packages/query/README.md` returns >=1.
  </acceptance_criteria>
  <done>packages/query/README.md follows the shared template, its marked Quickstart is self-contained and drift-free, and it passes the doc-check.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize the forms README, fix the html drift, and mark a self-contained Quickstart plus a /zod subpath block</name>
  <files>packages/forms/README.md</files>
  <read_first>
    - packages/forms/README.md (full — note the KitElement quickstart around line 59-77 with the combined kit import drift, and the Zod block around line 304-318)
    - packages/kit/README.md:14-15 (correct html-from-lit import)
    - tools/typecheck-smoke/consumer-rest.ts:23,25 (verified exports: form, FormInstance from @willram/forms; zodValidator from @willram/forms/zod)
    - packages/forms/src/index.ts (real export surface: form, FormController, bind, field, required, email, minLength, maxLength, min, max, pattern, etc.)
    - packages/forms/package.json:31-34 (the ./zod subpath the marked block must import from)
    - 03-RESEARCH.md Pitfall 1, Pitfall 2, and Code Example 3 (subpath-coverage requirement, D-03)
  </read_first>
  <reversibility rating="reversible">Documentation-only; revertible.</reversibility>
  <action>
    Normalize packages/forms/README.md to the shared template. The Install line lists the required peers: the package, @tanstack/form-core, and lit, plus the optional zod peer for the /zod subpath.

    Fix the confirmed import drift in the KitElement quickstart the same way as query: split the combined import so KitElement comes from @willram/kit and html comes from the lit package on its own line. Do NOT add an html re-export to kit.

    Mark exactly one self-contained Quickstart block with the doc-check marker. Replace any undefined identifiers (for example the async submit handler that currently calls an undefined login) with an inline stub or literal so the block compiles standalone (Pitfall 2). Use initialValues (the shipped API), not defaultValues (a stale INTEGRATIONS.md note).

    Additionally, in the Zod Integration section, mark a second self-contained block that imports from the @willram/forms/zod subpath (for example zodValidator) and defines its own schema inline, so the doc-check exercises the ./zod published subpath under both resolutions (D-03 subpath-coverage). Keep the remaining reference tables/fragments as unmarked illustrative content. Add the closing root-README pointer.

    Run the harness after editing to confirm both marked forms blocks compile.
  </action>
  <verify>
    <automated>npm run doc-check:snippets</automated>
  </verify>
  <acceptance_criteria>
    - `npm run doc-check:snippets` exits 0 (both marked forms blocks compile under both resolutions; a kit html import or an unresolved /zod subpath would fail here).
    - `grep -c 'doc-check' packages/forms/README.md` returns >=2 (one Quickstart marker + one /zod-subpath marker).
    - `grep -c 'forms/zod' packages/forms/README.md` returns >=1 and appears inside a marked block.
    - `grep -c '## Install' packages/forms/README.md` and `grep -c '## Quickstart' packages/forms/README.md` each return >=1.
    - `grep -c 'form-core' packages/forms/README.md` returns >=1 (peer listed).
    - `grep -c 'root README' packages/forms/README.md` returns >=1.
  </acceptance_criteria>
  <done>packages/forms/README.md follows the shared template, its marked Quickstart and /zod block are self-contained and drift-free, the doc-check exercises the ./zod subpath, and everything compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Static documentation edits only; no runtime code path, external input, or secret handled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02a | Tampering | README snippet drifting from shipped API | low | mitigate | doc-check compiles every marked block against dist/*.d.ts under both resolutions; drift becomes a hard error. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs this plan. |
</threat_model>

<verification>
- `npm run doc-check` (full) exits 0 with the query and forms marked blocks compiling.
- No marked block imports html from @willram/kit; forms exercises the ./zod subpath.
- Both READMEs follow the shared section template.
</verification>

<success_criteria>
The @willram/query and @willram/forms READMEs have drift-free, copy-pasteable quickstarts verified by the doc-check under both module resolutions (DOCS-01), with forms additionally covering the ./zod subpath.
</success_criteria>

<output>
Create `.planning/phases/03-docs/03-02-SUMMARY.md` when done.
</output>
