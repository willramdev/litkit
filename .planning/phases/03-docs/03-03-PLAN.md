---
phase: 03-docs
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - packages/router/README.md
  - packages/store/README.md
autonomous: true
requirements: [DOCS-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "`npm run doc-check` exits 0 with the @willram/router and @willram/store Quickstarts compiling against their shipped dist/*.d.ts under BOTH node16 and bundler (DOCS-01)"
    - "The router README carries a marked, self-contained block importing from the @willram/router/core and @willram/router/lit subpaths, so the doc-check exercises those published subpaths (DOCS-01, D-03)"
    - "The store Quickstart is self-contained: the previously-undefined User type is defined inline (or removed) so the block compiles standalone (DOCS-01, Pitfall 2)"
    - "Each of packages/router/README.md and packages/store/README.md carries at least one marked, self-contained Quickstart and follows the shared section template (DOCS-01)"
  artifacts:
    - "packages/router/README.md normalized + marked Quickstart + marked /core and /lit subpath block"
    - "packages/store/README.md normalized + marked, self-contained Quickstart"
  key_links:
    - "the router README's existing top-level render() fragment (router README:26-34) is NOT self-contained and must stay unmarked; the marked Quickstart is the createRouter/defineRoutes setup (self-contained)"
    - "router imports ESM-only; no require() anywhere (Phase 1 D-01)"
---

<objective>
Expand the DOCS-01 guarantee to the last two package READMEs: @willram/router and @willram/store. Normalize both to the shared template and author marked self-contained Quickstarts, with router additionally carrying a marked block that imports its ./core and ./lit subpaths so the doc-check covers every router subpath (D-03). Together with 03-02 (forms ./zod), the doc-check then exercises the full published subpath surface.

Purpose: complete the "every package README quickstart compiles against the shipped API" requirement.
Output: normalized router and store READMEs whose marked blocks pass `npm run doc-check`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-docs/03-CONTEXT.md
@.planning/phases/03-docs/03-RESEARCH.md
@.planning/phases/03-docs/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize the router README with a marked self-contained Quickstart and a marked /core + /lit subpath block</name>
  <files>packages/router/README.md</files>
  <read_first>
    - packages/router/README.md (full — note the top-level render() fragment at router README:26-34 that is NOT self-contained, and the Sub-path Imports section at router README:330-336)
    - packages/kit/README.md (canonical shared template)
    - tools/typecheck-smoke/consumer-router.ts (verified real exports across all three subpaths: createRouter + Router from '.', RouterOutlet from '/lit', CompiledPathMatcher from '/core')
    - packages/router/src/index.ts (real export surface: createRouter, defineRoutes, RouteController, SearchParamsController, link, routeState, searchParams, createMockRouter, mockMatch)
    - packages/router/package.json:27-40 (the ., ./core, ./lit exports the marked block must import)
    - 03-RESEARCH.md Pitfall 2 (self-contained requirement) and Code Example 3 (subpath-coverage, D-03)
  </read_first>
  <reversibility rating="reversible">Documentation-only; revertible.</reversibility>
  <action>
    Normalize packages/router/README.md to the shared template (title + description; Install with the lit peer; Quickstart; Core API; Subpath exports; closing root-README pointer).

    Author ONE marked, self-contained Quickstart. The current Quickstart mixes a self-contained setup (defineRoutes + createRouter) with a top-level render() fragment that lives outside any class and references html — that fragment cannot compile standalone and must stay UNMARKED illustrative content. Mark only a self-contained slice: the defineRoutes([...]) + createRouter({ routes }) setup, importing createRouter and defineRoutes from @willram/router (and html from lit only if the marked block actually renders inside a class). Keep it ESM-only — ESM import syntax, no CommonJS calls (Phase 1 D-01).

    In the Subpath exports section, mark a second self-contained block that imports representative real symbols from the @willram/router/core and @willram/router/lit subpaths (for example CompiledPathMatcher from /core and RouterOutlet from /lit) and references them, so the doc-check resolves those subpaths under both resolutions (D-03). Keep the existing Guards / Navigation / Options reference sections as unmarked illustrative content, trimming only genuine drift. Add the closing root-README pointer.

    Run the harness after editing to confirm both marked router blocks compile.
  </action>
  <verify>
    <automated>npm run doc-check:snippets</automated>
  </verify>
  <acceptance_criteria>
    - `npm run doc-check:snippets` exits 0 (both marked router blocks compile; an unresolved /core or /lit subpath would fail here).
    - `grep -c 'doc-check' packages/router/README.md` returns >=2 (Quickstart marker + subpath-block marker).
    - `grep -c 'router/core' packages/router/README.md` returns >=1 and `grep -c 'router/lit' packages/router/README.md` returns >=1, appearing inside a marked block.
    - `grep -c '## Install' packages/router/README.md` and `grep -c '## Quickstart' packages/router/README.md` each return >=1.
    - `grep -c 'require(' packages/router/README.md` returns 0 (ESM-only, no CJS).
    - `grep -c 'root README' packages/router/README.md` returns >=1.
  </acceptance_criteria>
  <done>packages/router/README.md follows the shared template, its marked Quickstart is self-contained, its subpath block exercises ./core and ./lit, and everything compiles under both resolutions.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize the store README with a marked, self-contained Quickstart</name>
  <files>packages/store/README.md</files>
  <read_first>
    - packages/store/README.md (full — note the Quickstart at store README:13-38 references an undefined User type at line 20)
    - packages/kit/README.md (canonical shared template + html-from-lit pattern)
    - tools/typecheck-smoke/consumer-rest.ts:19 (verified exports: createStore, Store from @willram/store)
    - packages/store/src/index.ts (real export surface: createStore, storeSlice, StoreSliceController, derived, batch, Store, DerivedStore)
    - 03-RESEARCH.md Pitfall 2 (self-contained requirement)
  </read_first>
  <reversibility rating="reversible">Documentation-only; revertible.</reversibility>
  <action>
    Normalize packages/store/README.md to the shared template (title + description; Install with the lit peer; Quickstart; Core API; closing root-README pointer).

    Mark the Quickstart block (store README:13-38) with the doc-check marker and make it self-contained: the block currently annotates state with an undefined User type, so define a minimal User type inline (or drop the annotation) and import KitElement from @willram/kit plus html from lit. Every imported symbol must be a real @willram/store export (createStore, storeSlice). Keep the Core API / Usage Patterns reference sections as unmarked illustrative content, trimming only genuine drift. Add the closing root-README pointer.

    Run the harness after editing to confirm the marked store Quickstart compiles.
  </action>
  <verify>
    <automated>npm run doc-check:snippets</automated>
  </verify>
  <acceptance_criteria>
    - `npm run doc-check:snippets` exits 0 (store marked Quickstart compiles under both resolutions; the previously-undefined User reference would fail here if left unresolved).
    - `grep -c 'doc-check' packages/store/README.md` returns >=1.
    - `grep -c '## Install' packages/store/README.md` and `grep -c '## Quickstart' packages/store/README.md` each return >=1.
    - `grep -c 'root README' packages/store/README.md` returns >=1.
  </acceptance_criteria>
  <done>packages/store/README.md follows the shared template and its marked, self-contained Quickstart passes the doc-check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Static documentation edits only; no runtime code path, external input, or secret handled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03a | Tampering | README snippet / subpath drifting from shipped API | low | mitigate | doc-check compiles marked blocks against dist/*.d.ts under both resolutions; router subpath block proves ./core and ./lit resolve. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs this plan. |
</threat_model>

<verification>
- `npm run doc-check` (full) exits 0 with the router and store marked blocks compiling.
- Router subpath block covers ./core and ./lit; store Quickstart is self-contained.
- Both READMEs follow the shared section template; no require() in router docs.
</verification>

<success_criteria>
The @willram/router and @willram/store READMEs have copy-pasteable quickstarts verified by the doc-check under both module resolutions (DOCS-01), with router covering its ./core and ./lit subpaths — completing full subpath coverage alongside forms ./zod.
</success_criteria>

<output>
Create `.planning/phases/03-docs/03-03-SUMMARY.md` when done.
</output>
